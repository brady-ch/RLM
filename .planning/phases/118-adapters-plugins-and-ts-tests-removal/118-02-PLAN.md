---
phase: 118-adapters-plugins-and-ts-tests-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/adapters/
  - tests/plugins/
  - tests/runtime/
autonomous: true
requirements:
  - RETIRE-118-02
user_setup: []
must_haves:
  truths:
    - "tests/adapters/ directory no longer exists"
    - "tests/plugins/ directory no longer exists"
    - "tests/runtime/ directory no longer exists (idempotent if already removed Phase 115)"
    - "Rust adapter and plugin tests under crates/rlm-core/tests/ remain"
  artifacts:
    - path: crates/rlm-core/tests/adapters/
      provides: "Mirrored Rust adapter tests"
    - path: crates/rlm-core/tests/plugins/
      provides: "Mirrored Rust plugin tests"
  key_links:
    - from: crates/rlm-core/tests/plugins/
      to: crates/rlm-core/src/plugins/
      via: "Rust unit/integration tests with #[path] stubs"
      pattern: "manifest|remote_fetch|write_file|shell"
---

<objective>
Delete mirrored TypeScript adapter, plugin, and runtime test trees per 113-AUDIT.md Phase 118 inventory.

Purpose: TS runtime tests duplicate Rust coverage in `crates/rlm-core/tests/adapters/` and `crates/rlm-core/tests/plugins/`; keeping them blocks clean `src/` retirement and references deleted modules.
Output: Removed `tests/adapters/` (2 persistence test files), `tests/plugins/` (2 test files), and `tests/runtime/` if present.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-adapters-plugins-and-ts-tests-removal/118-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md

<interfaces>
113-AUDIT.md mirrored tests to delete — **adapters** (2 files):

```
tests/adapters/persistence/run-state-store.test.ts
tests/adapters/persistence/session-store.test.ts
```

113-AUDIT.md mirrored tests to delete — **plugins** (2+ files):

```
tests/plugins/remote-fetch.test.ts
tests/plugins/builtin/web/web-tools.test.ts
```

Note: `tests/plugins/plugin-loader.test.ts` was deleted in Phase 115.

113-AUDIT.md mirrored tests to delete — **runtime**:
- Entire `tests/runtime/` tree (already deleted Phase 115; idempotent `git rm -r` if absent is no-op via conditional)

113-AUDIT.md TS integration tests:
- `tests/integration/integration-v15.test.ts` — already deleted Phase 116-03
- `tests/integration/` directory — verify absent; no action if already gone

Rust counterparts KEEP:
```
crates/rlm-core/tests/adapters/ollama_embedding.rs
crates/rlm-core/tests/adapters/ollama_language_model.rs
crates/rlm-core/tests/plugins/manifest.rs
crates/rlm-core/tests/plugins/remote_fetch.rs
crates/rlm-core/tests/plugins/runtime.rs
crates/rlm-core/tests/plugins/builtin/write_file.rs
crates/rlm-core/tests/plugins/builtin/shell.rs
crates/rlm-core/tests/plugins/builtin/web_fetch.rs
crates/rlm-core/tests/plugins/builtin/web_search.rs
```

Explicitly KEEP (113-AUDIT.md):
- `tests/ui/` — static UI wiring tests (no TS server boot)
- `tests/fixtures/` — shared golden fixture JSON
- `tests/depcruise/` — until Phase 119

Scope guard:
- Do NOT delete `crates/rlm-core/tests/adapters/` or `crates/rlm-core/tests/plugins/`
- Do NOT delete `tests/helpers/` — Plan 03
- Do NOT delete `tests/ui/` or `tests/fixtures/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tests/adapters/, tests/plugins/, tests/runtime/</name>
  <files>tests/adapters/, tests/plugins/, tests/runtime/</files>
  <action>
    Delete mirrored TypeScript runtime test trees per 113-AUDIT.md Phase 118:

    1. `git rm -r tests/adapters/` (if directory exists)
    2. `git rm -r tests/plugins/` (if directory exists)
    3. `git rm -r tests/runtime/` (if directory exists — may already be absent after Phase 115)

    Expected files removed from adapters:
    - `tests/adapters/persistence/run-state-store.test.ts`
    - `tests/adapters/persistence/session-store.test.ts`

    Expected files removed from plugins:
    - `tests/plugins/remote-fetch.test.ts`
    - `tests/plugins/builtin/web/web-tools.test.ts`

    Coverage transfer rationale:
    - Persistence store tests → `crates/rlm-core/tests/persistence/` (run_state_store, session_store, memory_store)
    - Plugin/remote-fetch/web tool tests → `crates/rlm-core/tests/plugins/` mirror tree

    After deletion:
    - `find tests/adapters tests/plugins tests/runtime -type f 2>/dev/null | wc -l` must be 0
  </action>
  <verify>
    <automated>test ! -d tests/adapters && test ! -d tests/plugins && test ! -d tests/runtime</automated>
  </verify>
  <done>Mirrored TS adapter, plugin, and runtime test directories fully removed</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no npm scripts reference deleted test paths</name>
  <files>package.json, scripts/agent-safe-verify.mjs</files>
  <action>
    Verify no kept test runner or verify profile still targets deleted runtime test paths:

    1. Search: `grep -rE "tests/adapters|tests/plugins|tests/runtime|tests/integration" package.json scripts/agent-safe-verify.mjs scripts/run-test-suite.mjs 2>/dev/null`

    2. If any match targets deleted paths (not `.planning/` comments), remove or replace with Rust equivalent:
       - Adapter coverage → `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core adapters`
       - Plugin coverage → `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core plugins`

    3. Expected: `scripts/run-test-suite.mjs` still runs `dist/tests` glob — document in SUMMARY that full `npm test` may fail until Phase 119 prunes TS build chain (transitional, not a Phase 118 gate).

    4. Verify `tests/integration/` absent: `test ! -d tests/integration` — if directory exists with TS files importing deleted runtime, `git rm -r tests/integration/`.

    Do NOT modify depcruise scripts or `.dependency-cruiser.js` — Phase 119.
  </action>
  <verify>
    <automated>! grep -rqE "tests/adapters|tests/plugins|tests/runtime" package.json scripts/agent-safe-verify.mjs 2>/dev/null && test ! -d tests/integration</automated>
  </verify>
  <done>No test runner references deleted adapter/plugin/runtime/integration paths; integration dir absent</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deleted TS tests → Rust tests | Coverage must exist in crates/rlm-core/tests/ mirror |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-03 | T | Deleted TS persistence/plugin test coverage gap | transfer | Rust `crates/rlm-core/tests/persistence/` and `crates/rlm-core/tests/plugins/` |
| T-118-04 | D | Accidental deletion of Rust test mirror | mitigate | Scope guard: only delete under `tests/adapters/`, `tests/plugins/`, `tests/runtime/`; never touch `crates/rlm-core/tests/` |
</threat_model>

<verification>
- `test ! -d tests/adapters && test ! -d tests/plugins && test ! -d tests/runtime` — exit 0
- `test ! -d tests/integration` — exit 0
</verification>

<success_criteria>
- Mirrored TS adapter, plugin, and runtime test trees deleted
- Rust test mirror preserved
</success_criteria>

<output>
After completion, create `.planning/phases/118-adapters-plugins-and-ts-tests-removal/118-02-SUMMARY.md`
</output>

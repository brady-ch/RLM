---
phase: 118-adapters-plugins-and-ts-tests-removal
plan: 03
type: execute
wave: 2
depends_on:
  - "118-01"
  - "118-02"
files_modified:
  - tests/helpers/
  - src/
  - docs/ARCHITECTURE.md
autonomous: true
requirements:
  - RETIRE-118-03
  - RETIRE-118-04
user_setup: []
must_haves:
  truths:
    - "Entire src/ directory absent"
    - "No kept file under ui/, scripts/, or tests/ imports from deleted src/adapters/ or src/plugins/ paths"
    - "tests/helpers/ removed (no UI test dependency)"
    - "cargo test -p rlm-core passes"
    - "tests/domain/ absent (Phase 117 straggler check per 113-GATES)"
  artifacts:
    - path: tests/ui/
      provides: "Retained static UI wiring tests"
    - path: crates/rlm-core/
      provides: "Sole runtime implementation"
  key_links:
    - from: npm run rlm
      to: crates/rlm-cli
      via: scripts/rlm-runtime.mjs Rust-only dispatcher
      pattern: "ask --help"
---

<objective>
Prune orphaned TS helper files, remove empty `src/` directory, fix stale doc references, then run all 113-GATES Phase 118 verification commands.

Purpose: ROADMAP success criterion 3 requires entire `src/` absent; 118-CONTEXT requires pruning tests that import deleted modules; Rust gate proves runtime unaffected.
Output: Clean import graph in kept paths; Phase 118 gates green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-adapters-plugins-and-ts-tests-removal/118-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/phases/117-domain-and-ports-removal/117-03-PLAN.md

<interfaces>
113-GATES Phase 118 gate commands (ALL must pass):

1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — exit 0
2. `test ! -d src/adapters && test ! -d src/plugins` — exit 0
3. `test ! -d tests/domain` — exit 0 (mirrored domain tests removed Phase 117; verify no stragglers)

ROADMAP success criterion 3 — entire `src/` absent:
```bash
test ! -d src
```

Import grep gates for kept paths:

```bash
# ui/ — no imports from deleted src/ runtime paths
grep -r "src/adapters\|src/plugins\|src/domain\|src/ports\|from ['\"].*\/adapters\/\|from ['\"].*\/plugins\/" ui/ --include='*.ts' --include='*.tsx' \
  | grep -v '^[^:]*:.*\/\/' && exit 1 || exit 0

# scripts/ — no imports from deleted src/ runtime paths
grep -r "src/adapters\|src/plugins\|src/domain\|src/ports" scripts/ --include='*.ts' --include='*.mjs' \
  && exit 1 || exit 0

# tests/ (kept) — no imports from deleted src/ paths
grep -r "src/adapters\|src/plugins\|src/domain\|src/ports" tests/ --include='*.ts' \
  && exit 1 || exit 0
```

Test helpers to delete (orphaned — zero importers in tests/ui/):

| File | Imports deleted | Action |
|------|-----------------|--------|
| `tests/helpers/mock-language-model.ts` | `src/ports/language-model-port.js` | **Delete** |
| `tests/helpers/quality-loop-helpers.ts` | `src/domain/types.js` | **Delete** |
| `tests/helpers/simple-tools.ts` | `src/ports/tool-port.js`, `runtime-logger-port.js` | **Delete** |
| `tests/helpers/recursion-fixtures.ts` | (none — unused fixture data) | **Delete** — no importers in tests/ui/ |

After deleting all helpers, remove empty `tests/helpers/` directory.

Stale doc reference:
- `docs/ARCHITECTURE.md` line ~21 references `src/adapters/*` — update to Rust paths or strike TS adapter note

Transitional exceptions (document in SUMMARY, do NOT block gate):
- `npm run build` / `npm run typecheck` / full `npm test` may still fail until Phase 119 — not Phase 118 gates
- `.dependency-cruiser.js` still references `src/` paths — Phase 119 prunes depcruise
- `tests/depcruise/probe.dependency-cruiser.js` references `src/plugins` — Phase 119
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned tests/helpers/ and remove src/ directory</name>
  <files>tests/helpers/, src/</files>
  <action>
    1. Delete entire `tests/helpers/` tree:
       - `git rm -r tests/helpers/`
       - Rationale: all four files import deleted domain/ports or are unused; `grep -r "tests/helpers" tests/ui/` returns zero matches

    2. Remove entire `src/` directory (must be empty after Plans 118-01 and Phase 117):
       - Verify no files remain: `find src -type f 2>/dev/null | wc -l` must be 0 before removal
       - If stray files exist (unexpected), list them in SUMMARY and delete only if they are TS runtime remnants per 113-AUDIT; do NOT delete `ui/`, `crates/`, or `scripts/`
       - `git rm -r src/` or `rmdir src/` after confirming empty

    3. Confirm postconditions:
       - `test ! -d src` — exit 0
       - `test ! -d tests/helpers` — exit 0

    Scope guard: Do NOT modify `crates/`, `ui/`, or `tests/ui/`.
  </action>
  <verify>
    <automated>test ! -d src && test ! -d tests/helpers</automated>
  </verify>
  <done>`src/` and `tests/helpers/` fully absent; no stray TS runtime files under src/</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale doc references and run Phase 118 gates</name>
  <files>docs/ARCHITECTURE.md</files>
  <action>
    1. **docs/ARCHITECTURE.md:** Replace or remove reference to `src/adapters/*` TS implementations. Point adapter/persistence/plugin implementations to `crates/rlm-core/src/adapters/`, `crates/rlm-core/src/persistence/`, and `crates/rlm-core/src/plugins/`. Keep change minimal — one paragraph or bullet list update only.

    2. Run import grep gates (all must exit 0):
       - ui/ gate (see interfaces block)
       - scripts/ gate
       - tests/ gate (tests/ui/ and tests/depcruise/ should pass; depcruise reads config text only)

    3. Run ALL 113-GATES Phase 118 commands:
       ```bash
       node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core
       test ! -d src/adapters && test ! -d src/plugins
       test ! -d tests/domain
       test ! -d src
       ```

    4. If `cargo test -p rlm-core` fails, diagnose whether failure is pre-existing (document in SUMMARY) or caused by this phase's deletions. Phase 118 gate requires exit 0 — fix in-scope regressions only; do not touch unrelated crates.

    Document transitional status in SUMMARY:
    - `npm run build` / `typecheck` expected fail until Phase 119
    - depcruise still references deleted `src/` — Phase 119
  </action>
  <verify>
    <automated>test ! -d src && node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core</automated>
  </verify>
  <done>All Phase 118 gate commands pass; no kept paths import deleted src/ runtime modules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deleted src/ → kept tests/ui | UI static tests must not import deleted TS runtime |
| Rust-only runtime → npm scripts | Dispatcher must still invoke Rust CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | I | Orphan imports to deleted src/ in kept files | mitigate | Import grep gates on ui/, scripts/, tests/ |
| T-118-06 | D | Accidental deletion of ui/ or crates/ | mitigate | Scope guard: only `git rm -r tests/helpers/ src/` after file count verification |
| T-118-07 | R | cargo test gate failure blocks Phase 119 | mitigate | Run full `cargo test -p rlm-core` with RAM gate; fix in-scope regressions |
</threat_model>

<verification>
Phase 118 complete when ALL pass:
1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — exit 0
2. `test ! -d src/adapters && test ! -d src/plugins` — exit 0
3. `test ! -d tests/domain` — exit 0
4. `test ! -d src` — exit 0
5. Import grep gates on ui/, scripts/, tests/ — exit 0
</verification>

<success_criteria>
- Entire `src/` directory absent
- Mirrored TS runtime tests and helpers removed
- `cargo test -p rlm-core` passes
- Phase 119 preconditions met (113-GATES: entire `src/` absent)
</success_criteria>

<output>
After completion, create `.planning/phases/118-adapters-plugins-and-ts-tests-removal/118-03-SUMMARY.md`
</output>

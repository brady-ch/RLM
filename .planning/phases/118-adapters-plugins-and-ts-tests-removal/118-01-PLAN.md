---
phase: 118-adapters-plugins-and-ts-tests-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/
  - src/plugins/
  - AGENTS.md
autonomous: true
requirements:
  - RETIRE-118-01
user_setup: []
must_haves:
  truths:
    - "src/adapters/ directory no longer exists"
    - "src/plugins/ directory no longer exists"
    - "Rust adapters and plugins remain sole implementation under crates/rlm-core/src/adapters/ and crates/rlm-core/src/plugins/"
    - "AGENTS.md documents TS adapters/plugins removed Phase 118"
  artifacts:
    - path: crates/rlm-core/src/adapters/
      provides: "Canonical model hosts (Ollama, etc.)"
      contains: "mod.rs"
    - path: crates/rlm-core/src/plugins/
      provides: "Canonical plugin manifest, registry, builtins"
      contains: "mod.rs"
  key_links:
    - from: crates/rlm-core/src/application/
      to: crates/rlm-core/src/plugins/
      via: "Rust bootstrap composition"
      pattern: "crate::plugins::"
---

<objective>
Delete the TypeScript adapters and plugins layers per 113-AUDIT.md Phase 118 inventory and update AGENTS.md concern map.

Purpose: Rust `crates/rlm-core/src/adapters/`, `crates/rlm-core/src/persistence/`, and `crates/rlm-core/src/plugins/` are canonical; TS duplicates must not remain after Phase 117 domain/ports removal.
Output: Removed `src/adapters/` (9 files) and `src/plugins/` (24 files including builtin/, remote-fetch/, __depcruise-fixtures__/); AGENTS.md reflects Phase 118 boundary.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-adapters-plugins-and-ts-tests-removal/118-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@AGENTS.md

<interfaces>
113-AUDIT.md Phase 118 delete inventory — **adapters** (9 files):

```
src/adapters/index.ts
src/adapters/models/http-language-model.ts
src/adapters/models/ollama-embedding-model.ts
src/adapters/models/ollama-language-model.ts
src/adapters/persistence/file-memory-store.ts
src/adapters/persistence/file-run-state-store.ts
src/adapters/persistence/file-session-store.ts
src/adapters/persistence/file-vector-index.ts
src/adapters/persistence/in-memory-trace.ts
```

113-AUDIT.md Phase 118 delete inventory — **plugins** (24 files under):

```
src/plugins/index.ts
src/plugins/plugin-loader.ts
src/plugins/manifest-schema.ts
src/plugins/categories.ts
src/plugins/types.ts
src/plugins/paths.ts
src/plugins/legacy-extensions.ts
src/plugins/remote-fetch/
src/plugins/builtin/
src/plugins/__depcruise-fixtures__/
```

Rust counterparts (KEEP):
- `crates/rlm-core/src/adapters/`
- `crates/rlm-core/src/persistence/`
- `crates/rlm-core/src/plugins/`
- `crates/rlm-core/tests/adapters/` (Rust mirrored adapter tests)
- `crates/rlm-core/tests/plugins/` (Rust mirrored plugin tests)

113-GATES Phase 118 gate (partial — full suite in Plan 03):
- `test ! -d src/adapters && test ! -d src/plugins` — exit 0

Scope guard (118-CONTEXT):
- Do NOT delete `tests/adapters/`, `tests/plugins/` — Plan 02 scope
- Do NOT delete `tests/helpers/` — Plan 03 orphan pruning
- Do NOT touch `crates/rlm-core/src/adapters/`, `persistence/`, or `plugins/`
- Do NOT remove entire `src/` directory in this plan — Plan 03 removes empty `src/` after Phase 117 already removed domain/ports
- Transitional: `npm run build` / `typecheck` may still fail until Phase 119 — not Phase 118 gates

AGENTS.md updates required:
- Opening paragraph: state no TS runtime layers remain in `src/` after Phase 118; Rust-only runtime in `crates/`
- TS concern map diagram: annotate `~~adapters~~` and `~~plugins~~` **Removed Phase 118**; entire TS `src/` tree retired
- TS concern table: remove or strike adapters/plugins rows; point to Rust paths
- Layout section: remove all `src/adapters/*` and `src/plugins/*` bullet paths
- Plugin taxonomy section: replace TS manifest/loader paths with `crates/rlm-core/src/plugins/` guidance
- Extending section: built-in tools, persistence, model hosts → Rust paths only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/adapters/ and src/plugins/ trees</name>
  <files>src/adapters/, src/plugins/</files>
  <action>
    Delete TypeScript adapters and plugins layers per 113-AUDIT.md Phase 118:

    1. `git rm -r src/adapters/`
    2. `git rm -r src/plugins/`

    Expected subdirs removed with plugins: `builtin/` (shell, files, web), `remote-fetch/`, `__depcruise-fixtures__/`.

    Scope guard:
    - Do NOT touch `crates/rlm-core/src/adapters/`, `crates/rlm-core/src/persistence/`, or `crates/rlm-core/src/plugins/` — Rust canonical layers
    - Do NOT delete `tests/adapters/` or `tests/plugins/` in this task — Plan 02
    - Do NOT delete `tests/helpers/` — Plan 03
    - Do NOT run `rmdir src/` here even if empty — Plan 03 handles entire `src/` removal and gate verification

    After deletion, confirm zero files remain:
    - `find src/adapters -type f 2>/dev/null | wc -l` must be 0
    - `find src/plugins -type f 2>/dev/null | wc -l` must be 0
  </action>
  <verify>
    <automated>test ! -d src/adapters && test ! -d src/plugins</automated>
  </verify>
  <done>`src/adapters/` and `src/plugins/` fully removed; Rust adapters/plugins layers untouched</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md for Phase 118 boundary</name>
  <files>AGENTS.md</files>
  <action>
    Update AGENTS.md to document TS adapters/plugins removal (mirror Phase 117 domain/ports pattern):

    1. **Opening paragraph:** State Rust `rlm-cli` is sole CLI; no TypeScript runtime layers remain in `src/` after Phase 118; all orchestration, domain, ports, adapters, and plugins are Rust-only in `crates/rlm-core/`.

    2. **TS concern map diagram:** Annotate `~~adapters~~` and `~~plugins~~` **Removed Phase 118**. Note entire TS `src/` tree retired (Phases 115–118).

    3. **TS concern table:** Remove or strike adapters/plugins rows; point to `crates/rlm-core/src/adapters/`, `crates/rlm-core/src/persistence/`, and `crates/rlm-core/src/plugins/`.

    4. **Layout section:** Remove all `src/adapters/*` and `src/plugins/*` bullet paths. Remove Plugin taxonomy bullets referencing `src/plugins/`.

    5. **Tests mirror table:** Mark `tests/adapters/` and `tests/plugins/` **Removed Phase 118** (deleted in Plan 02).

    6. **Extending section:** Replace built-in tool, external plugin, persistence, and model host guidance with Rust paths only (`crates/rlm-core/src/plugins/builtin/`, `crates/rlm-core/src/persistence/`, etc.).

    Do NOT remove depcruise or npm toolchain sections — Phase 119 scope.
  </action>
  <verify>
    <automated>grep -v '^#' AGENTS.md | grep -c 'Removed Phase 118' | grep -qv '^0$' && ! grep -q 'src/plugins/manifest-schema' AGENTS.md</automated>
  </verify>
  <done>AGENTS.md reflects Rust-only adapters/plugins; no active `src/adapters/` or `src/plugins/` path references in Layout/Extending</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deleted TS adapters/plugins → Rust crates | Runtime capability must exist in crates/rlm-core |
| TS tests → deleted src/ | Mirrored tests deleted in Plan 02; coverage in Rust test mirror |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | T | Deleted TS adapter/plugin coverage gap | transfer | Rust `crates/rlm-core/tests/adapters/` and `crates/rlm-core/tests/plugins/` mirror trees |
| T-118-02 | D | Accidental deletion of Rust adapters/plugins | mitigate | Scope guard: only `git rm -r src/adapters/ src/plugins/`; never touch `crates/rlm-core/src/adapters/` or `plugins/` |
</threat_model>

<verification>
- `test ! -d src/adapters && test ! -d src/plugins` — exit 0
- AGENTS.md documents Phase 118 removal
</verification>

<success_criteria>
- `src/adapters/` and `src/plugins/` deleted entirely
- AGENTS.md updated for Rust-only adapters/plugins
</success_criteria>

<output>
After completion, create `.planning/phases/118-adapters-plugins-and-ts-tests-removal/118-01-SUMMARY.md`
</output>

---
phase: 59-rust-cli-parity-ci
plan: 01
wave: 1
requirements: [CLI-01, CLI-02, REG-02]
autonomous: true
---

# Phase 59 Plan 01: Rust CLI + Parity CI

## Goal

Expand Rust `rlm` binary for key run modes, add `RLM_RUNTIME` strangler switch, and wire golden fixture parity gate in npm scripts.

## Tasks

### 1. Expand rlm-cli subcommands (type=auto)
- `ui`, `ask` (stub), `plugin` (list/install/enable/disable/uninstall/doctor/inspect/validate)
- Stubs for plan-node/session/workflow with actionable errors

### 2. RLM_RUNTIME dispatcher (type=auto)
- `scripts/rlm-runtime.mjs` + npm scripts `rlm:node`, `rlm:rust`
- Document env var behavior

### 3. Parity fixture gate (type=auto)
- TS integration test against golden fixtures
- `npm run check:parity` orchestrates TS + Rust fixture tests

### 4. Verification (type=auto)
- `npm run check:rust` and `cargo test -p rlm-core` green

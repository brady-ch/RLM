---
phase: 114-control-server-and-ui-bootstrap-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/parity/compare-runtimes.mjs
  - tests/integration/control-server-fixtures.test.ts
  - package.json
autonomous: true
requirements:
  - RETIRE-114-04
user_setup: []
must_haves:
  truths:
    - "check:parity runs Rust golden fixtures only — no TS build or TS server boot"
    - "compare-runtimes.mjs cross-runtime parity script is removed"
    - "TS control-server fixture integration test is removed"
  artifacts:
    - path: package.json
      provides: "Rust-only check:parity script"
      contains: "cargo-with-ram-gate.mjs"
  key_links:
    - from: package.json
      to: crates/rlm-core/tests/control_server_fixtures.rs
      via: "check:parity invokes control_server_matches_golden_fixtures"
      pattern: "control_server_matches_golden_fixtures"
---

<objective>
Retire TS-side HTTP parity scripts and narrow `check:parity` to Rust golden fixtures only.

Purpose: Phase 114 removes dual-runtime HTTP verification; Rust `control_server_matches_golden_fixtures` becomes the sole CI contract gate per 113-GATES.md.
Output: Deleted parity scripts/tests; updated `package.json` `check:parity`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-control-server-and-ui-bootstrap-removal/114-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/notes/rust-only-runtime-migration-decisions.md
@package.json

<interfaces>
Current `check:parity` (replace entirely):

```json
"check:parity": "npm run build && node --test dist/tests/integration/control-server-fixtures.test.js && node scripts/parity/compare-runtimes.mjs && cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture"
```

Target shape from 113-GATES.md:

```json
"check:parity": "node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture"
```

Keep unchanged:
- `tests/fixtures/control-server/*.json` — shared golden data for Rust test
- `crates/rlm-core/tests/control_server_fixtures.rs` — sole HTTP gate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete TS parity scripts and fixture test</name>
  <files>scripts/parity/compare-runtimes.mjs, tests/integration/control-server-fixtures.test.ts</files>
  <action>
    Delete these files per 113-AUDIT.md Phase 114 inventory:
    - `scripts/parity/compare-runtimes.mjs` — boots TS + Rust servers for session route compare
    - `tests/integration/control-server-fixtures.test.ts` — boots TS control server for golden routes

    If `scripts/parity/` is empty after deletion, remove the directory.

    Do NOT delete `tests/fixtures/control-server/` — Rust golden test still reads those JSON files.
  </action>
  <verify>
    <automated>test ! -f scripts/parity/compare-runtimes.mjs && test ! -f tests/integration/control-server-fixtures.test.ts && test -d tests/fixtures/control-server</automated>
  </verify>
  <done>TS parity script and TS fixture integration test removed; shared fixture JSON preserved</done>
</task>

<task type="auto">
  <name>Task 2: Narrow check:parity to Rust golden fixtures</name>
  <files>package.json</files>
  <action>
    Replace `package.json` `scripts.check:parity` with the Rust-only target from 113-GATES.md:

    `"check:parity": "node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture"`

    Requirements:
    - Remove `npm run build` prefix — no TS compile needed for HTTP contract gate
    - Remove `node --test dist/tests/integration/control-server-fixtures.test.js`
    - Remove `node scripts/parity/compare-runtimes.mjs`
    - Use RAM gate wrapper for agent/autonomous safety (matches project convention)

    Grep package.json to confirm no remaining references to `compare-runtimes` or `control-server-fixtures`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const s=p.scripts['check:parity']; if(!s.includes('control_server_matches_golden_fixtures')) process.exit(1); if(s.includes('compare-runtimes')||s.includes('control-server-fixtures')||s.includes('npm run build')) process.exit(1);"</automated>
  </verify>
  <done>check:parity invokes Rust golden fixtures only via cargo-with-ram-gate.mjs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → HTTP contract | Parity gate must not silently weaken — Rust fixtures are authoritative post-114 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | T | check:parity script | mitigate | Exact target string from 113-GATES.md; grep verify excludes TS boot paths |
| T-114-02 | I | Golden fixture JSON | accept | Read-only shared data; not modified in this plan |
</threat_model>

<verification>
```bash
test ! -f scripts/parity/compare-runtimes.mjs
test ! -f tests/integration/control-server-fixtures.test.ts
node -e "const s=require('./package.json').scripts['check:parity']; process.exit(s.includes('compare-runtimes')?1:0)"
```
</verification>

<success_criteria>
- TS parity artifacts deleted
- `check:parity` is Rust-only with RAM gate wrapper
- No references to compare-runtimes or control-server-fixtures.test in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/114-control-server-and-ui-bootstrap-removal/114-01-SUMMARY.md`
</output>

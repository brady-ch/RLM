---
phase: 114-control-server-and-ui-bootstrap-removal
plan: 03
type: execute
wave: 2
depends_on:
  - "114-01"
  - "114-02"
files_modified:
  - ui/vite.config.ts
  - package.json
autonomous: true
requirements:
  - RETIRE-114-02
  - RETIRE-114-03
user_setup: []
must_haves:
  truths:
    - "cargo test control_server_matches_golden_fixtures passes"
    - "RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui --port 0 prints listening URL"
    - "Vite dev:ui proxies /api to Rust control server for local UI development"
  artifacts:
    - path: ui/vite.config.ts
      provides: "Dev proxy /api → Rust server"
      contains: "proxy"
    - path: crates/rlm-core/tests/control_server_fixtures.rs
      provides: "Sole HTTP contract golden test"
      contains: "control_server_matches_golden_fixtures"
  key_links:
    - from: ui/vite.config.ts
      to: crates/rlm-cli/src/commands/ui.rs
      via: "proxy /api to RLM_CONTROL_PORT or default 8787"
      pattern: "proxy.*api"
    - from: crates/rlm-cli/src/commands/ui.rs
      to: ui/dist
      via: "RLM_UI_DIST serves static assets + API routes"
      pattern: "ui_dist_dir"
---

<objective>
Wire Vite dev proxy to Rust server and run Phase 114 verification gates from 113-GATES.md.

Purpose: Confirm Rust Axum is sole HTTP transport with passing golden fixtures and working UI serve path; restore dev:ui workflow without TS server.
Output: Updated vite dev proxy; all 113-GATES Phase 114 gate commands green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-control-server-and-ui-bootstrap-removal/114-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@ui/vite.config.ts
@crates/rlm-core/tests/control_server_fixtures.rs
@crates/rlm-core/tests/reg01_uat_smoke.rs
@package.json

<interfaces>
Phase 114 gate commands from 113-GATES.md:

```bash
# 1. Primary HTTP contract gate
node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture

# 2. UI serve smoke
RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui --port 0

# 3-4. Deletion confirmations (Plans 01-02)
test ! -d src/application/control-server
test ! -f tests/integration/control-server-fixtures.test.ts

# 5. check:parity shape (Plan 01)
npm run check:parity
```

Tauri dev config (reference only — uses Vite on 5173):

```json
"beforeDevCommand": "npm run dev:ui",
"devUrl": "http://127.0.0.1:5173"
```

Current vite.config.ts has no server.proxy — UI fetch uses relative `/api/*` paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vite dev proxy for Rust control server API</name>
  <files>ui/vite.config.ts, package.json</files>
  <action>
    Update `ui/vite.config.ts` to proxy API requests to the Rust control server during local UI development (per 114-CONTEXT discretion: "UI dev proxy may need update to point at Rust server only"):

    Add `server.proxy` configuration:
    - Proxy `/api` → `http://127.0.0.1:${RLM_CONTROL_PORT || 8787}`
    - Use `changeOrigin: true`
    - Read port from `process.env.RLM_CONTROL_PORT` with fallback `8787` (document in comment)

    Optionally add `dev:ui:full` script to `package.json` that documents the two-terminal workflow:
    - Terminal 1: `RLM_UI_DIST= cargo run -p rlm-cli -- ui --port 8787` (or use env port)
    - Terminal 2: `RLM_CONTROL_PORT=8787 npm run dev:ui`

    Keep existing `dev:ui` script unchanged for Tauri compatibility. Do NOT modify `ui/` React source or `src-tauri/` in this plan.

    Do NOT add TS server boot paths.
  </action>
  <verify>
    <automated>grep -q "proxy" ui/vite.config.ts && grep -q "/api" ui/vite.config.ts</automated>
  </verify>
  <done>vite.config.ts proxies /api to Rust server; dev workflow documented via comment or optional script</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 114 verification gates</name>
  <files></files>
  <action>
    Execute all Phase 114 gates from 113-GATES.md in order:

    1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture` — must exit 0

    2. Build UI if needed: `npm run build:ui` (required for reg01 smoke and RLM_UI_DIST gate)

    3. UI serve smoke — start server briefly and confirm stderr contains listening URL:
       `timeout 5 env RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui --port 0 2>&1 | grep -q "RLM UI listening"` (or equivalent non-interactive smoke; server may run until timeout kills it)

    4. Deletion gates:
       - `test ! -d src/application/control-server`
       - `test ! -f tests/integration/control-server-fixtures.test.ts`

    5. `npm run check:parity` — must exit 0 (Rust-only per Plan 01)

    6. Optional supplementary: `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core reg01_uat_smoke -- --nocapture` if ui/dist exists

    If any gate fails, fix in-scope issues (do not restore deleted TS server). Document gate results in SUMMARY.md.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture && test ! -d src/application/control-server && npm run check:parity</automated>
  </verify>
  <done>All 113-GATES Phase 114 gate commands pass; golden fixtures green; check:parity Rust-only</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite dev → Rust API | Proxy must target local Rust server only, not external hosts |
| Golden fixtures → production API | Fixtures validate contract shape, not auth secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-06 | S | vite proxy target | mitigate | Hardcode localhost; port from env with safe default 8787 |
| T-114-07 | T | Golden fixture gate | mitigate | Run via cargo-with-ram-gate.mjs; fail phase if exit non-zero |
| T-114-08 | R | Dev proxy misconfig | accept | Local dev only; production uses RLM_UI_DIST bundled serve |
</threat_model>

<verification>
Full Phase 114 gate suite from 113-GATES.md — all must pass before marking phase complete.
</verification>

<success_criteria>
- control_server_matches_golden_fixtures passes
- RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui --port 0 prints listening URL
- check:parity exits 0
- vite.config.ts has /api proxy to Rust server
</success_criteria>

<output>
After completion, create `.planning/phases/114-control-server-and-ui-bootstrap-removal/114-03-SUMMARY.md`
</output>

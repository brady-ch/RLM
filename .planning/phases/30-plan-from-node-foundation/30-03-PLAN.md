---
phase: 30-plan-from-node-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/application/control-server.ts
  - src/index.ts
  - src/cli/args.ts
autonomous: true
requirements:
  - PLAN-02
  - PLAN-07
must_haves:
  truths:
    - "POST /api/nodes/:id/plan awaits async planNode"
    - "UI session receives PurposeRoutingLanguageModel for planning"
    - "CLI can invoke plan-from-node with same MutationError codes"
  artifacts:
    - path: src/index.ts
      provides: "planModel wired into createInteractiveExecutionSession"
      pattern: "planModel"
    - path: src/cli/args.ts
      provides: "plan-node CLI subcommand or flag"
  key_links:
    - from: src/application/control-server.ts
      to: src/application/execution-controller.ts
      via: "await session.planNode(nodeId)"
      pattern: "await session\\.planNode"
    - from: src/index.ts
      to: src/application/model-provider.ts
      via: "PurposeRoutingLanguageModel as planModel"
      pattern: "planModel:"
---

<objective>
Wire async planNode through the control server, inject the purpose-routing model into UI sessions, and add CLI plan-from-node parity.

Purpose: PLAN-07 requires explicit API/CLI failure states matching UI (per CONTEXT Failure States & CLI Parity).
Output: Async API route, session model wiring, CLI command.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-plan-from-node-foundation/30-02-SUMMARY.md
@src/application/control-server.ts
@src/index.ts
@src/cli/args.ts

<interfaces>
Async planNode from plan 02:
```typescript
async planNode(nodeId: string): Promise<{ plannedNodeIds: string[]; budget: ComposerPlanBudget; exhausted: boolean }>
```

PurposeRoutingLanguageModel construction in index.ts (~line 344):
```typescript
const routingModel = new PurposeRoutingLanguageModel({ config: projectConfig, agent: defaultAgent, ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Async control-server plan routes</name>
  <files>src/application/control-server.ts, tests/recursive-language-model.test.ts</files>
  <read_first>
    - src/application/control-server.ts (plan route ~262, error handler ~449)
    - tests/recursive-language-model.test.ts (control server plan test ~1887)
  </read_first>
  <action>
    Update `createControlServer` handler for `POST /api/nodes/:id/plan` and `/breakdown`:
    ```typescript
    const result = await session.planNode(nodeId);
    ```
    Handler function must be async (wrap in async IIFE if outer handler is sync).

    Ensure `toMutationError` mapping unchanged — MutationError codes `planning_failed`, `invalid_planner_output`, `invalid_prompt` return 409 JSON with `{ code, message, nodeIds, details, suggestedFix }`.

    Extend existing control-server integration test (~1887):
    - Pass mock planModel into session used by test server OR use real mock via session factory option on server creation
    - Add test: POST plan with empty prompt on root → 409 + code invalid_prompt
    - Add test: POST plan with mock failing model → 409 + code invalid_planner_output

    If test server factory doesn't expose planModel, extend `createControlServer` options with optional `planModel` passthrough to session constructor.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/application/control-server.ts | grep -c 'await session.planNode'` >= 1
    - `npm test -- tests/recursive-language-model.test.ts -t "control server"` exits 0 OR matching API test name passes
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/recursive-language-model.test.ts -t "planResponse\|control server\|/api/nodes"</automated>
  </verify>
  <done>API routes await async planner and return structured MutationError payloads</done>
</task>

<task type="auto">
  <name>Task 2: Wire planModel in index.ts and add CLI plan-node command</name>
  <files>src/index.ts, src/cli/args.ts, tests/recursive-language-model.test.ts</files>
  <read_first>
    - src/index.ts (UI branch ~300, PurposeRoutingLanguageModel creation)
    - src/cli/args.ts (command parsing)
    - .planning/phases/30-plan-from-node-foundation/30-CONTEXT.md (CLI parity section)
  </read_first>
  <action>
    In `src/index.ts` UI path: after creating `PurposeRoutingLanguageModel` for the UI runner (or reuse same instance), pass to session:
    ```typescript
    const session = createInteractiveExecutionSession({
      seedRootPrompt: options.prompt,
      planModel: routingModel,
    });
    ```
    Use the same agent profile as default agent for purpose routing config.

    Add CLI command per CONTEXT "matching CLI flag/command":
    - Extend `CliOptions.command` with `"plan-node"`
    - Parse: `rlm plan-node [--node-id root-composer] [--prompt "text"]`
    - Behavior: create ephemeral session with planModel, optionally seed/ edit prompt, `await session.planNode(nodeId)`, print JSON `{ plannedNodeIds, budget, graph node count }` to stdout
    - On MutationError: print JSON error to stderr, exit code 1 with same `code` field

    Add test in `tests/recursive-language-model.test.ts`:
    - `parseArgs(["plan-node", "--node-id", "root-composer"])` parses correctly
    - Optional: integration test calling plan-node handler with mock model (if extractable)

    Update help text in args.ts for new command.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/index.ts | grep -c 'planModel:'` >= 1
    - `grep -v '^#' src/cli/args.ts | grep -c 'plan-node'` >= 1
    - `npm test -- tests/recursive-language-model.test.ts -t "plan-node\|parses ui command"` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test -- tests/recursive-language-model.test.ts -t "plan-node"</automated>
  </verify>
  <done>UI sessions use purpose-routing planner; CLI plan-node command available with error parity</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client→control-server | Untrusted nodeId in URL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | I | POST /api/nodes/:id/plan | mitigate | requireEditableNode validates node exists and status |
| T-30-08 | I | CLI plan-node | accept | Local CLI; same session rules as UI |
</threat_model>

<verification>
```bash
npm run build
npm test -- tests/recursive-language-model.test.ts -t "plan"
```
</verification>

<success_criteria>
- API and CLI both invoke async planNode with wired planModel
- Error codes match GraphMutationError shape on 409 responses
</success_criteria>

<output>
After completion, create `.planning/phases/30-plan-from-node-foundation/30-03-SUMMARY.md`
</output>

---
phase: 72-human-uat-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/72-human-uat-sign-off/72-UAT.md
  - .planning/phases/72-human-uat-sign-off/72-VERIFICATION.md
autonomous: true
requirements: [REG-01]
must_haves:
  truths:
    - "Operator has a merged UAT checklist with evidence columns ready before manual testing"
    - "Targeted automated preflight passes without full test suite"
    - "Rust control server start command and Ollama prerequisites are documented for operator"
  artifacts:
    - path: .planning/phases/72-human-uat-sign-off/72-UAT.md
      provides: "Merged checklist + evidence template + operator runbook"
    - path: .planning/phases/72-human-uat-sign-off/72-VERIFICATION.md
      provides: "Phase 72 automated preflight record"
  key_links:
    - from: .planning/phases/72-human-uat-sign-off/72-UAT.md
      to: .planning/milestones/v1.8-phases/61-ui-shell-rewrite/61-06-VERIFICATION.md
      via: "checklist items 1-7 copied and extended"
      pattern: "61-06-VERIFICATION"
    - from: .planning/phases/72-human-uat-sign-off/72-VERIFICATION.md
      to: crates/rlm-core/src/control_server/routes.rs
      via: "grep confirms Phase 62 routes"
      pattern: "pause-future-auto-approvals|model-library/download"
---

<objective>
Prepare REG-01 human UAT: merged checklist with evidence template, operator runbook, and targeted automated preflight (no full test suite).

Purpose: Operator can execute 61-06 + Phase 62 human items on Rust-served UI with clear pass/fail evidence slots before sign-off checkpoint.
Output: `72-UAT.md`, `72-VERIFICATION.md` with automated preflight PASS.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-human-uat-sign-off/72-CONTEXT.md
@.planning/milestones/v1.8-phases/61-ui-shell-rewrite/61-06-VERIFICATION.md
@.planning/milestones/v1.9-phases/62-ui-regression-fixes/62-VERIFICATION.md
@.planning/milestones/v1.8-phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md

<interfaces>
From 61-06-VERIFICATION.md human checklist (items 1-7):
1. Start Rust control server + built UI static assets
2. Workflow view: top bar + canvas only (no sidebar models/plugins/sessions/memory)
3. Inline prompt edit on card; right-click → Plan children
4. Select node → Run panel (~360px); canvas click → panel hidden
5. Advanced → each tab loads; ← Back to workflow preserves graph
6. Sessions tab: save and reopen session
7. Run workflow / Stop respond without console errors

From 62-VERIFICATION.md human items (append as items 8-10):
8. Pause future auto-approvals during recursive run (TopBar → POST /api/pause-future-auto-approvals)
9. Install HF model from Advanced → Models (ModelLibraryRow → /api/model-library/download)
10. Canvas-first shell workflows unchanged (regression guard)

Operator start command (per D-02):
npm run build:ui
RLM_UI_DIST=$PWD/ui/dist cargo run -p rlm-cli -- ui --port 0

Targeted tests (per D-06 — NOT npm run check):
- npm run build:ui
- npm run lint -- ui/src
- cargo check -p rlm-cli -p rlm-core
- node --test --test-name-pattern="approval mode contract" tests/domain/recursion/recursive-language-model.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merged UAT checklist and operator runbook</name>
  <files>.planning/phases/72-human-uat-sign-off/72-UAT.md</files>
  <read_first>.planning/milestones/v1.8-phases/61-ui-shell-rewrite/61-06-VERIFICATION.md, .planning/milestones/v1.9-phases/62-ui-regression-fixes/62-VERIFICATION.md, .planning/milestones/v1.8-phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md, .planning/phases/72-human-uat-sign-off/72-CONTEXT.md</read_first>
  <action>
    Create 72-UAT.md per D-03 with YAML frontmatter: status pending, phase 72-human-uat-sign-off, source lists 61-06 and 62-VERIFICATION.
    Include Operator Runbook section (per D-02): prerequisites (Ollama running if testing items 7/10 live runs), build command, Rust serve command, how to read listen URL from stderr, browser open step.
    Add Checklist table with columns: # | Item | Steps | Result | Notes | Evidence — 10 rows covering 61-06 items 1-7 plus 62 items 8-10. Pre-fill Steps from source artifacts; leave Result/Notes/Evidence blank or PENDING for operator.
    Add Sign-off section with blank Approved timestamp and operator name fields for Plan 02.
    Create optional evidence/ subdirectory note for screenshot paths if operator attaches files.
  </action>
  <acceptance_criteria>
    - test -f .planning/phases/72-human-uat-sign-off/72-UAT.md exits 0
    - grep -c "RLM_UI_DIST" .planning/phases/72-human-uat-sign-off/72-UAT.md returns at least 1
    - grep -c "Pause future auto-approvals" .planning/phases/72-human-uat-sign-off/72-UAT.md returns at least 1
    - grep -c "| 7 |" .planning/phases/72-human-uat-sign-off/72-UAT.md returns at least 1
    - grep -c "| 10 |" .planning/phases/72-human-uat-sign-off/72-UAT.md returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/72-human-uat-sign-off/72-UAT.md && grep -q "RLM_UI_DIST" .planning/phases/72-human-uat-sign-off/72-UAT.md && grep -q "Pause future auto-approvals" .planning/phases/72-human-uat-sign-off/72-UAT.md</automated>
  </verify>
  <done>72-UAT.md exists with 10-item merged checklist, runbook, and empty evidence columns per D-01 and D-03</done>
</task>

<task type="auto">
  <name>Task 2: Run targeted automated preflight and record results</name>
  <files>.planning/phases/72-human-uat-sign-off/72-VERIFICATION.md</files>
  <read_first>crates/rlm-core/src/control_server/routes.rs, ui/src/app/TopBar.tsx, ui/src/legacy/panels.tsx, .planning/phases/72-human-uat-sign-off/72-CONTEXT.md</read_first>
  <action>
    Run targeted preflight per D-06 (do NOT run npm run check or full test suite). Execute in order: npm run build:ui; npm run lint -- ui/src; cargo check -p rlm-cli -p rlm-core; node --test --test-name-pattern="approval mode contract" tests/domain/recursion/recursive-language-model.test.ts.
    Static contract grep (no server start required): confirm crates/rlm-core/src/control_server/routes.rs registers /api/pause-future-auto-approvals and /api/model-library/download; confirm ui/src/app/TopBar.tsx posts to /api/pause-future-auto-approvals; confirm ui/src/legacy/panels.tsx posts to /api/model-library/download for huggingface installs.
    Create 72-VERIFICATION.md with frontmatter status human_needed, automated passed (or failed if any command fails — fix nothing in this phase unless preflight reveals blocker; document failure in file).
    Record Automated preflight table: each command, PASS/FAIL, notes. Record Static contract table for the three grep checks.
    Set Human verification section: pending operator UAT using 72-UAT.md; resume signal "approved".
  </action>
  <acceptance_criteria>
    - npm run build:ui exits 0
    - npm run lint -- ui/src exits 0
    - cargo check -p rlm-cli -p rlm-core exits 0
    - node --test --test-name-pattern="approval mode contract" tests/domain/recursion/recursive-language-model.test.ts exits 0
    - grep -q "pause-future-auto-approvals" crates/rlm-core/src/control_server/routes.rs
    - grep -q "model-library/download" crates/rlm-core/src/control_server/routes.rs
    - test -f .planning/phases/72-human-uat-sign-off/72-VERIFICATION.md
  </acceptance_criteria>
  <verify>
    <automated>npm run build:ui && npm run lint -- ui/src && cargo check -p rlm-cli -p rlm-core && node --test --test-name-pattern="approval mode contract" tests/domain/recursion/recursive-language-model.test.ts</automated>
  </verify>
  <done>72-VERIFICATION.md records all targeted preflight PASS and static route/UI wiring confirmed per D-06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → Rust /api/* | Live UAT exercises untrusted operator actions against local control server |
| Evidence files → git | Screenshots may capture local paths; no secrets in notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | I | 72-UAT.md evidence | accept | Local-only UAT notes; operator redacts secrets |
| T-72-02 | D | False PASS without live test | mitigate | Plan 02 human checkpoint requires operator approval per D-04; no auto-approve |
</threat_model>

<verification>
npm run build:ui && npm run lint -- ui/src && cargo check -p rlm-cli -p rlm-core
72-UAT.md and 72-VERIFICATION.md exist with preflight recorded
</verification>

<success_criteria>
- Merged 10-item checklist ready in 72-UAT.md
- Targeted preflight green; full test suite not run
- Operator runbook documents Rust UI stack start
</success_criteria>

<output>
After completion, create `.planning/phases/72-human-uat-sign-off/72-01-SUMMARY.md`
</output>

---
phase: 01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - crates/rlm-core/src/bootstrap/mod.rs
  - crates/rlm-core/src/bootstrap/cli_runtime.rs
  - crates/rlm-core/src/lib.rs
  - crates/rlm-cli/src/commands/ask.rs
  - crates/rlm-cli/tests/ask_smoke.rs
autonomous: true
requirements: [CLI-01]
must_haves:
  truths:
    - "rlm ask runs RecursiveLanguageModel on Rust path instead of exit 2 stub"
    - "rlm ask --json emits structured result matching TS renderResult shape"
    - "Missing Ollama host produces actionable non-zero exit with clear message"
    - "RLM_RUNTIME=node escape hatch remains documented and functional"
    - "Workflow subcommand documented as staged if not fully implemented in this plan"
  artifacts:
    - path: crates/rlm-cli/src/commands/ask.rs
      provides: "Real ask execution via engine"
    - path: crates/rlm-core/src/bootstrap/cli_runtime.rs
      provides: "Shared config load + build_runtime_context + model resolution"
    - path: crates/rlm-cli/tests/ask_smoke.rs
      provides: "Non-stub exit code test with QueueModel fixture"
  key_links:
    - from: crates/rlm-cli/src/commands/ask.rs
      to: crates/rlm-core/src/bootstrap/cli_runtime.rs
      via: "load project, build runtime, resolve exec model"
      pattern: "build_runtime_context|RecursiveLanguageModel"
    - from: crates/rlm-cli/src/commands/ask.rs
      to: crates/rlm-core/src/domain/recursive_language_model.rs
      via: "engine.run(prompt, config)"
      pattern: "RecursiveLanguageModel::new"
---

<objective>
Replace Rust CLI ask stub with real recursive engine execution, reusing control-server bootstrap patterns.

Purpose: Closes CLI-01 partial requirement per D-01 Wave 3 — ask-first; workflow staged per Claude's discretion.
Output: Working `rlm ask`, shared bootstrap helper, smoke tests; RLM_RUNTIME=node documented as fallback.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-CONTEXT.md
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-RESEARCH.md
@crates/rlm-cli/src/commands/ask.rs
@crates/rlm-core/src/control_server/mod.rs
@scripts/rlm-runtime.mjs

<interfaces>
Current ask stub (crates/rlm-cli/src/commands/ask.rs):
```rust
pub fn run(prompt: Vec<String>, json: bool) -> Result<(), Box<dyn std::error::Error>> {
    // exits 2 with stub message
}
```

Bootstrap pattern from control_server/mod.rs (extract, do not duplicate):
- load_project_config(project_root, config_path)
- build_runtime_context(BuildRuntimeContextInput { project_root, project_config, on_init_stage })
- resolve_language_models / exec model selection
- RecursiveLanguageModel::new(exec_model, trace, runtime.tools)
- engine.run(&prompt, config, None).await

TS output reference: src/cli/render.ts renderResult — JSON mode prints structured answer; text mode stderr narrative.

RLM_RUNTIME=node in scripts/rlm-runtime.mjs must remain — update stub message to mention Rust path now works for ask.

Workflow discretion (D-01): Stage 1 = ask only. If time permits, add workflow.rs stub pointing to graph sidecar path with actionable error — do NOT block ask on full workflow parity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CLI bootstrap helper from control-server patterns</name>
  <files>crates/rlm-core/src/bootstrap/mod.rs, crates/rlm-core/src/bootstrap/cli_runtime.rs, crates/rlm-core/src/lib.rs</files>
  <action>
    Per RESEARCH Pattern 2: create bootstrap/cli_runtime.rs with async function (e.g. build_cli_runtime or prepare_ask_execution) that:
    1. Resolves project root and loads project config via existing persistence helpers
    2. Calls build_runtime_context (includes MCP from plan 01-03)
    3. Resolves execution language model using same logic as RouterState in control_server/mod.rs — extract shared helpers rather than duplicating model resolution
    4. Returns struct with loaded config, runtime context, exec model, and RecursiveModelConfig
    Export from lib.rs. Do not import CLI into rlm-core — rlm-cli depends on rlm-core only.
    Detect missing Ollama host: if resolution falls back to queue/canned models in non-test context, return Err with actionable message (RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>cargo test -p rlm-core --lib bootstrap 2>/dev/null || cargo check -p rlm-core && cargo check -p rlm-cli</automated>
  </verify>
  <done>Shared CLI bootstrap helper compiles; model resolution extracted not duplicated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rlm ask command</name>
  <files>crates/rlm-cli/src/commands/ask.rs</files>
  <behavior>
    - Test: rlm ask with QueueModel fixture returns exit 0 and prints answer (not stub exit 2)
    - Test: rlm ask --json emits JSON with ok:true and answer field
    - Test: empty prompt returns non-zero with usage message
  </behavior>
  <action>
    Per D-01 Wave 3 / CLI-01: replace stub in ask.rs. Join prompt args, validate non-empty.
    Call bootstrap helper, construct InMemoryTrace or no-op trace, RecursiveLanguageModel::new, await engine.run.
    --json flag: print serde_json result matching TS shape (ok, answer/summary fields — read render.ts for keys).
    Text mode: print answer to stdout, diagnostics to stderr.
    Exit 0 on success; non-zero on config/model errors with actionable messages.
    Update stub message removal — no longer exit 2 by default.
    Keep scripts/rlm-runtime.mjs behavior unchanged; add comment in ask.rs that RLM_RUNTIME=node still available for workflow/other modes.
  </action>
  <verify>
    <automated>cargo test -p rlm-cli ask</automated>
  </verify>
  <done>rlm ask executes engine; no stub exit 2 on happy path</done>
</task>

<task type="auto">
  <name>Task 3: Ask smoke test and workflow staging note</name>
  <files>crates/rlm-cli/tests/ask_smoke.rs, crates/rlm-cli/src/commands/ask.rs</files>
  <action>
    Create ask_smoke.rs integration test: temp project dir with minimal rlm.config.yaml, QueueModel or test fixture config, run ask subcommand via assert_cmd or direct fn call, assert exit code 0 and output contains non-stub content.
    Per Claude's discretion on workflow: if workflow command is still stub, update its error message to state "use rlm ask for single prompts; graph workflow via rlm ui or RLM_RUNTIME=node" — do not implement full GraphExecutor CLI unless bootstrap reuse is trivial. Document staged workflow in 01-04-SUMMARY.md if deferred.
    Run npm run check:rust smoke.
  </action>
  <verify>
    <automated>cargo test -p rlm-cli && npm run check:rust</automated>
  </verify>
  <done>ask_smoke test passes; workflow scope documented in summary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → Engine prompt | User-supplied prompt text executed by recursive model |
| CLI → Local Ollama | Model host on localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Prompt injection to model | accept | Standard LLM trust model; no elevated privileges |
| T-01-09 | Information Disclosure | --json error output | mitigate | Safe user-facing messages; no internal paths in errors |
</threat_model>

<verification>
- cargo test -p rlm-cli passes
- rlm ask no longer exits 2 on valid fixture project
- npm run check:rust passes
</verification>

<success_criteria>
- CLI-01 ask path functional on Rust runtime
- RLM_RUNTIME=node escape hatch preserved
- Workflow explicitly staged or implemented with graph sidecar load
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-04-SUMMARY.md`
</output>

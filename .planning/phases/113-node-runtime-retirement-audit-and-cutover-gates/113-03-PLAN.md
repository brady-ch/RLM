---
phase: 113-node-runtime-retirement-audit-and-cutover-gates
plan: 03
type: execute
wave: 2
depends_on:
  - "113-01"
files_modified:
  - .planning/notes/rust-only-runtime-migration-decisions.md
  - .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
autonomous: true
requirements:
  - AUDIT-113-03
  - AUDIT-113-04
user_setup: []
must_haves:
  truths:
    - "Each teardown phase 114–120 has explicit pass/fail verification commands"
    - "Rust golden fixtures documented as sole HTTP contract gate after Phase 114"
    - "TS control-server fixture test and compare-runtimes marked for retirement in Phase 114"
    - "CI gate evolution path documented (check:parity → Rust-only)"
  artifacts:
    - path: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
      provides: "Per-phase verification gate checklist for executors"
      contains: "## Phase 114"
    - path: .planning/notes/rust-only-runtime-migration-decisions.md
      provides: "Updated verification gates section with concrete commands"
      contains: "control_server_matches_golden_fixtures"
  key_links:
    - from: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
      to: crates/rlm-core/tests/control_server_fixtures.rs
      via: "golden fixture gate reference"
      pattern: "control_server_matches_golden_fixtures"
    - from: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
      to: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
      via: "inventory cross-reference"
      pattern: "113-AUDIT.md"
---

<objective>
Document per-layer verification gates for Phases 114–120 and establish Rust golden fixtures as the sole HTTP contract gate post-114.

Purpose: Downstream deletion phases need executable pass/fail commands, not prose intentions. CI must not boot TS server after Phase 114.
Output: `113-GATES.md` plus expanded verification section in migration note.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/notes/rust-only-runtime-migration-decisions.md
@.planning/ROADMAP.md
@crates/rlm-core/tests/control_server_fixtures.rs
@package.json

<interfaces>
Golden fixture test (Rust — becomes sole HTTP gate post-114):
```rust
// crates/rlm-core/tests/control_server_fixtures.rs
async fn control_server_matches_golden_fixtures()
```

Current dual-runtime parity CI (retire TS portions in Phase 114):
```json
"check:parity": "npm run build && node --test dist/tests/integration/control-server-fixtures.test.js && node scripts/parity/compare-runtimes.mjs && cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture"
```

Standard per-deletion gate from migration note:
- `cargo test -p rlm-core` green after each deletion step
- `RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui` serves UI against Rust APIs
- Tauri dev/build smoke after control-server and CLI phases

RAM gate note (from STATE.md): `control_server_matches_golden_fixtures` may fail on low-RAM hosts — document `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures` as the agent-safe invocation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 113-GATES.md per-phase checklist</name>
  <files>.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md</files>
  <read_first>
    - .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
    - .planning/ROADMAP.md (Phases 114–120 success criteria)
    - .planning/notes/rust-only-runtime-migration-decisions.md
    - package.json (check:parity script)
  </read_first>
  <action>
Create `113-GATES.md` with title `# Phase 113: Per-Phase Verification Gates`.

For each phase 114–120, add a section `## Phase {N}: {name from ROADMAP}` containing:

**Scope:** one-line from ROADMAP goal
**Preconditions:** prior phase gates passed
**Delete:** paths from 113-AUDIT.md for this phase
**Keep:** paths explicitly not touched
**Gate commands:** numbered list of exact shell commands with expected exit code 0
**Rollback:** one-line `git checkout` hint if gate fails

Required gate commands by phase:

**Phase 114:**
1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures -- --nocapture`
2. `RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui --port 0` (smoke: server prints listening URL)
3. Verify deleted: `test ! -d src/application/control-server`
4. Verify deleted: `test ! -f tests/integration/control-server-fixtures.test.ts`
5. `check:parity` must NOT contain TS fixture test (document target script shape for Phase 114 executor)

**Phase 115:**
1. `cargo test -p rlm-core`
2. `npm run rlm -- --help` (via dispatcher → Rust CLI)
3. Verify: `test ! -f src/index.ts`
4. Tauri smoke: `cargo check -p rlm-tauri` or document `npm run tauri:dev` manual checkpoint

**Phase 116:**
1. `cargo test -p rlm-core`
2. Verify: `test ! -d src/application`
3. `npm run rlm -- ask --help` (Rust ask path)

**Phase 117:**
1. `cargo test -p rlm-core`
2. Verify: `test ! -d src/domain && test ! -d src/ports`

**Phase 118:**
1. `cargo test -p rlm-core`
2. Verify: `test ! -d src/adapters && test ! -d src/plugins`
3. Verify: `test ! -d tests/domain` (mirrored tests removed)

**Phase 119:**
1. `npm run check:rust`
2. `npm run build:ui`
3. Verify: `npm run typecheck` removed or no-op documented
4. `npm run test:agent:verify:light`

**Phase 120:**
1. `cargo test -p rlm-core`
2. Reference `.planning/research/TOOL-CALLING-CONSTRAINED-DECODING.md` for envelope-specific tests

Add section `## HTTP contract gate (post-114)` stating:
- **Sole gate:** `control_server_matches_golden_fixtures` in `crates/rlm-core/tests/control_server_fixtures.rs`
- **Retired in 114:** `tests/integration/control-server-fixtures.test.ts`, `scripts/parity/compare-runtimes.mjs` TS server boot
- **Fixture files:** `tests/fixtures/control-server/*.json` (shared data, kept)
  </action>
  <acceptance_criteria>
    - `113-GATES.md` exists with sections for Phases 114, 115, 116, 117, 118, 119, and 120
    - Each phase section contains subheading `Gate commands:`
    - Section `## HTTP contract gate (post-114)` exists
    - Section mentions `control_server_matches_golden_fixtures` by exact name
    - Section mentions retirement of `control-server-fixtures.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md && grep -q 'control_server_matches_golden_fixtures' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md && grep -cE '^## Phase 11[4-9]|^## Phase 120' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md | awk '$1 >= 7'</automated>
  </verify>
  <done>Executable gate checklist exists for all teardown phases</done>
</task>

<task type="auto">
  <name>Task 2: Expand migration note verification gates section</name>
  <files>.planning/notes/rust-only-runtime-migration-decisions.md</files>
  <read_first>
    - .planning/notes/rust-only-runtime-migration-decisions.md
    - .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
  </read_first>
  <action>
In `.planning/notes/rust-only-runtime-migration-decisions.md`, replace the `## Verification gates (per phase)` section with an expanded version:

1. Keep the four bullet principles from the original (cargo test, golden fixtures Rust-only, RLM_UI_DIST ui serve, Tauri smoke).

2. Add sub-section `### Per-phase commands` with a table:

| Phase | Primary gate | Secondary smoke |
|-------|-------------|-----------------|
| 113 | `npm run test:rlm-runtime` | Audit docs exist |
| 114 | `cargo test ... control_server_matches_golden_fixtures` | `RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui` |
| 115–118 | `cargo test -p rlm-core` | `npm run rlm -- --help` |
| 119 | `npm run check:rust` + `npm run build:ui` | `test:agent:verify:light` |
| 120 | `cargo test -p rlm-core` | envelope tests per TOOL-CALLING doc |

3. Add sub-section `### HTTP contract (post-114)`:
- Golden fixtures in Rust only — `control_server_matches_golden_fixtures`
- TS fixture test deleted Phase 114 — do not boot TS server in CI
- Target `check:parity` after 114: `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core control_server_matches_golden_fixtures`

4. Add link: `Detailed checklist: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md`

5. Document RAM gate: use `scripts/cargo-with-ram-gate.mjs` for golden fixture in agent/autonomous runs.
  </action>
  <acceptance_criteria>
    - Migration note contains `### Per-phase commands` sub-section
    - Migration note contains `### HTTP contract (post-114)` sub-section
    - Migration note contains exact string `control_server_matches_golden_fixtures`
    - Migration note links to `113-GATES.md`
    - Table includes row for Phase 113 with `test:rlm-runtime`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '### Per-phase commands' .planning/notes/rust-only-runtime-migration-decisions.md && grep -q '### HTTP contract (post-114)' .planning/notes/rust-only-runtime-migration-decisions.md && grep -q '113-GATES.md' .planning/notes/rust-only-runtime-migration-decisions.md</automated>
  </verify>
  <done>Migration note verification section expanded with concrete commands and post-114 HTTP gate policy</done>
</task>

<task type="auto">
  <name>Task 3: Cross-link audit, gates, and ROADMAP success criteria</name>
  <files>.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 113 success criteria)
    - .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
    - .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
  </read_first>
  <action>
Append section `## Phase 113 completion checklist` to `113-GATES.md` mapping ROADMAP success criteria to verification:

| # | ROADMAP success criterion | Verification command / artifact |
|---|---------------------------|--------------------------------|
| 1 | TS-only path inventory documented | `test -f 113-AUDIT.md` + deletion order section |
| 2 | Default npm rlm dispatches to Rust | `npm run test:rlm-runtime` |
| 3 | Per-phase verification gates written | `test -f 113-GATES.md` + migration note updated |
| 4 | Rust golden fixtures sole HTTP gate post-114 | `113-GATES.md` HTTP contract section |

Add `## References` section listing:
- `113-AUDIT.md`
- `113-GATES.md` (self)
- `.planning/notes/rust-only-runtime-migration-decisions.md`
- `crates/rlm-core/tests/control_server_fixtures.rs`
- `tests/fixtures/control-server/`

No ROADMAP.md edit in this task — orchestrator/planner updates plan list separately.
  </action>
  <acceptance_criteria>
    - `113-GATES.md` contains `## Phase 113 completion checklist`
    - Checklist table has 4 rows matching ROADMAP success criteria
    - `113-GATES.md` contains `## References` with path to `113-AUDIT.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Phase 113 completion checklist' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md && grep -q '113-AUDIT.md' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md</automated>
  </verify>
  <done>Phase 113 success criteria traceable to concrete artifacts and commands</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate docs → Phase 114 executor | Incorrect gate docs could leave TS server in CI after claimed removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | I | 113-GATES.md | mitigate | Copy exact command strings from package.json and existing test names |
| T-113-06 | D | check:parity evolution | mitigate | Document explicit target script for Phase 114; do not modify check:parity in Phase 113 |
</threat_model>

<verification>
- All phase 114–120 sections present in 113-GATES.md
- Migration note cross-links gates doc
- Phase 113 completion checklist maps to ROADMAP success criteria
</verification>

<success_criteria>
- Per-phase verification gates written with executable commands
- Rust golden fixtures identified as sole HTTP contract gate post-114
- Phase 113 deliverables traceable to ROADMAP success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-03-SUMMARY.md`
</output>

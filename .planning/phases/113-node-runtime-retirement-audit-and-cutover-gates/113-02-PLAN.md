---
phase: 113-node-runtime-retirement-audit-and-cutover-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/rlm-runtime.mjs
  - package.json
  - scripts/rlm-runtime.test.mjs
autonomous: true
requirements:
  - AUDIT-113-02
user_setup: []
must_haves:
  truths:
    - "Unqualified npm run rlm dispatches to Rust binary when RLM_RUNTIME is unset"
    - "RLM_RUNTIME=node still reaches Node dist/src/index.js (escape hatch preserved)"
    - "package.json bin.rlm routes through dispatcher, not direct dist entry"
  artifacts:
    - path: scripts/rlm-runtime.mjs
      provides: "Runtime dispatcher defaulting to rust"
      contains: '?? "rust"'
    - path: scripts/rlm-runtime.test.mjs
      provides: "Automated default-runtime behavior test"
      contains: "defaults to rust"
    - path: package.json
      provides: "npm bin entry through dispatcher"
      contains: "scripts/rlm-runtime.mjs"
  key_links:
    - from: package.json
      to: scripts/rlm-runtime.mjs
      via: "bin.rlm and scripts.rlm"
      pattern: "rlm-runtime\\.mjs"
    - from: scripts/rlm-runtime.mjs
      to: target/release/rlm or target/debug/rlm
      via: "runRust() when runtime !== node"
      pattern: "runRust\\(\\)"
---

<objective>
Flip the default runtime from Node to Rust in the dispatcher, align package.json bin entry with the dispatcher, and add an automated test proving default behavior.

Purpose: Cutover signal — `npm rlm` becomes Rust-first while preserving `RLM_RUNTIME=node` escape hatch until Phase 115 removes Node CLI.
Output: Updated dispatcher, package.json bin, and test file.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-CONTEXT.md
@.planning/notes/rust-only-runtime-migration-decisions.md
@scripts/rlm-runtime.mjs
@package.json
@scripts/rust-boundary-check.test.mjs

<interfaces>
Current dispatcher default (line 18):
```javascript
const runtime = (process.env.RLM_RUNTIME ?? "node").toLowerCase();
```

Target default:
```javascript
const runtime = (process.env.RLM_RUNTIME ?? "rust").toLowerCase();
```

Current package.json bin (bypasses dispatcher):
```json
"bin": {
  "rlm": "dist/src/index.js"
}
```

Target bin (use dispatcher):
```json
"bin": {
  "rlm": "scripts/rlm-runtime.mjs"
}
```

Comment block at top of rlm-runtime.mjs must update lines 5–6 to show rust as default.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test for rust default dispatch</name>
  <files>scripts/rlm-runtime.test.mjs</files>
  <read_first>
    - scripts/rlm-runtime.mjs
    - scripts/rust-boundary-check.test.mjs (test pattern reference)
    - package.json
  </read_first>
  <behavior>
    - Test 1: Reading rlm-runtime.mjs source, default fallback is `"rust"` not `"node"` (grep-based, no subprocess boot)
    - Test 2: Spawn `node scripts/rlm-runtime.mjs --help` with RLM_RUNTIME unset and a mock PATH — verify it attempts Rust binary path (check stderr/stdout for cargo build or rlm binary invocation, NOT "Node CLI entry missing")
    - Test 3: Spawn with RLM_RUNTIME=node and missing dist — verify "Node CLI entry missing" or dist/src/index.js path referenced
  </behavior>
  <action>
Create `scripts/rlm-runtime.test.mjs` using `node:test` and `node:assert/strict` (match `scripts/rust-boundary-check.test.mjs` style).

Test `default runtime constant is rust`:
- Read `scripts/rlm-runtime.mjs` as UTF-8
- Assert it matches `/\?\?\s*"rust"/` (the nullish coalescing default)
- Assert it does NOT match `/\?\?\s*"node"/` as the default (node may appear in comments or RLM_RUNTIME=node branch — test the `??` line specifically via regex on `process.env.RLM_RUNTIME ??`)

Test `unset RLM_RUNTIME invokes rust path`:
- Use `spawnSync(process.execPath, ['scripts/rlm-runtime.mjs', '--version'], { env: { ...process.env, RLM_RUNTIME: undefined }, cwd: REPO_ROOT })` 
- If Rust binary missing, expect cargo build attempt OR "Rust rlm binary" in output — NOT "Node CLI entry missing"
- Delete RLM_RUNTIME from env explicitly: `const env = { ...process.env }; delete env.RLM_RUNTIME;`

Test `RLM_RUNTIME=node preserves node path`:
- With RLM_RUNTIME=node and no dist build, spawnSync should produce stderr containing `Node CLI entry missing` OR exit non-zero referencing dist

Do NOT require full cargo build in CI — tests validate dispatch decision, not Rust CLI functionality.
  </action>
  <acceptance_criteria>
    - `scripts/rlm-runtime.test.mjs` exists
    - File contains `import test from "node:test"`
    - File contains test name including `defaults to rust`
    - Running tests before implementation change: default-constant test FAILS (proves red state) OR passes after flip
  </acceptance_criteria>
  <verify>
    <automated>node --import ./scripts/test-ram-preload.mjs --test scripts/rlm-runtime.test.mjs</automated>
  </verify>
  <done>Test file exists; fails on old default or passes once Task 2 completes</done>
</task>

<task type="auto">
  <name>Task 2: Flip default to rust and update file header comments</name>
  <files>scripts/rlm-runtime.mjs</files>
  <read_first>
    - scripts/rlm-runtime.mjs
    - scripts/rlm-runtime.test.mjs
  </read_first>
  <action>
In `scripts/rlm-runtime.mjs`:

1. Change line 18 from `(process.env.RLM_RUNTIME ?? "node")` to `(process.env.RLM_RUNTIME ?? "rust")`.

2. Update file header comment block (lines 3–6) to:
```
 *   RLM_RUNTIME=rust  (default) — cargo-built rlm binary (release preferred)
 *   RLM_RUNTIME=node            — Node dist/src/index.js (escape hatch until Phase 115)
```

3. Do NOT remove the `runNode()` branch or `runtime === "node"` handling — escape hatch must remain functional per migration decisions.

4. Re-run tests from Task 1 — all must pass.
  </action>
  <acceptance_criteria>
    - `scripts/rlm-runtime.mjs` contains `?? "rust"` on the runtime default line
    - Header comment contains `(default)` next to `RLM_RUNTIME=rust`
    - Header comment contains `escape hatch` or `Phase 115` for node path
    - `runNode` function still present in file
  </acceptance_criteria>
  <verify>
    <automated>node --import ./scripts/test-ram-preload.mjs --test scripts/rlm-runtime.test.mjs</automated>
  </verify>
  <done>Default runtime is rust; node escape hatch preserved; tests green</done>
</task>

<task type="auto">
  <name>Task 3: Align package.json bin with dispatcher</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - scripts/rlm-runtime.mjs
  </read_first>
  <action>
In `package.json`:

1. Change `bin.rlm` from `"dist/src/index.js"` to `"scripts/rlm-runtime.mjs"`.

2. Add shebang compatibility note: `rlm-runtime.mjs` already has `#!/usr/bin/env node` — no change needed there.

3. Keep `"rlm": "node scripts/rlm-runtime.mjs"` in scripts section unchanged (already correct).

4. Add npm script `"test:rlm-runtime": "node --import ./scripts/test-ram-preload.mjs --test scripts/rlm-runtime.test.mjs"` for isolated verification (do not wire into full test suite yet — Phase 119 owns CI consolidation).

5. Re-run `node --import ./scripts/test-ram-preload.mjs --test scripts/rlm-runtime.test.mjs` to confirm still green.
  </action>
  <acceptance_criteria>
    - `package.json` contains `"rlm": "scripts/rlm-runtime.mjs"` inside `bin` object
    - `package.json` does NOT contain `"rlm": "dist/src/index.js"` in bin
    - `package.json` contains `"test:rlm-runtime"` script
    - `npm run test:rlm-runtime` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"rlm": "scripts/rlm-runtime.mjs"' package.json && npm run test:rlm-runtime</automated>
  </verify>
  <done>npm bin and npm run rlm both route through Rust-default dispatcher</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dispatcher → CLI binary | Wrong default breaks developer workflows mid-migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-03 | D | scripts/rlm-runtime.mjs | mitigate | Preserve RLM_RUNTIME=node branch; automated tests for both paths |
| T-113-04 | E | package.json bin | mitigate | Route bin through dispatcher; test default constant |
</threat_model>

<verification>
- `npm run test:rlm-runtime` passes
- `grep '?? "rust"' scripts/rlm-runtime.mjs` succeeds
- `RLM_RUNTIME=node npm run rlm -- --help` still attempts Node path (manual spot-check documented in SUMMARY)
</verification>

<success_criteria>
- Default `npm rlm` dispatches to Rust binary
- Node escape hatch preserved via RLM_RUNTIME=node
- Automated test covers default dispatch behavior
</success_criteria>

<output>
After completion, create `.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-02-SUMMARY.md`
</output>

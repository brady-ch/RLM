---
phase: 60.1-close-v1-8-milestone-gaps
plan: 03
type: execute
wave: 2
depends_on: ["60.1-01"]
files_modified:
  - crates/rlm-core/src/execution/session.rs
  - crates/rlm-core/src/execution/session_graph.rs
  - crates/rlm-core/src/graph/workflow.rs
  - crates/rlm-core/src/control_server/routes.rs
  - crates/rlm-core/tests/chat_routes.rs
autonomous: true
gap_closure: true
requirements: [ENGN-02, GRPH-02]
must_haves:
  truths:
    - "POST /api/chat/message returns snapshot with proposal from chat text"
    - "POST /api/chat/apply and /api/chat/cancel mutate pending chat proposals"
    - "POST /api/clarifications/abort stops run from clarification checkpoint"
    - "POST /api/chat/confirm-run respects request body variant and input for pipeline template"
  artifacts:
    - path: crates/rlm-core/src/execution/session.rs
      provides: preview/apply/clear chat mutations and abortRunFromClarification
    - path: crates/rlm-core/src/control_server/routes.rs
      provides: chat refine and clarifications/abort routes
    - path: crates/rlm-core/tests/chat_routes.rs
      provides: route integration tests
  key_links:
    - from: crates/rlm-core/src/control_server/routes.rs
      to: crates/rlm-core/src/execution/session.rs
      via: preview_mutation_from_chat, apply_pending_mutation, abort_run_from_clarification
      pattern: preview_mutation_from_chat
    - from: crates/rlm-core/src/control_server/routes.rs
      to: crates/rlm-core/src/graph/workflow.rs
      via: apply_pipeline_template before confirm_graph_and_run
      pattern: apply_pipeline_template
---

<objective>
Port chat refine panel and clarification abort authority from TS ExecutionController, wire missing Axum routes, and fix chat_confirm_run request body handling.

Purpose: Closes ENGN-02 (clarifications/abort) and GRPH-02 (chat message/apply/cancel + confirm-run variant) partial gaps.
Output: UI chat refine panel and abort checkpoint work on Rust runtime.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-CONTEXT.md
@src/application/control-server/handlers/session.ts
@src/application/execution/execution-controller.ts
@src/application/graph/graph-workflow-serializer.ts
@crates/rlm-core/src/execution/session.rs
@crates/rlm-core/src/control_server/routes.rs

<interfaces>
TS chat handler behavior (session.ts tryChatClarifyStopApproval):
- POST /api/chat/message body { message } → { ...snapshot, proposal }
- POST /api/chat/apply body { proposalId?, deleteStrategy? } → { ...snapshot, applied }
- POST /api/chat/cancel → snapshot with pending mutation cleared
- POST /api/chat/confirm-run body { variant, input } → applies pipeline template when variant=pipeline and input non-empty, then confirmGraphAndRun; response includes runVariant from body
- POST /api/clarifications/abort body { questionId } → snapshot after abort

Rust session currently has: answer_clarification, confirm_graph_and_run, stop — missing preview/apply/clear/abort and pipeline template apply.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold chat_routes integration tests</name>
  <files>crates/rlm-core/tests/chat_routes.rs</files>
  <read_first>crates/rlm-core/tests/graph_executor_routes.rs, tests/domain/recursion/recursive-language-model.test.ts</read_first>
  <behavior>
    - chat_message_returns_proposal: POST chat/message with "edit root-composer: new prompt" returns proposal
    - chat_apply_and_cancel: POST chat/apply applies mutation; POST chat/cancel clears pending
    - chat_confirm_run_pipeline_variant: POST chat/confirm-run with variant pipeline returns runVariant pipeline
    - clarifications_abort: POST clarifications/abort with active pending clarification stops session
    - Tests compile and fail (404 or unimplemented) until Tasks 2–3 wire routes
  </behavior>
  <action>
    Create crates/rlm-core/tests/chat_routes.rs with failing integration tests using start_server before route implementation. Use InteractiveExecutionSession seeded with root node. Per D-05 testing decision — test scaffold first, session methods and routes follow.
  </action>
  <acceptance_criteria>
    - File crates/rlm-core/tests/chat_routes.rs exists with at least 3 async tests
    - `cargo test -p rlm-core chat_routes -- --nocapture` compiles (may fail until routes wired)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core chat_routes -- --nocapture 2>&1 | grep -qE 'test result:|running [0-9]+ test'</automated>
  </verify>
  <done>Failing chat route integration test scaffold exists before wiring</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port chat mutation and clarification abort session methods</name>
  <files>crates/rlm-core/src/execution/session.rs, crates/rlm-core/src/execution/session_graph.rs, crates/rlm-core/src/graph/workflow.rs</files>
  <read_first>src/application/execution/execution-controller.ts (previewMutationFromChat, applyPendingMutation, clearPendingMutation, abortRunFromClarification), src/application/graph/graph-workflow-serializer.ts (applyPipelineTemplate, graphHasPipelineTemplate)</read_first>
  <behavior>
    - preview_mutation_from_chat parses "edit nodeId: prompt" and "add parent: prompt" patterns like TS
    - apply_pending_mutation rejects missing or stale proposalId with MUTATION: errors
    - abort_run_from_clarification rejects unknown questionId
    - graph_has_pipeline_template and apply_pipeline_template match TS pipeline slot behavior
  </behavior>
  <action>
    Add InteractiveExecutionSession methods: preview_mutation_from_chat, apply_pending_mutation, clear_pending_mutation, abort_run_from_clarification. Store pending_mutation in session state (snapshot already exposes pending_mutation field). Port minimal chat command parsing from execution-controller.ts lines 883-990 and abort logic from lines 1209+. Add graph_has_pipeline_template and apply_pipeline_template to graph/workflow.rs mirroring graph-workflow-serializer.ts. Add unit tests in session.rs or crates/rlm-core/tests/ with preview_mutation prefix. Per D-05 chat/clarification decision.
  </action>
  <acceptance_criteria>
    - `grep -n 'pub fn preview_mutation_from_chat' crates/rlm-core/src/execution/session.rs` returns a match
    - `grep -n 'pub fn abort_run_from_clarification' crates/rlm-core/src/execution/session.rs` returns a match
    - `grep -n 'pub fn apply_pipeline_template' crates/rlm-core/src/graph/workflow.rs` returns a match
    - Unit tests cover preview+apply edit command and abort with valid questionId
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core preview_mutation -- --nocapture</automated>
  </verify>
  <done>Session authority supports chat refine and clarification abort without route wiring</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire chat refine, abort, and confirm-run body routes</name>
  <files>crates/rlm-core/src/control_server/routes.rs</files>
  <read_first>src/application/control-server/handlers/session.ts, crates/rlm-core/src/control_server/routes.rs (chat_confirm_run handler)</read_first>
  <behavior>
    - chat_confirm_run reads Json body; runVariant in response reflects request variant not hardcoded "playbook"
    - Pipeline variant with non-empty input applies template before confirm when graph has pipeline template
    - clarifications/abort returns 400 on unknown questionId matching TS MutationError vocabulary
  </behavior>
  <action>
    Register POST /api/chat/message, /api/chat/apply, /api/chat/cancel, /api/clarifications/abort. Rewrite chat_confirm_run to accept Json body with variant (pipeline|playbook) and input string: when variant=pipeline and input non-empty and graph_has_pipeline_template, restore_snapshot with apply_pipeline_template result before confirm_graph_and_run. Return snapshot_with_extra including runVariant from request. Map session errors to ApiError status codes matching existing MUTATION: handling. Per D-05.
  </action>
  <acceptance_criteria>
    - `grep -n 'chat/message' crates/rlm-core/src/control_server/routes.rs` returns a match
    - `grep -n 'clarifications/abort' crates/rlm-core/src/control_server/routes.rs` returns a match
    - chat_confirm_run handler accepts Json body parameter (not State-only)
    - All chat_routes integration tests from Task 1 pass
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core chat_routes -- --nocapture</automated>
  </verify>
  <done>All chat refine and clarification abort routes respond on Rust runtime</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → chat mutation | Untrusted chat text parsed into graph edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.1-06 | I | preview_mutation_from_chat | mitigate | Reject empty messages; validate node IDs exist before apply |
| T-60.1-07 | E | apply_pending_mutation | mitigate | Require matching proposalId when provided; use existing graph mutation ACL |
| T-60.1-08 | D | abort_run_from_clarification | accept | Idempotent abort only for active questionId; low risk stop signal |
</threat_model>

<verification>
- `cargo test -p rlm-core chat_routes` passes
- `npm run check:rust` passes
</verification>

<success_criteria>
- Chat refine panel routes no longer 404
- chat_confirm_run no longer hardcodes runVariant to playbook
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-03-SUMMARY.md`
</output>

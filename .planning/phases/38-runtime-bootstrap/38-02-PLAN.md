---
phase: 38-runtime-bootstrap
plan: "02"
type: execute
wave: 2
depends_on:
  - "38-01"
files_modified:
  - src/index.ts
  - src/cli/run-modes/agent-workflow.ts
  - src/cli/run-modes/plan-node.ts
  - src/cli/run-modes/ui.ts
  - src/cli/run-modes/workflow-graph-io.ts
  - src/cli/run-modes/session-commands.ts
autonomous: true
requirements:
  - BOOT-02
  - BOOT-03
  - REG-01
  - REG-02
must_haves:
  truths:
    - "Each command path that requires full composition imports a function from cli/run-modes/* taking RuntimeContext (or narrowed typed ctx) plus any pre-bootstrap deps only where unavoidable: session/memory stores for early synchronous commands)."
    - "src/index.ts line count lands under 160 lines (stretch <150 BOOT-02) excluding shebang/comments blank lines miscount allowance — target functional slimming."
    - "`npm run check` green REG-02; full tests REG-01."
  artifacts:
    - path: src/cli/run-modes/agent-workflow.ts
      provides: "default ask + workflow + approval gate using ctx"
    - path: src/cli/run-modes/plan-node.ts
      provides: "plan-node interactive session"
    - path: src/cli/run-modes/ui.ts
      provides: "ui control server lifecycle"
    - path: src/cli/run-modes/workflow-graph-io.ts
      provides: "workflow export/import CLI"
    - path: src/cli/run-modes/session-commands.ts
      provides: "session list inspect memory preference handlers"
---

<objective>

Thin `src/index.ts` into parse → early exits → config → `buildRuntimeContext` → dispatch; move run bodies into `cli/run-modes/*` consuming `RuntimeContext` (BOOT-02, BOOT-03).

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/38-runtime-bootstrap/38-CONTEXT.md
@src/index.ts

</context>

<threat_model>

Mitigations:

- Maintain approval wait stdin loop and identical JSON error envelope in main.catch.

Test strategy: Full `npm run check`; spot-check `--help`.

</threat_model>

<tasks>

<task id="38-02-t1" type="auto">

<action>

Implement `workflow-graph-io.ts` exporting `handleWorkflowExport` and `handleWorkflowImport` carrying logic from index (parseWorkflowExportVariant local), `session-commands.ts` for sessionList/sessionInspect/memoryInspect/preference mutations with parsePreferenceAssignment local, `plan-node.ts` exporting `runPlanNodeMode(ctx)`, `ui.ts` exporting `runUiMode(ctx)` preserving session restore paths, `agent-workflow.ts` exporting async `runAskOrWorkflowWithOptionalApproval(ctx)` covering renderResult and requireApproval branching.

Rewrite `main()` in src/index.ts to call these modules after `buildRuntimeContext` for routed commands; retain first-run launcher + help + lightweight branches before bootstrap consistent with refactor order.

Move shared tiny helpers (`setExitCodeIfRunFailed`, `waitForApproval`) next to consumer file or duplicate private if minimal.

</action>

<read_first>

src/index.ts
src/cli/render.ts
src/application/control-server.ts
src/application/ui-execution-runner.ts

</read_first>

<acceptance_criteria>

Ripgrep "`command ===`" occurrences in src/index.ts: at most dispatcher switches (no inlined server startup beyond dispatch calls).
Every file under cli/run-modes exports at least one function used by index.ts.

</acceptance_criteria>

</task>

</tasks>

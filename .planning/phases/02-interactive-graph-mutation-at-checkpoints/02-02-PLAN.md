---
phase: "02"
plan: "02-02"
type: "standard"
wave: 2
depends_on: ["02-01"]
requirements_addressed: ["APRV-02", "APRV-03", "APRV-04", "ERRO-02"]
files_modified:
  - src/application/control-server.ts
  - ui/src/main.tsx
  - ui/src/styles.css
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "Transport layers proxy controller mutation APIs only."
    - "Mutation failures are explicit and structured in UI/CLI paths."
    - "Checkpoint mutation actions are available for edit/add/delete/connect during pause."
---

<objective>
Expose controller mutation APIs through control-server and UI so paused checkpoints support edit/add/delete/connect with explicit, structured failure visibility.
</objective>

<tasks>
  <task id="1" type="implementation">
    <action>Add control-server endpoints for graph mutation intents and ensure they proxy controller responses/status codes without route-level mutation logic.</action>
    <files>
      <file>src/application/control-server.ts</file>
    </files>
    <verify>Endpoint responses include structured error contract on mutation failures.</verify>
    <acceptance_criteria>Server routes remain transport-only and preserve controller authority.</acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <action>Update UI inspector/canvas actions to support add/delete/connect operations at checkpoints and render mutation errors clearly.</action>
    <files>
      <file>ui/src/main.tsx</file>
      <file>ui/src/styles.css</file>
    </files>
    <verify>Manual flow shows successful mutation operations and visible structured errors for invalid requests.</verify>
    <acceptance_criteria>No UI mutation action fails silently; backend-rejected mutations are user-visible.</acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <action>Update CLI rendering path where needed to preserve structured mutation failure context in non-UI runs.</action>
    <files>
      <file>src/cli/render.ts</file>
    </files>
    <verify>CLI output includes useful mutation error context for failed operations during approval checkpoints.</verify>
    <acceptance_criteria>Headless workflows receive explicit mutation failure detail.
</acceptance_criteria>
  </task>

  <task id="4" type="testing">
    <action>Add integration tests covering control-server mutation endpoints and user-visible failure propagation from backend to UI/CLI contracts.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` includes endpoint behavior and structured error assertions for mutation failures.</verify>
    <acceptance_criteria>Failures in mutation transport/surfacing are caught by automated tests.
</acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`
- Run `npm test`
- Validate checkpoint mutation operations and structured failure surfaces in UI.
</verification>

<success_criteria>
- Mutation endpoints exist and proxy controller APIs.
- UI supports checkpoint mutation actions with explicit errors.
- CLI/UI both surface mutation failure context.
- Phase requirements remain fully covered.
</success_criteria>

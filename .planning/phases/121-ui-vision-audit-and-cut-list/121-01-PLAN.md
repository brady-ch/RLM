---
phase: 121-ui-vision-audit-and-cut-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
  - .planning/notes/ui-product-simplification-decisions.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Every auditable .tsx file under ui/src/ (excluding main.tsx and shared/ThemeToggle.tsx) has exactly one Keep/Demote/Delete verdict"
    - "styles.css appears as a separate audit row with split/defer guidance for Phase 124"
    - "Mandatory surfaces RefineGraphPanel, QualityLoopInspector, NodeInspector, WorkflowOverview, GraphWorkflowPanel, AppShell, and styles.css are explicitly scored in summary"
    - "Cut list document states Phase 121 is audit-only — no ui/src edits"
    - "Phase owner column maps Demote/Delete verdicts to Phases 122–127"
  artifacts:
    - path: .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
      provides: "Authoritative UI surface cut list for Phases 122–127"
      contains: "## Summary"
  key_links:
    - from: .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
      to: .planning/notes/ui-product-simplification-decisions.md
      via: "audit framework cross-reference"
      pattern: "ui-product-simplification-decisions"
    - from: .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
      to: .planning/notes/ui-shell-architecture.md
      via: "vision contract reference"
      pattern: "ui-shell-architecture"
---

<objective>
Produce a complete UI surface cut list scoring every auditable component under `ui/src/` against the locked product vision, with Keep/Demote/Delete verdicts and Phase 122–127 owner assignments.

Purpose: Phase 121 is audit-and-documentation only — executors in Phases 122–127 need a single authoritative cut list before any UI removals or refactors.
Output: `121-CUT-LIST.md` in the phase directory.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-ui-vision-audit-and-cut-list/121-CONTEXT.md
@.planning/phases/121-ui-vision-audit-and-cut-list/121-RESEARCH.md
@.planning/phases/121-ui-vision-audit-and-cut-list/121-PATTERNS.md
@.planning/notes/ui-product-simplification-decisions.md
@.planning/notes/ui-shell-architecture.md
@.planning/notes/product-shell-entry-model.md
@.planning/notes/ui-canvas-visual-polish-decision.md
@.planning/ROADMAP.md

<interfaces>
Phase owner mapping (authoritative — cut list MUST use these values):

| Phase | Owner scope |
|-------|-------------|
| 122 | Advanced hub deletes |
| 123 | Workflow chrome (TopBar, RunPanel, WorkflowOverview) |
| 124 | CSS split / dead rule removal |
| 125 | AppShell fetch decomposition |
| 126 | Node inspector & settings slim-down |
| 127 | Lazy routes & bundle impact |

Auditable inventory (25 `.tsx` rows + 1 CSS row = 26 total):

Excluded: `main.tsx`, `shared/ThemeToggle.tsx`

| Directory | Files |
|-----------|-------|
| `app/` | AppShell.tsx, TopBar.tsx, FirstRunLauncher.tsx |
| `canvas/` | GraphCanvas.tsx |
| `nodes/` | ExecutionNodeCard.tsx, GraphActionModal.tsx, NodeContextMenu.tsx, QualityLoopCardSummary.tsx |
| `run-panel/` | RunPanel.tsx, WorkflowOverview.tsx |
| `advanced/` | AdvancedHub.tsx, ModelsView.tsx, PluginsView.tsx, MemoryView.tsx, SettingsView.tsx, SessionsView.tsx, PluginPanel.tsx |
| `advanced/models/` | ModelLibraryPanel.tsx |
| `advanced/memory/` | MemoryPanel.tsx |
| `advanced/sessions/` | SavedSessionPanel.tsx |
| `advanced/settings/` | RefineGraphPanel.tsx, QualityLoopInspector.tsx, NodeInspector.tsx, GraphWorkflowPanel.tsx, inspectorHelpers.tsx |
| `styles.css` | Monolith (~1,803 lines) — separate row |

5-question audit framework (weight order):
1. Graph-first (highest)
2. Default path visibility
3. Recoverable via Advanced
4. Vision match (no domain panels on workflow view)
5. Weight cost (LOC, CSS, bundle KB, mount-time fetches)

Verdicts: Keep · Demote · Delete (exactly one per row)

Research hypotheses to reconcile (initial → final verdict required):
- RefineGraphPanel: Demote or Delete
- QualityLoopInspector: Demote or Delete
- NodeInspector: Demote (slim)
- WorkflowOverview: Demote or trim
- GraphWorkflowPanel: Demote
- AppShell.tsx: Keep structure; flag Phase 125 decomposition
- styles.css: Demote monolith structure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate ui/src inventory and audit framework section</name>
  <files>.planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md</files>
  <read_first>
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CONTEXT.md
    - .planning/notes/ui-product-simplification-decisions.md
    - .planning/milestones/v1.18-phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
    - ui/src/ (directory listing)
  </read_first>
  <action>
Create `121-CUT-LIST.md` with title `# Phase 121: UI Vision Cut List`.

First line after title must state:
**Phase 121 scope is audit-only — no component removals, refactors, or CSS edits in this phase.**

Add section `## Audit framework` copying the 5-question framework verbatim from `.planning/notes/ui-product-simplification-decisions.md` lines 20–30.

Run these commands and embed verified counts in an `## Inventory` section:

```bash
find ui/src -type f -name '*.tsx' | sort
find ui/src -type f -name '*.tsx' | wc -l
wc -l ui/src/styles.css
```

Document exclusions explicitly:
- `main.tsx` — bootstrap, excluded
- `shared/ThemeToggle.tsx` — shared primitive without domain fetch weight, excluded

Add section `## Phase owner summary` with the phase owner table from interfaces above.

Add `## References` listing:
- `.planning/notes/ui-shell-architecture.md`
- `.planning/notes/product-shell-entry-model.md`
- `.planning/notes/ui-canvas-visual-polish-decision.md`
- `.planning/notes/desktop-product-vision.md`
  </action>
  <acceptance_criteria>
    - `121-CUT-LIST.md` exists with heading `# Phase 121: UI Vision Cut List`
    - Document contains phrase `audit-only — no component removals`
    - Document contains heading `## Audit framework` with all 5 numbered questions
    - Document contains heading `## Inventory` with `.tsx` file count matching `find ui/src -type f -name '*.tsx' | wc -l`
    - Document lists `styles.css` line count from `wc -l ui/src/styles.css`
    - Document contains heading `## Phase owner summary`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q 'audit-only — no component removals' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q '## Audit framework' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q '## Phase owner summary' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md</automated>
  </verify>
  <done>Cut list scaffold with inventory counts and audit framework present</done>
</task>

<task type="auto">
  <name>Task 2: Score all surfaces with verdict table grouped by directory</name>
  <files>.planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md</files>
  <read_first>
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md (from Task 1)
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-RESEARCH.md (inventory hypotheses and spec drift)
    - .planning/notes/ui-shell-architecture.md
    - ui/src/app/AppShell.tsx
    - ui/src/app/TopBar.tsx
    - ui/src/run-panel/RunPanel.tsx
    - ui/src/run-panel/WorkflowOverview.tsx
    - ui/src/advanced/settings/RefineGraphPanel.tsx
    - ui/src/advanced/settings/QualityLoopInspector.tsx
    - ui/src/advanced/settings/NodeInspector.tsx
    - ui/src/advanced/settings/GraphWorkflowPanel.tsx
    - tests/ui/shell-boundaries.test.ts
  </read_first>
  <action>
Append section `## Verdict table` to `121-CUT-LIST.md`.

Create directory subsections in this order: `app/`, `canvas/`, `nodes/`, `run-panel/`, `advanced/` (including nested models/, memory/, sessions/, settings/), then `styles.css`.

Each subsection contains a markdown table with columns:
`| Surface | Path | Verdict | Rationale | Phase owner |`

Rules:
- **Verdict** — exactly one of: Keep, Demote, Delete
- **Rationale** — one line citing primary audit criterion (e.g., "Graph-first core surface" or "Spec drift: RunPanel shows overview when no node selected")
- **Phase owner** — 122, 123, 124, 125, 126, 127, or `—` for Keep-with-no-change

Score all 25 auditable `.tsx` files. Add row 26 for `styles.css`:
- Surface: `styles.css (monolith)`
- Path: `ui/src/styles.css`
- Verdict: Demote
- Rationale: Keep styles; split monolith structure deferred to Phase 124 — include top CSS concern clusters in rationale (canvas, node cards, run panel, advanced hub)
- Phase owner: 124

Mandatory surface scoring (must appear in table with these minimum dispositions):
- RefineGraphPanel → Demote or Delete (Phase 122 or 126)
- QualityLoopInspector → Demote or Delete (Phase 126)
- NodeInspector → Demote (Phase 126)
- WorkflowOverview → Demote (Phase 123) unless Delete with recoverability rationale
- GraphWorkflowPanel → Demote (Phase 126)
- AppShell.tsx → Keep with decomposition flag (Phase 125)
- styles.css → Demote (Phase 124)

Keep anchors (must be Keep):
- GraphCanvas.tsx, ExecutionNodeCard.tsx, NodeContextMenu.tsx, GraphActionModal.tsx
- RunPanel.tsx (slim approve/clarify — note spec drift to fix in 123)
- FirstRunLauncher.tsx
- AdvancedHub.tsx and tab views ModelsView, PluginsView, SessionsView, MemoryView, SettingsView

No edits under `ui/src/` in this task.
  </action>
  <acceptance_criteria>
    - `121-CUT-LIST.md` contains heading `## Verdict table`
    - Verdict table has at least 26 data rows with Verdict column values matching `(Keep|Demote|Delete)`
    - Rows exist for all mandatory surfaces: RefineGraphPanel, QualityLoopInspector, NodeInspector, WorkflowOverview, GraphWorkflowPanel, AppShell.tsx, styles.css
    - Every Demote or Delete row has Phase owner 122–127 (not `—`)
    - GraphCanvas.tsx row has Verdict Keep
    - main.tsx and ThemeToggle.tsx do NOT appear as verdict rows
  </acceptance_criteria>
  <verify>
    <automated>grep -cE '\| (Keep|Demote|Delete) \|' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md | awk '$1 >= 26' && grep -q 'RefineGraphPanel' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q 'QualityLoopInspector' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q 'NodeInspector' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q 'WorkflowOverview' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q 'GraphWorkflowPanel' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q 'AppShell' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q 'styles.css' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md</automated>
  </verify>
  <done>All 26 audit rows scored with verdicts and phase owners</done>
</task>

<task type="auto">
  <name>Task 3: Add summary counts, mandatory callouts, and cross-references</name>
  <files>
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
    - .planning/notes/ui-product-simplification-decisions.md
  </files>
  <read_first>
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md (from Tasks 1–2)
    - .planning/milestones/v1.18-phases/113-node-runtime-retirement-audit-and-cutover-gates/113-VERIFICATION.md (Ready for Phase pattern)
    - .planning/notes/ui-product-simplification-decisions.md
  </read_first>
  <action>
Append section `## Summary` to `121-CUT-LIST.md` with verdict count table:

```markdown
| Verdict | Count |
|---------|-------|
| Keep | N |
| Demote | N |
| Delete | N |
```

Counts must match the Verdict table rows.

Append subsection `### Mandatory scored surfaces`:

```markdown
| Surface | Verdict | Phase owner |
|---------|---------|-------------|
| RefineGraphPanel | {verdict} | {owner} |
| QualityLoopInspector | {verdict} | {owner} |
| NodeInspector | {verdict} | {owner} |
| WorkflowOverview | {verdict} | {owner} |
| GraphWorkflowPanel | {verdict} | {owner} |
| AppShell.tsx | {verdict} | {owner} |
| styles.css | {verdict} | {owner} |
```

Append subsection `### Spec drift callouts` listing at least:
- RunPanel renders WorkflowOverview when no node selected (conflicts with ui-shell-architecture.md)
- NodeInspector duplicates node card / context menu actions
- TopBar exceeds thin-bar contract (run-variant, approval mode, theme toggle)
- AdvancedHub eager-imports all tabs (Phase 127 bundle concern)

Append footer section:

```markdown
## Ready for Phase 122

All Phase 121 deliverables present. Phase 122 executor should use `121-CUT-LIST.md` Delete/Demote rows with Phase owner 122 for Advanced hub pruning; Phases 123–127 use Phase owner column.
```

In `.planning/notes/ui-product-simplification-decisions.md`, under `## Phase sequence (121–128)` item 1, append parenthetical:
`(cut list: .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md)`
  </action>
  <acceptance_criteria>
    - `121-CUT-LIST.md` contains heading `## Summary` with Keep/Demote/Delete counts
    - `121-CUT-LIST.md` contains heading `### Mandatory scored surfaces` with 7 rows
    - `121-CUT-LIST.md` contains heading `## Ready for Phase 122`
    - `121-CUT-LIST.md` contains heading `### Spec drift callouts` with at least 3 bullet items
    - `ui-product-simplification-decisions.md` references `121-CUT-LIST.md`
    - `git diff --quiet ui/src/` — no ui/src changes after this plan
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Summary' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q '### Mandatory scored surfaces' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q '## Ready for Phase 122' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && grep -q '121-CUT-LIST.md' .planning/notes/ui-product-simplification-decisions.md && grep -A20 '### Spec drift callouts' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md | grep -cE '^- ' | awk '$1 >= 3' && git diff --quiet ui/src/</automated>
  </verify>
  <done>Summary, mandatory callouts, and downstream cross-references complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cut list → executor | Inaccurate verdicts cause wrong deletions in Phases 122–127 |
| Audit doc → product vision | Misaligned scores undermine v1.19 simplification goal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | I | 121-CUT-LIST.md | mitigate | Generate inventory from `find`/`wc` commands; read source files for spec drift |
| T-121-02 | D | Delete verdict on approve/clarify UI | mitigate | Q3 recoverability check; Keep RunPanel approve paths; cross-check shell-boundaries.test.ts |
</threat_model>

<verification>
- `121-CUT-LIST.md` exists with all required sections
- 26 verdict rows present; 7 mandatory surfaces in summary
- Migration note cross-references cut list doc
- No files under `ui/src/` modified (audit-only phase)
</verification>

<success_criteria>
- Every auditable UI surface mapped to Keep/Demote/Delete verdict
- Mandatory surfaces explicitly scored in summary
- Cut list committed and cross-referenced for Phases 122–127
</success_criteria>

<output>
After completion, create `.planning/phases/121-ui-vision-audit-and-cut-list/121-01-SUMMARY.md`
</output>

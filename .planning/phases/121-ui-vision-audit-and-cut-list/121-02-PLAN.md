---
phase: 121-ui-vision-audit-and-cut-list
plan: 02
type: execute
wave: 2
depends_on:
  - "121-01"
files_modified:
  - tests/ui/cut-list-completeness.test.ts
  - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Automated test asserts every auditable ui/src .tsx file appears in 121-CUT-LIST.md"
    - "Automated test asserts mandatory scored surfaces appear in cut list"
    - "Bundle baseline (JS/CSS KB) recorded in cut list summary for Phase 127 comparison"
    - "tests/ui/cut-list-completeness.test.ts passes via node --test"
  artifacts:
    - path: tests/ui/cut-list-completeness.test.ts
      provides: "Static gate that cut list covers full UI inventory"
      contains: "121-CUT-LIST.md"
    - path: .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
      provides: "Bundle baseline line in summary"
      contains: "Bundle baseline"
  key_links:
    - from: tests/ui/cut-list-completeness.test.ts
      to: .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
      via: "reads cut list and compares to find ui/src inventory"
      pattern: "121-CUT-LIST"
    - from: tests/ui/cut-list-completeness.test.ts
      to: tests/ui/shell-boundaries.test.ts
      via: "same test runner pattern"
      pattern: "node --import tsx --test"
---

<objective>
Add automated cut-list completeness verification and record pre-milestone bundle baseline in the cut list summary.

Purpose: Prevent Phase 122+ executors from relying on an incomplete inventory; give Phase 127 a measured starting point.
Output: `tests/ui/cut-list-completeness.test.ts` and updated `121-CUT-LIST.md` summary section.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
@.planning/phases/121-ui-vision-audit-and-cut-list/121-RESEARCH.md
@tests/ui/shell-boundaries.test.ts
@ui/vite.config.ts

<interfaces>
Auditable files (must appear in cut list Path or Surface column):
- All `ui/src/**/*.tsx` except `main.tsx` and `shared/ThemeToggle.tsx`
- `ui/src/styles.css`

Mandatory surface names (must appear in cut list text):
RefineGraphPanel, QualityLoopInspector, NodeInspector, WorkflowOverview, GraphWorkflowPanel, AppShell, styles.css

Bundle baseline capture command:
```bash
npm run build:ui 2>&1 | tail -20
```
Record dist asset sizes from Vite output (e.g., `522.60 kB` JS, `45.52 kB` CSS from research).

Test invocation (match existing ui test pattern):
```bash
node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/cut-list-completeness.test.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cut-list-completeness static test</name>
  <files>tests/ui/cut-list-completeness.test.ts</files>
  <read_first>
    - tests/ui/shell-boundaries.test.ts
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CONTEXT.md
  </read_first>
  <action>
Create `tests/ui/cut-list-completeness.test.ts` using Node.js built-in `node:test` and `node:assert/strict`.

Implementation requirements:
1. Read cut list from `.planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md` (resolve path relative to repo root via `import.meta.url` or `process.cwd()` — match pattern in `shell-boundaries.test.ts`)
2. Glob or `readdir` all `ui/src/**/*.tsx` files; exclude `main.tsx` and `shared/ThemeToggle.tsx`
3. For each auditable file, assert its basename (e.g., `AppShell.tsx`) appears in cut list content
4. Assert cut list contains each mandatory surface string: RefineGraphPanel, QualityLoopInspector, NodeInspector, WorkflowOverview, GraphWorkflowPanel, AppShell, styles.css
5. Assert cut list contains phrase `audit-only`
6. Count verdict rows: assert at least 26 matches of `| Keep |`, `| Demote |`, or `| Delete |` in table rows (use regex on `\| (Keep|Demote|Delete) \|`)

Do not import React or mount components — file read only.
  </action>
  <acceptance_criteria>
    - `tests/ui/cut-list-completeness.test.ts` exists
    - File imports from `node:test` and `node:assert/strict`
    - Running `node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/cut-list-completeness.test.ts` exits 0
    - Test fails if any auditable `.tsx` basename is removed from cut list (verify by reading test logic)
  </acceptance_criteria>
  <verify>
    <automated>node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/cut-list-completeness.test.ts</automated>
  </verify>
  <done>Cut list completeness test passes against 121-CUT-LIST.md</done>
</task>

<task type="auto">
  <name>Task 2: Record bundle baseline in cut list summary</name>
  <files>.planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md</files>
  <read_first>
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-RESEARCH.md (bundle sizes)
    - ui/vite.config.ts
  </read_first>
  <action>
For autonomous executors: **prefer research fallback values** (522.60 kB JS / 45.52 kB CSS) without running build. Only run `npm run build:ui` when RAM gate allows (`RAM_GATE_DISABLED=1` or sufficient MemAvailable per `scripts/lib/ram-gate.mjs`).

If build is run, capture dist asset size lines from Vite output.

Append to `121-CUT-LIST.md` under `## Summary` a subsection:

```markdown
### Bundle baseline (pre-v1.19)

| Asset | Size | Notes |
|-------|------|-------|
| JS (main chunk) | {size from build} | Single chunk, no lazy routes yet — Phase 127 target |
| CSS | {size from build} | From styles.css monolith — Phase 124 target |
| Build date | {YYYY-MM-DD} | Captured during Phase 121 audit |
```

If build fails due to RAM gate, use research doc values (522.60 kB JS / 45.52 kB CSS) and note `(from research — build skipped due to RAM gate)`.

Re-run completeness test after edit to confirm still green.
  </action>
  <acceptance_criteria>
    - `121-CUT-LIST.md` contains heading `### Bundle baseline (pre-v1.19)`
    - Table includes JS and CSS size values (numeric kB)
    - `node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/cut-list-completeness.test.ts` exits 0
    - No changes under `ui/src/` except none expected — only docs and test file modified
  </acceptance_criteria>
  <verify>
    <automated>grep -q '### Bundle baseline (pre-v1.19)' .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md && node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/cut-list-completeness.test.ts</automated>
  </verify>
  <done>Bundle baseline recorded; completeness test still passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → cut list | Stale test file list causes false failures or missed gaps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-03 | I | cut-list-completeness.test.ts | mitigate | Derive auditable file list from filesystem, not hardcoded stale array |
</threat_model>

<verification>
- `tests/ui/cut-list-completeness.test.ts` passes
- Bundle baseline present in cut list
- `ui/src/` unchanged
</verification>

<success_criteria>
- Automated gate ensures cut list covers full UI inventory
- Bundle baseline documented for Phase 127 measurement
</success_criteria>

<output>
After completion, create `.planning/phases/121-ui-vision-audit-and-cut-list/121-02-SUMMARY.md`
</output>

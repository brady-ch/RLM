---
phase: 120-constrained-ollama-tool-envelope-rust
plan: 02
type: execute
wave: 2
depends_on:
  - "120-01"
files_modified:
  - crates/rlm-core/src/adapters/ollama_language_model/mod.rs
  - crates/rlm-core/src/adapters/ollama_language_model/request.rs
  - crates/rlm-core/src/adapters/ollama_language_model/response.rs
  - crates/rlm-core/tests/adapters/ollama_language_model.rs
autonomous: true
requirements:
  - RETIRE-120-03
user_setup: []
must_haves:
  truths:
    - "When response_format is set, Ollama chat body includes format and omits tools array"
    - "Adapter maps parsed envelope to LanguageModelResponse content or tool_calls"
    - "Envelope path does not combine tools and format in same request (ollama#8095)"
  artifacts:
    - path: crates/rlm-core/src/adapters/ollama_language_model/request.rs
      provides: "build_chat_body accepts optional format"
      contains: "format"
    - path: crates/rlm-core/src/adapters/ollama_language_model/mod.rs
      provides: "complete() envelope branch"
  key_links:
    - from: OllamaLanguageModel.complete
      to: build_chat_body
      via: "format without tools when response_format Some"
      pattern: "\"format\""
    - from: OllamaLanguageModel.complete
      to: parse_envelope_response
      via: "JSON content parse after stream"
      pattern: "parse_envelope"
---

<objective>
Wire the Ollama adapter to use Ollama `format` (JSON Schema envelope) instead of `tools` when `response_format` is provided.

Purpose: ROADMAP criterion 3 — token-level constrained decoding via GBNF; research doc forbids tools+format in same request.
Output: Adapter envelope path with unit tests for request body shape and response mapping.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-constrained-ollama-tool-envelope-rust/120-CONTEXT.md
@.planning/research/TOOL-CALLING-CONSTRAINED-DECODING.md
@.planning/phases/120-constrained-ollama-tool-envelope-rust/120-01-PLAN.md
@crates/rlm-core/src/adapters/ollama_language_model/mod.rs
@crates/rlm-core/src/adapters/ollama_language_model/request.rs

<interfaces>
Current `build_chat_body` (request.rs):

```rust
pub(super) fn build_chat_body(&self, messages: &[ChatMessage], tools: Option<&[Value]>) -> Value
```

Extend signature (Claude discretion — minimal change):

```rust
pub(super) fn build_chat_body(
    &self,
    messages: &[ChatMessage],
    tools: Option<&[Value]>,
    format: Option<&Value>,
) -> Value
```

When `format.is_some()`: set `body["format"] = format`, do **not** set `body["tools"]` even if tools arg passed.

Current `complete()` branching (mod.rs):
- `constrained` → two-phase (format-free) when `constrained_tool_calling && !allow_unconstrained && tools non-empty`
- else → single call with tools

New branch **first** (highest priority):

```rust
if options.response_format.is_some() && options.tools_enabled && !options.tools.is_empty() {
    // envelope path: single chat_stream with format, no tools
    let schema = options.response_format.as_ref().unwrap();
    // after stream: parse_envelope_response(&content, &options.tools)
}
```

On parse error: return `LanguageModelResponse` with error content (match existing Err handling pattern) — do not emit tool_calls for unknown tools.

Use `temperature: 0` in envelope requests per research §0.6 caveat (override self.temperature for format calls only).

Existing two-phase path: unchanged when `response_format` is None (ROADMAP criterion 4 deferred to 120-03 config gate).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend build_chat_body and add envelope request tests</name>
  <files>crates/rlm-core/src/adapters/ollama_language_model/request.rs, crates/rlm-core/tests/adapters/ollama_language_model.rs</files>
  <behavior>
    - build_chat_body with format Some and tools Some produces body with "format" key and no "tools" key
    - build_chat_body with format None and tools Some produces body with "tools" only (existing behavior)
  </behavior>
  <action>
1. Add optional `format` param to `build_chat_body`; when present, insert `format` and skip `tools`.
2. Update all call sites in mod.rs (may need follow-up in Task 2).
3. Add tests in `ollama_language_model.rs` test module for body shape assertions.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core builds_tool_definitions_from_options chat_request -- --nocapture</automated>
  </verify>
  <done>Chat body correctly exclusive format vs tools</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement complete() envelope branch and response mapping</name>
  <files>crates/rlm-core/src/adapters/ollama_language_model/mod.rs, crates/rlm-core/src/adapters/ollama_language_model/response.rs</files>
  <behavior>
    - Unit test: mock parse path — given envelope JSON string, complete() maps to tool_calls or content (use parse_envelope_response directly in adapter tests if HTTP mock too heavy)
    - Unknown tool in envelope content returns error response, empty tool_calls
  </behavior>
  <action>
1. Refactor `chat_stream` to accept `format: Option<&Value>` and pass through to `build_chat_body`.
2. In `complete()`, when `options.response_format.is_some()` && tools enabled && tools non-empty:
   - Call `chat_stream(messages, None, options.response_format.as_ref())` — **never** pass tools.
   - Set temperature 0 in body for envelope calls.
   - Parse accumulated content via `parse_envelope_response(&content, &options.tools)`.
   - Map `ParsedEnvelope::Final(s)` → `LanguageModelResponse { content: s, tool_calls: [] }`.
   - Map `ParsedEnvelope::ToolCall { name, args }` → single `ToolCallRequest` in tool_calls.
   - On `EnvelopeError`: return error content string (consistent with existing OllamaLanguageModelError handling).
3. Preserve existing `constrained` two-phase branch when `response_format` is None.
4. Add tests for envelope parse mapping (valid tool_call, valid final, unknown tool rejection) — can test via public helpers or thin wrapper exposing parse for tests.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core ollama_language_model -- --nocapture</automated>
  </verify>
  <done>Adapter uses format-only envelope path; two-phase untouched when response_format None</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ollama API | Untrusted model output parsed before tool dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-03 | T | complete() envelope branch | mitigate | Never send tools+format together; parse allowlist before tool_calls |
| T-120-04 | D | OllamaLanguageModel | accept | Parse errors surface as failed inference content, not silent tool execution |
</threat_model>

<verification>
`node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core ollama_language_model tool_envelope -- --nocapture`
</verification>

<success_criteria>
Ollama adapter sends format without tools; envelope JSON maps to content/tool_calls; two-phase path unchanged when response_format absent.
</success_criteria>

<output>
After completion, create `.planning/phases/120-constrained-ollama-tool-envelope-rust/120-02-SUMMARY.md`
</output>

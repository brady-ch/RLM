---
phase: 120-constrained-ollama-tool-envelope-rust
plan: 03
type: execute
wave: 3
depends_on:
  - "120-02"
files_modified:
  - crates/rlm-core/src/domain/recursion/tool_round_loop.rs
  - crates/rlm-core/src/adapters/ollama_language_model/mod.rs
  - crates/rlm-core/src/control_server/mod.rs
  - crates/rlm-core/tests/domain/recursion/tool_round_loop.rs
  - tests/fixtures/persistence/default-project-config.json
autonomous: true
requirements:
  - RETIRE-120-04
  - RETIRE-120-05
user_setup: []
must_haves:
  truths:
    - "Config flag useToolEnvelope on Ollama host gates envelope mode (default false)"
    - "When useToolEnvelope true, tool_round_loop passes response_format built from registered tools"
    - "When useToolEnvelope false, existing two-phase constrained path unchanged"
    - "Tests cover valid envelope parse and unknown-tool rejection end-to-end at domain layer"
  artifacts:
    - path: crates/rlm-core/src/domain/recursion/tool_round_loop.rs
      provides: "response_format wiring when config enabled"
    - path: tests/fixtures/persistence/default-project-config.json
      provides: "useToolEnvelope default false on local_ollama host"
  key_links:
    - from: tool_round_loop
      to: build_tool_envelope_schema
      via: "response_format on LanguageModelCompleteOptions"
      pattern: "build_tool_envelope_schema"
    - from: control_server bootstrap
      to: OllamaLanguageModel
      via: "useToolEnvelope from hosts config"
      pattern: "useToolEnvelope"
---

<objective>
Config-gate envelope mode from YAML host config through orchestration to the Ollama adapter, preserving the legacy two-phase path when off.

Purpose: ROADMAP criteria 4–5 require opt-in envelope mode without breaking existing constrained_tool_calling behavior.
Output: Config field, bootstrap wiring, tool_round_loop integration, domain-level tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-constrained-ollama-tool-envelope-rust/120-CONTEXT.md
@.planning/research/TOOL-CALLING-CONSTRAINED-DECODING.md
@crates/rlm-core/src/domain/recursion/tool_round_loop.rs
@crates/rlm-core/src/control_server/mod.rs
@tests/fixtures/persistence/default-project-config.json

<interfaces>
Host config pattern (control_server/mod.rs reads hosts as JSON Value):

```json
"hosts": {
  "local_ollama": {
    "kind": "ollama",
    "baseUrl": "http://127.0.0.1:11434",
    "allowUnconstrainedToolCalls": false,
    "useToolEnvelope": false
  }
}
```

Bootstrap: pass `use_tool_envelope: bool` into `OllamaLanguageModel` (new field alongside `allow_unconstrained_tool_calls`).

`tool_round_loop` complete call (lines ~107–112):

```rust
LanguageModelCompleteOptions {
    purpose,
    tools_enabled,
    tools: tool_definitions,
    constrained_tool_calling: tools_enabled,
    response_format: None, // set when model host has envelope enabled
}
```

When envelope enabled on model host:
```rust
response_format: if envelope_enabled {
    Some(build_tool_envelope_schema(&tool_definitions))
} else {
    None
},
constrained_tool_calling: tools_enabled && !envelope_enabled, // two-phase only when envelope off
```

Expose envelope flag on `ModelCompletionHost` trait (e.g. `fn use_tool_envelope(&self) -> bool`) or pass via existing host config accessor — follow nearest existing pattern for `allow_unconstrained` (read from config at bootstrap, store on model adapter or execution host).

Default fixture: `useToolEnvelope: false` — existing behavior unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useToolEnvelope config and bootstrap wiring</name>
  <files>crates/rlm-core/src/adapters/ollama_language_model/mod.rs, crates/rlm-core/src/control_server/mod.rs, tests/fixtures/persistence/default-project-config.json</files>
  <action>
Per ROADMAP criterion 4:

1. Add `use_tool_envelope: bool` field to `OllamaLanguageModel`; extend `new`/`with_cancellation` constructors (default `false`).
2. In `control_server/mod.rs` `resolve_ollama_host` (or equivalent), read `useToolEnvelope` from host JSON (default `false` when absent).
3. Pass flag when constructing `OllamaLanguageModel`.
4. Add `"useToolEnvelope": false` to `tests/fixtures/persistence/default-project-config.json` under `hosts.local_ollama`.
5. Grep for other OllamaLanguageModel construction sites (tests, bootstrap) — pass `false` default.

Do **not** change two-phase logic in adapter when `use_tool_envelope` is false.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-core</automated>
  </verify>
  <done>Config flag plumbed to OllamaLanguageModel; default off in fixture</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire tool_round_loop and add envelope integration tests</name>
  <files>crates/rlm-core/src/domain/recursion/tool_round_loop.rs, crates/rlm-core/tests/domain/recursion/tool_round_loop.rs</files>
  <behavior>
    - Mock host with use_tool_envelope true: complete options include response_format Some and constrained_tool_calling false
    - Mock host with use_tool_envelope false: response_format None and constrained_tool_calling true (when tools enabled)
    - Envelope parse integration: mock model returns valid envelope JSON → tool round executes; unknown tool → error path
  </behavior>
  <action>
1. Extend `ModelCompletionHost` with `fn use_tool_envelope(&self) -> bool` (default false in test mocks).
2. In `run_completion_with_tool_rounds`, when building `LanguageModelCompleteOptions`:
   - If `host.use_tool_envelope()` && tools_enabled: set `response_format: Some(build_tool_envelope_schema(&tool_definitions))`, set `constrained_tool_calling: false`.
   - Else: keep current behavior (`constrained_tool_calling: tools_enabled`, `response_format: None`).
3. Add/update tests in `tool_round_loop.rs` test module:
   - Assert options shape for envelope on vs off (use a recording mock LanguageModel).
   - Test unknown tool rejection propagates (mock returns envelope JSON with disallowed tool name → no tool execution, error recorded).

Follow existing `QueueModel` / `ToolRoundModel` mock patterns in ports/language_model.rs for new test mock if needed.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core tool_round_loop tool_envelope ollama_language_model -- --nocapture</automated>
  </verify>
  <done>Orchestration config-gated; tests cover envelope on/off and unknown-tool rejection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config→execution | Host YAML config trusted; model output untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-05 | E | tool_round_loop | mitigate | Unknown tool names rejected at parse before Tool::execute |
| T-120-06 | T | useToolEnvelope config | accept | Opt-in only; default false preserves existing two-phase security posture |
</threat_model>

<verification>
113-GATES Phase 120:
1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — exit 0
2. Envelope tests in tool_envelope + ollama_language_model + tool_round_loop modules pass
</verification>

<success_criteria>
useToolEnvelope config gates envelope mode; two-phase unchanged when off; valid parse and unknown-tool rejection tested.
</success_criteria>

<output>
After completion, create `.planning/phases/120-constrained-ollama-tool-envelope-rust/120-03-SUMMARY.md`
</output>

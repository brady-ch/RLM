---
phase: "01"
plan: "01-02"
type: "standard"
wave: 2
depends_on: ["01-01"]
requirements_addressed: ["PLAN-01", "APRV-01"]
files_modified:
  - src/application/control-server.ts
  - ui/src/main.tsx
  - ui/src/styles.css
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "UI reflects backend-confirmed approval state; no optimistic authority drift."
    - "Approval and planning failures surface explicitly in both UI and CLI."
    - "Control server routes are thin proxies over controller decisions."
---

<objective>
Align control-server and UI behavior with backend hard-gate semantics so planning/approval status is visible, actionable, and never silently inconsistent.
</objective>

<tasks>
  <task id="1" type="implementation">
    <action>Update control-server approval endpoints to proxy a single controller API contract and return normalized success/error payloads for stale/duplicate/invalid transitions.</action>
    <files>
      <file>src/application/control-server.ts</file>
      <file>src/domain/types.ts</file>
    </files>
    <verify>Endpoint behavior tests or integration assertions confirm explicit status codes/messages for approval failures.</verify>
    <acceptance_criteria>No route-level shortcut mutates approval state outside controller semantics.</acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <action>Update UI node/inspector flows to render backend status as authoritative, surface checkpoint failures clearly, and keep plan-vs-run status legible for developers.</action>
    <files>
      <file>ui/src/main.tsx</file>
      <file>ui/src/styles.css</file>
    </files>
    <verify>Manual run through UI shows paused checkpoints, successful approvals, and clear error states when approvals are rejected.</verify>
    <acceptance_criteria>UI never marks node approved unless confirmed via backend response/event.</acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <action>Improve CLI render paths for planning/approval transition errors so failures are explicit and actionable during headless runs.</action>
    <files>
      <file>src/cli/render.ts</file>
    </files>
    <verify>Failure output includes actionable node/status/error context.</verify>
    <acceptance_criteria>No silent failure path remains in CLI output for planning/approval transitions covered by this phase.</acceptance_criteria>
  </task>

  <task id="4" type="testing">
    <action>Add integration assertions covering control-server + UI-facing approval semantics and visible error propagation.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` passes and includes cases where invalid approvals produce explicit surfaced errors.</verify>
    <acceptance_criteria>Regressions in UI/CLI surfacing or route/controller consistency are caught automatically.</acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`
- Run `npm test`
- Start UI mode and validate: prompt -> plan visible -> pause -> approve via UI -> resume; validate rejected/stale approval error visibility.
</verification>

<success_criteria>
- Control server is contract-aligned with backend controller.
- UI displays backend-authoritative checkpoint behavior.
- CLI and UI both surface approval/planning failures explicitly.
- Phase 1 requirements remain fully covered.
</success_criteria>

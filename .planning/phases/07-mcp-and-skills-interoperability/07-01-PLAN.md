---
phase: "07"
plan: "07-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["INT-01", "INT-02"]
files_modified:
  - src/application/project-config.ts
  - src/application/mcp-skill-runtime.ts
  - src/index.ts
  - src/ports/tool-port.ts
autonomous: true
must_haves:
  truths:
    - "MCP servers are configurable per server entry with required/optional policy."
    - "Skill lookup uses configurable ordered paths with first-match precedence."
    - "Skill caching is configurable and defaults are documented."
    - "Skill strictness is configurable per path (`strict|lenient`)."
  artifacts:
    - path: "src/application/mcp-skill-runtime.ts"
      provides: "Policy orchestration for MCP connectivity and skill resolution"
      exports: ["McpSkillRuntime"]
  key_links:
    - from: "src/application/project-config.ts"
      to: "src/application/mcp-skill-runtime.ts"
      via: "Typed MCP/skill policy config consumed by runtime manager"
      pattern: "mcp|skills|strict"
---

<objective>
Implement Phase 7 Wave 1 policy and configuration surface for MCP + skills interoperability without changing existing non-MCP behavior.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-mcp-and-skills-interoperability/07-CONTEXT.md
@.planning/phases/07-mcp-and-skills-interoperability/07-RESEARCH.md
@src/application/project-config.ts
@src/index.ts
</context>

<tasks>
  <task id="1" type="implementation">
    <name>Task 1: Extend config schema for MCP and skill interoperability</name>
    <files>
      <file>src/application/project-config.ts</file>
    </files>
    <action>
Add typed config for:
- MCP servers with per-server required policy.
- Skill paths ordered search list.
- Skill duplicate strategy set to first-match by path order.
- Skill strictness policy per path and caching toggle.

Schema must remain backward-compatible when these blocks are absent.
    </action>
    <verify>
`npm run build` passes and existing config files still parse.
    </verify>
    <acceptance_criteria>
Config supports required/optional MCP policy and ordered skill path resolution settings with no regressions.
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <name>Task 2: Add runtime policy manager for MCP + skills</name>
    <files>
      <file>src/application/mcp-skill-runtime.ts</file>
      <file>src/ports/tool-port.ts</file>
    </files>
    <action>
Create an application-layer runtime manager that:
- Reads typed config
- Resolves skill paths in configured order (first-match)
- Applies strict/lenient behavior per path
- Provides MCP server policy hooks (required/optional) for connect/disconnect handling

Do not wire final event/audit escalation details yet (handled in 07-02).
    </action>
    <verify>
`npm run build` passes.
    </verify>
    <acceptance_criteria>
Runtime manager is composable from index and policy decisions are encoded as explicit functions/types.
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <name>Task 3: Integrate manager into composition root with no behavior regressions</name>
    <files>
      <file>src/index.ts</file>
    </files>
    <action>
Wire the new runtime manager from index.ts after config load and before agent registry creation.
Ensure existing non-MCP tool adapter behavior remains unchanged when MCP config is absent.
    </action>
    <verify>
`npm run build` and `npm test` pass.
    </verify>
    <acceptance_criteria>
Default runs without MCP config remain behavior-compatible; composition root uses typed policy manager.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Manual smoke: `node dist/src/index.js --help`
</verification>

<success_criteria>
- MCP/skill policy config exists and is typed.
- Runtime manager exists and is wired from composition root.
- Existing non-MCP behavior remains compatible.
</success_criteria>

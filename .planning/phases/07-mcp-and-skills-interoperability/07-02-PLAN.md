---
phase: "07"
plan: "07-02"
type: "standard"
wave: 2
depends_on: ["07-01"]
requirements_addressed: ["INT-01", "INT-02"]
files_modified:
  - src/application/mcp-skill-runtime.ts
  - src/application/runtime-events.ts
  - src/index.ts
  - tests/mcp-skill-interoperability.test.ts
  - .planning/phases/07-mcp-and-skills-interoperability/07-02-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "Shared event schema covers both MCP lifecycle and skill-load warnings."
    - "Every event includes required machine code and severity enum."
    - "Event identity includes UUID plus deterministic fingerprint."
    - "Timestamps include UTC ISO plus monotonic per-run seq from central atomic counter."
    - "State-store outage pauses run; severity escalates at 10s/60s; recovery emits verbose metrics."
  artifacts:
    - path: "src/application/runtime-events.ts"
      provides: "Shared event schema and identity helpers"
      exports: ["RuntimeEvent", "createRuntimeEventFingerprint"]
    - path: "tests/mcp-skill-interoperability.test.ts"
      provides: "Coverage for policy, lifecycle, and event semantics"
      exports: []
  key_links:
    - from: "src/application/mcp-skill-runtime.ts"
      to: "src/application/runtime-events.ts"
      via: "Runtime manager emits structured MCP+skill lifecycle events"
      pattern: "severity|code|seq|fingerprint"
---

<objective>
Implement Wave 2 lifecycle event/audit semantics and test coverage for Phase 7 so MCP+skill interoperability behavior is explicit, queryable, and operationally safe.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-mcp-and-skills-interoperability/07-CONTEXT.md
@.planning/phases/07-mcp-and-skills-interoperability/07-01-PLAN.md
@src/application/mcp-skill-runtime.ts
</context>

<tasks>
  <task id="4" type="implementation">
    <name>Task 4: Implement shared runtime event schema and identity</name>
    <files>
      <file>src/application/runtime-events.ts</file>
      <file>src/application/mcp-skill-runtime.ts</file>
    </files>
    <action>
Add shared event schema with:
- severity enum: info|warn|error
- required stable machine code
- event_id UUID
- deterministic fingerprint
- UTC timestamp + monotonic run seq

Integrate for both skill load and MCP lifecycle events.
    </action>
    <verify>
`npm run build` passes.
    </verify>
    <acceptance_criteria>
Skill and MCP events share one schema and include all required identity/order fields.
    </acceptance_criteria>
  </task>

  <task id="5" type="implementation">
    <name>Task 5: Implement pause/escalation/recovery semantics</name>
    <files>
      <file>src/application/mcp-skill-runtime.ts</file>
      <file>src/index.ts</file>
    </files>
    <action>
Implement policy behavior:
- Optional disconnect: checkpoint pause and continue on reconnect.
- Required disconnect: indefinite pause for reconnect.
- On state-store outage: pause run; no degraded event mode.
- Escalation thresholds: warn at 10s, error at 60s.
- Emit explicit RECOVERED event and reset severity flow to info.
- Escalation and recovery events always include verbose metrics required in context.
    </action>
    <verify>
`npm run build` passes.
    </verify>
    <acceptance_criteria>
Lifecycle behavior matches all locked context decisions and emits verbose metric payloads for escalation/recovery.
    </acceptance_criteria>
  </task>

  <task id="6" type="testing">
    <name>Task 6: Add integration tests and phase summary</name>
    <files>
      <file>tests/mcp-skill-interoperability.test.ts</file>
      <file>.planning/phases/07-mcp-and-skills-interoperability/07-02-SUMMARY.md</file>
    </files>
    <action>
Add tests covering:
- Required vs optional disconnect behaviors
- Skill strict/lenient behaviors and warning persistence
- Event identity/ordering fields
- Escalation thresholds and RECOVERED behavior

Write 07-02 summary after implementation and tests pass.
    </action>
    <verify>
`npm run build && npm test` passes.
    </verify>
    <acceptance_criteria>
New tests enforce Phase 7 policy semantics; summary accurately documents behavior and operational implications.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Focused grep checks for event fields (`event_id`, `fingerprint`, `seq`, `severity`, `code`)
</verification>

<success_criteria>
- Shared MCP+skill lifecycle event model is implemented.
- Pause/escalation/recovery semantics match locked decisions.
- Integration tests prove behavior and backward compatibility constraints.
</success_criteria>

---
phase: 97-persistence-config-facade
plan: 02
type: execute
wave: 2
depends_on:
  - 97-01
files_modified:
  - crates/rlm-core/src/application/config/mod.rs
  - crates/rlm-core/src/application/config/defaults.rs
  - crates/rlm-core/src/application/config/yaml_merge.rs
  - crates/rlm-core/src/application/config/validation.rs
  - crates/rlm-core/src/application/config/loader.rs
  - crates/rlm-core/src/application/mod.rs
  - scripts/rust-boundary-baseline.json
  - AGENTS.md
requirements:
  - PERS-97-02
  - PERS-97-03
  - PERS-97-04
autonomous: true
must_haves:
  truths:
    - "application/config/ module removed; no dead re-export facade remains"
    - "no-persistence-to-application baseline entry removed"
    - "npm run check:rust:boundaries passes with zero suppressed persistence→application arcs"
  artifacts:
    - path: scripts/rust-boundary-baseline.json
      provides: "Baseline without no-persistence-to-application entry"
    - path: AGENTS.md
      provides: "Updated transitional baseline table without persistence/config row"
  key_links:
    - from: scripts/check-rust-boundaries.sh
      to: scripts/rust-boundary-baseline.json
      via: "baseline mode suppresses known arcs"
      pattern: "no-persistence-to-application"
---

<objective>
Remove the transitional `application/config/` module and ratchet the Rust boundary baseline so `no-persistence-to-application` is fully enforced.

Purpose: Complete Phase 97 goal — config resolution owned by persistence with no baseline suppression.
Output: Deleted application/config tree, updated baseline JSON and AGENTS.md, green boundary check.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-persistence-config-facade/97-CONTEXT.md
@.planning/phases/97-persistence-config-facade/97-01-SUMMARY.md
@scripts/rust-boundary-baseline.json
@AGENTS.md

<interfaces>
Baseline entry to remove (sole `no-persistence-to-application` violation):

```json
{
  "rule": "no-persistence-to-application",
  "from": "crates/rlm-core/src/persistence/config.rs",
  "import": "pub use crate::application::config::{load_project_config, merge_yaml_layers, LoadedProjectConfig};"
}
```

AGENTS.md transitional table row to delete:

```markdown
| `no-persistence-to-application` | `persistence/config.rs` | Transitional config loader re-export ... | Move `load_project_config` ... |
```

Callers already import via persistence (no application/config consumers):

- `application/bootstrap/cli_runtime.rs` → `crate::persistence::{load_project_config, LoadedProjectConfig}`
- `control_server/mod.rs` → `crate::persistence::{load_project_config, LoadedProjectConfig, ...}`
- `plugins/registry/service.rs` → `crate::persistence::LoadedProjectConfig`
- `rlm-core` lib.rs → persistence re-exports
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove application/config module</name>
  <files>crates/rlm-core/src/application/mod.rs, crates/rlm-core/src/application/config/mod.rs, crates/rlm-core/src/application/config/defaults.rs, crates/rlm-core/src/application/config/yaml_merge.rs, crates/rlm-core/src/application/config/validation.rs, crates/rlm-core/src/application/config/loader.rs</files>
  <read_first>
    crates/rlm-core/src/application/mod.rs
    crates/rlm-core/src/application/config/mod.rs
    crates/rlm-core/src/persistence/config/mod.rs
  </read_first>
  <action>
    1. Delete entire directory `crates/rlm-core/src/application/config/` (all five files: mod.rs, defaults.rs, yaml_merge.rs, validation.rs, loader.rs).
    2. Remove `pub mod config;` from `crates/rlm-core/src/application/mod.rs`.
    3. Grep the workspace for `application::config` and `application/config` — fix any remaining references to use `crate::persistence::config` or `crate::persistence::{load_project_config, LoadedProjectConfig}` instead. Expected: zero hits after cleanup (persistence was the only consumer of application::config).
    4. Do NOT move `tests/application/config/loader.rs` — test path stub stays on persistence loader per deferred scope.
  </action>
  <acceptance_criteria>
    - `test ! -d crates/rlm-core/src/application/config` succeeds
    - `grep -v '^#' crates/rlm-core/src/application/mod.rs | grep -c 'pub mod config'` equals 0
    - `rg 'application::config' crates/rlm-core/src --glob '!**/target/**'` returns no matches
    - `rg 'application/config' crates/rlm-core/src --glob '!**/target/**'` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-core -p rlm-cli 2>&1</automated>
  </verify>
  <done>application/config removed; all config access routes through persistence</done>
</task>

<task type="auto">
  <name>Task 2: Drop baseline entry and update docs</name>
  <files>scripts/rust-boundary-baseline.json, AGENTS.md</files>
  <read_first>
    scripts/rust-boundary-baseline.json
    AGENTS.md
    scripts/rust-boundary-rules.toml
  </read_first>
  <action>
    1. Remove the entire JSON object with `"rule": "no-persistence-to-application"` from `scripts/rust-boundary-baseline.json`. File must remain valid JSON array (6 entries → 5 entries).
    2. In `AGENTS.md`, delete the transitional baseline table row for `no-persistence-to-application` / `persistence/config.rs` under "### Transitional boundary baseline (ratchet plan)".
    3. Update the note in AGENTS.md Rust workspace section if it still says config loader re-export is transitional — config is now persistence-owned.
    4. Run boundary check in baseline mode (default CI path) and strict mode to confirm no persistence→application arcs exist.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' scripts/rust-boundary-baseline.json | grep -c 'no-persistence-to-application'` equals 0
    - `grep -v '^#' AGENTS.md | grep -c 'no-persistence-to-application.*persistence/config'` equals 0
    - `npm run check:rust:boundaries` exits 0
    - `npm run check:rust:boundaries:strict` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check:rust:boundaries 2>&1 && npm run check:rust:boundaries:strict 2>&1 && node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core -- --test-threads=1 2>&1</automated>
  </verify>
  <done>Baseline ratcheted; boundary checks pass in baseline and strict modes; full rlm-core test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → boundary baseline | Baseline JSON defines suppressed known violations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04 | E | rust-boundary-baseline.json | mitigate | Verify strict mode passes after entry removal — no hidden persistence→application arcs |
</threat_model>

<verification>
- `npm run check:rust:boundaries` (baseline mode)
- `npm run check:rust:boundaries:strict` (zero suppressed arcs)
- `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core`
</verification>

<success_criteria>
- ROADMAP Phase 97 success criteria 1–4 all satisfied
- `scripts/rust-boundary-baseline.json` has no `no-persistence-to-application` entry
- Config load behavior unchanged; tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/97-persistence-config-facade/97-02-SUMMARY.md`
</output>

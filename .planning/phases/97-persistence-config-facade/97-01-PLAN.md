---
phase: 97-persistence-config-facade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/persistence/config/mod.rs
  - crates/rlm-core/src/persistence/config/defaults.rs
  - crates/rlm-core/src/persistence/config/yaml_merge.rs
  - crates/rlm-core/src/persistence/config/budget.rs
  - crates/rlm-core/src/persistence/config/validation.rs
  - crates/rlm-core/src/persistence/config/loader.rs
  - crates/rlm-core/src/persistence/config.rs
  - crates/rlm-core/src/application/memory/ram_budget.rs
requirements:
  - PERS-97-01
  - PERS-97-04
autonomous: true
must_haves:
  truths:
    - "Config loader implementation lives under persistence/config/, not application/config/"
    - "persistence::load_project_config and LoadedProjectConfig behave identically to pre-move"
    - "persistence/config has zero imports from crate::application"
  artifacts:
    - path: crates/rlm-core/src/persistence/config/mod.rs
      provides: "Public config facade exports"
      exports: ["load_project_config", "LoadedProjectConfig", "merge_yaml_layers", "validate_memory_budget"]
    - path: crates/rlm-core/src/persistence/config/loader.rs
      provides: "Filesystem config loading orchestration"
    - path: crates/rlm-core/src/persistence/config/budget.rs
      provides: "Config-load-time memory budget validation"
  key_links:
    - from: crates/rlm-core/src/persistence/config/loader.rs
      to: crates/rlm-core/src/persistence/config/validation.rs
      via: "validate_config_shape, validate_config_references, validate_memory_budget after merge"
      pattern: "validate_config_shape|validate_memory_budget"
    - from: crates/rlm-core/src/application/memory/ram_budget.rs
      to: crates/rlm-core/src/persistence/config/budget.rs
      via: "pub use re-export of validate_memory_budget"
      pattern: "pub use crate::persistence::config::validate_memory_budget"
---

<objective>
Move config loader implementation from `application/config/` into `persistence/config/` submodule tree so persistence owns config resolution.

Purpose: Eliminate the `no-persistence-to-application` arc by reversing Phase 69's transitional layout — config loading is persistence I/O, not application orchestration.
Output: `persistence/config/{mod,defaults,yaml_merge,budget,validation,loader}.rs` with identical load behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-persistence-config-facade/97-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md

<interfaces>
From `crates/rlm-core/src/persistence/config.rs` (public surface to preserve):

```rust
pub struct LoadedProjectConfig {
    pub config: Value,
    pub path: Option<PathBuf>,
}

pub fn load_project_config(
    project_root: &Path,
    explicit_path: Option<&Path>,
) -> io::Result<LoadedProjectConfig>;

pub fn merge_yaml_layers(left: Value, right: Value) -> Value;
```

Loader merge order (implicit path) must remain identical:
`~/.rlm` fragments → project `.rlm` scoped dir (+ legacy scoped file) → `rlm.config.yaml` discovery → validate shape → validate references → validate memory budget.

Current violation to eliminate (Plan 02 removes baseline; Plan 01 removes the import):

```rust
// persistence/config.rs — DELETE this re-export pattern
pub use crate::application::config::{load_project_config, merge_yaml_layers, LoadedProjectConfig};
```

Memory-budget dependency today (`application/config/validation.rs`):

```rust
use crate::application::memory::validate_memory_budget as check_memory_budget;
```

Resolution: move `validate_memory_budget` logic to `persistence/config/budget.rs`; re-export from `application/memory/ram_budget.rs` so runtime callers and tests keep the same path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold persistence/config/ and move defaults + yaml_merge</name>
  <files>crates/rlm-core/src/persistence/config/mod.rs, crates/rlm-core/src/persistence/config/defaults.rs, crates/rlm-core/src/persistence/config/yaml_merge.rs, crates/rlm-core/src/persistence/config.rs</files>
  <read_first>
    crates/rlm-core/src/persistence/config.rs
    crates/rlm-core/src/persistence/mod.rs
    crates/rlm-core/src/application/config/defaults.rs
    crates/rlm-core/src/application/config/yaml_merge.rs
    crates/rlm-core/src/application/config/mod.rs
  </read_first>
  <action>
    1. Delete `crates/rlm-core/src/persistence/config.rs` (single-file module).
    2. Create directory `crates/rlm-core/src/persistence/config/` with `mod.rs` declaring submodules: `defaults`, `yaml_merge`, `budget`, `validation`, `loader`.
    3. Move `application/config/defaults.rs` → `persistence/config/defaults.rs` verbatim (keep `include_str!` fixture path unchanged).
    4. Move `application/config/yaml_merge.rs` → `persistence/config/yaml_merge.rs` verbatim.
    5. In `persistence/config/mod.rs`, add:
       `pub use defaults::default_project_plain;`
       `pub use yaml_merge::{is_plain_record, merge_yaml_layers};`
    6. Do NOT touch loader/validation yet — `application/config/mod.rs` still compiles for Plan 02 removal.
  </action>
  <acceptance_criteria>
    - `test -d crates/rlm-core/src/persistence/config` succeeds
    - `test ! -f crates/rlm-core/src/persistence/config.rs` succeeds (file deleted, directory module replaces it)
    - `grep -q 'pub fn merge_yaml_layers' crates/rlm-core/src/persistence/config/yaml_merge.rs`
    - `grep -q 'pub fn default_project_plain' crates/rlm-core/src/persistence/config/defaults.rs`
    - `grep -v '^#' crates/rlm-core/src/persistence/config/mod.rs | grep -c 'application'` equals 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-core 2>&1</automated>
  </verify>
  <done>defaults and yaml_merge compile under persistence/config/; no persistence→application imports</done>
</task>

<task type="auto">
  <name>Task 2: Move budget validation, validation, loader; wire public exports</name>
  <files>crates/rlm-core/src/persistence/config/budget.rs, crates/rlm-core/src/persistence/config/validation.rs, crates/rlm-core/src/persistence/config/loader.rs, crates/rlm-core/src/persistence/config/mod.rs, crates/rlm-core/src/application/memory/ram_budget.rs</files>
  <read_first>
    crates/rlm-core/src/application/config/validation.rs
    crates/rlm-core/src/application/config/loader.rs
    crates/rlm-core/src/application/memory/ram_budget.rs
    crates/rlm-core/src/persistence/config/mod.rs
    crates/rlm-core/src/persistence/mod.rs
  </read_first>
  <action>
    1. Create `persistence/config/budget.rs` with `pub fn validate_memory_budget(config: &Value) -> Result<(), String>` — copy logic verbatim from `application/memory/ram_budget.rs` lines 106-131, including a private `configured_cap_mb` helper (copy lines 29-36). Do NOT import from `application::memory`.
    2. Move `application/config/validation.rs` → `persistence/config/validation.rs`; replace the `application::memory` import with `use super::budget::validate_memory_budget as check_memory_budget;`.
    3. Move `application/config/loader.rs` → `persistence/config/loader.rs`; update internal `super::` imports to persistence paths; keep `use crate::persistence::ProjectPaths;`.
    4. Keep the existing test stub path unchanged: `#[path = "../../../tests/application/config/loader.rs"]` on the loader test module (deferred test-path move per CONTEXT).
    5. In `persistence/config/mod.rs`, export public surface:
       `pub use budget::validate_memory_budget;`
       `pub use loader::{load_project_config, LoadedProjectConfig};`
       `pub use yaml_merge::merge_yaml_layers;`
    6. In `application/memory/ram_budget.rs`, replace the `validate_memory_budget` function body (lines 106-131) with:
       `pub use crate::persistence::config::validate_memory_budget;`
       Leave all other functions (`configured_cap_mb`, `available_model_ram_mb`, etc.) unchanged in ram_budget.rs.
    7. Temporarily leave `application/config/{mod,loader,validation,defaults,yaml_merge}.rs` in place as dead code OR stub `application/config/mod.rs` to re-export from persistence until Plan 02 deletes them — choose whichever keeps `cargo check` green between commits; Plan 02 will delete application/config entirely.
  </action>
  <acceptance_criteria>
    - `grep -q 'pub fn load_project_config' crates/rlm-core/src/persistence/config/loader.rs`
    - `grep -q 'pub fn validate_memory_budget' crates/rlm-core/src/persistence/config/budget.rs`
    - `grep -v '^#' crates/rlm-core/src/persistence/config/validation.rs | grep -c 'application'` equals 0
    - `grep -q 'pub use crate::persistence::config::validate_memory_budget' crates/rlm-core/src/application/memory/ram_budget.rs`
    - `grep -v '^#' crates/rlm-core/src/persistence/config/loader.rs | grep -c 'application'` equals 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core persistence_dual_read -- --test-threads=1 2>&1 && node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core loader_tests -- --test-threads=1 2>&1</automated>
  </verify>
  <done>Config loads through persistence-owned modules; dual-read integration and loader unit tests pass; zero persistence→application imports in config tree</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → loader | Untrusted YAML from user project and `~/.rlm` fragments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | I | loader.rs | accept | Refactor-only; no new file read paths |
| T-97-02 | D | validation.rs | mitigate | Preserve existing validate_config_shape/reference/budget checks; no validation bypass |
| T-97-03 | T | budget.rs | accept | Same tier-vs-cap logic moved verbatim; ram_budget re-exports single source |
</threat_model>

<verification>
- `node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-core -p rlm-cli`
- Config integration tests in verify task
</verification>

<success_criteria>
- All config loader logic under `persistence/config/`
- Public API unchanged at `crate::persistence::{load_project_config, LoadedProjectConfig}`
- No `crate::application` imports anywhere under `persistence/config/`
</success_criteria>

<output>
After completion, create `.planning/phases/97-persistence-config-facade/97-01-SUMMARY.md`
</output>

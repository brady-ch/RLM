---
phase: 36-dev-tooling-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - .prettierignore
  - .prettierrc.json
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
must_haves:
  truths:
    - "`npm run lint` runs ESLint 10 flat config over `src/` and `ui/src/` and exits 0."
    - "Prettier 3 is wired with `.prettierrc.json` and `--check`; `eslint-config-prettier` disables conflicting ESLint stylistic rules."
    - "`npm run typecheck` remains available and unchanged in meaning."
    - "No formatter or ESLint autofix commits are bundled with this plan (TOOL-02 commit isolation handled in Execution notes / follow-on task)."
  artifacts:
    - path: eslint.config.js
      provides: "Flat ESLint 10 config with typescript-eslint for backend + UI TS/TSX"
    - path: .prettierrc.json
      provides: "Prettier defaults aligned to repo (minimal options)"
    - path: package.json
      provides: "Scripts `lint`, `lint:eslint` (alias if needed), `format:check`, `format`, devDependencies"
  key_links:
    - from: package.json scripts
      to: eslint.config.js
      via: ESLint resolves flat config from repo root
      pattern: "eslint"
---

<objective>

Add ESLint 10 flat config with typescript-eslint (TOOL-01) and Prettier 3 with eslint-config-prettier (TOOL-02), plus npm scripts needed before Plan 02 expands CI `check`.

Purpose: Enables consistent static analysis for `src/` and `ui/src/` without widening scope to dependency-cruiser yet.

Output: Lint + format toolchain installed and configured.

</objective>

<execution_context>

@/home/brady/.claude/get-shit-done/workflows/execute-plan.md
@/home/brady/.claude/get-shit-done/templates/summary.md

</execution_context>

<context>

@AGENTS.md
@tsconfig.json
@ui/tsconfig.json
@package.json
@/home/brady/workspace/RLM/.planning/ROADMAP.md (Phase 36 success criterion 1–2)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ESLint 10 + typescript-eslint + Prettier 3 toolchain</name>
  <files>package.json, package-lock.json</files>
  <action>
    Add devDependencies compatible with ESLint 10 (`eslint` ^10, `@eslint/js`), `typescript-eslint` monorepo packages at versions upstream documents as compatible with ESLint 10, `eslint-config-prettier`, Prettier `^3`, and `globals` only if ESLint recommends it for Flat config browser/node envs. Persist `package-lock.json` via npm install. Do not add eslint-plugin-prettier unless you need it; Prettier runs as separate `format*` scripts.

  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && node -e "const d=require('./package.json').devDependencies||{}; ['eslint','prettier','eslint-config-prettier'].forEach(k=>{if(!d[k])process.exit(1)}); const tsLint=typeof d['typescript-eslint']==='string'||(d['@typescript-eslint/parser']&&d['@typescript-eslint/eslint-plugin']); if(!tsLint)process.exit(1)" && npx eslint --version | grep -E '^v?10\\.'</automated>
  </verify>
  <done>Required devDeps present; ESLint CLI reports major version 10.</done>
</task>

<task type="auto">
  <name>Task 2: Add eslint flat config for src + ui/src</name>
  <files>eslint.config.js</files>
  <action>
    Create root `eslint.config.js` using Flat config only (no `.eslintrc`). Lint `src/**/*.ts`, `tests/**/*.ts` if present under root tsconfig paths, and `ui/src/**/*.{ts,tsx}`. Configure `typescript-eslint` with separate parser options/context for backend vs UI: backend uses repository `tsconfig.json`; UI uses `ui/tsconfig.json` (root tsconfig excludes `ui`). Extend `eslint:recommended`, `typescript-eslint` recommended typed or type-aware rules conservatively aligned to strict TS (`noUnusedLocals` off initially if noisy—prefer minimal baseline that passes cleanly). Ignore `dist/`, `node_modules/`, `ui/dist/`, `.planning/`, build outputs. Finish config with `eslintConfigPrettier` from `eslint-config-prettier` so Prettier owns formatting deltas. Align `npm run lint` to invoke ESLint (ROADMAP success criterion 1); keep `npm run typecheck` as pure `tsc --noEmit` (do not make `lint` an alias for typecheck anymore).

  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run lint</automated>
  </verify>
  <done>`npm run lint` completes with ESLint 10 flat config covering both trees; no deprecation errors from flat-config migration.</done>
</task>

<task type="auto">
  <name>Task 3: Prettier config, ignores, format scripts</name>
  <files>.prettierrc.json, .prettierignore, package.json</files>
  <action>
    Add minimal `.prettierrc.json` (match existing style defaults; keep options few). `.prettierignore` must exclude `dist/`, `node_modules/`, `package-lock.json`, generated assets, `.planning/` if appropriate. Scripts: `format` runs `prettier --write` on `src/**/*.{ts,tsx}` and `ui/src/**/*.{ts,tsx}` (adjust globs OS-portably via explicit paths or prettier-native patterns); `format:check` mirrors with `--check`. Per TOOL-02: avoid mixing Prettier autofix churn with logic edits in Plan 02—either leave tree unformatted until a dedicated formatter-only commit sequence, or if `format:check` fails after config, isolate a formatter-only git commit explicitly noted in SUMMARY.md (no functional change). Confirm `eslint-config-prettier` resolves rule overlap by running ESLint after Prettier toolchain exists.

  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run format:check</automated>
  </verify>
  <done>Format check wired; rationale documented when check is muted by baseline/format commit strategy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devDependencies npm registry | Unsigned package risk on install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | T | npm dependencies | mitigate | Prefer pinned semver ranges; keep lockfile authoritative; audit with `npm audit` when touching lockfile |

</threat_model>

<verification>

After Plan 02: expanded `npm run check` aggregates all gates including regression tests (REG-01/REG-02).

</verification>

<success_criteria>

- ESLint 10 flat config runs via `npm run lint` across `src/` and `ui/src/`.
- Prettier 3 has check/write scripts + ignores; ESLint stylistic overlaps disabled via `eslint-config-prettier`.

</success_criteria>

<output>

After completion, create `.planning/phases/36-dev-tooling-guardrails/36-01-SUMMARY.md`.

</output>

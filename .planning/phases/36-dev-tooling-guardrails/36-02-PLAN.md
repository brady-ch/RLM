---
phase: 36-dev-tooling-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - '36-01'
files_modified:
  - package.json
  - package-lock.json
  - .dependency-cruiser.js
  - dependency-cruiser-baseline.json
autonomous: true
requirements:
  - TOOL-03
  - TOOL-04
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`npm run check` runs typecheck, ESLint lint, Prettier `--check`, dependency-cruise against baseline (warn-level rules), then tests."
    - "dependency-cruiser encodes layer intent from ARCHITECTURE/AGENTS for `src/**`with warn severity plus committed baseline triage."
    - "Full test suite passes; no deliberate behavior changes to CLI, YAML config semantics, or control-server/graph/session paths (REG-01)."
    - "`npm run check` stays the CI aggregate gate (REG-02)."
  artifacts:
    - path: .dependency-cruiser.js
      provides: Forbidden arcs matching ARCHITECTURE/AGENTS layering for backend src (+ tests carve-outs)
    - path: dependency-cruiser-baseline.json
      provides: Checked-in WARN-level baseline triage (no error escalation in this phase)
    - path: package.json
      provides: "`depcruise` / validation script and expanded `check`"
  key_links:
    - from: npm run check
      to: dependency-cruise validate
      via: scripted sequence after Plan 01 gates
      pattern: "check"
---

<objective>

Add dependency-cruise layer-boundary guardrails (TOOL-03) with warn severity and baseline, expand `npm run check` with lint + cruiser + optional format check (TOOL-04), and prove regressions/tests stay green (REG-01, REG-02).

Purpose: Locks maintainers into the same layered shape described in ARCHITECTURE/AGENTS before large extraction work.

Output: Runnable validation pipeline suitable for CI.

</objective>

<execution_context>

@/home/brady/.claude/get-shit-done/workflows/execute-plan.md
@/home/brady/.claude/get-shit-done/templates/summary.md

</execution_context>

<context>

@AGENTS.md
@/home/brady/workspace/RLM/.planning/codebase/ARCHITECTURE.md
@package.json
@/home/brady/workspace/RLM/.planning/ROADMAP.md (Phase 36 success criteria 3–5)
@eslint.config.js

</context>

<tasks>

<task type="auto">
  <name>Task 1: dependency-cruiser config + baseline for src layers</name>
  <files>.dependency-cruiser.js, dependency-cruiser-baseline.json, package.json, package-lock.json</files>
  <action>
    Install `dependency-cruiser` devDependency aligned with ESLint toolchain changes. Implement `.dependency-cruiser.js` (ES module or CJS consistent with `"type":"module"` in package.json—prefer `.js` with `export default`). Scope analysis to backend TypeScript paths under `src/` (excluding `dist/`); document in SUMMARY why `ui/src` is out of cruiser layer graph for this milestone if UI arcs would dilute boundary signal. Encode forbidden arcs reflecting ARCHITECTURE/AGENTS layering for `src/` (domain→application/adapters/cli, ports→application/adapters, adapters→cli, etc.—derive exact rules from CURRENT imports and DOCUMENT intentional exceptions known today such as ports importing domain types). Set rule severity to `warn` globally for forbidden edges. Generate checked-in `dependency-cruiser-baseline.json` using dependency-cruiser baseline output documenting known WARN-level arcs so the CI validation command passes on main without ratcheting to error (ARCH-02 deferred per CONTEXT). Exclude `tests/` from unrealistic rules if runners must import internals; gate test-only arcs explicitly.

  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm exec -- dependency-cruise --help >/dev/null && node -e "const fs=require('fs'); ['.dependency-cruiser.js','dependency-cruiser-baseline.json'].forEach(f=>{if(!fs.existsSync(f))process.exit(1)})"</automated>
  </verify>
  <done>Cruiser runs with config; baseline artifact committed alongside config; WARN-level failures triaged—not suppressed silently without documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire npm scripts and expand `npm run check`</name>
  <files>package.json</files>
  <action>
    Add concise script aliases, for example `depcruise:ci` invoking dependency-cruise with config + baseline validation flags per upstream docs. Expand `npm run check` ordering to match roadmap text: run `npm run typecheck`, then Plan-01 ESLint alias (`npm run lint`), then `npm run format:check` if Prettier is part of Phase success (include when green), then cruise validation, finally `npm test`/`npm run test` unchanged semantics. Maintain backwards compatibility hooks if CI calls `npm test` standalone. Document any skip if format check staged later—preference is gate once TOOL-02 satisfied.

  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && node -p "JSON.stringify(require('./package.json').scripts.check)"</automated>
  </verify>
  <done>`check` script visibly chains every required gate via single command string or npm-run-series; aligns with REQUIREMENTS TOOL-04.</done>
</task>

<task type="auto">
  <name>Task 3: Regression — full check + sanity spot-check</name>
  <files>—</files>
  <action>
    Execute `npm run check` locally and confirm CLI smoke still launches (`npm run dev -- --help` or equivalent lightweight non-interactive path) ensuring no unintended flag/config behavior drift (REG-01). Confirm graph/control-server/session modules untouched unless lint auto-fix unavoidable—if unavoidable, isolate autofix commits. Capture command transcript summary in SUMMARY.md.

  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check</automated>
  </verify>
  <done>`npm run check` green post-baseline triage; unit/integration tests untouched behaviorally.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency-cruise input graph | Parses local source; malformed ignore patterns could hide violations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | E | cruiser ignores | mitigate | Narrow includeOnly globs to `src/`; exclude `dist`; review baseline deltas in PR |

</threat_model>

<verification>

Whole phase complete when BOTH plans succeed and success criteria enumerated in PLAN frontmatter validated.

</verification>

<success_criteria>

- TOOL-03 satisfied with warn severity + baseline.
- TOOL-04 aggregation live on `npm run check`.
- REG-01 / REG-02 satisfied via failing tests blocking completion.

</success_criteria>

<output>

After completion, create `.planning/phases/36-dev-tooling-guardrails/36-02-SUMMARY.md`.

</output>

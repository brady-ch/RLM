---
phase: "08"
plan: "08-02"
type: "standard"
wave: 2
depends_on: ["08-01"]
requirements_addressed: ["HOST-01", "TCON-01"]
files_modified:
  - src/ports/language-model-port.ts
  - src/domain/recursive-language-model.ts
  - src/adapters/ollama-language-model.ts
  - src/adapters/http-language-model.ts
  - src/cli/render.ts
  - ui/src/types/execution.ts
  - tests/constrained-tool-calling.test.ts
autonomous: true
must_haves:
  truths:
    - "D-08-05: Tool-calling constraint enforcement is adapter-owned per host under a shared contract/event surface."
    - "D-08-06: Unconstrained degraded mode is allowed only via explicit opt-in flag and must emit warning/audit events."
    - "D-08-07: Ollama constrained tool rounds use two-step execution with explicit trace markers."
    - "HOST-01 and TCON-01 remain explicit in CLI and UI runtime output without silent fallback behavior."
  artifacts:
    - path: "src/adapters/ollama-language-model.ts"
      provides: "Two-step constrained selection and tool execution protocol"
      exports: ["OllamaLanguageModel"]
    - path: "src/adapters/http-language-model.ts"
      provides: "Remote host adapter constrained-envelope implementation"
      exports: ["HttpLanguageModel"]
  key_links:
    - from: "src/domain/recursive-language-model.ts"
      to: "src/adapters/ollama-language-model.ts"
      via: "Adapter capability-aware constrained round orchestration"
      pattern: "tool_round|constrained|selection"
---

<objective>
Implement adapter-owned constrained tool-calling envelopes per host, including Ollama two-step behavior, degraded-mode guardrails, and host/endpoint observability in CLI/UI outputs.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-model-host-extensibility-and-constrained-tool-calling/08-CONTEXT.md
@.planning/phases/08-model-host-extensibility-and-constrained-tool-calling/08-RESEARCH.md
@src/domain/recursive-language-model.ts
@src/adapters/ollama-language-model.ts
</context>

<tasks>
  <task id="4" type="implementation">
    <name>Task 4: Define shared constrained-envelope adapter contract</name>
    <files>
      <file>src/ports/language-model-port.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
    </files>
    <action>
Define capability and envelope contract fields needed for constrained tool rounds while keeping enforcement adapter-owned.
Update recursive orchestration to invoke adapter capability-specific constrained round paths without introducing domain-level host-specific branching.
    </action>
    <verify>
`npm run build` passes and existing non-tooling model calls remain compatible.
    </verify>
    <acceptance_criteria>
Shared contract enables adapter-owned constrained behavior with stable domain orchestration.
    </acceptance_criteria>
  </task>

  <task id="5" type="implementation">
    <name>Task 5: Implement per-host constrained calling + degraded-mode policy</name>
    <files>
      <file>src/adapters/ollama-language-model.ts</file>
      <file>src/adapters/http-language-model.ts</file>
    </files>
    <action>
Implement host-specific constrained envelope paths.
For Ollama, implement two-step protocol: constrained selection pass, then executable tool call pass with explicit trace markers.
For hosts lacking strict support, allow degraded unconstrained mode only when opt-in config is true; otherwise emit explicit unsupported-capability error.
    </action>
    <verify>
`npm run build && npm test` passes; tests confirm two-step markers and degraded-mode gating.
    </verify>
    <acceptance_criteria>
Adapter-owned enforcement is complete, Ollama two-step semantics are explicit, and degraded mode is opt-in only.
    </acceptance_criteria>
  </task>

  <task id="6" type="implementation">
    <name>Task 6: Surface host/endpoint metadata in CLI and UI execution outputs</name>
    <files>
      <file>src/cli/render.ts</file>
      <file>ui/src/types/execution.ts</file>
    </files>
    <action>
Add effective host/endpoint metadata fields to runtime output contracts and display path (`--json-stream` + UI node/event payloads).
Apply redaction rules for sensitive endpoint details while preserving debuggability.
    </action>
    <verify>
`npm run build` passes and output snapshots/assertions include host metadata.
    </verify>
    <acceptance_criteria>
CLI and UI show effective host/endpoint used per node in a safe, explicit format.
    </acceptance_criteria>
  </task>

  <task id="7" type="testing">
    <name>Task 7: Add constrained-calling integration tests</name>
    <files>
      <file>tests/constrained-tool-calling.test.ts</file>
    </files>
    <action>
Add tests for:
- Adapter capability branching
- Ollama two-step constrained selection/tool execution markers
- Unsupported host behavior with and without `allow_unconstrained_tool_calls`
- Emitted warning/audit events in degraded mode
    </action>
    <verify>
`npm test` passes.
    </verify>
    <acceptance_criteria>
Tests lock TCON-01 behavior and prevent regressions across host adapters.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Focused assertions for host metadata in JSON stream/UI payload contracts
- Focused assertions for two-step Ollama trace markers and degraded-mode warnings
</verification>

<success_criteria>
- Adapter-owned constrained envelope behavior is implemented for local+remote hosts.
- Ollama tools/format mutual exclusion is handled via explicit two-step protocol.
- Degraded unconstrained mode is impossible without explicit opt-in.
- CLI and UI expose effective host/endpoint metadata per node.
</success_criteria>

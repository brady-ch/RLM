---
phase: "08"
plan: "08-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["HOST-01"]
files_modified:
  - src/application/project-config.ts
  - src/application/model-provider.ts
  - src/application/agent-runner.ts
  - src/application/execution-controller.ts
  - src/index.ts
  - tests/model-host-routing.test.ts
autonomous: true
must_haves:
  truths:
    - "D-08-01: Host definitions are declared in a YAML hosts map keyed by host id."
    - "D-08-02: Effective value precedence is env > CLI > config > defaults."
    - "D-08-03: Unavailable requested host pauses execution for explicit user decision."
    - "D-08-04: Pause options are retry same host, switch allowed host, or abort run."
  artifacts:
    - path: "src/application/project-config.ts"
      provides: "Typed host config schema + precedence resolver"
      exports: ["resolveRuntimeConfig"]
    - path: "src/application/model-provider.ts"
      provides: "Runtime host selection with explicit unavailable-host branching"
      exports: ["PurposeRoutingLanguageModel"]
  key_links:
    - from: "src/application/project-config.ts"
      to: "src/application/model-provider.ts"
      via: "Resolved host catalog and precedence-normalized runtime settings"
      pattern: "hosts|precedence|endpoint"
---

<objective>
Implement host configuration and runtime routing substrate for local/remote endpoints with explicit unavailable-host checkpoint behavior.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-model-host-extensibility-and-constrained-tool-calling/08-CONTEXT.md
@.planning/phases/08-model-host-extensibility-and-constrained-tool-calling/08-RESEARCH.md
@src/application/project-config.ts
@src/application/model-provider.ts
</context>

<tasks>
  <task id="1" type="implementation">
    <name>Task 1: Add typed host catalog schema and precedence resolution</name>
    <files>
      <file>src/application/project-config.ts</file>
    </files>
    <action>
Add host catalog config parsing for a `hosts` map keyed by host id, with typed per-host-kind fields.
Implement precedence resolver honoring `env > CLI > config > defaults` and produce normalized runtime host settings.
Preserve backward compatibility when host map is omitted.
    </action>
    <verify>
`npm run build` passes and precedence unit tests pass.
    </verify>
    <acceptance_criteria>
Host schema is typed, validated, and runtime settings reflect the locked precedence policy.
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <name>Task 2: Wire host-aware runtime routing and unavailable-host pause semantics</name>
    <files>
      <file>src/application/model-provider.ts</file>
      <file>src/application/agent-runner.ts</file>
      <file>src/application/execution-controller.ts</file>
      <file>src/index.ts</file>
    </files>
    <action>
Thread normalized host selection into runtime model routing. If a requested host is unavailable, emit explicit checkpoint state and block for user choice.
Implement checkpoint actions: retry same host, switch allowed host, abort run.
Ensure no silent fallback path exists.
    </action>
    <verify>
`npm run build && npm test` passes; checkpoint behavior tests pass for retry/switch/abort branches.
    </verify>
    <acceptance_criteria>
Runtime host routing is explicit and unavailable-host behavior matches D-08-03/D-08-04 exactly.
    </acceptance_criteria>
  </task>

  <task id="3" type="testing">
    <name>Task 3: Add precedence and host-availability integration tests</name>
    <files>
      <file>tests/model-host-routing.test.ts</file>
    </files>
    <action>
Add coverage for:
- Precedence matrix (`env`, `CLI`, `config`, defaults)
- Host lookup and selection path
- Unavailable-host pause and each decision branch outcome
    </action>
    <verify>
`npm test` passes with new test cases.
    </verify>
    <acceptance_criteria>
Regression tests lock host precedence and unavailable-host decision semantics.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Focused test run for host routing and checkpoint semantics
</verification>

<success_criteria>
- Host catalog contract and precedence are implemented and tested.
- Runtime uses explicit host-aware routing and blocks on unavailable hosts.
- Retry/switch/abort checkpoint controls are wired and validated.
</success_criteria>

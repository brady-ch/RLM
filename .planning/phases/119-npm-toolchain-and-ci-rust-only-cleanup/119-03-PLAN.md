---
phase: 119-npm-toolchain-and-ci-rust-only-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - "119-01"
  - "119-02"
files_modified:
  - AGENTS.md
  - README.md
  - .github/workflows/package-smoke.yml
  - .github/workflows/deb-smoke.yml
autonomous: true
requirements:
  - RETIRE-119-04
user_setup: []
must_haves:
  truths:
    - "AGENTS.md documents Rust-only runtime; no src/ concern map for deleted TS layers"
    - "113-GATES Phase 119 gate commands all pass"
    - "npm run typecheck absent (113-GATES item 3)"
  artifacts:
    - path: AGENTS.md
      provides: "Rust-only architecture documentation post Phase 119"
      contains: "Phase 119"
  key_links:
    - from: AGENTS.md
      to: crates/rlm-core/
      via: "canonical runtime path"
      pattern: "crates/rlm-core"
---

<objective>
Update architecture docs for Rust-only runtime and run all Phase 119 verification gates.

Purpose: ROADMAP success criterion 4 requires AGENTS.md to reflect the post-119 toolchain; gates prove CI-relevant scripts still work.
Output: Updated AGENTS.md/README; Phase 119 gates green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-npm-toolchain-and-ci-rust-only-cleanup/119-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/notes/rust-only-runtime-migration-decisions.md
@AGENTS.md

<interfaces>
113-GATES Phase 119 gate commands (ALL must pass):

1. `npm run check:rust` — exit 0
2. `npm run build:ui` — exit 0
3. Verify `npm run typecheck` removed (script absent from package.json)
4. `npm run test:agent:verify:light` — exit 0

Additional ROADMAP gate:
5. `npm run check` — exit 0 (UI lint/format + check:rust)

AGENTS.md updates required:
- Mark Phase 118 adapters/plugins TS paths as removed
- Mark Phase 119: npm toolchain Rust-only; `npm run check` = UI + Rust; depcruise/tsc removed
- Remove or update `npm run depcruise:strict` references in concern map / verification sections
- Confirm `src/` tree fully absent (entire directory, not just adapters/plugins)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AGENTS.md and README for Rust-only npm toolchain</name>
  <files>AGENTS.md, README.md</files>
  <action>
Per ROADMAP criterion 4 and rust-only-runtime-migration-decisions.md:

1. **AGENTS.md** — Update concern map: remove remaining `src/adapters/`, `src/plugins/` rows or mark **Removed Phase 118**; add Phase 119 note that entire `src/` is gone and npm toolchain is UI-only (Vite/Tauri/scripts).
2. Replace verification instructions: `npm run check` = `lint ui/src` + `format:check ui/src` + `check:rust`. Remove `depcruise:strict` and `typecheck` from documented check flows.
3. Update Layout table: delete or strike through any `src/` paths; note `tests/ui/` and `tests/fixtures/` as kept Node test paths.
4. **README.md** — If it documents `npm run build`, `typecheck`, or depcruise, update to `npm run build:ui` + `npm run check:rust`. Minimal diff only where stale.
5. Scan `.github/workflows/*.yml` — if any step invokes removed scripts (`npm run build`, `typecheck`, `depcruise`), remove or replace. Current workflows use `npm ci`, `build:ui`, `tauri:build`, `package:*` — likely no change needed; fix only if grep finds stale refs.
  </action>
  <verify>
    <automated>grep -v '^#' AGENTS.md | grep -c 'Phase 119' | grep -qv '^0$' && grep -v '^#' AGENTS.md | grep -c 'depcruise:strict' | grep -q '^0$' && echo ok</automated>
  </verify>
  <done>AGENTS.md reflects Rust-only runtime and Phase 119 toolchain; no stale depcruise docs</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 119 gate commands</name>
  <files></files>
  <action>
Run 113-GATES Phase 119 verification sequentially using agent-safe patterns:

1. `npm run check:rust`
2. `npm run build:ui`
3. Confirm `npm run typecheck` fails with "Missing script" or script absent: `node -e "const p=require('./package.json'); if(p.scripts.typecheck) process.exit(1)"`
4. `npm run test:agent:verify:light`
5. `npm run check` (full new check chain)

Use RAM-gated cargo where applicable per AGENTS.md. Do not run full `npm run check` with hand-rolled build chains outside agent-safe-verify unless RAM allows.

If any gate fails, fix in-place (package.json, eslint, runner) before marking done.
  </action>
  <verify>
    <automated>npm run test:agent:verify:light</automated>
  </verify>
  <done>All 113-GATES Phase 119 commands pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → contributors | Documentation only; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-05 | I | AGENTS.md | accept | Doc accuracy only |
</threat_model>

<verification>
Full Phase 119 gate suite per 113-GATES.md.
</verification>

<success_criteria>
AGENTS.md updated; npm run check, check:rust, build:ui, test:agent:verify:light all pass; typecheck script absent.
</success_criteria>

<output>
After completion, create `.planning/phases/119-npm-toolchain-and-ci-rust-only-cleanup/119-03-SUMMARY.md`
</output>

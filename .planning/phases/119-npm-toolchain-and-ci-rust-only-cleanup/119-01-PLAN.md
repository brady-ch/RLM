---
phase: 119-npm-toolchain-and-ci-rust-only-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - RETIRE-119-01
  - RETIRE-119-02
  - RETIRE-119-03
user_setup: []
must_haves:
  truths:
    - "package.json bin.rlm does not point at dist/src/index.js"
    - "LangChain and deepagents runtime dependencies removed from package.json"
    - "UI dependencies (@radix-ui, @xyflow/react, react, lucide-react, vite stack) retained"
    - "npm run check runs UI lint/format then npm run check:rust (no tsc, no depcruise, no dist/tests)"
  artifacts:
    - path: package.json
      provides: "Rust-only runtime deps; UI + packaging scripts only"
      contains: "\"check\":"
  key_links:
    - from: package.json scripts.check
      to: npm run check:rust
      via: "check script chain"
      pattern: "check:rust"
    - from: package.json bin.rlm
      to: scripts/rlm-runtime.mjs
      via: "Rust CLI dispatcher"
      pattern: "rlm-runtime"
---

<objective>
Strip Node runtime dependencies and rewrite package.json scripts so `npm run check` is UI lint/format plus Rust gates only.

Purpose: Phase 118 removes `src/`; LangChain/tsc/depcruise scripts are dead weight and confuse the Rust-only architecture (ROADMAP success criteria 1–3).
Output: Lean `package.json` + regenerated lockfile with UI/Vite/Tauri toolchain intact.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-npm-toolchain-and-ci-rust-only-cleanup/119-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/notes/rust-only-runtime-migration-decisions.md
@package.json

<interfaces>
Current `package.json` scripts to remove or replace (113-AUDIT Phase 119 inventory):

| Script | Current | Action |
|--------|---------|--------|
| `build` | `tsc -p tsconfig.json` | **Remove** |
| `typecheck` | `tsc --noEmit` | **Remove** |
| `depcruise:ci` | dependency-cruise src | **Remove** |
| `depcruise:strict` | dependency-cruise src | **Remove** |
| `check` | typecheck + lint + format + depcruise + test | **Replace** |
| `lint` | eslint src tests ui/src | **Narrow to ui/src** |
| `format` / `format:check` | prettier src tests ui/src | **Narrow to ui/src** |
| `test` | run-test-suite.mjs (tsc + dist/tests) | **Replace** (119-02 rewires runner) |

Runtime deps to **remove** (no remaining importers after Phase 118):
`@langchain/core`, `@langchain/langgraph`, `@langchain/ollama`, `langchain`, `deepagents`, `zod`, `yaml`, `tar`

Runtime deps to **keep**: `@radix-ui/react-context-menu`, `@xyflow/react`, `lucide-react`, `react`, `react-dom`

DevDeps to **remove**: `dependency-cruiser`

DevDeps to **keep**: `@eslint/js`, `@tauri-apps/cli`, `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `eslint`, `eslint-config-prettier`, `globals`, `prettier`, `tsx`, `typescript`, `typescript-eslint`, `vite`, `@types/node` (if tests/ui or scripts still need it)

`bin.rlm` must remain `scripts/rlm-runtime.mjs` (Phase 115); verify it does **not** reference `dist/src/index.js`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove runtime deps and dead scripts from package.json</name>
  <files>package.json</files>
  <action>
Per 113-AUDIT Phase 119 and ROADMAP criterion 2:

1. Remove from `dependencies`: `@langchain/core`, `@langchain/langgraph`, `@langchain/ollama`, `langchain`, `deepagents`, `zod`, `yaml`, `tar`.
2. Remove from `devDependencies`: `dependency-cruiser`.
3. Delete scripts: `build`, `typecheck`, `depcruise:ci`, `depcruise:strict`.
4. Confirm `bin.rlm` is `scripts/rlm-runtime.mjs` (not `dist/src/index.js`). If any stale `dist/src/index.js` reference exists, fix it per ROADMAP criterion 1.
5. Do **not** remove UI/Tauri/packaging scripts: `dev:ui`, `dev:ui:full`, `build:ui`, `tauri:dev`, `tauri:build`, `package:*`, `check:rust*`, `check:parity`, `rlm`, `rlm:rust`, `test:agent:verify*`, `test:rlm-runtime`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const bad=['@langchain/core','langchain','deepagents','zod','yaml','tar']; const deps=Object.keys({...p.dependencies,...p.devDependencies}); if(bad.some(d=>deps.includes(d))) process.exit(1); if(p.bin?.rlm?.includes('dist/src/index.js')) process.exit(1); if(p.scripts.build||p.scripts.typecheck||p.scripts['depcruise:strict']) process.exit(1); console.log('ok')"</automated>
  </verify>
  <done>LangChain/runtime deps gone; bin.rlm safe; tsc/depcruise scripts absent</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite check/lint/format/test scripts and refresh lockfile</name>
  <files>package.json, package-lock.json</files>
  <action>
Per ROADMAP criterion 3 and 119-CONTEXT discretion (keep ui/ build scripts):

1. Set `"check": "npm run lint && npm run format:check && npm run check:rust"`.
2. Set `"lint": "eslint ui/src"` and `"lint:eslint": "eslint ui/src"`.
3. Set `"format": "prettier --write ui/src"` and `"format:check": "prettier --check ui/src"`.
4. Set `"test": "node scripts/run-test-suite.mjs"` (119-02 updates the runner; leave placeholder if runner still references tsc — 119-02 must land same phase wave before gate).
5. Run `npm install` to regenerate `package-lock.json` without removed packages.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.check.includes('check:rust')) process.exit(1); if(p.scripts.check.includes('typecheck')||p.scripts.check.includes('depcruise')) process.exit(1); if(p.scripts.lint.includes('src ')) process.exit(1); console.log('ok')"</automated>
  </verify>
  <done>`npm run check` definition matches ROADMAP; lockfile pruned</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → npm install | Dependency manifest is trusted project source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | T | package.json scripts | accept | No runtime secrets; script changes are dev-tooling only |
| T-119-02 | D | npm install | mitigate | Run npm install in CI-equivalent clean tree; verify lockfile diff only removes expected packages |
</threat_model>

<verification>
113-GATES Phase 119 items 1–3 depend on 119-02/119-03 completing the full gate chain.
</verification>

<success_criteria>
package.json has no LangChain/runtime deps; check script chains to check:rust; bin.rlm does not reference dist/src/index.js.
</success_criteria>

<output>
After completion, create `.planning/phases/119-npm-toolchain-and-ci-rust-only-cleanup/119-01-SUMMARY.md`
</output>

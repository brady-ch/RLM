---
phase: "08.5"
plan: "08.5-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["ARTF-01"]
files_modified:
  - src/domain/types.ts
  - src/domain/recursive-language-model.ts
  - src/application/execution-controller.ts
  - src/application/runtime-events.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "D-08.5-01: Node input/output schemas are source-of-truth with optional edge narrowing."
    - "D-08.5-02: Artifact validation is strict-by-default with explicit per-node override."
    - "D-08.5-03: Edge schema rules may narrow but never widen node contracts."
    - "D-08.5-08: Code-only nodes stay in the same lifecycle model as model nodes."
    - "D-08.5-09: Code-only execution emits a dedicated code_execution event subtype."
  artifacts:
    - path: "src/domain/types.ts"
      provides: "Typed artifact contract and validation result structures"
      exports: ["ExecutionNode", "ExecutionEdge", "ExecutionGraph"]
    - path: "src/domain/recursive-language-model.ts"
      provides: "Boundary validation hooks in recursive execution flow"
      exports: ["runRecursivePrompt"]
  key_links:
    - from: "src/domain/types.ts"
      to: "src/domain/recursive-language-model.ts"
      via: "Node/edge artifact schemas and validation policy applied at runtime boundaries"
      pattern: "artifact|schema|validation|edge"
---

<objective>
Implement typed artifact contract boundaries in recursive execution, including strict default validation and code-only lifecycle/event parity.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.5-typed-artifact-stateful-workflow-runtime/08.5-CONTEXT.md
@.planning/phases/08.5-typed-artifact-stateful-workflow-runtime/08.5-RESEARCH.md
@src/domain/types.ts
@src/domain/recursive-language-model.ts
</context>

<tasks>
  <task id="1" type="implementation">
    <name>Task 1: Add typed artifact contract structures and policy in domain types</name>
    <files>
      <file>src/domain/types.ts</file>
    </files>
    <action>
Introduce artifact contract fields for node input/output schemas and optional edge narrowing rules.
Add explicit per-node validation policy controls with strict rejection as default behavior.
Keep schema representation implementation-choice flexible while enforcing hybrid precedence.
    </action>
    <verify>
`npm run build` passes with updated types.
    </verify>
    <acceptance_criteria>
Domain graph types can express node source-of-truth schemas and edge narrowing constraints without widening.
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <name>Task 2: Enforce boundary validation in recursive execution flow</name>
    <files>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/execution-controller.ts</file>
    </files>
    <action>
Insert node-boundary artifact validation before downstream node execution.
On mismatch under strict mode, fail explicitly with actionable diagnostics and no silent continuation.
Honor per-node non-strict override where configured.
    </action>
    <verify>
`npm run build && npm test` passes with boundary validation tests.
    </verify>
    <acceptance_criteria>
Typed boundary handoff is enforced and failures are explicit in runtime status/output.
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <name>Task 3: Preserve code-only lifecycle parity and emit dedicated subtype events</name>
    <files>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/runtime-events.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <action>
Ensure code-only nodes traverse the same lifecycle states/transitions as model-backed nodes.
Emit `code_execution` runtime event subtype for code-only execution observability.
Add tests for event subtype emission and lifecycle parity.
    </action>
    <verify>
`npm test` passes with new code-only lifecycle and event assertions.
    </verify>
    <acceptance_criteria>
Code-only nodes remain first-class in the same execution model with explicit observability markers.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Focused tests for artifact boundary validation and code-only event subtype behavior
</verification>

<success_criteria>
- Artifact schemas are typed at node boundaries with optional edge narrowing.
- Strict-by-default validation enforces explicit failure on mismatch.
- Code-only nodes share lifecycle semantics and emit `code_execution` subtype events.
</success_criteria>


---
phase: "08.5"
plan: "08.5-02"
type: "standard"
wave: 2
depends_on: ["08.5-01"]
requirements_addressed: ["ARTF-01"]
files_modified:
  - src/ports/run-state-store-port.ts
  - src/adapters/file-run-state-store.ts
  - src/application/execution-controller.ts
  - src/application/control-server.ts
  - src/application/mcp-skill-runtime.ts
  - src/application/runtime-events.ts
  - src/index.ts
  - tests/run-state-store.test.ts
autonomous: true
must_haves:
  truths:
    - "D-08.5-04: Run-state persistence is introduced behind a pluggable store interface with a file-backed adapter."
    - "D-08.5-05: Persist standard scope: run metadata, node statuses, artifact refs, mutation log, checkpoint snapshots, resume cursor."
    - "D-08.5-06: State mutations use reject-on-conflict optimistic concurrency via version/etag."
    - "D-08.5-07: State mutation writes are guarded by path-prefix ACL and per-node capability tokens."
    - "D-08.5-10: Audit records include actor/path/action/decision/reason/timestamp/sequence."
    - "D-08.5-11: Replay target is operational replay, not full deterministic payload replay."
  artifacts:
    - path: "src/ports/run-state-store-port.ts"
      provides: "Run-state store contract with concurrency, ACL, and audit operation surfaces"
      exports: ["RunStateStorePort"]
    - path: "src/adapters/file-run-state-store.ts"
      provides: "Reference file-backed run-state adapter with append-only mutation logging"
      exports: ["FileRunStateStore"]
  key_links:
    - from: "src/application/execution-controller.ts"
      to: "src/ports/run-state-store-port.ts"
      via: "Checkpoint and node-state persistence through typed store API"
      pattern: "checkpoint|resume|mutation|etag|acl"
    - from: "src/application/runtime-events.ts"
      to: "src/application/control-server.ts"
      via: "Mutation audit event stream and operational replay visibility"
      pattern: "audit|seq|replay|state"
---

<objective>
Implement guarded external run-state continuity with optimistic concurrency, path ACL, append-only audit events, and operational replay support.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.5-typed-artifact-stateful-workflow-runtime/08.5-CONTEXT.md
@.planning/phases/08.5-typed-artifact-stateful-workflow-runtime/08.5-RESEARCH.md
@.planning/phases/08.5-typed-artifact-stateful-workflow-runtime/08.5-01-PLAN.md
@src/application/execution-controller.ts
@src/application/control-server.ts
@src/application/runtime-events.ts
</context>

<tasks>
  <task id="1" type="implementation">
    <name>Task 1: Introduce pluggable run-state store contract and file adapter</name>
    <files>
      <file>src/ports/run-state-store-port.ts</file>
      <file>src/adapters/file-run-state-store.ts</file>
      <file>src/index.ts</file>
    </files>
    <action>
Define a store port covering run metadata, node status snapshots, artifact references, mutation log, checkpoint snapshots, and resume cursor.
Implement a file-backed adapter as the reference persistence mechanism.
Wire adapter construction in composition root without coupling domain logic to storage details.
    </action>
    <verify>
`npm run build` passes and adapter smoke tests pass.
    </verify>
    <acceptance_criteria>
Runtime can persist and load standard run-state scope through a typed store boundary.
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <name>Task 2: Enforce optimistic concurrency and path-level ACL on mutations</name>
    <files>
      <file>src/application/execution-controller.ts</file>
      <file>src/adapters/file-run-state-store.ts</file>
      <file>src/application/control-server.ts</file>
    </files>
    <action>
Add etag/version checks for mutable state operations; reject conflicts explicitly with retry-required responses.
Apply path-prefix ACL + per-node capability token validation for writes.
Surface conflict and ACL-denial errors through CLI/JSON/control-server response paths.
    </action>
    <verify>
`npm test` includes conflict and ACL denial coverage.
    </verify>
    <acceptance_criteria>
No last-write-wins fallback exists; unauthorized/ conflicting mutations are rejected and visible.
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <name>Task 3: Add append-only mutation audit stream and operational replay helpers</name>
    <files>
      <file>src/application/runtime-events.ts</file>
      <file>src/application/mcp-skill-runtime.ts</file>
      <file>src/application/control-server.ts</file>
      <file>tests/run-state-store.test.ts</file>
    </files>
    <action>
Emit mutation audit events for accepted and rejected writes with required core payload fields.
Maintain stable sequence ordering for mutation records.
Expose operational replay data sufficient to reconstruct run decisions/checkpoints without materialized payload replay.
Add tests for audit completeness and replay reconstruction semantics.
    </action>
    <verify>
`npm test` passes with audit and replay coverage.
    </verify>
    <acceptance_criteria>
Audit trail is complete for all mutation attempts and replay support matches operational target contract.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Focused run-state tests for etag conflicts, ACL denials, mutation audit payloads, and operational replay reconstruction
</verification>

<success_criteria>
- Run-state store port and file adapter are wired and usable.
- Mutation writes are concurrency-safe and ACL-guarded.
- Append-only audit events and operational replay helpers are available and tested.
</success_criteria>


# Phase 8.5: Typed Artifact + Stateful Workflow Runtime - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-11
**Phase:** 08.5-typed-artifact-stateful-workflow-runtime
**Areas discussed:** Artifact contract shape, run-state store boundary, concurrency + ACL rules, code-only node runtime model, audit event + replay contract

---

## Artifact contract shape

| Option | Description | Selected |
|--------|-------------|----------|
| Node-level IO schemas | Node owns input/output schema contracts | |
| Edge-level schemas | Edge owns handoff schema contract | |
| Hybrid | Node source-of-truth + optional edge narrowing | ✓ |

**User's choice:** Hybrid model (`3`)
**Notes:** User requested pros/cons before locking.

| Option | Description | Selected |
|--------|-------------|----------|
| Strict reject | Reject mismatches, no coercion | |
| Safe coercion + warning | Coerce selected primitive mismatches with audit warning | |
| Per-node configurable, default strict | Default strict; allow explicit node-level policy | ✓ |

**User's choice:** Strictness configurable per node, default strict (`3`)
**Notes:** Locked after recommendation.

---

## Run-state store boundary

| Option | Description | Selected |
|--------|-------------|----------|
| File-backed only | Single file implementation now | |
| Interface + file adapter | Pluggable store boundary now, file reference adapter | ✓ |
| Full multi-adapter | Multiple adapters in this phase | |

**User's choice:** Boundary `2`
**Notes:** Spelling typo in response (`boundry`) interpreted as boundary.

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal scope | Metadata + statuses + refs only | |
| Standard scope | Minimal + mutation log + checkpoints + resume cursor | ✓ |
| Full scope | Standard + materialized IO payload history | |

**User's choice:** Scope `2`
**Notes:** Pros/cons reviewed before lock.

---

## Concurrency + ACL rules

| Option | Description | Selected |
|--------|-------------|----------|
| Reject-on-conflict | Etag/version mismatch rejects and requires retry | ✓ |
| Last-write-wins | Later write overwrites prior state | |
| Field merge else reject | Merge compatible fields, reject conflicts | |

**User's choice:** Conflict `1`
**Notes:** Chosen after tradeoff review.

| Option | Description | Selected |
|--------|-------------|----------|
| Static role path ACL | Path prefixes by actor role only | |
| Path ACL + capability tokens | Path prefixes plus per-node capability tokens | ✓ |
| Dynamic policy engine | Runtime policy evaluation engine | |

**User's choice:** ACL `2`
**Notes:** Selected with least-privilege focus.

---

## Code-only node runtime model

| Option | Description | Selected |
|--------|-------------|----------|
| Shared lifecycle | Same lifecycle as model nodes | |
| Separate lifecycle | Dedicated lifecycle for code-only nodes | |
| Shared + code subtype event | Shared lifecycle with dedicated `code_execution` subtype | ✓ |

**User's choice:** Contract `3`
**Notes:** Preferred added observability without splitting lifecycle model.

| Option | Description | Selected |
|--------|-------------|----------|
| In-process guarded APIs | Execute code nodes inside host process | |
| Subprocess isolation | Isolated subprocess per code node | |
| Configurable default subprocess | Configurable with safe subprocess default | ✓ |

**User's choice:** Sandbox `3`
**Notes:** Selected after pros/cons.

---

## Audit event + replay contract

| Option | Description | Selected |
|--------|-------------|----------|
| Core payload | actor/path/action/decision/reason/timestamp/seq | ✓ |
| Extended payload | core + hash/version/token/node metadata | |
| Full payload snapshots | extended + value snapshots | |

**User's choice:** Audit `1`
**Notes:** User chose lean core payload.

| Option | Description | Selected |
|--------|-------------|----------|
| Operational replay | Reconstruct decisions/checkpoints | ✓ |
| Deterministic structural replay | Rebuild state transitions with artifact refs | |
| Full deterministic replay | Include materialized payloads | |

**User's choice:** Replay `1`
**Notes:** User input used `operational=1`; interpreted as replay target.

---

## the agent's Discretion

- No explicit "you decide" responses were given.

## Deferred Ideas

- Deterministic full replay with payload materialization.
- Extended/full mutation audit payload variants.

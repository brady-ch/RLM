---
phase: 116-application-layer-removal
plan: 03
type: execute
wave: 2
depends_on:
  - "116-01"
  - "116-02"
files_modified:
  - tests/domain/recursion/recursive-language-model.test.ts
  - tests/integration/integration-v15.test.ts
  - tests/adapters/persistence/run-state-store.test.ts
  - tests/helpers/mock-plan-model.ts
  - src/plugins/__depcruise-fixtures__/forbidden-application-import.ts
  - tests/depcruise/concern-map-rules.unit.test.ts
autonomous: true
requirements:
  - RETIRE-116-03
  - RETIRE-116-04
user_setup: []
must_haves:
  truths:
    - "No kept TS file imports from deleted src/application/ modules"
    - "cargo test -p rlm-core passes"
    - "npm run rlm -- ask --help invokes Rust CLI"
    - "Ask, UI, graph workflow paths available via Rust subcommands"
  artifacts:
    - path: crates/rlm-cli/src/commands/
      provides: "Rust ask, ui, workflow subcommands"
  key_links:
    - from: npm run rlm
      to: crates/rlm-cli
      via: scripts/rlm-runtime.mjs
      pattern: "ask --help"
---

<objective>
Prune orphaned TS files that imported deleted application modules, then run all 113-GATES Phase 116 verification commands.

Purpose: ROADMAP success criterion 4 requires no remaining application imports in kept files; Rust gates prove ask/UI/workflow paths work without TS application layer.
Output: Clean import graph; Phase 116 gates green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-application-layer-removal/116-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/phases/115-cli-entry-and-runtime-composition-removal/115-03-SUMMARY.md

<interfaces>
113-GATES Phase 116 gate commands (ALL must pass):

1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — exit 0
2. `test ! -d src/application` — exit 0
3. `npm run rlm -- ask --help` — exit 0

Additional ROADMAP checks:
4. Subcommand smoke (Rust paths for ask, ui, workflow):
   - `npm run rlm -- ask --help`
   - `npm run rlm -- ui --help`
   - `npm run rlm -- workflow-export --help`
   - `npm run rlm -- workflow-import --help`

Import grep gate (success criterion 4):
```bash
grep -r "src/application\|from ['\"].*application/" src/ tests/ --include='*.ts' \
  | grep -v 'application/json' | grep -v 'application/xhtml' && exit 1 || exit 0
```

Files still importing application (must fix or delete):

| File | Action |
|------|--------|
| `tests/domain/recursion/recursive-language-model.test.ts` | **Delete entire file** — 100+ tests, 67 application import usages; Rust coverage in `crates/rlm-core/tests/recursive_engine_session.rs` and domain recursion tests; Phase 117 deletes remaining `tests/domain/` anyway |
| `tests/integration/integration-v15.test.ts` | **Delete entire file** — sole purpose is TS application orchestration integration (Phase 118 scope; delete now to satisfy import gate) |
| `tests/adapters/persistence/run-state-store.test.ts` | **Inline** minimal `createMutationAuditEvent` helper locally (copy logic from deleted `runtime-events.ts` — only used in one test at line ~89) |
| `tests/helpers/mock-plan-model.ts` | **Delete** — only consumer is recursive-language-model.test.ts |
| `src/plugins/__depcruise-fixtures__/forbidden-application-import.ts` | **Delete** — probe imports deleted application path |
| `tests/depcruise/concern-map-rules.unit.test.ts` | **Remove** test `"no-plugins-to-application fails strict depcruise with named rule"` (lines 32–61); keep rule registration test (fixture file gone) |

Pre-existing note (115-03): `cargo test -p rlm-core` had 2 failing `model_library_routes` tests. Executor MUST run full suite; if failures persist and are pre-existing unrelated failures, document in SUMMARY with test names and file issue — but gate requires exit 0, so fix if regressions are in-scope or isolate with `cargo test -p rlm-core --lib` only if full integration tests are genuinely out of scope (prefer fixing or skipping broken tests with `#[ignore]` + comment only as last resort per project norms).

Transitional exceptions (document in SUMMARY, do NOT block gate):
- `npm run build` / `npm run typecheck` may still fail until Phase 117–119 — not Phase 116 gates
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete application-coupled test files</name>
  <files>tests/domain/recursion/recursive-language-model.test.ts, tests/integration/integration-v15.test.ts, tests/helpers/mock-plan-model.ts, src/plugins/__depcruise-fixtures__/forbidden-application-import.ts</files>
  <action>
    Delete files that cannot load without deleted application modules:

    1. `git rm tests/domain/recursion/recursive-language-model.test.ts`
       - Rationale: module-level imports from `src/application/*` (agent-registry, memory-manager, workflow-runner, execution-controller, etc.); Rust has `crates/rlm-core/tests/recursive_engine_session.rs` and mirrored domain recursion tests
       - Do NOT delete other `tests/domain/recursion/*.unit.test.ts` files — they have no application imports

    2. `git rm tests/integration/integration-v15.test.ts`
       - Rationale: imports execution-controller, graph-workflow-store, workflow-runner, memory-manager from application

    3. `git rm tests/helpers/mock-plan-model.ts`
       - Rationale: sole consumer deleted; imports `PlannedChildSpec` from application graph-planner

    4. `git rm src/plugins/__depcruise-fixtures__/forbidden-application-import.ts`
       - Rationale: intentional violation probe imports deleted `application/agent-registry.js`

    Scope guard: Do NOT delete remaining `tests/domain/recursion/budget-guard.unit.test.ts`, `execution-graph-sync.unit.test.ts`, `prompt-utilities.unit.test.ts`, or `tests/domain/constrained-tool-calling.test.ts`.
  </action>
  <verify>
    <automated>test ! -f tests/domain/recursion/recursive-language-model.test.ts && test ! -f tests/integration/integration-v15.test.ts && test ! -f src/plugins/__depcruise-fixtures__/forbidden-application-import.ts</automated>
  </verify>
  <done>Application-coupled test files and depcruise probe removed; pure domain unit tests retained</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining application imports and depcruise test</name>
  <files>tests/adapters/persistence/run-state-store.test.ts, tests/depcruise/concern-map-rules.unit.test.ts</files>
  <action>
    1. **run-state-store.test.ts:** Remove import of `createMutationAuditEvent` from application. Add a local inline helper at top of file (after imports):

    ```typescript
    function createMutationAuditEvent(input: {
      runId: string; seq: number; actor: string; path: string;
      action: "set" | "delete"; accepted: boolean; reason: string; occurredAt: string;
    }) {
      const decision = input.accepted ? "accepted" : "rejected";
      return {
        runId: input.runId,
        code: "RUN_STATE_MUTATION",
        severity: input.accepted ? "info" : "warn",
        source: "run-state",
        subject: `${input.actor}:${input.path}`,
        occurredAt: input.occurredAt,
        seq: input.seq,
        message: `${decision} ${input.action} on ${input.path}: ${input.reason}`,
      };
    }
    ```
    Only fields used by the test need to be present — read test assertions at line ~89 and match shape.

    2. **concern-map-rules.unit.test.ts:** Delete the entire test block `"no-plugins-to-application fails strict depcruise with named rule"` (async test cruising forbidden-application-import.ts). Keep `"plugin and runtime depcruise rules are registered..."` test unchanged — rule still registered even though probe file removed.

    3. Run import grep gate to confirm zero application path imports remain in kept files.
  </action>
  <verify>
    <automated>grep -r "src/application\|from ['\"].*application/" src/ tests/ --include='*.ts' | grep -v 'application/json' | grep -v 'application/xhtml' | grep -v 'text/html,text/plain,application/xhtml' && exit 1 || exit 0</automated>
  </verify>
  <done>No kept `.ts` file imports deleted application modules; depcruise meta-test updated</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 116 verification gates</name>
  <files></files>
  <action>
    Run all 113-GATES Phase 116 commands sequentially:

    1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — must exit 0
    2. `test ! -d src/application` — must exit 0
    3. `test ! -d tests/application` — must exit 0
    4. `npm run rlm -- ask --help` — must exit 0
    5. **Subcommand smoke** (ROADMAP criterion 3 — Rust-only paths):
       - `npm run rlm -- ui --help`
       - `npm run rlm -- workflow-export --help`
       - `npm run rlm -- workflow-import --help`
    6. `npm run test:agent:verify:light` — confirm still green after Plan 02 changes

    Document in SUMMARY:
    - Gate results table (pass/fail per command)
    - Any pre-existing cargo test failures encountered and resolution
    - Transitional: `npm run build` / `typecheck` status (expected fail until Phase 117–119)
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core && test ! -d src/application && npm run rlm -- ask --help</automated>
  </verify>
  <done>All 113-GATES Phase 116 commands pass; Rust ask/UI/workflow subcommands expose --help</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm rlm → Rust CLI | User-facing ask/ui/workflow must not fall back to deleted TS application |
| Remaining TS tests → src/ | Must not reference deleted application paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-04 | T | Deleted recursive-language-model.test.ts coverage gap | transfer | Rust tests: `recursive_engine_session.rs`, domain recursion unit tests in crates/rlm-core |
| T-116-05 | E | Stale depcruise probe referencing deleted path | mitigate | Delete fixture + remove meta-test that cruises it |
| T-116-06 | D | cargo test gate failure | mitigate | Run RAM-gated full suite; fix in-scope failures before marking complete |
</threat_model>

<verification>
113-GATES Phase 116 full checklist:
1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — exit 0
2. `test ! -d src/application` — exit 0
3. `npm run rlm -- ask --help` — exit 0
4. Import grep gate — exit 0
5. `npm run test:agent:verify:light` — exit 0
</verification>

<success_criteria>
- No remaining TS imports from deleted application modules
- All Phase 116 gates pass
- Rust ask, ui, workflow-export, workflow-import subcommands available
</success_criteria>

<output>
After completion, create `.planning/phases/116-application-layer-removal/116-03-SUMMARY.md`
</output>

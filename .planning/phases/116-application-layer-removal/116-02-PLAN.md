---
phase: 116-application-layer-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/application/
  - scripts/agent-safe-verify.mjs
autonomous: true
requirements:
  - RETIRE-116-02
user_setup: []
must_haves:
  truths:
    - "tests/application/ directory no longer exists"
    - "agent-safe-verify light profile no longer runs deleted application tests"
    - "Rust application tests under crates/rlm-core/tests/application/ remain"
  artifacts:
    - path: scripts/agent-safe-verify.mjs
      provides: "Light verify profile without tests/application imports"
    - path: crates/rlm-core/tests/application/
      provides: "Mirrored Rust application tests"
  key_links:
    - from: scripts/agent-safe-verify.mjs
      to: crates/rlm-core
      via: "cargo check step (unchanged)"
      pattern: "cargo check -p rlm-cli -p rlm-core"
---

<objective>
Delete mirrored TypeScript application tests and update agent-safe-verify light profile to stay green without `tests/application/`.

Purpose: TS application tests duplicate Rust coverage; keeping them blocks clean deletion and breaks light verify.
Output: Removed `tests/application/` (~14 files); updated `scripts/agent-safe-verify.mjs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-application-layer-removal/116-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@scripts/agent-safe-verify.mjs

<interfaces>
113-AUDIT.md mirrored tests to delete (14 files under `tests/application/`):

```
tests/application/bootstrap/          (already deleted Phase 115)
tests/application/config/             (6 test files)
tests/application/execution/          (2 test files)
tests/application/graph/              (5 test files)
tests/application/memory/             (2 test files)
tests/application/plugins/            (1 test file)
```

Rust counterparts KEEP:
```
crates/rlm-core/tests/application/config/loader.rs
crates/rlm-core/tests/application/memory/*.rs
```

agent-safe-verify.mjs light profile (current — MUST update):
```javascript
light: [
  { label: "cargo check (rlm-core, rlm-cli)", cmd: "cargo check -p rlm-cli -p rlm-core", tier: "compile" },
  { label: "config validation (tsx, no full build)",
    cmd: `node --import ${PRELOAD} --import tsx --test tests/application/config/validation.unit.test.ts`, tier: "minimal" },
  { label: "reg03 static wiring (tsx)", cmd: `... tests/ui/reg03-static-wiring.test.ts`, tier: "minimal" },
]
```

Replace config validation step with Rust config test:
`node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core application::config -- --nocapture`
Or if module path differs, use: `cargo test -p rlm-core config_loader` or the loader test file name from `crates/rlm-core/tests/application/config/loader.rs`.

Fallback if no matching filter: use `cargo test -p rlm-core loader -- --nocapture` and verify at least one test runs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tests/application/ mirror</name>
  <files>tests/application/</files>
  <action>
    Delete entire mirrored TS application test tree per 113-AUDIT.md:

    1. `git rm -r tests/application/`

    Expected ~14 `.test.ts` files across config/, execution/, graph/, memory/, plugins/ subdirs. Bootstrap tests were already removed Phase 115.

    Scope guard:
    - Do NOT delete `crates/rlm-core/tests/application/` — Rust tests remain authoritative
    - Do NOT delete `tests/domain/`, `tests/adapters/`, `tests/plugins/` — Phase 117–118
  </action>
  <verify>
    <automated>test ! -d tests/application</automated>
  </verify>
  <done>`tests/application/` fully removed; Rust application tests untouched</done>
</task>

<task type="auto">
  <name>Task 2: Update agent-safe-verify light profile</name>
  <files>scripts/agent-safe-verify.mjs</files>
  <action>
    Update `PROFILES.light` in `scripts/agent-safe-verify.mjs`:

    1. **Remove** the step referencing `tests/application/config/validation.unit.test.ts`.

    2. **Replace** with a Rust config smoke step using RAM gate:
       ```javascript
       {
         label: "rust config loader smoke",
         cmd: "node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core loader -- --nocapture",
         tier: "compile",
       }
       ```
       If `loader` filter matches too broadly, use the specific test name from `crates/rlm-core/tests/application/config/loader.rs` (read file for `#[test]` fn names).

    3. Keep unchanged: `cargo check -p rlm-cli -p rlm-core` and `reg03 static wiring` steps.

    4. Run `npm run test:agent:verify:light` to confirm 3/3 steps pass.

    Do NOT modify reg01/reg03 profiles unless they reference `tests/application/` (grep first; remove if found).
  </action>
  <verify>
    <automated>npm run test:agent:verify:light</automated>
  </verify>
  <done>Light agent verify passes without any `tests/application/` reference</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-safe-verify → deleted tests | Light profile must not invoke missing test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | D | agent-safe-verify light profile | mitigate | Replace TS config test with `cargo test -p rlm-core loader` RAM-gated step |
</threat_model>

<verification>
- `test ! -d tests/application` — exit 0
- `npm run test:agent:verify:light` — exit 0
</verification>

<success_criteria>
- `tests/application/` deleted
- Agent-safe light verify green with Rust config smoke
</success_criteria>

<output>
After completion, create `.planning/phases/116-application-layer-removal/116-02-SUMMARY.md`
</output>

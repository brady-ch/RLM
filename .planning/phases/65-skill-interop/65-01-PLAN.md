---
phase: 65
plan: 01
type: execute
autonomous: true
requirements: [PLUG-01, PLUG-02, REG-02]
---

# Phase 65 Plan 01 — Skill Interop

## Objective

Port skill discovery, path policies, and `skill` tool to Rust; wire manifest skillLoaders; doctor warnings; preserve init order.

## Tasks

1. Create `65-CONTEXT.md`
2. Port skill discovery + path policies to `crates/rlm-core/src/interop/skill_runtime.rs`
3. Register `skill` tool in interop init (preserve `COMPOSITION_INIT_ORDER`)
4. Wire manifest `skillLoaders` through extension host
5. Plugin doctor warnings for invalid skill paths
6. Tests: load fixture skill; reject policy violation
7. Run `npm run check:rust`
8. Write plan/summary/verification, update ROADMAP.md STATE.md REQUIREMENTS.md

## Verification

- `npm run check:rust` green
- Skill tool loads fixture SKILL.md from configured search path
- Strict path policy rejects invalid skill with error
- Init order unchanged

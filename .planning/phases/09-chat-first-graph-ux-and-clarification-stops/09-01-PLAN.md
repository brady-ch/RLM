---
phase: "09"
plan: "09-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["CHAT-01"]
files_modified:
  - ui/src/main.tsx
  - src/application/control-server.ts
  - src/application/execution-controller.ts
  - src/domain/types.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "D-09-01: Ready-to-run requires explicit confirm action."
    - "D-09-02: Draft run control remains disabled with reason text."
    - "D-09-03: Ambiguous edits require clarification before mutation."
    - "D-09-04: Target by label/name with disambiguation prompt."
    - "D-09-05: Dependency delete requires explicit delete-subtree or rewire choice."
    - "D-09-06: Proposed mutations are previewed inline in chat before apply."
  artifacts:
    - path: "ui/src/main.tsx"
      provides: "Chat-first authoring UI, draft/ready control state, inline mutation preview interactions"
      exports: ["chat session state", "run readiness controls"]
    - path: "src/application/execution-controller.ts"
      provides: "Authoritative mutation application path reused by chat-issued mutations"
      exports: ["graph mutation handlers"]
  key_links:
    - from: "ui/src/main.tsx"
      to: "src/application/control-server.ts"
      via: "chat intent and mutation preview/apply flow"
      pattern: "chat|mutation|preview|confirm"
    - from: "src/application/control-server.ts"
      to: "src/application/execution-controller.ts"
      via: "validated apply path for add/edit/delete/rewire operations"
      pattern: "mutation|validate|apply"
---

<objective>
Deliver chat-first pre-run graph authoring so users can create and refine execution graphs conversationally before execution starts.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-UI-SPEC.md
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-RESEARCH.md
@ui/src/main.tsx
@src/application/control-server.ts
@src/application/execution-controller.ts
</context>

<tasks>
  <task id="1" type="implementation">
    <name>Task 1: Add chat session draft/ready state with explicit confirm-to-run gate</name>
    <files>
      <file>ui/src/main.tsx</file>
      <file>src/domain/types.ts</file>
    </files>
    <read_first>
      <file>ui/src/main.tsx</file>
      <file>src/domain/types.ts</file>
      <file>.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md</file>
      <file>.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-UI-SPEC.md</file>
    </read_first>
    <action>
Add chat-session state that tracks draft versus ready-to-run, with explicit `Confirm graph and run` action as the only transition to ready.
Keep run control disabled while draft and render deterministic reason text listing missing readiness conditions.
Wire UI state to typed model fields so readiness state is explicit and testable.
    </action>
    <verify>
`npm run build` passes.
    </verify>
    <acceptance_criteria>
Run cannot start from draft state without explicit confirm action, and disabled control always shows reason text.
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <name>Task 2: Implement inline chat mutation preview and disambiguation-first apply flow</name>
    <files>
      <file>ui/src/main.tsx</file>
      <file>src/application/control-server.ts</file>
      <file>src/application/execution-controller.ts</file>
    </files>
    <read_first>
      <file>ui/src/main.tsx</file>
      <file>src/application/control-server.ts</file>
      <file>src/application/execution-controller.ts</file>
      <file>.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md</file>
    </read_first>
    <action>
Implement chat intent handling that produces a proposed mutation card inline in chat before apply.
Require clarifying question for ambiguous node targets and use label/name matching with disambiguation prompt before emitting apply commands.
Route apply path through existing controller-authoritative validation, not direct client-side graph mutation.
    </action>
    <verify>
`npm run build && npm test` passes.
    </verify>
    <acceptance_criteria>
No ambiguous request mutates graph directly; each applied mutation has a preview step and validated server/controller apply path.
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <name>Task 3: Enforce dependency-impacting delete decision (delete subtree vs rewire)</name>
    <files>
      <file>src/application/execution-controller.ts</file>
      <file>src/application/control-server.ts</file>
      <file>ui/src/main.tsx</file>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <read_first>
      <file>src/application/execution-controller.ts</file>
      <file>src/application/control-server.ts</file>
      <file>ui/src/main.tsx</file>
      <file>tests/recursive-language-model.test.ts</file>
      <file>.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md</file>
    </read_first>
    <action>
Add dependency analysis for delete requests and block immediate delete when downstream edges exist.
Present explicit user choice model with two options: `delete_subtree` and `rewire_dependents`, and only apply through validated rewrite routines.
Add tests that assert blocked delete without choice and successful application for each explicit choice path.
    </action>
    <verify>
`npm test` passes with delete/rewire coverage.
    </verify>
    <acceptance_criteria>
Dependency-impacting deletes never proceed silently; exactly two explicit user choices are supported and validated.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Focused checks for draft readiness gate, disambiguation-before-mutation, and dependency delete choice flows
</verification>

<success_criteria>
- Users can build and refine pending graphs through conversation before execution.
- Ready-to-run transition is explicit and gated by confirm action.
- Mutation application is previewed, disambiguated, and controller-validated.
</success_criteria>

---
phase: "09"
plan: "09-02"
type: "standard"
wave: 2
depends_on: ["09-01"]
requirements_addressed: ["QUES-01"]
files_modified:
  - src/application/execution-controller.ts
  - src/application/control-server.ts
  - src/application/runtime-events.ts
  - src/domain/types.ts
  - ui/src/main.tsx
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "D-09-07: Clarification prompts are hard blocking."
    - "D-09-08: No skip/dismiss; only answer-and-continue or abort."
    - "D-09-09: Abort persists checkpoint snapshot plus pending question."
    - "D-09-10: Clarification history is visible in UI and CLI json-stream."
    - "D-09-11: Clarification records include required schema fields."
  artifacts:
    - path: "src/application/execution-controller.ts"
      provides: "Clarification hard-stop state machine and resume/abort transitions"
      exports: ["clarification checkpoint lifecycle"]
    - path: "src/application/runtime-events.ts"
      provides: "Canonical clarification question/answer/resume event payloads"
      exports: ["clarification history record schema"]
  key_links:
    - from: "src/application/execution-controller.ts"
      to: "src/application/runtime-events.ts"
      via: "ask->answer->resume and ask->abort transition event emission"
      pattern: "clarification|pause|resume|abort|history"
    - from: "src/application/runtime-events.ts"
      to: "ui/src/main.tsx"
      via: "UI timeline rendering for clarification records"
      pattern: "json-stream|timeline|record"
---

<objective>
Deliver explicit clarification-stop runtime behavior and auditable clarification history across UI and CLI surfaces with no silent continuation path.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-UI-SPEC.md
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-RESEARCH.md
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-01-PLAN.md
@src/application/execution-controller.ts
@src/application/control-server.ts
@src/application/runtime-events.ts
@src/cli/render.ts
</context>

<tasks>
  <task id="1" type="implementation">
    <name>Task 1: Add hard-blocking clarification checkpoint state machine</name>
    <files>
      <file>src/application/execution-controller.ts</file>
      <file>src/application/control-server.ts</file>
      <file>src/domain/types.ts</file>
    </files>
    <read_first>
      <file>src/application/execution-controller.ts</file>
      <file>src/application/control-server.ts</file>
      <file>src/domain/types.ts</file>
      <file>.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md</file>
    </read_first>
    <action>
Implement clarification-required checkpoint states that halt node/run advancement until explicit response.
Allow only two actions for clarification checkpoints: `answer_and_continue` and `abort_run`; reject skip/dismiss transitions.
Ensure unresolved clarification checkpoints block downstream execution scheduling.
    </action>
    <verify>
`npm run build` passes.
    </verify>
    <acceptance_criteria>
Runtime cannot continue past clarification checkpoints without explicit answer or abort; skip path does not exist.
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <name>Task 2: Persist abort snapshot + pending question and emit canonical history records</name>
    <files>
      <file>src/application/execution-controller.ts</file>
      <file>src/application/runtime-events.ts</file>
      <file>src/domain/types.ts</file>
    </files>
    <read_first>
      <file>src/application/execution-controller.ts</file>
      <file>src/application/runtime-events.ts</file>
      <file>src/domain/types.ts</file>
      <file>.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md</file>
    </read_first>
    <action>
On clarification-triggered abort, persist full checkpoint snapshot with pending question payload in run state.
Define and emit clarification history records with required fields: `question_id`, `node_id`, `prompt_text`, `user_answer`, `asked_at`, `answered_at`, `resume_event_id`.
Keep schema shared through domain types so UI and CLI consume identical record shape.
    </action>
    <verify>
`npm test` passes with abort persistence and field-level record assertions.
    </verify>
    <acceptance_criteria>
Abort path always preserves resume-relevant snapshot + question, and clarification records include all required fields.
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <name>Task 3: Surface clarification history in UI timeline and CLI json-stream</name>
    <files>
      <file>ui/src/main.tsx</file>
      <file>src/cli/render.ts</file>
      <file>src/application/control-server.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <read_first>
      <file>ui/src/main.tsx</file>
      <file>src/cli/render.ts</file>
      <file>src/application/control-server.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
      <file>.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-UI-SPEC.md</file>
    </read_first>
    <action>
Render clarification Q/A records in UI run history timeline using shared record schema.
Emit clarification events in `--json-stream` output with canonical field names and resume linkage.
Add tests verifying dual-surface visibility and schema parity for question/answer/resume sequences.
    </action>
    <verify>
`npm test` passes with UI/CLI clarification history assertions.
    </verify>
    <acceptance_criteria>
Clarification history is visible and schema-consistent across both UI and CLI json-stream outputs.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run build`
- `npm test`
- Focused tests for hard-stop transitions, abort snapshot persistence, and dual-surface clarification history
</verification>

<success_criteria>
- Clarification prompts hard-stop execution with answer-or-abort semantics only.
- Abort preserves complete resume context including pending question.
- Clarification history records are visible and consistent in both UI and CLI surfaces.
</success_criteria>

---
phase: 47-concern-map-tests-depcruise
plan: 01
type: execute
autonomous: true
wave: 1
requirements: [TAXN-01, TAXN-05, TAXN-06, DEPS-02]
---

# Phase 47 Plan 01: Concern Map, Tests Mirror & Depcruise Rules

## Objective

Publish canonical concern map in AGENTS.md, mirror tests/ to stabilized src/ layout, and add dependency-cruiser rules for plugins/ and runtime/ paths before Phase 48 ratchet.

## Tasks

### Task 1: Canonical concern map in AGENTS.md (auto)

Add Concern Map section covering cli, application, domain, ports, runtime, plugins, adapters, tests/, ui/, scripts/. Document dependency direction and depcruise rule names.

### Task 2: Mirror tests/ to src/ concerns (auto)

Move root-level tests into tests/runtime/, tests/application/*, tests/adapters/, tests/plugins/, tests/domain/, tests/integration/. Update relative imports. Keep tests/helpers/ shared.

### Task 3: Depcruise rules for plugins/ and runtime/ (auto)

Add warn-severity forbidden arcs (plugins→application/cli/domain, runtime→cli, builtin→external-loader). Rule comments reference AGENTS.md concern map. Add tests/depcruise/concern-map-rules.unit.test.ts verifying rule names. npm run check green.

## Success Criteria

1. AGENTS.md publishes canonical concern map
2. tests/ mirrors src/ concerns with helpers in tests/helpers/
3. dependency-cruiser rules encode taxonomy for plugins/ and runtime/
4. Violations show rule names referencing concern map

---
phase: "10"
plan: "10-02"
type: "standard"
wave: 2
depends_on: ["10-01"]
requirements_addressed: ["DIST-02", "DIST-03"]
files_modified:
  - src/application/project-config.ts
  - src/cli/args.ts
  - src/index.ts
  - src/cli/first-run.ts
  - tests/project-config-scopes.test.ts
autonomous: true
must_haves:
  truths:
    - "D-10-06: rlm on PATH via native install shim (primary) — satisfied by release stubs + doc; npm -g remains fallback per D-10-05."
    - "D-10-07: cwd is default project root."
    - "D-10-08–D-10-12: ~/.rlm and <project>/.rlm layout with project replacing global agent/model entries by id."
    - "D-10-13–D-10-17: Interactive numbered UI vs CLI choice; default UI."
    - "D-10-18–D-10-20: Prerequisite/setup offers are explicit; project-local seed only."
  artifacts:
    - path: "src/application/project-config.ts"
      provides: "Multi-scope YAML composition into ProjectConfig"
      exports: ["loadProjectConfigScoped()", or extended `loadProjectConfig`]
    - path: "src/cli/first-run.ts"
      provides: "TTY mode chooser and non-interactive bypass"
      exports: ["resolveLaunchMode()"]
  key_links:
    - from: "src/index.ts"
      to: "src/cli/first-run.ts"
      via: "pre-boot interactive mode"
      pattern: "launch|mode|TTY"
    - from: "src/application/project-config.ts"
      to: "src/index.ts"
      via: " Effective config drives agents/workflows"
      pattern: "loadProjectConfig"
---

<objective>
Deliver global + project config directory resolution with replace-by-id semantics, zero-doc first-run mode selection (UI default), and explicit starter seeding under the current project — without silent failures or silent privileged installs.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-cross-platform-executable-packaging-and-install-ux/10-CONTEXT.md
@.planning/phases/10-cross-platform-executable-packaging-and-install-ux/10-UI-SPEC.md
@.planning/phases/10-cross-platform-executable-packaging-and-install-ux/10-RESEARCH.md
@.planning/phases/10-cross-platform-executable-packaging-and-install-ux/10-PATTERNS.md
@src/application/project-config.ts
@src/cli/args.ts
@src/index.ts
@.planning/phases/09-chat-first-graph-ux-and-clarification-stops/09-CONTEXT.md
</context>

<tasks>
  <task id="1" type="implementation">
    <name>Task 1: Compose config from ~/.rlm and <cwd>/.rlm with project override by agent/model id</name>
    <files>
      <file>src/application/project-config.ts</file>
      <file>tests/project-config-scopes.test.ts</file>
    </files>
    <read_first>
      <file>src/application/project-config.ts</file>
      <file>.planning/phases/10-cross-platform-executable-packaging-and-install-ux/10-CONTEXT.md</file>
    </read_first>
    <action>
Implement layered load: parse optional global `~/.rlm/config.yaml` (or CONTEXT-approved filenames) plus `~/.rlm/agents/*.yaml` and `~/.rlm/models/*.yaml` if present; parse project `./.rlm/` mirror; merge into one `ProjectConfig` with rule: for `agents` and `models` maps, project entries **replace** global entries for the same key (D-10-11). Preserve backward compatibility: if `./rlm.config.yaml` exists and `.rlm/` absent, existing single-file behavior remains unchanged. Add pure-merge unit tests with temp dirs under `tests/project-config-scopes.test.ts`.
    </action>
    <verify>
`npm run build && node --test dist/tests/project-config-scopes.test.js`
    </verify>
    <acceptance_criteria>
Tests prove: global agent `a` + project agent `a` yields project definition; global-only agents remain; invalid YAML throws with message containing file path (no silent default).
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <name>Task 2: First-run interactive mode chooser with non-interactive bypass</name>
    <files>
      <file>src/cli/first-run.ts</file>
      <file>src/index.ts</file>
      <file>src/cli/args.ts</file>
    </files>
    <read_first>
      <file>src/index.ts</file>
      <file>src/cli/args.ts</file>
      <file>.planning/phases/10-cross-platform-executable-packaging-and-install-ux/10-UI-SPEC.md</file>
    </read_first>
    <action>
Before branching into `ui` vs `ask`, when stdin is TTY and env `RLM_NON_INTERACTIVE` is **not** `1`, print numbered UI vs CLI choice per UI-SPEC; default Enter maps to UI. When `RLM_NON_INTERACTIVE=1`, skip prompt and use default UI unless `RLM_LAUNCH_MODE=cli` explicitly set (exact env names documented in `helpText()` addition). Wire choice so UI path still uses existing `parseArgs` / control server startup; CLI path enters interactive prompt behavior described in CONTEXT (D-10-16).
    </action>
    <verify>
`npm run build`; add tests using mocked `process.stdin.isTTY` or refactor small pure function `resolveLaunchMode(env, isTTY, answers)` tested without real TTY.
    </verify>
    <acceptance_criteria>
`helpText()` includes the two env vars and their meanings; `resolveLaunchMode` tests cover: TTY + default → UI; `RLM_NON_INTERACTIVE=1` → no prompt; `RLM_LAUNCH_MODE=cli` forces CLI without prompt when non-interactive.
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <name>Task 3: Project-local starter seed for empty .rlm (no global seed)</name>
    <files>
      <file>src/application/project-config.ts</file>
      <file>src/index.ts</file>
    </files>
    <read_first>
      <file>.planning/phases/10-cross-platform-executable-packaging-and-install-ux/10-CONTEXT.md</file>
      <file>src/application/project-config.ts</file>
    </read_first>
    <action>
When no effective config exists in project scope and user enters UI mode for the first time, create `<cwd>/.rlm/` with minimal starter `config.yaml`, one sample agent file, one sample model file mirroring current `rlm.config.yaml` shape enough for UI to run sample graph (D-10-12, D-10-20). Do **not** write to `~/.rlm/` automatically. Log clearly to stderr what was created. If creation fails, exit with non-zero code and message.
    </action>
    <verify>
Extend `project-config-scopes.test.ts` or add integration test with temp directory; `npm run build && node --test dist/tests/project-config-scopes.test.js`
    </verify>
    <acceptance_criteria>
Seeding creates only under cwd `.rlm/`; a second run does not duplicate if files exist; failure paths throw or exit with message string `starter` or `seed` for grep in tests.
    </acceptance_criteria>
  </task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Home dir `~/.rlm` | User-owned; loader must reject symlink escape outside home when resolving paths |
| Project `.rlm/` | Writable by project; must not execute binaries from config dir |
| Setup offers | User must confirm before running install scripts |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | Malicious YAML in `.rlm/` | mitigate | Keep zod schema validation; reject unknown keys per existing schema rules |
| T-10-04 | Elevation | Auto-install prerequisite | mitigate | Require explicit `yes` on prompt before shelling out to installers; default deny |
| T-10-05 | Information disclosure | Starter files include secrets | mitigate | Seed only public template values; no API keys in templates |
</threat_model>

<verification>
- `npm test` passes
- New tests cover config merge and launch mode resolution
- Manual smoke: `RLM_NON_INTERACTIVE=1 node dist/src/index.js ui` skips prompt (documented)
</verification>

<success_criteria>
- DIST-02: Packaged `rlm` stub + cwd semantics documented in `helpText` / stderr hints
- DIST-03: Fresh cwd run reaches UI with sample graph without manual YAML edit
</success_criteria>

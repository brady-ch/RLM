---
phase: 69-large-file-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/plugins/registry/mod.rs
  - crates/rlm-core/src/plugins/registry/types.rs
  - crates/rlm-core/src/plugins/registry/catalog.rs
  - crates/rlm-core/src/plugins/registry/allowlist.rs
  - crates/rlm-core/src/plugins/registry/install.rs
  - crates/rlm-core/src/plugins/registry/doctor.rs
  - crates/rlm-core/src/plugins/registry/service.rs
  - crates/rlm-core/src/plugins/mod.rs
  - crates/rlm-core/src/plugins/registry_service.rs
requirements:
  - ARCH-04
  - REG-02
autonomous: true
must_haves:
  truths:
    - "Plugin registry public DTOs live in plugins/registry/types.rs"
    - "Catalog and allowlist I/O helpers live in dedicated registry submodules"
    - "Install and doctor flows are isolated from list/enable/disable core"
    - "PluginRegistryService API and handler imports unchanged"
    - "plugin_registry and plugin_routes integration tests pass"
  artifacts:
    - path: crates/rlm-core/src/plugins/registry/types.rs
      provides: "PluginListItem, PluginMutationResult, PluginDoctorResult, etc."
    - path: crates/rlm-core/src/plugins/registry/catalog.rs
      provides: "read_catalog, write_catalog, normalize_legacy_extensions"
    - path: crates/rlm-core/src/plugins/registry/allowlist.rs
      provides: "allowlist_key, read_allowlist, write_allowlist, pre_approve_allowlist"
    - path: crates/rlm-core/src/plugins/registry/install.rs
      provides: "install_local, install_remote, install_from_root"
    - path: crates/rlm-core/src/plugins/registry/doctor.rs
      provides: "doctor, inspect, validate_path, collect_skill_loader_issues"
    - path: crates/rlm-core/src/plugins/registry/service.rs
      provides: "PluginRegistryService impl for list/enable/disable/uninstall"
  key_links:
    - from: crates/rlm-core/src/plugins/mod.rs
      to: crates/rlm-core/src/plugins/registry/
      via: "pub mod registry; pub use registry::*"
      pattern: "PluginRegistryService"
    - from: control_server/handlers/plugins.rs
      to: PluginRegistryService
      via: "unchanged method signatures"
      pattern: "PluginRegistryService::"
---

<objective>
Decompose `plugins/registry_service.rs` (~699 LOC) into focused `plugins/registry/` submodules (D-05, ARCH-04).

Purpose: Separate catalog I/O, install flows, and doctor diagnostics for maintainability before Phase 70 boundary checks.
Output: Registry directory module + compatibility re-export at `plugins/registry_service.rs` or removed stub via `plugins/mod.rs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-large-file-decomposition/69-CONTEXT.md
@crates/rlm-core/src/plugins/registry_service.rs
@crates/rlm-core/src/plugins/mod.rs
@crates/rlm-core/tests/plugin_registry.rs
@crates/rlm-core/tests/plugin_routes.rs

<interfaces>
Public exports from plugins/mod.rs (must remain):

```rust
pub use registry_service::{
    PluginDoctorIssue, PluginDoctorResult, PluginListItem, PluginMutationResult,
    PluginRegistryService,
};
```

PluginRegistryService methods: new, with_catalog_overrides, list, install, install_local, install_remote, enable, disable, uninstall, doctor, inspect, validate_path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract types, catalog, and allowlist modules</name>
  <files>crates/rlm-core/src/plugins/registry/mod.rs, crates/rlm-core/src/plugins/registry/types.rs, crates/rlm-core/src/plugins/registry/catalog.rs, crates/rlm-core/src/plugins/registry/allowlist.rs</files>
  <action>
    Per D-05, create `plugins/registry/` directory:

    - Move all public Serialize/Deserialize DTOs and PluginListSource → `types.rs`.
    - Move InstalledPluginCatalog*, read_catalog, write_catalog, normalize_legacy_extensions, mutation_result → `catalog.rs`.
    - Move allowlist_key, read_allowlist, write_allowlist, copy_dir_recursive → `allowlist.rs`.

    `registry/mod.rs` declares submodules and re-exports public types. Keep `PluginRegistryService` struct definition in `service.rs` (next task) with fields unchanged.
  </action>
  <verify>
    <automated>cargo check -p rlm-core 2>&1</automated>
  </verify>
  <done>Registry support modules compile; no behavior change to catalog/allowlist semantics</done>
</task>

<task type="auto">
  <name>Task 2: Extract install, doctor, service; wire plugins facade</name>
  <files>crates/rlm-core/src/plugins/registry/install.rs, crates/rlm-core/src/plugins/registry/doctor.rs, crates/rlm-core/src/plugins/registry/service.rs, crates/rlm-core/src/plugins/registry_service.rs, crates/rlm-core/src/plugins/mod.rs</files>
  <action>
    Per D-05:

    - Move install*, set_enabled, pre_approve_allowlist usage → `install.rs` (impl blocks may use `impl PluginRegistryService` in service.rs calling install helpers).
    - Move doctor, inspect, validate_path, collect_skill_loader_issues, read_manifest_for_* → `doctor.rs`.
    - Move list, enable, disable, uninstall, to_list_item, new, with_catalog_overrides → `service.rs`.
    - Replace `registry_service.rs` with `pub use registry::{...};` re-export OR delete file and update `plugins/mod.rs` to `pub mod registry; pub use registry::*` while preserving `registry_service` path via `pub use registry as registry_service` if any internal imports use old path.

    Do not change JSON response shapes or error strings. Run targeted tests per D-06.
  </action>
  <verify>
    <automated>cargo test -p rlm-core plugin_registry plugin_routes -- --test-threads=1 2>&1 && cargo check -p rlm-core -p rlm-cli 2>&1</automated>
  </verify>
  <done>plugin_registry and plugin_routes tests green; PluginRegistryService callable from handlers with same API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote install → install.rs | Untrusted remote plugin sources |
| filesystem → catalog/allowlist | User plugin directories and catalog JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-03 | T | install.rs | accept | Refactor-only; preserve existing allowlist pre-approve and confirm gates |
| T-69-04 | I | catalog.rs | accept | No new catalog write paths introduced |
</threat_model>

<verification>
Targeted: `cargo test -p rlm-core plugin_registry plugin_routes`
</verification>

<success_criteria>
registry_service.rs eliminated or reduced to re-exports only; no single registry file exceeds ~400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/69-large-file-decomposition/69-02-SUMMARY.md`
</output>

---
phase: 69-large-file-decomposition
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/application/execution/session_graph/mod.rs
  - crates/rlm-core/src/application/execution/session_graph/mutations.rs
  - crates/rlm-core/src/application/execution/session_graph/nodes.rs
  - crates/rlm-core/src/application/execution/session_graph/layout.rs
  - crates/rlm-core/src/application/execution/session_graph/planning.rs
  - crates/rlm-core/src/application/execution/mod.rs
  - crates/rlm-core/src/application/execution/session_graph.rs
requirements:
  - ARCH-04
  - REG-02
autonomous: true
must_haves:
  truths:
    - "Graph node CRUD and connection logic isolated from layout and planning concerns"
    - "Chat mutation preview/apply flow lives in session_graph/mutations.rs"
    - "plan_node and budget extension logic lives in session_graph/planning.rs"
    - "InteractiveExecutionSession public method surface unchanged"
    - "Session graph integration tests pass after split"
  artifacts:
    - path: crates/rlm-core/src/application/execution/session_graph/nodes.rs
      provides: "add_node, connect_node, delete_node, skip_node, edit/set overrides"
    - path: crates/rlm-core/src/application/execution/session_graph/mutations.rs
      provides: "preview_mutation_from_chat, apply_pending_mutation, to_mutation_error"
    - path: crates/rlm-core/src/application/execution/session_graph/layout.rs
      provides: "restore_snapshot, update_graph_layout, set_graph_viewport, workflow metadata"
    - path: crates/rlm-core/src/application/execution/session_graph/planning.rs
      provides: "plan_node, extend_plan_budget, confirm_graph_and_run, default_plan_budget"
  key_links:
    - from: session_graph/mod.rs
      to: InteractiveExecutionSession
      via: "impl blocks in submodules use super::InteractiveExecutionSession"
      pattern: "impl InteractiveExecutionSession"
    - from: control_server/handlers/graph.rs
      to: session methods
      via: "unchanged pub fn names on InteractiveExecutionSession"
      pattern: "session\\."
---

<objective>
Decompose `application/execution/session_graph.rs` (~945 LOC) into concern-based submodules (D-04, ARCH-04).

Purpose: Align session graph maintainability with v1.6 handler split granularity; unblock Phase 70 boundary documentation.
Output: session_graph/ directory with nodes, mutations, layout, planning modules.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-large-file-decomposition/69-CONTEXT.md
@crates/rlm-core/src/execution/session_graph.rs
@crates/rlm-core/src/execution/session.rs
@crates/rlm-core/tests/preview_mutation.rs
@crates/rlm-core/tests/recursive_engine_session.rs
@crates/rlm-core/tests/chat_routes.rs
@crates/rlm-core/tests/graph_executor_routes.rs

<interfaces>
Path note: After Phase 68, session_graph lives at `application/execution/session_graph.rs`. If Phase 68 used a different path, adapt to wherever `mod session_graph` is declared in execution/mod.rs.

Key InteractiveExecutionSession pub methods to preserve (non-exhaustive):
restore_snapshot, update_graph_layout, set_graph_viewport, edit_node_prompt, set_node_model_override, add_node, connect_node, delete_node_with_strategy, skip_node, confirm_graph_and_run, preview_mutation_from_chat, apply_pending_mutation, plan_node, extend_plan_budget, accept_quality_loop, stop_quality_loop, abort_run_from_clarification.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session_graph directory; extract layout and mutations</name>
  <files>crates/rlm-core/src/application/execution/session_graph/mod.rs, crates/rlm-core/src/application/execution/session_graph/layout.rs, crates/rlm-core/src/application/execution/session_graph/mutations.rs, crates/rlm-core/src/application/execution/mod.rs</files>
  <action>
    Per D-04:

    - Convert `session_graph.rs` to directory module `session_graph/`.
    - Move restore_snapshot, update_graph_layout, set_graph_viewport, graph_workflow_metadata_* → `layout.rs`.
    - Move to_mutation_error, mutation_err, set_pending_mutation, resolve_node_target, preview_mutation_from_chat, apply_pending_mutation, clear_pending_mutation → `mutations.rs`.
    - `mod.rs` declares submodules; use `impl InteractiveExecutionSession` in each child module (Rust pattern: `use super::InteractiveExecutionSession`).

    Update `execution/mod.rs` to `mod session_graph;` pointing at directory. Delete old single-file session_graph.rs once compiles.
  </action>
  <verify>
    <automated>cargo test -p rlm-core preview_mutation -- --test-threads=1 2>&1</automated>
  </verify>
  <done>preview_mutation tests green; layout and mutation methods callable on session</done>
</task>

<task type="auto">
  <name>Task 2: Extract nodes and planning; run session integration gate</name>
  <files>crates/rlm-core/src/application/execution/session_graph/nodes.rs, crates/rlm-core/src/application/execution/session_graph/planning.rs, crates/rlm-core/src/application/execution/session_graph/mod.rs</files>
  <action>
    Per D-04:

    - Move add_node, connect_node, delete_node_with_strategy, collect_descendants, rewire_and_delete_node, update_depths_from, skip_node, edit_node_*, set_node_* → `nodes.rs`.
    - Move plan_node, extend_plan_budget, default_plan_budget, confirm_graph_and_run, accept_quality_loop, stop_quality_loop, abort_run_from_clarification → `planning.rs`.
    - Ensure no submodule exceeds ~400 lines; split further within discretion if needed.
    - Run full session-graph targeted test set per D-06.
  </action>
  <verify>
    <automated>cargo test -p rlm-core preview_mutation recursive_engine_session chat_routes graph_executor_routes -- --test-threads=1 2>&1 && cargo check -p rlm-core -p rlm-cli 2>&1</automated>
  </verify>
  <done>All four session graph test binaries green; no handler signature changes required</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP handlers → session graph | Untrusted node mutation payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05 | T | mutations.rs | accept | Refactor-only; preserve MUTATION: error code format |
| T-69-06 | E | nodes.rs | accept | No new graph mutation capabilities added |
</threat_model>

<verification>
Targeted session graph tests per D-06
</verification>

<success_criteria>
session_graph.rs monolith eliminated; submodule max ~400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/69-large-file-decomposition/69-03-SUMMARY.md`
</output>

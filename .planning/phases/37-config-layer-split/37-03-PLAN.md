---
phase: 37-config-layer-split
plan: 03
type: execute
wave: 3
depends_on:
  - "37-02"
files_modified:
  - src/application/config/runtime-resolution.ts
  - src/application/config/model-override.ts
  - src/application/config/host-resolution.ts
  - src/application/config/starter-seed.ts
  - src/application/config/index.ts
  - src/application/project-config.ts
  - tests/config-resolution.unit.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-04
  - CONF-05
  - CONF-06
  - REG-01
  - REG-02
must_haves:
  truths:
    - "applyModelOverride, resolveRuntimeConfig, resolveRuntimeHostSelection, resolveHostConfig, resolveModelTier match pre-phase semantics (CONF-04)."
    - "seedProjectRlmStarter creates same `.rlm` layout and echoes same stderr prefix strings on success/failure as monolith sample (CONF-05)."
    - "Unit tests execute via `npm test` (build + node --test) covering pure helpers without spawning CLI REPL subprocess (CONF-06)."
    - "project-config façade is barrel-only re-export of application/config barrel or equivalent explicit forwarding without widening exports (CONF-02)."
    - "`npm run check` green REG-02; no regression REG-01."
  artifacts:
    - path: src/application/config/runtime-resolution.ts
      provides: "mergeQualityLoop helpers, mergeResolvedQualityLoop, resolveRuntimeConfig exported"
    - path: src/application/config/model-override.ts
      provides: "applyModelOverride"
    - path: src/application/config/host-resolution.ts
      provides: "resolveRuntimeHostSelection, resolveHostConfig, resolveModelTier"
    - path: src/application/config/starter-seed.ts
      provides: "seedProjectRlmStarter(console stderr messages preserved)"
    - path: src/application/config/index.ts
      provides: "Barrel assembling only previously public exports consumers relied on via project-config.ts"
    - path: src/application/project-config.ts
      provides: "Backward-compatible façade (re-export barrel only or minimal stubs forbidden)"
    - path: tests/config-resolution.unit.test.ts
      provides: "Node:test assertions for resolveRuntimeHostSelection env/cli/config/default ordering; resolveModelTier fallback estimatedRamMb rule; applyModelOverride mutates tiers consistently"
  key_links:
    - from: project-config facade
      to: ./config/index.js
      via: Stable import path `./application/project-config.js` per CONF-02
      pattern: "facade forwards public API from ./config/index.js without new exports"
---

<objective>

Complete CONF-01 by extracting pure resolution logic and starter seeding into dedicated modules (CONF-04, CONF-05), add deterministic unit coverage for resolver surfaces (CONF-06), finalize `application/config/index.ts` + thin `project-config.ts` barrel (CONF-02).

Purpose: Ends phase with clear module boundaries aligned to ROADMAP success criteria.

Output: resolution + starter modules, barrel façade, focused unit test file shipped to dist tests.

</objective>

<execution_context>

@/home/brady/.claude/get-shit-done/workflows/execute-plan.md
@/home/brady/.claude/get-shit-done/templates/summary.md

</execution_context>

<context>

@/home/brady/workspace/RLM/.planning/phases/37-config-layer-split/37-CONTEXT.md
@/home/brady/workspace/RLM/tests/model-host-routing.test.ts
@src/application/project-config.ts

<interfaces>

Behavioral contracts extractable from existing code for executor reference:

resolveRuntimeHostSelection precedence: trimmed RLM_HOST env wins `{source:\"env\"}`; else trimmed CLI `--host` analogue via `cliHostId` `{source:\"cli\"}`; else `config.runtimeHost` trimmed `{source:\"config\"}`; else first key of `config.hosts` `{source:\"default\"}`; fallback `{hostId:\"local_ollama\", source:\"default\"}` when hosts empty/unavailable.

resolveModelTier returns named tier lookup else synthetic `{ name: selection, estimatedRamMb: config.models.tiers[\"small\"]?.estimatedRamMb ?? 4096 }`.

applyModelOverride clones config with models.default=modelOverride and overwrites tiers.small.name with override while merging smallTier base fields preserve estimatedRamMb if present.

seedProjectRlmStarter: mkdir `.rlm/` agents models; write stringifyYaml(DEFAULT_PROJECT_PLAIN) to `.rlm/config.yaml`; write coding.yaml and small tier yaml; stderr strings `[rlm starter] seeded ...` / `[rlm starter] FAILED ...` unchanged.

Merge quality loop overrides: recursive merge preserves phaseModels key union behavior from mergeQualityLoopPhaseModels + mergeResolvedQualityLoop.

resolveRuntimeConfig: overlays overrides on DEFAULT_PROJECT runtime merged with config.runtime; delete maxDepth property when undefined like current delete semantics.

resolveHostConfig: throws ``Unknown runtime host "${hostId}"`` when absent.

 </interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for host/tier/modelOverride/runtime merges</name>
  <files>tests/config-resolution.unit.test.ts</files>
  <read_first>src/application/project-config.ts, tests/model-host-routing.test.ts</read_first>
  <behavior>
    - Case A: Synthetic ProjectConfig fixtures — resolveRuntimeHostSelection picks env RLM_HOST over cli over config.runtimeHost when multiple set.
    - Case B: Missing hosts map — fallback local_ollama default source string.
    - Case C: resolveModelTier tier hit vs fallback estimatedRamMb from small tier vs 4096 when small missing.
    - Case D: applyModelOverride sets models.default + small tier name replaces with override preserves estimatedRamMb from prior small tier.
    - Case E: resolveRuntimeConfig merges qualityLoop.phaseModels overlays without dropping unspecified phases (mirror merged object keys assertions).
    - Assertions use node:test (`import test from \"node:test\"`) aligned with repo `dist/tests/*.test.js` build pipeline — place source under tests/ alongside existing naming; extend tsconfig/tests build inclusion if discovery requires (*.test.ts already built per package.json test script).
  </behavior>
  <action>

Create `tests/config-resolution.unit.test.ts` importing resolver functions **from façade path** (`../src/application/project-config.js` in TS source mirrored to relative dist placement after build):

- Use `.js` specifier in imports matching compiled output conventions used in sibling tests (`../src/application/project-config.js` from tests/).
- Fixtures build minimal ProjectConfig literals satisfying type minimal fields necessary (models.tiers.small/medium stubs, hosts optional blocks).
- Do not duplicate large DEFAULT merge tests — reuse DEFAULT_PROJECT_CONFIG from facade import where helpful.

Implement tests red-first expectation list then ensure green after Task 2 moves implementation (_executor order_: if TDD strictly, write failing assertions against expected export names briefly then migrate code — acceptable to land tests asserting current monolith-exported helpers if already present in facade during Plan execution).

Ensure `npm test` invokes this file (`package.json`: `node --test dist/tests/*.test.js` covers `tests/*.test.ts` compiled to root dist/tests when tsconfig echoes pattern — VERIFY existing graph places tests compilation; mimic `tests/model-host-routing.test.ts` path/import style verbatim).

CONF-06: assertions run without spawning `rlm` CLI binary.

  </action>
  <acceptance_criteria>
    - `dist/tests/config-resolution.unit.test.js` emitted after build.
    - At least five test cases enumerated in behavior block executed in single node:test file.
    - Tests pass after Task 2 relocates resolver bodies.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm test</automated>
  </verify>
  <done>New unit suite green post-resolver extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Extract resolution modules and starter seed</name>
  <files>src/application/config/runtime-resolution.ts, src/application/config/model-override.ts, src/application/config/host-resolution.ts, src/application/config/starter-seed.ts</files>
  <read_first>src/application/project-config.ts, src/application/config/types.ts, src/application/config/defaults.ts</read_first>
  <action>

Move implementations:

**runtime-resolution.ts**
- Helpers `mergeQualityLoopPhaseModels`, `mergeResolvedQualityLoop` (currently private siblings of resolveRuntimeConfig)
- `resolveRuntimeConfig` exported
- Depends on RecursiveModelConfig domain type from `../../domain/types.js` identical import path semantics as prior file depth

**model-override.ts**
- applyModelOverride

**host-resolution.ts**
- resolveRuntimeHostSelection, resolveHostConfig, resolveModelTier
- Imports ProjectConfig ModelHostConfig RuntimeHostSelection ModelTierConfig from ./types.js

**starter-seed.ts**
- Imports fs promises mkdir writeFile stringifyYaml yaml package, defaults DEFAULT_PROJECT_CONFIG + DEFAULT_PROJECT_PLAIN, dotFolder orchestration unchanged
- Preserve console.error literals exactly `[rlm starter] seeded ...` and `[rlm starter] FAILED to seed project .rlm layout:` prefixes

Leave façade after move with only forwarding exports pending Task 3.

No logic drift; copy/move + fix relative imports only.

  </action>
  <acceptance_criteria>
    - Behavioral equality: Diff of function bodies excluding import lines is empty versus git pre-move snapshot (conceptual checkpoint for executor discipline).
    - resolveHostConfig still throws `"Unknown runtime host \"${hostId}\"."`.
    - seedProjectRlmStarter returns boolean false early when `.rlm/config.yaml` exists as file (`safeStat`).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check && npm test</automated>
  </verify>
  <done>All resolution + starter implementations live under config/</done>
</task>

<task type="auto">
  <name>Task 3: Barrel index.ts + thin project-config façade</name>
  <files>src/application/config/index.ts, src/application/project-config.ts</files>
  <read_first>src/application/config/*.ts aggregated export list manually</read_first>
  <action>

- Create `src/application/config/index.ts` exporting ONLY symbols previously consumable via `application/project-config.js` façade (explicit named re-exports list — grep pre-refactor public exports enumerated in Plan 01 interfaces block plus loadProjectConfig, seedProject*, model override, resolvers discovered by import sites).

- Exclude internal helpers: configSchema unless previously exported publicly (it is internal), validateConfigReferences, mergeYamlLayers, parseYamlTagged, DEFAULT_PROJECT_PLAIN can remain export only if downstream uses — grep codebase; presently internal for loader/seeding — exporting DEFAULT_PROJECT_CONFIG + LoadedProject types already public.

Goal: façade `project-config.ts` shrinks ideally to sequential `export { ... } from "./config/index.js"` OR `export * from "./config/index.js"` AFTER confirming index excludes internals — **`export *` forbidden if it leaks validators** — prefer exhaustive named list mirroring grep results from import sites snapshot.

Ensure domain/agents importing types still compile: `src/domain/agents.ts` pulls AgentConfig only.

Run eslint fix only if autofix aligns style.

After barrel: rerun full `npm run check` final gate REG-02.

  </action>
  <acceptance_criteria>
    - Whole-repo `grep -r "from .*project-config" src tests` resolves with zero TypeScript compile errors (`npm run typecheck`).
    - `project-config.ts` line count drastically reduced (>80% LOC drop vs Wave0 baseline excluding blank lines heuristic).
    - No new externally visible exports compared to enumerated grep exports set from Phase start (automate optional: compare `grep "^export "` before/after from git show HEAD1:project-config ambiguous — acceptable manual note in SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check</automated>
  </verify>
  <done>CONF-02 barrel façade stable; milestone REG gates green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed writes CWD /.rlm starter | Writable directory could be abused if invoked on hostile path — unchanged risk profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | T | Starter seed writes | accept | Matches pre-split trust model — no path traversal expansion beyond join(projectRoot, '.rlm') |
| T-37-05 | E | Wrong host tier selection | mitigate | Unit tests CONF-06 ensure precedence ordering deterministic |

</threat_model>

<verification>
Full `npm run check` + targeted review that ~20 façade import paths unchanged (CONF-02) and CONF-06 tests present runnable.
</verification>

<success_criteria>

Phase CLOSES when modules list in ROADMAP Phase 37 success criterion 1 satisfied and façade import stability proven.

</success_criteria>

<output>

After completion, create `.planning/phases/37-config-layer-split/37-03-SUMMARY.md`

</output>


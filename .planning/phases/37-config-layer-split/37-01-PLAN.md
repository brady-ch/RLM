---
phase: 37-config-layer-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/config/types.ts
  - src/application/config/schema.ts
  - src/application/config/defaults.ts
  - src/application/config/yaml-merge.ts
  - src/application/project-config.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - REG-01
  - REG-02
must_haves:
  truths:
    - "Pure config types, zod schemas, baked defaults, and YAML merge helpers live under src/application/config/."
    - "All existing imports from project-config remain valid with identical exported names and shapes (CONF-02)."
    - "`npm run check` exits 0 (REG-02); full test count unchanged or increased only later (REG-01)."
  artifacts:
    - path: src/application/config/types.ts
      provides: "Exported MODEL_PURPOSES, CORE_MODEL_PURPOSES, domain interfaces, isGraph/isRamQueue type guards"
    - path: src/application/config/schema.ts
      provides: "configSchema zod object used by loader (preserve parse semantics)"
    - path: src/application/config/defaults.ts
      provides: "DEFAULT_PROJECT_CONFIG, DEFAULT_PROJECT_PLAIN deep clone seed, internal defaultQualityLoopConfig + defaultAgentModels()"
    - path: src/application/config/yaml-merge.ts
      provides: "mergeYamlLayers, mergeInterop, isPlainRecord (non-exported is OK if barrel does not widen surface)"
    - path: src/application/project-config.ts
      provides: "Still re-exports all public symbols from pre-split surface; internals removed for blocks now in config/*"
  key_links:
    - from: src/application/config/schema.ts
      to: src/application/config/defaults.ts
      via: runtimeSchema.defaults reference same literal defaults as today
      pattern: defaultQualityLoopConfig|DEFAULT_PROJECT_
---

<objective>

Carry the static half of CONF-01: extract types, Zod schemas, default objects, and YAML-layer merge helpers into `src/application/config/` while `project-config.ts` remains the backwards-compatible façade (CONF-02).

Purpose: Locks schema/default behavior in testable modules before moving filesystem I/O and resolution.

Output: `types.ts`, `schema.ts`, `defaults.ts`, `yaml-merge.ts` + slimmed `project-config.ts` still exporting the same public API.

</objective>

<execution_context>

@/home/brady/.claude/get-shit-done/workflows/execute-plan.md
@/home/brady/.claude/get-shit-done/templates/summary.md

</execution_context>

<context>

@AGENTS.md
@/home/brady/workspace/RLM/.planning/ROADMAP.md (Phase 37)
@/home/brady/workspace/RLM/.planning/phases/37-config-layer-split/37-CONTEXT.md
@/home/brady/workspace/RLM/src/application/project-config.ts

<interfaces>
Key exports that must remain available from `src/application/project-config.js` after this plan (re-export from new modules):

- MODEL_PURPOSES, CORE_MODEL_PURPOSES — const arrays as today
- Types: ModelPurpose, CoreModelPurpose, MemoryMode, ModelSelection, ModelTierConfig, SamplingConfig, AgentConfig, WorkflowDispatchConfig, WorkflowDispatchTierConfig, RamQueueWorkflowConfig, GraphWorkflowConfig, WorkflowConfig, WorkflowQaConfig, McpServerConfig, SkillPathPolicyConfig, SkillInteropConfig, InteropConfig, ModelHostKind, ModelHostConfig, RuntimeHostSelection, ProjectConfig, LoadedProjectConfig
- isGraphWorkflowConfig, isRamQueueWorkflowConfig — same predicates
- DEFAULT_PROJECT_CONFIG — same object shape and values as pre-refactor DEFAULT_PROJECT_CONFIG
- loadProjectConfig, seedProjectRlmStarter, applyModelOverride, resolveRuntimeConfig, resolveRuntimeHostSelection, resolveHostConfig, resolveModelTier — still declared in facade file until later plans move bodies (stub not allowed — keep implementations in place for any not yet extracted)

Implementation note: `configSchema` is module-internal unless it was exported before — it stays non-exported.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config/types.ts with public types and guards</name>
  <files>src/application/config/types.ts</files>
  <read_first>src/application/project-config.ts</read_first>
  <action>

Move verbatim (no semantic edits) from `project-config.ts` into `types.ts`:

- Imports from ../domain/types.js (QualityLoopConfig only if referenced), ../ports/extension-port.js, ../ports/language-model-port.js as needed for interfaces.
- MODEL_PURPOSES and CORE_MODEL_PURPOSES constants; derived ModelPurpose and CoreModelPurpose types.
- MemoryMode, ModelSelection.
- Interfaces: ModelTierConfig through LoadedProjectConfig (lines ~47–179 in current monolith ordering).
- isGraphWorkflowConfig and isRamQueueWorkflowConfig implementations unchanged.

Preserve relative import paths from `src/application/config/` (one deeper than application: use `../../domain/` and `../../ports/`).

Do not add new runtime behavior.

  </action>
  <acceptance_criteria>
    - `grep -r "from \\\"\\.\\\/project-config" src tests | wc -l` unchanged relative to baseline after full phase (facade URLs stay valid).
    - `types.ts` contains string `MODEL_PURPOSES` and exported interface `ProjectConfig`.
    - ESLint/typecheck accepts new file location per AGENTS.md layer rules.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check</automated>
  </verify>
  <done>New `types.ts` compiles; `project-config.ts` imports and re-exports types/guards OR delegates without removing any export yet.</done>
</task>

<task type="auto">
  <name>Task 2: Add defaults.ts and yaml-merge.ts; wire schema.ts</name>
  <files>src/application/config/defaults.ts, src/application/config/yaml-merge.ts, src/application/config/schema.ts</files>
  <read_first>src/application/project-config.ts, src/application/config/types.ts</read_first>
  <action>

- `defaults.ts`: Move `defaultQualityLoopConfig`, `DEFAULT_PROJECT_CONFIG`, JSON deep-clone `DEFAULT_PROJECT_PLAIN` pattern, `defaultAgentModels()`, and constants the schema `.default(...)` closures need — keep numeric and string literals identical to monolith (`granite4.1:3b`, QA keywords, interp defaults, hosts block, runtimeHost `"local_ollama"`).

- `yaml-merge.ts`: Move `isPlainRecord`, `mergeInterop`, `mergeYamlLayers` unchanged logic; import only what merges need (avoid importing full schema to prevent cycles).

- `schema.ts`: Move all zod schemas from monolith (`modelSelectionSchema` through `configSchema` inclusive), importing `ProjectConfig`-related types only where needed for transforms; preserve `.transform` on `agentModelsSchema` fill-ins for quality loop model keys exactly as today.

Export from `defaults.ts`: `DEFAULT_PROJECT_CONFIG`, `DEFAULT_PROJECT_PLAIN` (or getter equivalent that preserves structuredClone semantics). Export `defaultQualityLoopConfig` if required by schema file for defaults — prefer minimal exports; do not export `defaultAgentModels` publicly unless previously exported.

`configSchema` stays non-exported or exported only through project-config unchanged — today it is not in public grep exports; keep internal.

Ensure no barrel `config/index.ts` in this plan (Plan 03 adds it unless you need re-aggregation earlier — avoid premature index to reduce churn).

Per CONTEXT constraints: modular slice only; behavior unchanged.

  </action>
  <acceptance_criteria>
    - Byte-for-byte equality of serialized `DEFAULT_PROJECT_CONFIG` YAML-relevant subsets is not strictly required across JSON round-trip; instead: `DEFAULT_PROJECT_CONFIG.models.default === "granite4.1:3b"` and `DEFAULT_PROJECT_CONFIG.runtimeHost === "local_ollama"` in source assertions after move.
    - `mergeYamlLayers` merge rules for agents, models (tiers + sampling profiles), memory, runtime, workflows, hosts, interop behave identically — covered indirectly by Plan 03 tests and existing integration tests passing this plan’s `npm run check`.
    - Agent model transform fills `quality_loop_*` and `plan` defaults exactly as existing zod `.transform`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check</automated>
  </verify>
  <done>`schema.ts` imports `defaults.ts` cleanly; YAML merge/helpers compile; cyclic imports absent per `npm run check`.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire project-config.ts to compose new modules</name>
  <files>src/application/project-config.ts</files>
  <read_first>src/application/config/schema.ts, src/application/config/defaults.ts, src/application/config/yaml-merge.ts, src/application/project-config.ts</read_first>
  <action>

- Remove migrated blocks from `project-config.ts`; import `configSchema` (or parsing helper) + `DEFAULT_PROJECT_PLAIN`, `DEFAULT_PROJECT_CONFIG`, merge helpers from `./config/*.js` relative paths using `.js` extensions matching repo ESM convention.
- Re-export all **previously exported** surfaces from `project-config.ts` so no call site changes (`export { ... }` or `export * from "./config/types.js"` selectively — do not leak new internals).
- Keep **in place** implementations not yet migrated: loader (`loadProjectConfig`, `loadScopedFragments`, filesystem helpers), `validateConfigReferences`, `seedProjectRlmStarter`, `applyModelOverride`, runtime resolution trio, tier/host resolution — unchanged bodies until Plans 02–03 unless already impossible without duplication (if `parseYamlTagged` duplicates, consolidate only when Plan 02 moves loader).

Preserve `throw new Error(\`${pathLike}: ...\`)` shaping for YAML parse failures that exist today for CONF-03 follow-through in Plan 02.

Confirm dependency-cruise layer: `application/config/` remains under application layer importing domain/ports like current file.

  </action>
  <acceptance_criteria>
    - All import sites listed in codebase grep (`src/index.ts`, `src/application/**/*.ts`, `src/domain/agents.ts`, `tests/**/*.ts`) still resolve `from ".../application/project-config.js"` with no edits required after this task.
    - `npm run build` produces identical export surface check: optional quick script `rg "^export "` diff pre/post unacceptable — manual reviewer uses `grep` on facade file for MODEL_PURPOSES, loadProjectConfig, resolveModelTier presence.
    - Zero new exports appear on `project-config.ts` versus pre-task public API.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check</automated>
  </verify>
  <done>`npm run check` green; façade file shorter; config slice compiles.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local YAML → parsed config | Untrusted filesystem content crosses into runtime objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | I | YAML merge defaults | mitigate | Preserve existing merge precedence and zod coercion — no relax of validation strictness versus monolith |

</threat_model>

<verification>
Plan 01 complete when `npm run check` passes and all public imports from `application/project-config.js` remain valid unchanged (CONF-02, REG-01/REG-02).
</verification>

<success_criteria>
Static config pieces live under application/config per CONF-01; behavior and exports unchanged REG/CONF-02 gates.
</success_criteria>

<output>

After completion, create `.planning/phases/37-config-layer-split/37-01-SUMMARY.md`

</output>

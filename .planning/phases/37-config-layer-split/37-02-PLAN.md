---
phase: 37-config-layer-split
plan: 02
type: execute
wave: 2
depends_on:
  - "37-01"
files_modified:
  - src/application/config/loader.ts
  - src/application/config/validation.ts
  - src/application/project-config.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`loadProjectConfig` and YAML fragment scanning behavior identical to monolith layering order (HOME/.rlm, cwd/.rlm, rlm.config.yaml discovery)."
    - "Parse failures throw `${path}:${message}` (or strictly equivalent substring with path prefix) preserving CONF-03 file/path context."
    - "`validateConfigReferences` post-parse invariants unchanged (agents, workflows, dispatch QA rules)."
    - "`npm run check` green REG-02."
  artifacts:
    - path: src/application/config/loader.ts
      provides: "loadProjectConfig, loadScopedFragments, parseYamlTagged, safeStat, findDefaultConfigPath"
    - path: src/application/config/validation.ts
      provides: "validateConfigReferences(config: ProjectConfig) throws on invalid refs"
    - path: src/application/project-config.ts
      provides: "Thin imports from loader + validation; façade exports unchanged"
  key_links:
    - from: loadProjectConfig
      to: configSchema.parse
      via: Merge order then single parse identical to Wave 1
      pattern: "configSchema\\.parse"
---

<objective>

Extract filesystem-backed loading and structural reference validation into `application/config/` (CONF-01) while guaranteeing validation/error messages preserve file paths and shapes users see today (CONF-03).

Purpose: Isolate I/O-heavy code from pure resolution logic planned in 37-03.

Output: `loader.ts`, `validation.ts`, and façade wiring only.

</objective>

<execution_context>

@/home/brady/.claude/get-shit-done/workflows/execute-plan.md
@/home/brady/.claude/get-shit-done/templates/summary.md

</execution_context>

<context>

@/home/brady/workspace/RLM/.planning/phases/37-config-layer-split/37-CONTEXT.md
@/home/brady/workspace/RLM/tests/project-config-scopes.test.ts
@src/application/project-config.ts

<interfaces>
Loader orchestration must preserve ordering from current `loadProjectConfig`:

- Explicit path: read resolved file → merge into structuredClone(DEFAULT_PROJECT_PLAIN) → mergeYamlLayers with parsed root → parse → validateConfigReferences → return `{ config, path: resolvedExplicit }`.

- Implicit path: start DEFAULT_PROJECT_PLAIN; merge global ~/.rlm scoped fragments via loadScopedFragments; discover primaryPath rules for `.rlm/` directory vs lone `rlm.config.yaml`; merge legacy scoped file when `.rlm` exists and legacy file exists; then scoped fragments — match existing primaryPath inference line-for-line semantics.

Validation throws same string templates for unknown agents in workflows/dispatch tiers/QA gate as existing `validateConfigReferences`.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract loader.ts with identical merge and path semantics</name>
  <files>src/application/config/loader.ts, src/application/project-config.ts</files>
  <read_first>src/application/project-config.ts, src/application/config/schema.ts, src/application/config/defaults.ts, src/application/config/yaml-merge.ts</read_first>
  <action>

Move into `loader.ts`:

- Imports: fs/promises (access mkdir readFile readdir stat writeFile — only those used), constants from fs where access used, homedir, path helpers, yaml parse/stringify unused here (parse-only), structuredClone GLOBAL.

- Implement `safeStat`, `parseYamlTagged(pathLike: string, raw: string): unknown` — keep error format `throw new Error(\`${pathLike}: ${suffix}\`)`.

- Implement `findDefaultConfigPath` identical to resolve('rlm.config.yaml') access check.

- Export `loadScopedFragments(scopeRoot: string)`, `loadProjectConfig(path?: string)` returning Promise of LoadedProjectConfig — import `LoadedProjectConfig`/`ProjectConfig` types from `./types.js`, schema from `./schema.js`, `DEFAULT_PROJECT_PLAIN` from `./defaults.js`, merge helpers from `./yaml-merge.js`, `validateConfigReferences` can be imported from `./validation.js` (next task circular risk: loader imports validation; validation imports only types + isGraph helpers — OK).

Preserve `seedProjectRlmStarter` in façade until Plan 03 — DO NOT delete here.

Remove loader bodies from façade; re-export loadProjectConfig from façade as `export { loadProjectConfig } from "./config/loader.js"` OR wrap only if façade must augment — prefer direct export for identical API.

Ensure `mkdir`/`writeFile` not needed in loader (starter stays out — only read path).

CONF-06 note: extracting loader centralizes filesystem I/O; unit tests targeting pure merges stay in yaml-merge/tests later — do not regress integration tests here.

per CONTEXT Claude discretion: incremental verify with existing tests suite.

  </action>
  <acceptance_criteria>
    - `tests/project-config-scopes.test.ts` passes layered agent override case unchanged.
    - `tests/integration-v15.test.ts`, `tests/graph-*` importing loadProjectConfig pass.
    - `parseYamlTagged` preserves prefix test: injecting invalid YAML fixture path shows path string in `.message` substring.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check</automated>
  </verify>
  <done>`loadProjectConfig` fully lives in loader module; façade re-exports; check green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract validation.ts and wire validateConfigReferences</name>
  <files>src/application/config/validation.ts, src/application/project-config.ts</files>
  <read_first>src/application/project-config.ts, src/application/config/types.ts</read_first>
  <action>

Move `validateConfigReferences` implementation into `validation.ts`; import:

- ProjectConfig type
- WorkflowConfig entries iteration using `isGraphWorkflowConfig` / `isRamQueueWorkflowConfig` from `./types.js`

Preserve every throw message verbatim including template strings referencing agentId/workflowId/tier name.

Export `validateConfigReferences` as named export; façade re-exports or loader imports internally only — prefer loader imports validation without façade exposure if validateConfigReferences was not previously exported publicly.

Verification: grep `export function validateConfigReferences` in project-config BEFORE change — if not exported, validation.ts should **not** be re-exported from façade (prevent API surface widening). Current monolith: validateConfigReferences is private function NOT exported — keep it **non-exported** from index/facade: only internal use from loader.ts.

  </action>
  <acceptance_criteria>
    - `validateConfigReferences` remains non-exported from `src/application/project-config.ts` facade (confirmed by `grep validateConfigReferences src/application/project-config.ts` shows no export line — file may omit symbol entirely).
    - Loader still invokes validation after parse in both branches.
    - Existing tests that depended on workflow/agent reference errors behave same (coverage via graph/workflow suites).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && npm run check</automated>
  </verify>
  <done>Validation module isolated; no new public symbols.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem YAML → Loader | Paths from user.cwd and HOME traverse untrusted dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02 | I | YAML parse errors | mitigate | Maintain path-prefixed Error messages (CONF-03); no swallowed parse failures |
| T-37-03 | E | Workflow validation rejects | mitigate | Identical invariant checks preventing unknown agent refs |

</threat_model>

<verification>

Integration tests exercised via `npm run check`; spot-check malformed YAML raises path-prefixed error (manual optional if automate hard).

</verification>

<success_criteria>

Loader + validation split complete; CONF-03 path-context preserved.

</success_criteria>

<output>

After completion, create `.planning/phases/37-config-layer-split/37-02-SUMMARY.md`

</output>


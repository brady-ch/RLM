---
phase: 115-cli-entry-and-runtime-composition-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/cli/
  - src/runtime/
  - tests/runtime/
autonomous: true
requirements:
  - RETIRE-115-01
user_setup: []
must_haves:
  truths:
    - "src/index.ts does not exist"
    - "src/cli/ directory does not exist"
    - "src/runtime/ directory does not exist"
    - "tests/runtime/ mirror deleted (cannot import deleted modules)"
  artifacts:
    - path: src/
      provides: "Remaining TS layers without CLI/runtime"
      contains: "application"
  key_links:
    - from: src/application/bootstrap/
      to: deleted src/cli and src/runtime
      via: "broken imports until Phase 116 — expected transitional state"
      pattern: "N/A — compile break accepted per migration note"
---

<objective>
Delete the Node CLI entry point, CLI layer, runtime composition layer, and their mirrored runtime tests.

Purpose: Rust `rlm-cli` becomes sole CLI per phase boundary; application/domain/adapters remain until Phase 116+.
Output: Removed `src/index.ts`, `src/cli/`, `src/runtime/`, `tests/runtime/`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-cli-entry-and-runtime-composition-removal/115-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md

<interfaces>
Deletion inventory from 113-AUDIT.md:

**src/index.ts** (1 file) — Node CLI entry; Rust counterpart `crates/rlm-cli/src/main.rs`

**src/cli/** (12 files):
```
src/cli/args.ts, first-run.ts, render.ts, runtime-logger.ts, shutdown.ts, ui-dist-dir.ts
src/cli/run-modes/agent-workflow.ts, plan-node.ts, plugin-commands.ts, session-commands.ts, ui.ts, workflow-graph-io.ts
```

**src/runtime/** (8 files):
```
src/runtime/composition/build-runtime-context.ts, extension-host.ts, index.ts, init-order.ts, runtime-composition.ts
src/runtime/interop/index.ts, interop-runtime.ts, mcp-skill-runtime.ts
```

**tests/runtime/** (3 files — delete now; src gone):
```
tests/runtime/composition/extension-host.test.ts
tests/runtime/composition/runtime-composition-init-order.test.ts
tests/runtime/interop/mcp-skill-interoperability.test.ts
```

**Transitional compile breaks (DO NOT fix in this plan — Phase 116/118 scope):**
- `src/application/bootstrap/build-runtime-context.ts` imports cli/runtime
- `src/application/bootstrap/types.ts` imports cli/runtime types
- `src/plugins/plugin-loader.ts` imports ExtensionHost from runtime

113-GATES Phase 115 gate: `test ! -f src/index.ts` — does NOT require `npm run build` green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Node CLI entry and cli layer</name>
  <files>src/index.ts, src/cli/</files>
  <action>
    Delete per 113-AUDIT Phase 115 inventory:

    1. `git rm src/index.ts`
    2. `git rm -r src/cli/` (entire directory including `run-modes/`)

    Scope guard (115-CONTEXT boundary):
    - Do NOT delete `src/application/`, `src/domain/`, `src/ports/`, `src/adapters/`, `src/plugins/`
    - Do NOT modify remaining `src/` files to fix import errors — Plan 03 handles test orphans only; application bootstrap breaks until Phase 116

    Verify no stray files under `src/cli/` after deletion.
  </action>
  <verify>
    <automated>test ! -f src/index.ts && test ! -d src/cli && echo ok</automated>
  </verify>
  <done>src/index.ts and src/cli/ fully removed from repo</done>
</task>

<task type="auto">
  <name>Task 2: Delete runtime composition layer</name>
  <files>src/runtime/</files>
  <action>
    Delete entire `src/runtime/` tree per 113-AUDIT:

    1. `git rm -r src/runtime/` (composition + interop subdirs)

    Do NOT delete `crates/rlm-core/src/plugins/` or `crates/rlm-core/src/interop/` — those are Rust counterparts and stay.

    After deletion, confirm `find src/runtime -type f 2>/dev/null | wc -l` is 0.
  </action>
  <verify>
    <automated>test ! -d src/runtime && echo ok</automated>
  </verify>
  <done>src/runtime/ fully removed</done>
</task>

<task type="auto">
  <name>Task 3: Delete tests/runtime mirror</name>
  <files>tests/runtime/</files>
  <action>
    Delete `tests/runtime/` entirely. Although 113-AUDIT lists mirror removal in Phase 118, those tests import deleted `src/runtime/` modules and cannot run after Plan 02 Task 2.

    1. `git rm -r tests/runtime/`

    Do NOT delete `tests/application/`, `tests/domain/`, `tests/plugins/` in this task — Plan 03 prunes cli-dependent cases only.
  </action>
  <verify>
    <automated>test ! -d tests/runtime && echo ok</automated>
  </verify>
  <done>tests/runtime/ removed; no test files import src/runtime paths</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deletion scope | Must not remove application/domain layers prematurely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03 | D | git rm scope | mitigate | Use explicit paths from 113-AUDIT; verify with `test ! -d` gates before commit |
| T-115-04 | E | Transitional TS compile | accept | Documented broken bootstrap imports until Phase 116; Rust gates are authoritative |
</threat_model>

<verification>
```bash
test ! -f src/index.ts
test ! -d src/cli
test ! -d src/runtime
test ! -d tests/runtime
```
All exit 0.
</verification>

<success_criteria>
- All three TS CLI/runtime trees deleted
- tests/runtime mirror deleted
- No accidental deletion of application/domain/adapters/plugins
</success_criteria>

<output>
After completion, create `.planning/phases/115-cli-entry-and-runtime-composition-removal/115-02-SUMMARY.md`
</output>

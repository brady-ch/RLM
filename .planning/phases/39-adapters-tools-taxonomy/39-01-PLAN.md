---
phase: 39-adapters-tools-taxonomy
plan: "01"
type: execute
wave: 1
depends_on:
  - 38-runtime-bootstrap
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - ADPT-03
  - ADPT-04
  - ADPT-05
  - ADPT-06
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`src/adapters/tools/`, `persistence/`, and `models/` group adapters by concern; `search-query` colocates with web tools."
    - "Extension shims import core tool adapters from `adapters/tools/`; no duplicate implementations."
    - "CLI/bootstrap consumes concrete persistence + embedding adapters via `application/bootstrap/` re-exports; composition uses adapters barrel or subtree paths consistently."
    - "`npm run check` exits 0; test count unchanged except incidental import path churn."
---

<objective>

Reorganize `src/adapters/` by concern (tools, persistence, models), align `src/extensions/tools/*` imports, centralize bootstrap-facing adapter surface, and satisfy ADPT-* + REG-* without behavioral changes.

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/39-adapters-tools-taxonomy/39-CONTEXT.md

</context>

<threat_model>

Mitigations:

- Moves are path-only; preserve port contracts and public method signatures.

Test strategy: `npm run check` full gate after import updates.

</threat_model>

<tasks>

<task id="39-01-t1" type="auto">

<action>

Author this plan artifact in-repo; commits follow per executor protocol (plan doc first).

</action>

</task>

<task id="39-01-t2" type="auto">

<action>

1. Move files — `tools/`: guarded-shell-tool, workspace-file-write-tool, web-search-tool, web-fetch-tool, search-query. `persistence/`: file-session-store, file-memory-store, file-run-state-store, file-vector-index, in-memory-trace. `models/`: http-language-model, ollama-language-model, ollama-embedding-model.

2. Fix intra-adapter imports (`../ports` → `../../ports`, web-fetch → `../../application/content-tree.js`).

3. Add `src/adapters/index.ts` barrel re-exporting public adapter surface.

4. Add `src/application/bootstrap/adapters.ts` re-exporting bootstrap boundary types/instances consumed from `build-runtime-context.ts`, `bootstrap/types.ts`, and `src/index.ts`; update those call sites accordingly.

5. Update `runtime-composition.ts`, application modules, tests, extensions, and `dependency-cruiser-baseline.json` `from` paths.

6. Adjust `AGENTS.md` adapters row if directory layout references change materially.

Run `npm run check` until green.

</action>

</task>

<task id="39-01-t3" type="auto">

<action>

Write `39-SUMMARY.md`, `39-VERIFICATION.md` (gate = `npm run check`), mark ADPT-* and REG-* in REQUIREMENTS traceability where appropriate; update `.planning/STATE.md`, `ROADMAP.md` Phase 39 line; finalize with metadata commit covering planning artifacts only (per executor final_commit pattern alongside task commits).

</action>

</task>

</tasks>

---
phase: 40-domain-engine-decomposition
plan: "04"
type: execute
wave: 4
depends_on:
  - 40-03-PLAN.md
autonomous: true
requirements:
  - RLM-01
  - RLM-02
  - RLM-03
  - RLM-04
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`src/domain/recursion/tool-round-loop.ts` implements model completion with tool rounds via a host facade; orchestrator delegates."
    - "`npm run check` green."
---

<objective>

Extract the tool-round model completion loop without changing call/budget semantics.

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/40-domain-engine-decomposition/40-RESEARCH.md

</context>

<tasks>

<task id="40-04-t1" type="auto">

<action>

Move `complete` and `completeWithoutTools` logic into `tool-round-loop.ts` with exported `runCompletionWithToolRounds` and `runCompletionWithoutTools` taking a `ModelCompletionHost` interface. RecursiveLanguageModel implements the host via a private factory. Run `npm run check`.

</action>

</task>

</tasks>

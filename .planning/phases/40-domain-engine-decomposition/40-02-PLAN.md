---
phase: 40-domain-engine-decomposition
plan: "02"
type: execute
wave: 2
depends_on:
  - 40-01-PLAN.md
autonomous: true
requirements:
  - RLM-01
  - RLM-02
  - RLM-03
  - RLM-04
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`src/domain/recursion/budget-guard.ts` centralizes budget math used by the orchestrator."
    - "`npm run check` green."
---

<objective>

Extract pure budget estimation and guard helpers.

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/40-domain-engine-decomposition/40-RESEARCH.md

</context>

<tasks>

<task id="40-02-t1" type="auto">

<action>

Add `src/domain/recursion/budget-guard.ts` with `remainingModelCalls`, `canSpendAnyModelCall`, `maxToolRoundsFromLimit`, `hasCallReservedForDirectAnswer`, `estimateModelCalls`, `estimateToolRounds`. Replace private methods on the class with imports. Run `npm run check`.

</action>

</task>

</tasks>

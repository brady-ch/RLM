---
phase: 40-domain-engine-decomposition
plan: "03"
type: execute
wave: 3
depends_on:
  - 40-02-PLAN.md
autonomous: true
requirements:
  - RLM-01
  - RLM-02
  - RLM-03
  - RLM-04
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`src/domain/recursion/execution-graph-sync.ts` builds live `executionGraph` + `budget` snapshots from maps and counters."
    - "`npm run check` green."
---

<objective>

Extract execution graph / live budget snapshot construction from the orchestrator.

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/40-domain-engine-decomposition/40-RESEARCH.md

</context>

<tasks>

<task id="40-03-t1" type="auto">

<action>

Add `src/domain/recursion/execution-graph-sync.ts` exporting `buildLiveExecutionMetadata` (or equivalent) used by `updateExecutionGraph`. Keep mutating maps on the class; only snapshot math moves out. Run `npm run check`.

</action>

</task>

</tasks>

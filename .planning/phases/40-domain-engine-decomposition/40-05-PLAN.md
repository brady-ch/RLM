---
phase: 40-domain-engine-decomposition
plan: "05"
type: execute
wave: 5
depends_on:
  - 40-04-PLAN.md
autonomous: true
requirements:
  - RLM-01
  - RLM-02
  - RLM-03
  - RLM-04
  - RLM-05
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`src/domain/recursion/quality-loop.ts` holds quality-loop phases, parsers, rubrics, and `runQualityLoop` / `completeQualityLoopPhase` via a host facade."
    - "Orchestrator retains `run()` flow; quality path delegates to module."
    - "`npm run check` green."
---

<objective>

Extract quality-loop implementation after state-threading spike (40-RESEARCH.md).

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/40-domain-engine-decomposition/40-RESEARCH.md

</context>

<tasks>

<task id="40-05-t1" type="auto">

<action>

Add `src/domain/recursion/quality-loop.ts` with rubric tables, JSON parsers, phase messaging, `QualityLoopManualExit`, and `runQualityLoop` + `completeQualityLoopPhase` + `resolvePlannedModelAssignment` driven by `QualityLoopHost`. Remove duplicated helpers from `recursive-language-model.ts`. Run `npm run check`.

</action>

</task>

</tasks>

---
phase: 126-node-inspector-and-settings-slim-down
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - ui/src/advanced/settings/NodeInspector.tsx
  - ui/src/advanced/settings/GraphWorkflowPanel.tsx
  - ui/src/advanced/settings/inspectorHelpers.tsx
  - ui/src/advanced/SettingsView.tsx
  - ui/src/advanced/AdvancedHub.tsx
  - ui/src/app/AppShell.tsx
  - tests/ui/shell-boundaries.test.ts
requirements: []
---

<objective>
Slim NodeInspector to Advanced-only read-only metadata plus expert/model/sampling overrides; collapse GraphWorkflowPanel; remove duplicate prompt edit and canvas-duplicated plan/run/graph actions.
</objective>

<must_haves>
<truths>
- NodeInspector has no prompt textarea or Save prompt / Plan children / Approve / Skip / Add child / Connect / Delete actions
- GraphWorkflowPanel has no run-variant controls; collapsed by default in SettingsView
- RunPanel still has no prompt edit (Phase 123 invariant)
- Core plan/run/approve path on canvas and context menu unchanged
</truths>
</must_haves>

<tasks>

<task id="1" type="auto">
<name>Slim NodeInspector to metadata and Advanced overrides</name>
<read_first>
- ui/src/advanced/settings/NodeInspector.tsx
- ui/src/nodes/ExecutionNodeCard.tsx
- ui/src/nodes/NodeContextMenu.tsx
- .planning/phases/126-node-inspector-and-settings-slim-down/126-CONTEXT.md
</read_first>
<action>
In NodeInspector.tsx remove: prompt state/textarea/Save prompt; plan/run action row (Plan children, Break down, Extend budget, Approve, Skip); Add Child and Connect Parent sections including delete subtree button. Keep read-only composer metadata, model trail, expert binding display, sampling display (SamplingRows), approval metadata, and editable Model/Expert/Sampling override forms (Advanced path from context menu Expert overrides…). Drop unused lucide imports (Check, GitBranchPlus, Scissors, Trash2, X).
</action>
<acceptance_criteria>
- `grep -q 'Save prompt' ui/src/advanced/settings/NodeInspector.tsx` exits 1
- `grep -q 'Plan children' ui/src/advanced/settings/NodeInspector.tsx` exits 1
- `grep -q '/approve' ui/src/advanced/settings/NodeInspector.tsx` exits 1
- `grep -q 'textarea' ui/src/advanced/settings/NodeInspector.tsx` exits 1
- `grep -q 'Set expert' ui/src/advanced/settings/NodeInspector.tsx` exits 0
- NodeInspector.tsx line count under 280 (`wc -l` &lt; 280)
</acceptance_criteria>
</task>

<task id="2" type="auto">
<name>Collapse GraphWorkflowPanel and remove run-variant overlap</name>
<read_first>
- ui/src/advanced/settings/GraphWorkflowPanel.tsx
- ui/src/advanced/SettingsView.tsx
- ui/src/app/TopBar.tsx
</read_first>
<action>
In GraphWorkflowPanel.tsx remove runVariant, setRunVariant, pipelineInput, setPipelineInput props and the run-variant-controls block. In SettingsView.tsx wrap GraphWorkflowPanel in `details.settings-collapsible` with `summary` "Graph workflows" (closed by default). Remove runVariant/pipelineInput props passed to GraphWorkflowPanel. Update AdvancedHub.tsx and AppShell.tsx prop chain: stop passing runVariant/pipelineInput into SettingsView if only used for GraphWorkflowPanel (TopBar retains run variant for confirm-run).
</action>
<acceptance_criteria>
- `grep -q run-variant-controls ui/src/advanced/settings/GraphWorkflowPanel.tsx` exits 1
- `grep -q 'details' ui/src/advanced/SettingsView.tsx` exits 0
- `grep -q runVariant ui/src/advanced/settings/GraphWorkflowPanel.tsx` exits 1
- TopBar.tsx still contains runVariant for confirm-run
</acceptance_criteria>
</task>

<task id="3" type="auto">
<name>Trim inspectorHelpers and add shell-boundaries tests</name>
<read_first>
- ui/src/advanced/settings/inspectorHelpers.tsx
- tests/ui/shell-boundaries.test.ts
</read_first>
<action>
Remove unused exports from inspectorHelpers.tsx after NodeInspector slim (keep SamplingRows, PortRows, PolicyRows, toInputValue, parseOptionalNumber, parseJsonObject only if still imported). Add tests in shell-boundaries.test.ts: NodeInspector must not match Save prompt|Plan children|/nodes/add; GraphWorkflowPanel must not match run-variant-controls; SettingsView uses details for graph workflows section.
</action>
<acceptance_criteria>
- `npm run test:agent:verify:light` exits 0
- shell-boundaries.test.ts contains NodeInspector slim assertions
</acceptance_criteria>
</task>

</tasks>

<verification>
- npm run test:agent:verify:light
</verification>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| Expert overrides unreachable | mitigate | Keep override forms; context menu navigates to Advanced |
| Workflow save/import lost | mitigate | GraphWorkflowPanel retained inside collapsed details |
| Duplicate prompt edit reintroduced | mitigate | Static test asserts no textarea in NodeInspector |
</threat_model>

---
phase: 29.1
plan: 01
status: complete
completed: 2026-05-21
requirements-completed:
  - SESS-01
  - SESS-04
  - MEM-04
  - SURF-01
  - SURF-02
  - SURF-03
  - RETR-02
  - RETR-05
one_liner: Wired session save/reopen to export and restore live memory, vector index, and runId binding.
---

# Phase 29.1 Summary

## Delivered

- `session-memory-bridge.ts` — export/import of memory sections and runId
- `FileMemoryStore.restoreSessionData()` — bulk session memory import
- `FileVectorIndex.mergeSessionRecords()` — session-scoped vector merge
- Control server save/open uses live memory binding via `SessionRuntimeRef`
- CLI `--open-session` blocks unsafe restore (exit 1)
- Async vector indexing via `enqueueRebuild()` (no sync rebuild in search hot path)
- UI runner resolves memory dynamically after reopen

## Tests

Added `tests/session-memory-bridge.test.ts` (6 tests). Updated retrieval tests for async index model.

## Decisions

Persist runId in memory section v2; project preferences exported in preferences section; global vector index merged by sessionId on restore.

---
phase: 70-rust-boundary-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - "70-01"
files_modified:
  - scripts/check-rust-boundaries.sh
  - scripts/rust-boundary-baseline.json
  - scripts/rust-boundary-check.test.mjs
  - crates/rlm-core/src/domain/run_state_persistence.rs
  - crates/rlm-core/tests/run_state_persistence_boundary.rs
  - package.json
requirements:
  - ARCH-05
  - REG-02
autonomous: true
must_haves:
  truths:
    - "scripts/check-rust-boundaries.sh exits non-zero when domain imports persistence"
    - "Production domain modules have zero persistence imports after test relocation"
    - "npm run check:rust invokes boundary check"
    - "Transitional violations outside domain inner rules use baseline; domain→persistence has no baseline entries"
  artifacts:
    - path: scripts/check-rust-boundaries.sh
      provides: "Layer boundary scanner with named rule violations"
    - path: package.json
      provides: "check:rust includes boundary script; check:rust:boundaries alias"
    - path: scripts/rust-boundary-check.test.mjs
      provides: "Meta-test proving forbidden import detection"
  key_links:
    - from: package.json
      to: scripts/check-rust-boundaries.sh
      via: "npm run check:rust chain"
      pattern: "check-rust-boundaries"
    - from: scripts/check-rust-boundaries.sh
      to: scripts/rust-boundary-rules.toml
      via: "reads forbidden arc definitions"
      pattern: "rust-boundary-rules.toml"
---

<objective>
Implement `check-rust-boundaries.sh`, eliminate domain→persistence violations, and wire into `npm run check:rust` (D-03, D-04, D-05, ARCH-05 enforcement + REG-02 targeted gate).

Purpose: CI fails on layer violations like TS depcruise; domain inner boundary is strictly clean.
Output: Runnable boundary gate + npm wiring + meta-test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-rust-boundary-enforcement/70-CONTEXT.md
@.planning/phases/70-rust-boundary-enforcement/70-01-SUMMARY.md
@scripts/rust-boundary-rules.toml
@AGENTS.md
@package.json
@crates/rlm-core/src/domain/run_state_persistence.rs

<interfaces>
From crates/rlm-core/src/domain/run_state_persistence.rs — production imports (clean):

```rust
use crate::ports::{RunStateMutationRequest, RunStateSnapshot, RunStateStorePort};
```

Violation to eliminate (test-only, line ~160):

```rust
use crate::persistence::FileRunStateStore;
```

Current package.json check:rust:

```json
"check:rust": "cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace"
```

Replace test chain per D-05: keep fmt + clippy; run boundary script; use targeted cargo check instead of full workspace test in verification tasks (full test deferred).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Boundary script + meta-test (RED then GREEN)</name>
  <files>scripts/check-rust-boundaries.sh, scripts/rust-boundary-baseline.json, scripts/rust-boundary-check.test.mjs</files>
  <behavior>
    - Meta-test: temp fixture .rs under domain/ containing `use crate::persistence::FileRunStateStore` causes script exit code != 0 and stderr mentions `no-domain-to-persistence`
    - Meta-test: clean scan of current tree passes for domain→persistence after Task 2 (may use baseline for other known arcs per D-02 optional exceptions)
    - Script reads scripts/rust-boundary-rules.toml; prints rule name, from file, import target on violation
    - Script supports `--strict` flag that ignores baseline (used in meta-test for domain rule probe)
  </behavior>
  <action>
    Per D-02 and D-03:

    1. Write `scripts/check-rust-boundaries.sh` (bash, set -euo pipefail):
       - Parse TOML forbidden rules (awk/grep acceptable; no new npm deps required).
       - For each `.rs` file under scan roots, extract `use crate::{layer}` and `use rlm_core::{layer}` import targets.
       - Map file path to `from_layer` via first segment after `rlm-core/src/` or mark rlm-cli as cli.
       - Skip `#[cfg(test)]` mod bodies when scanning **production** paths OR scan all and rely on test relocation — executor must ensure domain production code has zero persistence imports.
       - Compare imports against forbidden list; emit violations with rule `name`.
       - Load `scripts/rust-boundary-baseline.json` (array of `{rule, from, import}`) unless `--strict`; baseline must NOT contain any `no-domain-to-persistence` entry.
       - Exit 1 on unmatched violations; exit 0 when clean.

    2. Initialize baseline JSON for documented transitional arcs only (e.g. plugins→application in runtime.rs, plugins→persistence in registry_service, plugins/builtin→domain::types) — empty array acceptable if executor fixes all violations instead.

    3. Add `scripts/rust-boundary-check.test.mjs` using node:test:
       - Creates temp dir with violating fixture; asserts script fails with named rule.
       - Runs script against repo (non-strict with baseline); expects exit 0 after Task 2.

    RED: run meta-test before script exists (expect fail). GREEN: implement script until meta-test passes.
  </action>
  <verify>
    <automated>node --test scripts/rust-boundary-check.test.mjs</automated>
  </verify>
  <done>Boundary script detects domain→persistence; meta-test passes; baseline has zero domain→persistence entries</done>
</task>

<task type="auto">
  <name>Task 2: Relocate domain persistence test import</name>
  <files>crates/rlm-core/src/domain/run_state_persistence.rs, crates/rlm-core/tests/run_state_persistence_boundary.rs</files>
  <action>
    Per D-03, remove `#[cfg(test)] mod tests` block from `domain/run_state_persistence.rs` (or remove only the `use crate::persistence::FileRunStateStore` by moving entire test module).

    Recreate equivalent coverage in `crates/rlm-core/tests/run_state_persistence_boundary.rs`:
    - Import `rlm_core::domain::run_state_persistence::{RunStatePersistence, LoadedResumeState}` (or public paths used today).
    - Import `rlm_core::persistence::FileRunStateStore` and `rlm_core::ports::RunStateStorePort` in the **integration test** (allowed — tests are not domain production code).
    - Port existing test cases: initialize, persist_node_status, resume cursor behavior — preserve assertions verbatim where possible.

    Confirm `bash scripts/check-rust-boundaries.sh` reports zero `no-domain-to-persistence` violations on production scan.
  </action>
  <verify>
    <automated>bash scripts/check-rust-boundaries.sh && cargo test -p rlm-core run_state_persistence -- --nocapture 2>&1</automated>
  </verify>
  <done>Domain module has no persistence imports; relocated integration tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Wire npm check:rust and phase verification</name>
  <files>package.json</files>
  <action>
    Per D-04 and D-05:

    1. Add npm script `"check:rust:boundaries": "bash scripts/check-rust-boundaries.sh"`.
    2. Update `"check:rust"` to invoke boundary check — recommended order: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && npm run check:rust:boundaries && cargo check -p rlm-core -p rlm-cli` (drop full `cargo test --workspace` from check:rust for this phase per D-05 deferred full suite; document in SUMMARY that milestone close restores full test in gate).

    3. Run verification bundle and record in 70-02-SUMMARY.md:
       - `node --test scripts/rust-boundary-check.test.mjs`
       - `npm run check:rust:boundaries`
       - `cargo check -p rlm-core -p rlm-cli`

    Do NOT run full `cargo test --workspace` or full `npm run check:rust` clippy+test if OOM risk — targeted commands above satisfy REG-02 for this phase.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); process.exit(p.scripts['check:rust'].includes('check-rust-boundaries')||p.scripts['check:rust'].includes('check:rust:boundaries')?0:1)"</automated>
  </verify>
  <done>package.json check:rust invokes boundary script; targeted verification green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → check-rust-boundaries.sh | Gate must not be bypassable via silent baseline expansion |
| Test relocation → coverage | Moved tests must preserve run-state persistence behavior checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03 | T | rust-boundary-baseline.json | mitigate | Baseline entries require rule name + path; forbid no-domain-to-persistence entries |
| T-70-04 | E | check-rust-boundaries.sh | mitigate | Script exits 1 on violations; meta-test asserts non-zero on fixture |
| T-70-05 | D | cfg(test) imports hidden in prod scan | mitigate | D-03 relocates domain tests; script documents scan scope in header |
</threat_model>

<verification>
```bash
node --test scripts/rust-boundary-check.test.mjs
npm run check:rust:boundaries
cargo check -p rlm-core -p rlm-cli
cargo test -p rlm-core run_state_persistence
node -e "require('./package.json').scripts['check:rust'].includes('boundaries') || process.exit(1)"
```
</verification>

<success_criteria>
1. scripts/check-rust-boundaries.sh fails on domain→persistence violations
2. npm run check:rust invokes boundary check
3. Domain production code has zero persistence imports
</success_criteria>

<output>
After completion, create `.planning/phases/70-rust-boundary-enforcement/70-02-SUMMARY.md` and `.planning/phases/70-rust-boundary-enforcement/70-VERIFICATION.md`
</output>

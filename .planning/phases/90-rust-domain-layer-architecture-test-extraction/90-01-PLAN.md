# Phase 90 Plan 01: Extract Domain Recursion Inline Tests

**Phase:** 90 — Rust Domain Layer Architecture & Test Extraction  
**Wave:** 1 (single plan)

## Tasks

### 1. Create mirrored test directory structure
- `crates/rlm-core/tests/domain/recursion/`

### 2. Extract tests (smallest first)
| Module | Source stub | Test file |
|--------|-------------|-----------|
| budget_guard | `#[path = "../../../tests/domain/recursion/budget_guard.rs"]` | `tests/domain/recursion/budget_guard.rs` |
| prompt_utilities | same pattern | `tests/domain/recursion/prompt_utilities.rs` |
| execution_graph_sync | same pattern | `tests/domain/recursion/execution_graph_sync.rs` |
| tool_round_loop | same pattern | `tests/domain/recursion/tool_round_loop.rs` |
| quality_loop | same pattern | `tests/domain/recursion/quality_loop.rs` |

### 3. Verify
- `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core`
- `npm run check:rust:boundaries`

## Success Criteria
- ARCH-90-01 through ARCH-90-05 satisfied
- No `#[cfg(test)] mod tests { ... }` bodies in `src/domain/`

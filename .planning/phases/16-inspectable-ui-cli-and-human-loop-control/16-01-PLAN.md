---
phase: 16-inspectable-ui-cli-and-human-loop-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/recursive-language-model.ts
  - src/application/execution-controller.ts
  - src/application/control-server.ts
  - src/cli/render.ts
  - ui/src/main.tsx
  - ui/src/styles.css
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - UXQL-01
  - UXQL-02
  - UXQL-03
  - UXQL-04
must_haves:
  truths:
    - "Collapsed UI cards summarize quality-loop status, score, iteration count, selected candidate, and stop reason."
    - "Inspector exposes expandable iteration history with rubric, gate, model trail, critique, and selection details."
    - "CLI compact output includes score and degraded/failure issue counts while JSON keeps full metadata."
    - "Manual loop accept/stop controls are loop-scoped and separate from node approval."
---

<objective>
Implement inspectable UI/CLI quality-loop output and loop-scoped manual accept/stop control.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add loop control contract and runtime handling</name>
  <files>src/domain/types.ts, src/domain/recursive-language-model.ts, src/application/execution-controller.ts, src/application/control-server.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Add loop decision hooks to execution control, session methods/API routes for accept/stop, and runtime checks that finish with human_accepted or stopped at safe loop boundaries.
  </action>
  <acceptance_criteria>
    - Session/control API tests can request loop accept/stop without using node approval endpoints.
    - Runtime tests show manual accept yields stopReason human_accepted and manual stop yields stopReason stopped.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render loop state in UI card and inspector</name>
  <files>ui/src/main.tsx, ui/src/styles.css, tests/recursive-language-model.test.ts</files>
  <action>
    Add frontend loop metadata types, card summary, inspector timeline/details, and loop-scoped action buttons.
  </action>
  <acceptance_criteria>
    - UI source contains quality-loop summary and timeline rendering.
    - UI build passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strengthen compact CLI loop output</name>
  <files>src/cli/render.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Include score and issue count in compact loop output while preserving JSON detail.
  </action>
  <acceptance_criteria>
    - Renderer test covers score and unresolved issue count.
    - Full test suite passes.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `npm run build`.
Run `npm run build:ui`.
Run `node --test --test-name-pattern='quality loop .*manual|quality loop .*render|approval mode contract' dist/tests/recursive-language-model.test.js`.
Run `npm test`.
</verification>

---
phase: 73-ui-resume-control
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - ui/src/app/TopBar.tsx
  - ui/src/app/AppShell.tsx
  - ui/src/styles.css
autonomous: true
requirements:
  - RESU-01
user_setup: []
must_haves:
  truths:
    - "User sees Resume control in TopBar when snapshot.runState.resumable is true and run is not active"
    - "Clicking Resume opens confirm modal — no POST until user confirms"
    - "After confirm, UI POSTs /api/chat/resume-run with confirm true and session updates to running/resumed state"
  artifacts:
    - path: ui/src/app/TopBar.tsx
      provides: "Resume button + confirm modal + POST wiring"
      contains: "resume-run"
  key_links:
    - from: ui/src/app/TopBar.tsx
      to: POST /api/chat/resume-run
      via: "runAction + post after modal confirm"
      pattern: "api/chat/resume-run"
    - from: ui/src/app/TopBar.tsx
      to: snapshot.runState.resumable
      via: "conditional render"
      pattern: "runState\\?.resumable"
    - from: ui/src/app/TopBar.tsx
      to: ui/src/nodes/GraphActionModal.tsx
      via: "confirm mode modal"
      pattern: "GraphActionModal"
---

<objective>
Wire TopBar resume control with explicit confirm modal calling POST /api/chat/resume-run (RESU-01).

Purpose: Close v1.9 tech debt — backend resume path exists since Phase 64 but UI never calls it.
Output: Visible, confirmed resume UX in workflow shell.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ui-resume-control/73-CONTEXT.md
@.planning/notes/ui-shell-architecture.md
@ui/src/app/TopBar.tsx
@ui/src/nodes/GraphActionModal.tsx
@ui/src/shared/api.ts

<interfaces>
TopBar existing patterns:
- Run workflow: `runAction(setErrorMessage, async () => { await post("/api/chat/confirm-run", {...}); }, refresh)`
- Stop: `post("/api/stop", { reason: "..." })`
- Pause auto-approvals: conditional on status + approvalMode

GraphActionModal confirm mode (NodeContextMenu delete pattern):
```tsx
<GraphActionModal
  open={confirmOpen}
  mode="confirm"
  title="..."
  description="..."
  confirmLabel="..."
  onCancel={() => setConfirmOpen(false)}
  onSubmit={() => { /* action */ }}
/>
```

SessionSnapshot.runState from Plan 01:
```typescript
runState?: { resumable: boolean; activeNodeId?: string };
```

Resume POST body per D-03: `{ confirm: true }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resume confirm modal state and visibility gate</name>
  <files>ui/src/app/TopBar.tsx</files>
  <action>
    Per D-01 and D-02, extend TopBar.tsx:

    1. Import `GraphActionModal` from `../nodes/GraphActionModal`.
    2. Add `useState` for `resumeConfirmOpen` boolean.
    3. Compute `showResumeControl`:
       - `snapshot.runState?.resumable === true`
       - AND `snapshot.status !== "running"`
       - AND no active execution pill conflict (same guard as Run workflow disabled when running)
    4. When showResumeControl, render secondary button `"Resume run"` with `aria-label="Resume interrupted run"` placed between pause-auto-approval button (if any) and Run workflow button.
    5. Button click sets `resumeConfirmOpen` true — does NOT POST yet.
    6. Render GraphActionModal mode confirm:
       - title: "Resume interrupted run?"
       - description: mention incomplete nodes will continue from saved state; include activeNodeId in description when present (`snapshot.runState.activeNodeId`).
       - confirmLabel: "Resume run"
       - cancelLabel: "Cancel"
       - onCancel closes modal
       - onSubmit closes modal then triggers Task 2 POST (inline or extracted handler)

    Match existing TopBar button classes (`secondary` for resume, not primary — Run workflow stays primary).
  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Resume button visible only when runState.resumable; opens confirm modal without network call.</done>
</task>

<task type="auto">
  <name>Task 2: Wire confirm to POST resume-run</name>
  <files>ui/src/app/TopBar.tsx</files>
  <action>
    Per D-03, in modal onSubmit handler:

    ```typescript
    runAction(
      setErrorMessage,
      () => post("/api/chat/resume-run", { confirm: true }),
      refresh,
    );
    ```

    Also call `setActiveRunVariant` if response needed — resume-run response may include readiness; refresh() via runAction handles snapshot update including status running and runState.resumable flipping false.

    On error, runAction already sets errorMessage and refreshes — no extra handling.

    Ensure confirm:true is literal boolean true in JSON body (not string).

    Do not add resume to RunPanel (D-01).
  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Confirmed resume calls POST with confirm true and refreshes session (success criteria 2–3).</done>
</task>

<task type="auto">
  <name>Task 3: TopBar layout polish for resume control</name>
  <files>ui/src/styles.css, ui/src/app/AppShell.tsx</files>
  <action>
    Minimal CSS if resume button wraps awkwardly in top bar — follow existing `.workflow-topbar` flex patterns; no new design tokens.

    AppShell.tsx: only touch if TopBar needs new props (e.g. setActiveRunVariant on resume) — pass through existing props already on TopBar; avoid scope creep.

    Add `data-testid="resume-run-button"` on resume button for future manual UAT (Phase 72 checklist may reference) — optional attribute, no test file (full UI suite deferred).

    Verify accessibility: resume button has aria-label; modal retains focus trap from GraphActionModal existing behavior.
  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Resume control fits TopBar layout; build passes; no RunPanel changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→POST /api/chat/resume-run | User-confirmed mutation; must never fire without modal confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-05 | E | TopBar resume button | mitigate | GraphActionModal confirm gate; POST always includes confirm:true per D-02/D-03 |
| T-73-06 | D | accidental double resume | mitigate | runAction disables via refresh; hide button when status running or resumable false |
</threat_model>

<verification>
- `npm run build` in ui/ passes
- Manual smoke (executor): seed run state, load UI, confirm button visible → modal → resume → status running
- Full UI test suite explicitly out of scope (deferred)
</verification>

<success_criteria>
- RESU-01: User resumes interrupted run from UI with explicit confirmation
- Roadmap success criteria 1–3 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/73-ui-resume-control/73-03-SUMMARY.md`
</output>

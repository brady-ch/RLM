---
phase: 73-ui-resume-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/server.rs
  - crates/rlm-core/tests/chat_routes.rs
autonomous: true
requirements:
  - RESU-02
user_setup: []
must_haves:
  truths:
    - "POST /api/chat/resume-run without confirm returns 400"
    - "POST /api/chat/resume-run with confirm:true returns 200 and resumed:true"
    - "After resume HTTP call, executor skips completed nodes (only incomplete node invokes model)"
  artifacts:
    - path: crates/rlm-core/tests/chat_routes.rs
      provides: "HTTP resume-run integration tests"
      contains: "resume-run"
  key_links:
    - from: crates/rlm-core/tests/chat_routes.rs
      to: POST /api/chat/resume-run
      via: "reqwest POST with JSON body"
      pattern: "api/chat/resume-run"
    - from: chat_resume_run handler
      to: spawn_graph_execution(resume: true)
      via: "confirm gate then spawn"
      pattern: "spawn_graph_execution.*true"
---

<objective>
Add HTTP integration coverage for the resume-run confirm gate and skip-completed-nodes behavior (RESU-02).

Purpose: Phase 64 proved resume at executor layer (`run_state_resume.rs`) but v1.9 audit flagged missing HTTP-level test for the control-server path.
Output: Automated tests in chat_routes.rs; optional ServerConfig hook for deterministic exec model.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ui-resume-control/73-CONTEXT.md
@crates/rlm-core/tests/run_state_resume.rs
@crates/rlm-core/tests/chat_routes.rs
@crates/rlm-core/src/control_server/handlers/chat.rs

<interfaces>
chat_resume_run confirm gate (chat.rs):
- `confirm: Option<bool>` — rejects with 400 when `!body.confirm.unwrap_or(false)`
- Error body: `"Resume requires explicit user confirmation (confirm: true)."`

Success response merges via snapshot_with_extra:
- `{ readiness, resumed: true, runId }`

Seed pattern from run_state_resume.rs:
- run_id = memory_session_id
- root node completed in nodeStatuses + resumeCursor with activeNodeId "child", completedNodeIds ["root"]
- Session registers root + child nodes, not actively running
- QueueModel with one "done" response — expect 1 model call, both nodes completed after await
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confirm-gate HTTP test</name>
  <files>crates/rlm-core/tests/chat_routes.rs</files>
  <action>
    Add `chat_resume_run_rejects_without_confirm` test:

    1. tempfile project_root with `.planning/runs` seeded via RunStatePersistence (same seed as run_state_resume.rs: root completed, cursor active on child). Use memory_session_id = run_id string.
    2. Seeded InteractiveExecutionSession with root + child nodes in Ready status; do NOT begin execution.
    3. start_server with project_root = temp dir, memory_session_id, session.
    4. POST `/api/chat/resume-run` with `{}` or `{ "confirm": false }`.
    5. Assert status 400 and error message mentions explicit confirmation.

    Also POST with missing body field only — same 400.

    Mirror reqwest patterns from existing chat_routes tests (client, server.close().await).
  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && cargo test -p rlm-core chat_resume_run_rejects_without_confirm -- --nocapture</automated>
  </verify>
  <done>HTTP test proves confirm gate rejects unconfirmed resume attempts.</done>
</task>

<task type="auto">
  <name>Task 2: Enable deterministic exec model in test server (if needed)</name>
  <files>crates/rlm-core/src/server.rs, crates/rlm-core/tests/chat_routes.rs</files>
  <action>
    If existing start_server cannot inject QueueModel, extend ServerConfig with optional `exec_model: Option<Arc<dyn LanguageModel>>` and apply via `router_state.with_exec_model(model)` in start_server before Arc wrap.

    Keep default None — production behavior unchanged.

    This is test-only wiring; do not change handler logic.
  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && cargo check -p rlm-core 2>&1 | tail -3</automated>
  </verify>
  <done>Tests can supply QueueModel for predictable model call counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HTTP resume accepts confirm and skips completed nodes</name>
  <files>crates/rlm-core/tests/chat_routes.rs</files>
  <behavior>
    - POST resume-run with confirm:false → 400 (covered in Task 1)
    - POST resume-run with confirm:true on seeded partial state → 200, body.resumed == true
    - After polling session until status not running (timeout ~5s): root and child both completed
    - QueueModel seeded with exactly one response — queue empty after resume (proves root skipped)
  </behavior>
  <action>
    Add `chat_resume_run_accepts_confirm_and_skips_completed_nodes`:

    1. Same temp project_root + run state seed + session graph as Task 1.
    2. `let model = Arc::new(QueueModel::new(["done"]));` passed via ServerConfig.exec_model.
    3. POST `{ "confirm": true }` to resume-run — assert 200, `resumed` true, `runId` matches.
    4. Poll GET /api/session until `status != "running"` (tokio time sleep loop, max 5s) or use session Arc directly after brief sleep.
    5. Assert session graph: root.status completed, child.status completed.
    6. Assert QueueModel queue drained — use `model.remaining()` or equivalent if exposed; otherwise clone session and verify single child execution via node statuses only (both completed with one queue item proves skip).

    Reference run_state_resume.rs assertion: exactly 1 model call for incomplete child only.
  </action>
  <verify>
    <automated>cd /home/brady/workspace/RLM && cargo test -p rlm-core chat_resume_run_accepts_confirm -- --nocapture</automated>
  </verify>
  <done>RESU-02 satisfied: HTTP path accepts confirm, executor skips completed nodes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client→POST /api/chat/resume-run | Tests verify confirm gate enforcement at HTTP boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-03 | E | resume-run without confirm | mitigate | Test asserts 400 — mirrors production gate in chat_resume_run |
| T-73-04 | T | test exec_model override | accept | Test-only ServerConfig field; not exposed to production callers |
</threat_model>

<verification>
- `cargo test -p rlm-core chat_resume_run_` passes both new tests
- `cargo test -p rlm-core chat_routes` — existing tests still pass
</verification>

<success_criteria>
- RESU-02: HTTP integration test covers confirm gate + skip completed nodes (roadmap success criterion 4)
</success_criteria>

<output>
After completion, create `.planning/phases/73-ui-resume-control/73-02-SUMMARY.md`
</output>

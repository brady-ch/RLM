---
phase: 78-legacy-panel-extraction
plan: 01
type: execute
wave: 1
autonomous: true
requirements:
  - SHEL-01
  - SHEL-05
---

# Plan 78-01: Extract Legacy Panels

## Objective

Extract all domain panels from `legacy/panels.tsx` into `advanced/*` and `nodes/`; delete monolith; keep `main.tsx` thin.

## Tasks

### T1 — Extract domain panels

Create modules:

- `advanced/models/ModelLibraryPanel.tsx`
- `advanced/plugins/PluginPanel.tsx`
- `advanced/sessions/SavedSessionPanel.tsx`
- `advanced/memory/MemoryPanel.tsx`
- `advanced/settings/` — RefineGraphPanel, GraphWorkflowPanel, NodeInspector, QualityLoopInspector, inspectorHelpers
- `nodes/QualityLoopCardSummary.tsx`

Update Advanced views and ExecutionNodeCard imports.

**Verify:** `npm run build:ui`

### T2 — Remove legacy monolith

Delete `ui/src/legacy/panels.tsx`. Update approval-mode contract test path.

**Verify:** `npm test` (full suite)

### T3 — Phase verification

Write `78-VERIFICATION.md` with `status: passed`.

## Success Criteria

1. No `legacy/panels` imports under `ui/src/`
2. `legacy/panels.tsx` deleted
3. `main.tsx` mounts AppShell only
4. UI build and tests pass

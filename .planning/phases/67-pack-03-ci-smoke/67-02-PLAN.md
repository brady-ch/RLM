---
phase: 67-pack-03-ci-smoke
plan: 02
type: execute
wave: 2
depends_on:
  - "67-01"
files_modified:
  - .github/workflows/deb-smoke.yml
  - docs/DESKTOP.md
  - .github/workflows/package-smoke.yml
autonomous: true
requirements:
  - PACK-01
  - REG-02
user_setup: []

must_haves:
  truths:
    - "GitHub Actions Linux job builds .deb via tauri:build and runs package:smoke:deb successfully"
    - "Headless CI uses xvfb-run; job does not set RLM_SKIP_DEB_SMOKE"
    - "docs/DESKTOP.md documents skip behavior and CI vs local prerequisites"
  artifacts:
    - path: .github/workflows/deb-smoke.yml
      provides: "Headless Linux deb build + smoke CI job"
      contains: "tauri:build"
    - path: docs/DESKTOP.md
      provides: "Updated Rust-only packaging docs + RLM_SKIP_DEB_SMOKE skip contract"
      contains: "RLM_SKIP_DEB_SMOKE"
  key_links:
    - from: .github/workflows/deb-smoke.yml
      to: npm run package:smoke:deb
      via: "workflow run step after tauri:build"
      pattern: "package:smoke:deb"
    - from: docs/DESKTOP.md
      to: scripts/packaging/smoke-deb.mjs
      via: "documents npm run package:smoke:deb"
      pattern: "package:smoke:deb"
---

<objective>
Wire headless Linux CI to build the Tauri `.deb` and run deb smoke, and document local skip behavior for unsupported developer hosts.

Purpose: Satisfies ROADMAP success criteria 1–2 and closes PACK-01 / REG-02 for release validation.
Output: `deb-smoke.yml` workflow, updated DESKTOP.md, optional cross-link in package-smoke.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-pack-03-ci-smoke/67-CONTEXT.md
@.planning/phases/67-pack-03-ci-smoke/67-01-PLAN.md
@.github/workflows/package-smoke.yml
@docs/DESKTOP.md
@package.json
@scripts/packaging/smoke-deb.mjs

<interfaces>
From Plan 67-01 (must exist before this plan runs):
- npm run package:smoke:deb → node scripts/packaging/smoke-deb.mjs
- RLM_SKIP_DEB_SMOKE=1 → exit 0 skip
- Deb artifact path: src-tauri/target/release/bundle/deb/*.deb

From .github/workflows/package-smoke.yml (pattern):
- on: push branches main/master + workflow_dispatch
- setup-node 22, npm ci, matrix jobs

Linux apt packages from docs/DESKTOP.md (install in CI before tauri:build):
- libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev
- Additional CI packages: xvfb, pkg-config, libssl-dev, build-essential (Rust/Tauri baseline)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Linux deb-smoke GitHub Actions workflow</name>
  <files>.github/workflows/deb-smoke.yml</files>
  <read_first>
    .github/workflows/package-smoke.yml
    .planning/phases/67-pack-03-ci-smoke/67-CONTEXT.md
    docs/DESKTOP.md
    package.json
  </read_first>
  <action>
Create .github/workflows/deb-smoke.yml (per D-01):

1. Triggers: match package-smoke — push to main/master and workflow_dispatch.

2. Single job deb-smoke on ubuntu-latest (no matrix).

3. Steps: checkout@v4, setup-node 22 with npm cache, setup Rust (dtolnay/rust-toolchain@stable or actions-rust-lang/rust-toolchain@stable with minimal components).

4. Install apt deps: Tauri Linux packages from DESKTOP.md plus xvfb, libssl-dev, build-essential, libdbus-1-dev, libudev-dev (Tauri 2 linux deps as needed for successful tauri:build).

5. npm ci

6. Install Tauri CLI if not hoisted: npx is acceptable via npm run tauri:build

7. npm run tauri:build — must produce deb under src-tauri/target/release/bundle/deb/

8. npm run package:smoke:deb — do NOT set RLM_SKIP_DEB_SMOKE; ensure xvfb-run is on PATH from xvfb package

9. Upload artifact: src-tauri/target/release/bundle/deb/*.deb with retention 7 days (name deb-smoke-artifact)

Job must fail if tauri:build or package:smoke:deb exits non-zero. Add workflow comment header referencing Phase 67 / PACK-01.
  </action>
  <acceptance_criteria>
deb-smoke.yml exists; contains ubuntu-latest, tauri:build, package:smoke:deb steps; does not set RLM_SKIP_DEB_SMOKE; uploads .deb artifact.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/deb-smoke.yml','utf8'); if(!/tauri:build/.test(y)||!/package:smoke:deb/.test(y)) process.exit(1); if(/RLM_SKIP_DEB_SMOKE/.test(y)) process.exit(1);"</automated>
  </verify>
  <done>Headless CI workflow builds and smoke-tests .deb on ubuntu-latest per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Update DESKTOP.md for Rust layout and skip contract</name>
  <files>docs/DESKTOP.md</files>
  <read_first>
    docs/DESKTOP.md
    scripts/packaging/build-release.mjs
    scripts/packaging/smoke-release.mjs
    scripts/packaging/smoke-deb.mjs
    .planning/phases/67-pack-03-ci-smoke/67-CONTEXT.md
  </read_first>
  <action>
Rewrite stale sections in docs/DESKTOP.md (per D-03, ROADMAP success criterion 2):

1. Fix staged release contents to match rust-binary layout: bin/rlm, ui-dist/, rlm launcher shim, desktop-manifest.json with runtime.kind rust-binary — remove references to bundled Node, dist/src/index.js, ensure-ollama.mjs.

2. Add section "Deb install smoke" documenting:
   - npm run tauri:build produces .deb under src-tauri/target/release/bundle/deb/
   - npm run package:smoke:deb installs and smoke-tests (requires sudo locally; CI uses same script)
   - RLM_SKIP_DEB_SMOKE=1 for explicit skip
   - Auto-skip on Linux when pkg-config glib-2.0 missing — exit 0 with message (unsupported host behavior)
   - CI validates on headless ubuntu-latest with xvfb; developers without apt deps are not blocked

3. Keep Linux apt prerequisites section accurate for tauri:build.

4. Add one-line cross-link that CI job lives in .github/workflows/deb-smoke.yml.

Do not add new README files.
  </action>
  <acceptance_criteria>
DESKTOP.md mentions RLM_SKIP_DEB_SMOKE, package:smoke:deb, deb-smoke.yml, rust-binary manifest; no bundled Node claims remain.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'RLM_SKIP_DEB_SMOKE' docs/DESKTOP.md && grep -q 'package:smoke:deb' docs/DESKTOP.md && grep -q 'rust-binary' docs/DESKTOP.md && ! grep -q 'bin/node' docs/DESKTOP.md</automated>
  </verify>
  <done>Packaging docs describe CI smoke path and local skip behavior per ROADMAP criterion 2.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-link workflows and targeted verification</name>
  <files>.github/workflows/package-smoke.yml</files>
  <read_first>
    .github/workflows/package-smoke.yml
    .github/workflows/deb-smoke.yml
    package.json
  </read_first>
  <action>
Add a brief comment at top of .github/workflows/package-smoke.yml pointing to deb-smoke.yml for Linux .deb install validation (staging-only vs full deb smoke split).

Run targeted verification only (per autonomous run policy — defer full npm run check:rust):
- node --test scripts/packaging/deb-smoke-lib.test.mjs
- YAML structure check on deb-smoke.yml (node script or actionlint if available)
- npm run package:smoke still passes on current host OR document skip if Linux without deps (must not fail)

Update .planning/REQUIREMENTS.md: mark PACK-01 complete when CI workflow merged (checkbox).

Write 67-02-SUMMARY.md and 67-VERIFICATION.md noting deb-smoke workflow added; REG-02 partial — targeted tests green, full check:rust deferred to milestone close (match Phase 66 pattern).
  </action>
  <acceptance_criteria>
package-smoke.yml references deb-smoke.yml; deb-smoke-lib tests pass; REQUIREMENTS.md PACK-01 checked when workflow present; 67-VERIFICATION.md exists.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/packaging/deb-smoke-lib.test.mjs && test -f .github/workflows/deb-smoke.yml && grep -q deb-smoke.yml .github/workflows/package-smoke.yml</automated>
  </verify>
  <done>Workflows cross-linked; targeted verification and phase verification doc complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → apt/sudo | CI runner installs system packages and runs smoke with sudo |
| Public fork PRs → workflow | workflow uses checkout only; no secrets in deb-smoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-04 | E | deb-smoke CI job | mitigate | Pin action versions (@v4); no curl\|bash; build from repo source only |
| T-67-05 | T | sudo in smoke script | accept | Ephemeral GH runners; deb built from same workflow |
| T-67-06 | I | workflow_dispatch | accept | Public repo pattern matches existing package-smoke |
</threat_model>

<verification>
- deb-smoke.yml structure grep gate passes
- docs/DESKTOP.md contains skip contract strings
- node --test scripts/packaging/deb-smoke-lib.test.mjs
- Full deb smoke on CI confirmed after push (note in VERIFICATION.md — local may skip)
</verification>

<success_criteria>
- Linux CI job builds .deb and runs package:smoke:deb without skip env
- DESKTOP.md documents unsupported-host skip behavior
- PACK-01 requirement marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/67-pack-03-ci-smoke/67-02-SUMMARY.md` and `.planning/phases/67-pack-03-ci-smoke/67-VERIFICATION.md`
</output>

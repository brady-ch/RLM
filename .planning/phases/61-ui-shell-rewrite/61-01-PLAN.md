---
phase: 61-ui-shell-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/shared/types.ts
  - ui/src/shared/api.ts
  - ui/src/shared/tokens.css
  - ui/src/styles.css
autonomous: true
requirements: [UI-SC-01, UI-SC-02, UI-SC-03, UI-SC-04, UI-SC-05]
must_haves:
  truths:
    - "Shared types and API helpers exist as importable modules (not inline in main.tsx)"
    - "Design tokens live in shared/tokens.css and styles.css imports them"
  artifacts:
    - path: ui/src/shared/types.ts
      provides: "ExecutionNode, SessionSnapshot, and all panel-related types"
      exports: ["ExecutionNode", "SessionSnapshot", "FlowNodeData"]
    - path: ui/src/shared/api.ts
      provides: "post, postJson, runAction, formatPlanningError helpers"
      exports: ["post", "runAction"]
    - path: ui/src/shared/tokens.css
      provides: "Phase 11/30 CSS custom properties"
      contains: "--color-accent"
  key_links:
    - from: ui/src/styles.css
      to: ui/src/shared/tokens.css
      via: "@import"
      pattern: "tokens\\.css"
---

<objective>
Extract shared types, API helpers, and design tokens from the 3,000-line `ui/src/main.tsx` monolith into `ui/src/shared/`.

Purpose: Every downstream shell component imports from one shared layer — no duplicate type or fetch logic.
Output: `types.ts`, `api.ts`, `tokens.css`; `main.tsx` still compiles via re-exports or unchanged imports until Plan 06 teardown.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ui-shell-rewrite/61-CONTEXT.md
@.planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md
@.planning/notes/ui-shell-architecture.md
@ui/src/main.tsx

<interfaces>
From ui/src/main.tsx (extract verbatim — do not rename fields):
- ExecutionStatus union (planned | ready | awaiting_approval | approved | running | completed | skipped | failed | cancelled)
- ExecutionNode, SessionSnapshot, FlowNodeData, ModelLibrarySnapshot, PluginSnapshot, MemorySnapshot
- Helper signatures: post(path, body), postJson(path, body), runAction(setErrorMessage, operation, refresh), formatPlanningError(message)
- uiRunStatusLabels Record<ExecutionStatus, string>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared types module</name>
  <files>ui/src/shared/types.ts</files>
  <read_first>ui/src/main.tsx (lines 35–450 for type definitions), ui/src/main.tsx (lines 473–513 for FlowNodeData and graph helpers)</read_first>
  <action>
    Create ui/src/shared/types.ts exporting every type currently defined in main.tsx before the App function: ExecutionStatus, QualityLoop*, ExecutionNode, SamplingOptions, ExecutionGraph, SessionSnapshot, ModelLibrary*, Plugin*, SavedSession*, GraphWorkflow*, MemorySnapshot, FlowNodeData.
    Move executionToFlowNode and graphToFlowEdges into ui/src/shared/graph-utils.ts (or keep in types.ts if small) — export both.
    Move uiRunStatusLabels constant into types.ts or a ui/src/shared/labels.ts sibling.
    Do NOT change field names or union members — frozen HTTP/SSE contract per CONTEXT locked decision.
    Leave temporary re-exports in main.tsx (export type { ... } from './shared/types') so the app still builds until Plan 06 removes them.
  </action>
  <acceptance_criteria>
    - ui/src/shared/types.ts exports ExecutionNode and SessionSnapshot
    - grep -c "export type ExecutionNode" ui/src/shared/types.ts equals 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>All domain types importable from ui/src/shared/types.ts; typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Extract API helpers and design tokens</name>
  <files>ui/src/shared/api.ts, ui/src/shared/tokens.css, ui/src/styles.css</files>
  <read_first>ui/src/main.tsx (post/runAction/formatPlanningError at file bottom), ui/src/styles.css (:root block lines 1–28), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Design System table</read_first>
  <action>
    Create ui/src/shared/api.ts with post, postJson, runAction, formatPlanningError, truncateFailureMessage, approvalModeLabel, phaseLabel, and other pure string/format helpers currently below App in main.tsx — preserve exact error parsing behavior in post().
    Create ui/src/shared/tokens.css containing the :root custom properties from styles.css (--space-*, --radius-*, --font-*, --color-* including --color-accent: #2d6cdf).
    Update ui/src/styles.css: @import './shared/tokens.css'; at top; remove duplicated :root token block (keep element rules in styles.css).
    Add temporary re-exports in main.tsx for api helpers until Plan 06.
  </action>
  <acceptance_criteria>
    - ui/src/shared/api.ts exports async function post(
    - ui/src/shared/tokens.css contains --color-accent: #2d6cdf
    - ui/src/styles.css contains @import './shared/tokens.css'
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint -- ui/src/shared</automated>
  </verify>
  <done>API helpers and tokens extracted; lint and typecheck green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → /api/* | Existing JSON POST endpoints; no new routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | I | post() error text | accept | Relocate only — same error surfacing as monolith |
| T-61-01-02 | T | fetch helpers | accept | No new endpoints; frontend restructure only |
</threat_model>

<verification>
npm run typecheck && npm run lint -- ui/src
</verification>

<success_criteria>
- shared/types.ts and shared/api.ts exist with all exports needed by shell components
- tokens.css holds design tokens per UI-SPEC; styles.css imports it
- No behavior change — extraction only
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-shell-rewrite/61-01-SUMMARY.md`
</output>

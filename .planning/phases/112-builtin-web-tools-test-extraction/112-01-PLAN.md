---
phase: 112-builtin-web-tools-test-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/plugins/builtin/web_fetch.rs
  - crates/rlm-core/tests/plugins/builtin/web_fetch.rs
autonomous: true
requirements:
  - PLUG-112-01
  - PLUG-112-02
  - PLUG-112-03
  - PLUG-112-04
must_haves:
  truths:
    - "web_fetch.rs contains no inline test bodies — only a #[path] stub module"
    - "extracts_title_from_html runs from crates/rlm-core/tests/plugins/builtin/web_fetch.rs"
    - "cargo test -p rlm-core web_fetch_tests passes without regression"
    - "No new rust-boundary-baseline.json entries introduced"
  artifacts:
    - path: crates/rlm-core/tests/plugins/builtin/web_fetch.rs
      provides: "Extracted builtin web_fetch unit tests"
      contains: "extracts_title_from_html"
    - path: crates/rlm-core/src/plugins/builtin/web_fetch.rs
      provides: "Production web_fetch logic with path stub only"
      contains: "#[path = \"../../../tests/plugins/builtin/web_fetch.rs\"]"
  key_links:
    - from: crates/rlm-core/src/plugins/builtin/web_fetch.rs
      to: crates/rlm-core/tests/plugins/builtin/web_fetch.rs
      via: "#[cfg(test)] #[path = ...] mod web_fetch_tests"
      pattern: "#\\[path = \"\\.\\./\\.\\./\\.\\./tests/plugins/builtin/web_fetch\\.rs\"\\]"
---

<objective>
Extract inline tests from `plugins/builtin/web_fetch.rs` to mirrored `tests/plugins/builtin/web_fetch.rs` using the Phase 110 #[path] stub pattern.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create mirrored tests/plugins/builtin/web_fetch.rs with extracted test body</name>
  <files>crates/rlm-core/tests/plugins/builtin/web_fetch.rs</files>
  <action>
    1. Create `crates/rlm-core/tests/plugins/builtin/web_fetch.rs`.
    2. Add `use super::*;` at top.
    3. Move `extracts_title_from_html` test verbatim from web_fetch.rs lines 193-199.
    4. Do NOT modify web_fetch.rs yet.
  </action>
  <verify>
    <automated>test -f crates/rlm-core/tests/plugins/builtin/web_fetch.rs && grep -q 'use super::\*' crates/rlm-core/tests/plugins/builtin/web_fetch.rs && grep -q 'extracts_title_from_html' crates/rlm-core/tests/plugins/builtin/web_fetch.rs</automated>
  </verify>
  <done>Mirrored test file exists with test fn; source still has inline body temporarily</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline tests with #[path] stub and verify</name>
  <files>crates/rlm-core/src/plugins/builtin/web_fetch.rs</files>
  <action>
    1. Delete inline `#[cfg(test)] mod tests { ... }` block from web_fetch.rs.
    2. Replace with:
       ```rust
       #[cfg(test)]
       #[path = "../../../tests/plugins/builtin/web_fetch.rs"]
       mod web_fetch_tests;
       ```
    3. Confirm zero `#[test]` attributes in web_fetch.rs.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core web_fetch_tests -- --test-threads=1 2>&1</automated>
    <automated>grep -q '#\[path = "../../../tests/plugins/builtin/web_fetch.rs"\]' crates/rlm-core/src/plugins/builtin/web_fetch.rs</automated>
  </verify>
  <done>web_fetch.rs is stub-only; web_fetch_tests pass</done>
</task>

</tasks>

<success_criteria>
- PLUG-112-01: Zero inline test bodies in `web_fetch.rs`
- PLUG-112-02: Tests mirrored under `crates/rlm-core/tests/plugins/builtin/web_fetch.rs`
- PLUG-112-03: `#[path]` stub wired with correct 3-level relative path
- PLUG-112-04: `cargo test -p rlm-core web_fetch_tests` passes
</success_criteria>

<output>
After completion, create `.planning/phases/112-builtin-web-tools-test-extraction/112-01-SUMMARY.md`
</output>

---
phase: 112-builtin-web-tools-test-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/plugins/builtin/web_search.rs
  - crates/rlm-core/tests/plugins/builtin/web_search.rs
autonomous: true
requirements:
  - PLUG-112-05
  - PLUG-112-06
  - PLUG-112-07
  - PLUG-112-08
must_haves:
  truths:
    - "web_search.rs contains no inline test bodies — only a #[path] stub module"
    - "builds_query_from_terms runs from crates/rlm-core/tests/plugins/builtin/web_search.rs"
    - "cargo test -p rlm-core web_search_tests passes without regression"
    - "No new rust-boundary-baseline.json entries introduced"
  artifacts:
    - path: crates/rlm-core/tests/plugins/builtin/web_search.rs
      provides: "Extracted builtin web_search unit tests"
      contains: "builds_query_from_terms"
    - path: crates/rlm-core/src/plugins/builtin/web_search.rs
      provides: "Production web_search logic with path stub only"
      contains: "#[path = \"../../../tests/plugins/builtin/web_search.rs\"]"
  key_links:
    - from: crates/rlm-core/src/plugins/builtin/web_search.rs
      to: crates/rlm-core/tests/plugins/builtin/web_search.rs
      via: "#[cfg(test)] #[path = ...] mod web_search_tests"
      pattern: "#\\[path = \"\\.\\./\\.\\./\\.\\./tests/plugins/builtin/web_search\\.rs\"\\]"
---

<objective>
Extract inline tests from `plugins/builtin/web_search.rs` to mirrored `tests/plugins/builtin/web_search.rs` using the Phase 110 #[path] stub pattern.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create mirrored tests/plugins/builtin/web_search.rs with extracted test body</name>
  <files>crates/rlm-core/tests/plugins/builtin/web_search.rs</files>
  <action>
    1. Create `crates/rlm-core/tests/plugins/builtin/web_search.rs`.
    2. Add `use super::*;` at top.
    3. Move `builds_query_from_terms` test verbatim from web_search.rs lines 227-231.
    4. Do NOT modify web_search.rs yet.
  </action>
  <verify>
    <automated>test -f crates/rlm-core/tests/plugins/builtin/web_search.rs && grep -q 'use super::\*' crates/rlm-core/tests/plugins/builtin/web_search.rs && grep -q 'builds_query_from_terms' crates/rlm-core/tests/plugins/builtin/web_search.rs</automated>
  </verify>
  <done>Mirrored test file exists with test fn; source still has inline body temporarily</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline tests with #[path] stub and verify</name>
  <files>crates/rlm-core/src/plugins/builtin/web_search.rs</files>
  <action>
    1. Delete inline `#[cfg(test)] mod tests { ... }` block from web_search.rs.
    2. Replace with:
       ```rust
       #[cfg(test)]
       #[path = "../../../tests/plugins/builtin/web_search.rs"]
       mod web_search_tests;
       ```
    3. Confirm zero `#[test]` attributes in web_search.rs.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core web_search_tests -- --test-threads=1 2>&1</automated>
    <automated>grep -q '#\[path = "../../../tests/plugins/builtin/web_search.rs"\]' crates/rlm-core/src/plugins/builtin/web_search.rs</automated>
  </verify>
  <done>web_search.rs is stub-only; web_search_tests pass</done>
</task>

</tasks>

<success_criteria>
- PLUG-112-05: Zero inline test bodies in `web_search.rs`
- PLUG-112-06: Tests mirrored under `crates/rlm-core/tests/plugins/builtin/web_search.rs`
- PLUG-112-07: `#[path]` stub wired with correct 3-level relative path
- PLUG-112-08: `cargo test -p rlm-core web_search_tests` passes
</success_criteria>

<output>
After completion, create `.planning/phases/112-builtin-web-tools-test-extraction/112-02-SUMMARY.md`
</output>

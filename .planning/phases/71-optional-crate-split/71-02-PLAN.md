---
phase: 71-optional-crate-split
plan: 02
type: execute
wave: 2
depends_on:
  - "71-01"
files_modified:
  - Cargo.toml
  - crates/rlm-ports/Cargo.toml
  - crates/rlm-ports/src/lib.rs
  - crates/rlm-domain/Cargo.toml
  - crates/rlm-domain/src/lib.rs
  - crates/rlm-core/Cargo.toml
  - crates/rlm-core/src/lib.rs
  - scripts/measure-rust-compile-baseline.sh
  - .planning/phases/71-optional-crate-split/71-BASELINE-POST-SPLIT.md
autonomous: true
requirements:
  - ARCH-06
  - REG-02
must_haves:
  truths:
    - "rlm-ports and rlm-domain compile independently without axum/tokio in their manifests"
    - "cargo test -p rlm-domain runs domain tests without linking Axum"
    - "rlm-cli and Tauri build unchanged — same rlm_core:: public paths"
    - "Post-split baseline shows measurable delta vs 71-BASELINE.md"
  artifacts:
    - path: "crates/rlm-ports/Cargo.toml"
      provides: "Ports crate with trait-only deps"
      contains: "name = \"rlm-ports\""
    - path: "crates/rlm-domain/Cargo.toml"
      provides: "Domain crate depending on rlm-ports"
      contains: "rlm-ports"
    - path: "crates/rlm-core/Cargo.toml"
      provides: "Facade crate depending on rlm-domain + rlm-ports"
      contains: "rlm-domain"
    - path: "crates/rlm-core/src/lib.rs"
      provides: "Preserved public re-exports"
      contains: "pub use domain::RecursiveLanguageModel"
  key_links:
    - from: "crates/rlm-domain/Cargo.toml"
      to: "crates/rlm-ports"
      via: "path dependency"
      pattern: "rlm-ports = \\{ path"
    - from: "crates/rlm-core/src/lib.rs"
      to: "crates/rlm-domain"
      via: "pub use re-export facade"
      pattern: "pub use rlm_domain"
    - from: "crates/rlm-cli/Cargo.toml"
      to: "crates/rlm-core"
      via: "unchanged path dependency"
      pattern: "rlm-core = \\{ path"
---

<objective>
Execute workspace crate split when Plan 01 decision is SPLIT — extract `rlm-ports` + `rlm-domain` from `rlm-core` while preserving public API (D-03, D-04).

Purpose: ARCH-06 split path; ROADMAP success criterion 2.
Output: Two new workspace members, rlm-core facade, post-split baseline.

**Entry gate:** Read `71-DECISION.md`. If `decision: DEFER`, skip this plan entirely and execute `71-03-PLAN.md` instead.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-optional-crate-split/71-CONTEXT.md
@.planning/phases/71-optional-crate-split/71-01-SUMMARY.md
@.planning/seeds/rust-crate-split.md
@crates/rlm-core/src/lib.rs
@crates/rlm-core/src/ports/mod.rs
@crates/rlm-core/src/domain/mod.rs

<interfaces>
Ports modules to move (`crates/rlm-core/src/ports/`):
- `language_model.rs`, `run_state_store.rs`, `skill_loader.rs`, `tool.rs`, `trace.rs`

Domain modules to move (`crates/rlm-core/src/domain/`):
- `recursion/`, `recursive_language_model.rs`, `run_state_persistence.rs`, `run_state_types.rs`, `types.rs`, `mod.rs`

Known coupling to resolve (D-03): ports import `domain::types::{ChatMessage, LanguageModelResponse, ToolCallRequest, TraceEvent, ToolExecutionResult}` — move these DTOs into `rlm-ports` (e.g. `rlm-ports/src/types.rs`) before extracting domain.

rlm-core public surface to preserve (from lib.rs):
```rust
pub use domain::RecursiveLanguageModel;
pub use ports::{InMemoryTrace, LanguageModelCompleteOptions, QueueModel};
// ... all existing pub use lines unchanged at call sites
```

Target dependency DAG:
```text
rlm-ports (no project-local deps; serde/async-trait only)
rlm-domain → rlm-ports
rlm-core → rlm-domain + rlm-ports + axum/tokio/...
rlm-cli → rlm-core (unchanged)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold rlm-ports with traits and shared DTOs</name>
  <files>Cargo.toml, crates/rlm-ports/Cargo.toml, crates/rlm-ports/src/lib.rs, crates/rlm-ports/src/types.rs, crates/rlm-core/src/ports/</files>
  <action>
Per D-03, create `crates/rlm-ports`:

1. Add `crates/rlm-ports` to workspace `members` in root `Cargo.toml`.
2. `Cargo.toml`: deps limited to `serde`, `serde_json`, `async-trait`, `thiserror` — **no** tokio, axum, reqwest.
3. Move `crates/rlm-core/src/ports/*.rs` into `crates/rlm-ports/src/` preserving module tree.
4. Extract port-facing DTOs from `domain/types.rs` into `rlm-ports/src/types.rs` (`ChatMessage`, `LanguageModelResponse`, `ToolCallRequest`, `TraceEvent`, `ToolExecutionResult`, plus run-state port types from `run_state_store.rs` if not already self-contained). Update trait signatures to use `crate::types::*`.
5. `rlm-ports/src/lib.rs` re-exports all public port traits and DTOs.
6. In `rlm-core`, replace `pub mod ports` with `pub use rlm_ports as ports` (or `pub mod ports { pub use rlm_ports::*; }`) so `rlm_core::ports::*` paths unchanged.

Verify: `cargo check -p rlm-ports` passes with zero references to rlm-core, tokio, or axum.
  </action>
  <verify>
    <automated>cargo check -p rlm-ports 2>&1 | tail -3 | grep -q Finished</automated>
  </verify>
  <done>rlm-ports builds standalone; port DTO coupling to domain eliminated</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold rlm-domain and wire rlm-core facade</name>
  <files>crates/rlm-domain/Cargo.toml, crates/rlm-domain/src/lib.rs, crates/rlm-core/Cargo.toml, crates/rlm-core/src/lib.rs, crates/rlm-core/src/domain/</files>
  <action>
Per D-03/D-04:

1. Create `crates/rlm-domain` with `rlm-ports = { path = "../rlm-ports" }` as sole project-local dep. Allowed external deps: serde, serde_json, async-trait, thiserror, tracing (no axum/tokio).
2. Move remaining `crates/rlm-core/src/domain/**` into `crates/rlm-domain/src/**`. Update imports: `crate::ports` → `rlm_ports`, `crate::domain::types` → split between `rlm_ports::types` and domain-local types.
3. Fix `run_state_persistence.rs` test imports — use port mock or move integration test to rlm-core; no `use crate::persistence::FileRunStateStore` in rlm-domain library code (A1 boundary).
4. Add `rlm-domain` and `rlm-ports` path deps to `rlm-core/Cargo.toml`.
5. Replace `pub mod domain` in rlm-core with re-export facade: `pub use rlm_domain as domain` preserving `rlm_core::domain::RecursiveLanguageModel` and all lib.rs `pub use domain::*` lines.
6. Move domain-focused unit tests (quality_loop, budget_guard, execution_graph_sync) into `rlm-domain` `#[cfg(test)]` modules or `crates/rlm-domain/tests/` as appropriate per discretion.

Verify independent compile: `cargo check -p rlm-domain` and `cargo check -p rlm-core -p rlm-cli`.
  </action>
  <verify>
    <automated>cargo check -p rlm-domain -p rlm-core -p rlm-cli 2>&1 | tail -3 | grep -q Finished</automated>
  </verify>
  <done>rlm-domain builds with rlm-ports only; rlm-core/cli compile with preserved public paths</done>
</task>

<task type="auto">
  <name>Task 3: Post-split baseline and targeted regression</name>
  <files>scripts/measure-rust-compile-baseline.sh, .planning/phases/71-optional-crate-split/71-BASELINE-POST-SPLIT.md</files>
  <action>
Per D-01/D-05 and ROADMAP criterion 1 (after measurement):

1. Extend `measure-rust-compile-baseline.sh` to also time `cargo build -p rlm-ports`, `cargo build -p rlm-domain`, and incremental rebuild after touching `rlm-domain` only (should NOT rebuild axum-dependent rlm-core if only domain changed — document actual behavior).
2. Run script → write `71-BASELINE-POST-SPLIT.md`; include delta table vs `71-BASELINE.md`.
3. Run targeted REG-02 tests:
   - `cargo test -p rlm-domain`
   - `cargo test -p rlm-core --test recursive_engine_session --test quality_loop_parity`
4. Confirm `src-tauri` still builds: `cargo check --manifest-path src-tauri/Cargo.toml`.

If domain-only incremental still rebuilds full rlm-core, note in SUMMARY — split may still help domain test isolation even if fan-out unchanged.
  </action>
  <verify>
    <automated>cargo test -p rlm-domain --no-fail-fast 2>&1 | tail -5 | grep -E '(test result|passed)'</automated>
  </verify>
  <done>Post-split baseline recorded; domain tests pass without Axum in rlm-domain crate graph</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rlm-ports → callers | Trait contracts define tool/model/store surfaces |
| rlm-domain → rlm-ports | Domain policy uses port abstractions only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | E | rlm-domain boundary | mitigate | No persistence/adapters imports; verify with Phase 70 boundary script |
| T-71-04 | D | Public API break | mitigate | Preserve all rlm_core:: re-exports; cargo check rlm-cli + src-tauri |
| T-71-05 | I | Type migration | mitigate | Move shared DTOs to rlm-ports before domain extraction (D-03) |
</threat_model>

<verification>
- `cargo check -p rlm-ports -p rlm-domain -p rlm-core -p rlm-cli` green
- `cargo test -p rlm-domain` passes
- Post-split baseline compared to pre-split
</verification>

<success_criteria>
- rlm-ports + rlm-domain compile independently (ROADMAP criterion 2)
- rlm-cli/Tauri API unchanged
- Before/after baseline documented (ROADMAP criterion 1)
</success_criteria>

<output>
After completion, create `.planning/phases/71-optional-crate-split/71-02-SUMMARY.md` and `71-VERIFICATION.md`
</output>

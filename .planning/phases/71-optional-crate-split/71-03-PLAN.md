---
phase: 71-optional-crate-split
plan: 03
type: execute
wave: 2
depends_on:
  - "71-01"
files_modified:
  - .planning/phases/71-optional-crate-split/71-SUMMARY.md
  - .planning/phases/71-optional-crate-split/71-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ARCH-06
  - REG-02
must_haves:
  truths:
    - "Defer rationale documented with numeric baseline from 71-BASELINE.md"
    - "Seed trigger conditions copied with current PASS/FAIL status for future re-evaluation"
    - "ARCH-06 marked satisfied via evaluated defer path"
  artifacts:
    - path: ".planning/phases/71-optional-crate-split/71-SUMMARY.md"
      provides: "Phase closure for defer path"
      contains: "DEFER"
    - path: ".planning/phases/71-optional-crate-split/71-VERIFICATION.md"
      provides: "ARCH-06 verification record"
      contains: "ARCH-06"
  key_links:
    - from: ".planning/phases/71-optional-crate-split/71-SUMMARY.md"
      to: ".planning/seeds/rust-crate-split.md"
      via: "trigger conditions for re-evaluation"
      pattern: "trigger"
    - from: ".planning/phases/71-optional-crate-split/71-VERIFICATION.md"
      to: ".planning/phases/71-optional-crate-split/71-BASELINE.md"
      via: "baseline metrics citation"
      pattern: "71-BASELINE"
---

<objective>
Close ARCH-06 via documented defer when Plan 01 decision is DEFER — no crate extraction (D-02).

Purpose: ROADMAP success criterion 3 — rationale + seed trigger conditions preserved for future splits.
Output: `71-SUMMARY.md`, `71-VERIFICATION.md`, REQUIREMENTS traceability update.

**Entry gate:** Read `71-DECISION.md`. If `decision: SPLIT`, skip this plan — execute `71-02-PLAN.md` instead.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-optional-crate-split/71-CONTEXT.md
@.planning/phases/71-optional-crate-split/71-01-SUMMARY.md
@.planning/phases/71-optional-crate-split/71-BASELINE.md
@.planning/phases/71-optional-crate-split/71-DECISION.md
@.planning/seeds/rust-crate-split.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write defer closure summary</name>
  <files>.planning/phases/71-optional-crate-split/71-SUMMARY.md</files>
  <action>
Create `71-SUMMARY.md` for defer path per ROADMAP criterion 3:

1. **Decision:** DEFER — cite `71-DECISION.md` and numeric metrics from `71-BASELINE.md` (clean build sec, incremental sec, test wall sec).
2. **Rationale:** Why metrics fall below seed thresholds; note structural cleanup (Phases 68–70) reduced monolith pain; mention type-coupling cost (ports→domain DTOs) if split would add overhead without compile benefit.
3. **Re-evaluation triggers:** Copy verbatim from `.planning/seeds/rust-crate-split.md` trigger_condition block; add operational thresholds:
   - `cargo test -p rlm-core` wall time >180s sustained
   - incremental domain edit rebuild >30s sustained
   - rlm-core LOC exceeds ~20k or workspace members frequently conflict on unrelated modules
4. **Future execution:** Point to `71-02-PLAN.md` as ready plan if triggers fire; note prerequisite A5 boundary script must stay green.
5. **REG-02:** Document targeted gate used (`cargo check -p rlm-core -p rlm-cli`); full suite deferred to milestone close.

Do NOT extract crates in this plan.
  </action>
  <verify>
    <automated>grep -q 'DEFER' .planning/phases/71-optional-crate-split/71-SUMMARY.md && grep -q 'trigger' .planning/phases/71-optional-crate-split/71-SUMMARY.md</automated>
  </verify>
  <done>71-SUMMARY.md documents defer with metrics and re-evaluation triggers</done>
</task>

<task type="auto">
  <name>Task 2: Verification doc and requirements traceability</name>
  <files>.planning/phases/71-optional-crate-split/71-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
1. Write `71-VERIFICATION.md` with goal-backward checklist:
   - ROADMAP criterion 1: baseline measured — PASS (cite 71-BASELINE.md)
   - ROADMAP criterion 2: split crates — N/A (defer path)
   - ROADMAP criterion 3: defer rationale + triggers — PASS (cite 71-SUMMARY.md)
   - ARCH-06: evaluated, defer chosen — PASS
   - REG-02: targeted compile green — PASS/PARTIAL (note full suite deferred)

2. Update `.planning/REQUIREMENTS.md` traceability: ARCH-06 → Phase 71 → Complete (evaluated defer) with link to 71-SUMMARY.md.

3. Run targeted REG-02 gate: `cargo check -p rlm-core -p rlm-cli` — record pass/fail in VERIFICATION.
  </action>
  <verify>
    <automated>cargo check -p rlm-core -p rlm-cli 2>&1 | tail -3 | grep -q Finished</automated>
  </verify>
  <done>71-VERIFICATION.md complete; ARCH-06 traceability updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts → runtime | No code changes; documentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-06 | I | Stale baseline metrics | accept | Script re-runnable; triggers define when to re-measure |
</threat_model>

<verification>
- 71-SUMMARY.md and 71-VERIFICATION.md exist
- ARCH-06 marked complete via evaluation
- `cargo check -p rlm-core -p rlm-cli` green
</verification>

<success_criteria>
- Defer rationale and seed triggers documented (ROADMAP criterion 3)
- ARCH-06 requirement closed
</success_criteria>

<output>
Phase 71 complete on defer path after this plan.
</output>

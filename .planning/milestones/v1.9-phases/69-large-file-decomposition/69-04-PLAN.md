---
phase: 69-large-file-decomposition
plan: 04
type: execute
wave: 2
depends_on:
  - "69-01"
  - "69-02"
  - "69-03"
files_modified:
  - crates/rlm-core/src/domain/recursive_language_model/mod.rs
  - crates/rlm-core/src/domain/recursive_language_model/execution_control.rs
  - crates/rlm-core/src/domain/recursive_language_model/engine_state.rs
  - crates/rlm-core/src/domain/recursive_language_model/engine_hosts.rs
  - crates/rlm-core/src/domain/recursive_language_model/execution_bridge.rs
  - crates/rlm-core/src/domain/mod.rs
  - crates/rlm-core/src/domain/recursive_language_model.rs
requirements:
  - ARCH-04
  - REG-02
autonomous: true
must_haves:
  truths:
    - "ExecutionControl trait lives outside the main orchestrator file"
    - "EngineState and ID/metadata helpers isolated in engine_state.rs"
    - "QualityLoopHostAdapter and EngineHost impls live in engine_hosts.rs"
    - "Execution graph mutation bridge methods live in execution_bridge.rs"
    - "domain::RecursiveLanguageModel re-export unchanged"
    - "quality_loop_parity tests pass after infrastructure peel"
  artifacts:
    - path: crates/rlm-core/src/domain/recursive_language_model/execution_control.rs
      provides: "ExecutionControl trait"
    - path: crates/rlm-core/src/domain/recursive_language_model/engine_state.rs
      provides: "EngineState, empty_metadata, create_id"
    - path: crates/rlm-core/src/domain/recursive_language_model/engine_hosts.rs
      provides: "QualityLoopHostAdapter, EngineHost"
    - path: crates/rlm-core/src/domain/recursive_language_model/execution_bridge.rs
      provides: "ensure_execution_node, add_edge, update_execution_graph, mark_* helpers"
  key_links:
    - from: engine_hosts.rs
      to: domain/recursion/quality_loop.rs
      via: "QualityLoopHost trait impl delegating to RecursiveLanguageModel"
      pattern: "impl QualityLoopHost"
    - from: engine_hosts.rs
      to: domain/recursion/tool_round_loop.rs
      via: "ModelCompletionHost trait impl"
      pattern: "impl ModelCompletionHost"
---

<objective>
Begin RLM decomposition: extract infrastructure (trait, state, host adapters, execution bridge) from `recursive_language_model.rs` (D-03, ARCH-04).

Purpose: Isolate state-threading and host-facade wiring before peeling orchestrator phase methods in 69-05.
Output: recursive_language_model/ directory with infrastructure modules; orchestrator methods remain temporarily in mod.rs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-large-file-decomposition/69-CONTEXT.md
@crates/rlm-core/src/domain/recursive_language_model.rs
@crates/rlm-core/src/domain/recursion/mod.rs
@crates/rlm-core/tests/quality_loop_parity.rs
@crates/rlm-core/tests/recursive_engine_session.rs

<interfaces>
From domain/recursive_language_model.rs — preserve public surface:

```rust
pub trait ExecutionControl: Send + Sync { /* plan_only, is_cancelled, on_event, ... */ }

pub struct RecursiveLanguageModel {
    model: Arc<dyn LanguageModel>,
    trace: Arc<dyn Trace>,
    tools: HashMap<String, Arc<dyn Tool>>,
    state: Mutex<EngineState>,
}

impl RecursiveLanguageModel {
    pub fn new(...) -> Self;
    pub async fn run(...) -> Result<RecursivePromptResult, String>;
}
```

domain/mod.rs: `pub use recursive_language_model::RecursiveLanguageModel;`

Existing recursion modules (DO NOT MODIFY): budget_guard, tool_round_loop, quality_loop, execution_graph_sync, prompt_utilities.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert to directory module; extract execution_control and engine_state</name>
  <files>crates/rlm-core/src/domain/recursive_language_model/mod.rs, crates/rlm-core/src/domain/recursive_language_model/execution_control.rs, crates/rlm-core/src/domain/recursive_language_model/engine_state.rs, crates/rlm-core/src/domain/mod.rs</files>
  <action>
    Per D-03:

    - Rename `recursive_language_model.rs` → directory `recursive_language_model/mod.rs`.
    - Move `ExecutionControl` trait → `execution_control.rs`; re-export from mod.rs.
    - Move `EngineState` struct, `empty_metadata`, `fallback_from_messages_slice`, `EngineState::create_id` → `engine_state.rs`.
    - Keep `RecursiveLanguageModel` struct in mod.rs with same field types; `state` field uses `EngineState` from engine_state module.

    Update `domain/mod.rs` to `pub mod recursive_language_model;` (directory). Ensure `ExecutionControl` remains importable at `domain::recursive_language_model::ExecutionControl` for tests.
  </action>
  <verify>
    <automated>cargo check -p rlm-core 2>&1</automated>
  </verify>
  <done>Directory module compiles; ExecutionControl and EngineState extracted</done>
</task>

<task type="auto">
  <name>Task 2: Extract engine_hosts and execution_bridge; run RLM targeted tests</name>
  <files>crates/rlm-core/src/domain/recursive_language_model/engine_hosts.rs, crates/rlm-core/src/domain/recursive_language_model/execution_bridge.rs, crates/rlm-core/src/domain/recursive_language_model/mod.rs</files>
  <action>
    Per D-03:

    - Move `QualityLoopHostAdapter` + `impl QualityLoopHost` and `EngineHost` + `impl ModelCompletionHost` → `engine_hosts.rs`.
    - Move execution graph bridge methods from impl block → `execution_bridge.rs` as `impl RecursiveLanguageModel` methods: ensure_execution_node, add_edge, update_execution_graph, wait_for_node_approval, throw_if_cancelled, emit_execution, set_execution_status, sync_execution_status_with_outcome, mark_execution_node_*, update_node, write_loop_metadata, summarize_quality_loop_usage, record, record_limit, get_model_calls, get_max_model_calls, build_result.

    mod.rs retains: new, run, run_quality_loop_path, solve_inner, classify, decompose, answer_directly, summarize, synthesize, select_depth, complete (to be peeled in 69-05).

    Run quality_loop_parity + recursive_engine_session per D-06.
  </action>
  <verify>
    <automated>cargo test -p rlm-core quality_loop_parity recursive_engine_session -- --test-threads=1 2>&1</automated>
  </verify>
  <done>Host adapters and execution bridge extracted; RLM targeted tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model host → LanguageModel | LLM completion calls via EngineHost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-07 | E | engine_state.rs | mitigate | Preserve Mutex lock semantics; no new lock bypass |
| T-69-08 | D | execution_bridge.rs | accept | Refactor-only; budget counters unchanged |
</threat_model>

<verification>
Targeted: quality_loop_parity, recursive_engine_session
</verification>

<success_criteria>
Host adapter and execution bridge code removed from monolithic orchestrator file; mod.rs still contains phase methods temporarily
</success_criteria>

<output>
After completion, create `.planning/phases/69-large-file-decomposition/69-04-SUMMARY.md`
</output>

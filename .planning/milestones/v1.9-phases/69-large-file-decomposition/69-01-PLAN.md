---
phase: 69-large-file-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/application/config/defaults.rs
  - crates/rlm-core/src/application/config/yaml_merge.rs
  - crates/rlm-core/src/application/config/validation.rs
  - crates/rlm-core/src/application/config/loader.rs
  - crates/rlm-core/src/application/config/mod.rs
  - crates/rlm-core/src/persistence/config.rs
requirements:
  - ARCH-04
  - REG-02
autonomous: true
must_haves:
  truths:
    - "Config defaults and YAML merge helpers live in application/config/defaults.rs and yaml_merge.rs"
    - "Config shape and reference validation live in application/config/validation.rs"
    - "Filesystem loading and fragment merge orchestration live in application/config/loader.rs"
    - "persistence::load_project_config and LoadedProjectConfig remain reachable at prior import paths"
    - "Targeted config integration tests pass after extraction"
  artifacts:
    - path: crates/rlm-core/src/application/config/defaults.rs
      provides: "default_project_plain fixture seed"
    - path: crates/rlm-core/src/application/config/yaml_merge.rs
      provides: "merge_yaml_layers, merge_interop, is_plain_record"
    - path: crates/rlm-core/src/application/config/validation.rs
      provides: "validate_config_shape, validate_config_references"
    - path: crates/rlm-core/src/application/config/loader.rs
      provides: "load_project_config, load_explicit, parse_yaml_file, load_scoped_fragments"
    - path: crates/rlm-core/src/persistence/config.rs
      provides: "Thin re-export facade to application::config"
  key_links:
    - from: crates/rlm-core/src/persistence/config.rs
      to: crates/rlm-core/src/application/config/mod.rs
      via: "pub use re-export preserving persistence::load_project_config"
      pattern: "pub use crate::application::config::"
    - from: load_project_config
      to: validate_config_references
      via: "called after merge before returning LoadedProjectConfig"
      pattern: "validate_config_references"
---

<objective>
Split `persistence/config.rs` into `application/config/` submodules mirroring v1.6 Phase 37 TS loader/validation/defaults/yaml-merge separation (D-02, ARCH-04).

Purpose: Isolate testable config concerns before boundary enforcement in Phase 70.
Output: Four focused config modules + thin persistence facade; behavior-identical loading.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-large-file-decomposition/69-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/application/config/index.ts
@crates/rlm-core/src/persistence/config.rs
@crates/rlm-core/src/application/config/mod.rs

<interfaces>
From crates/rlm-core/src/persistence/config.rs (public surface to preserve):

```rust
#[derive(Debug, Clone)]
pub struct LoadedProjectConfig {
    pub config: Value,
    pub path: Option<PathBuf>,
}

pub fn load_project_config(
    project_root: &Path,
    explicit_path: Option<&Path>,
) -> io::Result<LoadedProjectConfig>;

pub fn merge_yaml_layers(left: Value, right: Value) -> Value;
```

Loader merge order (implicit path) must remain identical:
HOME/.rlm fragments → project .rlm scoped dir (+ legacy scoped file) → rlm.config.yaml discovery → validate shape → validate references.

Parse errors must retain path prefix in message (existing unit test `parse_yaml_includes_path_context`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract defaults and yaml_merge modules</name>
  <files>crates/rlm-core/src/application/config/defaults.rs, crates/rlm-core/src/application/config/yaml_merge.rs, crates/rlm-core/src/application/config/mod.rs</files>
  <action>
    Per D-02, move verbatim (no semantic edits):

    - `default_project_plain()` → `defaults.rs` (keep same fixture include path).
    - `merge_yaml_layers`, `merge_interop`, `is_plain_record` → `yaml_merge.rs`.

    Wire `application/config/mod.rs` to declare and `pub use` these modules. Do not delete bodies from `persistence/config.rs` yet — leave forwarding stubs if needed for compile during transition.
  </action>
  <verify>
    <automated>cargo check -p rlm-core 2>&1</automated>
  </verify>
  <done>defaults.rs and yaml_merge.rs compile; merge_yaml_layers exported from application::config</done>
</task>

<task type="auto">
  <name>Task 2: Extract validation and loader; slim persistence facade</name>
  <files>crates/rlm-core/src/application/config/validation.rs, crates/rlm-core/src/application/config/loader.rs, crates/rlm-core/src/persistence/config.rs, crates/rlm-core/src/application/config/mod.rs</files>
  <action>
    Per D-02:

    - Move `validate_config_shape`, `validate_config_references` → `validation.rs`.
    - Move `load_project_config`, `load_explicit`, `find_default_config_path`, `parse_yaml_file`, `load_scoped_fragments` → `loader.rs`; import defaults + yaml_merge + validation.
    - Export `LoadedProjectConfig`, `load_project_config`, `merge_yaml_layers` from `application/config/mod.rs`.
    - Replace `persistence/config.rs` body with `pub use crate::application::config::{LoadedProjectConfig, load_project_config, merge_yaml_layers};` plus move `#[cfg(test)]` unit test to `loader.rs` or `validation.rs` (same assertion).

    Preserve `persistence/mod.rs` re-exports unchanged. No new public exports beyond pre-split surface.
  </action>
  <verify>
    <automated>cargo test -p rlm-core persistence_dual_read -- --test-threads=1 2>&1 && cargo test -p rlm-core --lib config -- --test-threads=1 2>&1</automated>
  </verify>
  <done>Config dual-read integration tests green; parse_yaml path-context unit test passes; persistence::load_project_config import path unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → loader | Untrusted YAML from user project and ~/.rlm fragments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I | loader.rs | accept | Refactor-only; no new file read paths |
| T-69-02 | D | validation.rs | mitigate | Preserve existing validate_config_shape/reference throws; no validation bypass |
</threat_model>

<verification>
- `cargo check -p rlm-core -p rlm-cli`
- Config targeted tests per D-06
</verification>

<success_criteria>
- No monolithic config logic remains in persistence/config.rs beyond re-exports
- TS-parity module names: defaults, yaml_merge, validation, loader
</success_criteria>

<output>
After completion, create `.planning/phases/69-large-file-decomposition/69-01-SUMMARY.md`
</output>

---
phase: 69-large-file-decomposition
plan: 05
type: execute
wave: 3
depends_on:
  - "69-04"
files_modified:
  - crates/rlm-core/src/domain/recursive_language_model/mod.rs
  - crates/rlm-core/src/domain/recursive_language_model/orchestrator_phases.rs
  - crates/rlm-core/src/domain/recursive_language_model/solve_tree.rs
requirements:
  - ARCH-04
  - REG-02
autonomous: true
must_haves:
  truths:
    - "classify/decompose/answer/summarize/synthesize/select_depth live in orchestrator_phases.rs"
    - "solve_inner recursive tree walk lives in solve_tree.rs"
    - "mod.rs retains only new, run, run_quality_loop_path, and thin delegation"
    - "recursive_language_model/mod.rs under ~400 lines"
    - "All RLM targeted integration tests pass"
  artifacts:
    - path: crates/rlm-core/src/domain/recursive_language_model/orchestrator_phases.rs
      provides: "Model-call phase methods: classify, decompose, answer_directly, summarize, synthesize, select_depth, complete"
    - path: crates/rlm-core/src/domain/recursive_language_model/solve_tree.rs
      provides: "solve_inner recursive decomposition loop"
    - path: crates/rlm-core/src/domain/recursive_language_model/mod.rs
      provides: "run() top-level flow and RecursiveLanguageModel struct"
  key_links:
    - from: mod.rs run()
      to: solve_tree.rs
      via: "delegates root task to solve_inner"
      pattern: "solve_inner"
    - from: solve_tree.rs
      to: orchestrator_phases.rs
      via: "classify/decompose/synthesize calls"
      pattern: "self\\.classify|self\\.decompose"
---

<objective>
Complete RLM decomposition: peel orchestrator phase methods and solve tree from mod.rs (D-03, ARCH-04).

Purpose: Finish ARCH-04 criterion 1 for recursive_language_model; leave orchestrator as readable top-level flow.
Output: Slim mod.rs (~300 LOC) with run() orchestration delegating to solve_tree and orchestrator_phases.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-large-file-decomposition/69-CONTEXT.md
@.planning/phases/69-large-file-decomposition/69-04-SUMMARY.md
@crates/rlm-core/src/domain/recursive_language_model/mod.rs
@crates/rlm-core/tests/recursive_engine_session.rs
@crates/rlm-core/tests/quality_loop_parity.rs

<interfaces>
mod.rs after 69-04 still contains (move these):
- run_quality_loop_path, solve_inner, classify, decompose, answer_directly, summarize, synthesize, synthesize_without_model, select_depth, complete

mod.rs must retain:
- RecursiveLanguageModel struct + new()
- run() — top-level entry configuring state and dispatching quality-loop vs recursive path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract orchestrator_phases module</name>
  <files>crates/rlm-core/src/domain/recursive_language_model/orchestrator_phases.rs, crates/rlm-core/src/domain/recursive_language_model/mod.rs</files>
  <action>
    Per D-03, move impl RecursiveLanguageModel methods to orchestrator_phases.rs:

    classify, decompose, answer_directly, summarize, synthesize, synthesize_without_model, select_depth, complete, run_quality_loop_path.

    Use `impl RecursiveLanguageModel` in orchestrator_phases.rs with `use super::RecursiveLanguageModel` and `use super::execution_control::ExecutionControl`.

    mod.rs declares `mod orchestrator_phases;` and re-exports nothing extra (methods stay on impl via same module path pattern — Rust allows split impl across submodules only if using pub(crate) forwarding; prefer keeping all impl blocks in child modules under `impl InteractiveExecutionSession`-style pattern OR use `include!` — recommended: child module `impl RecursiveLanguageModel` blocks in orchestrator_phases.rs).

    No algorithm changes. Preserve DIRECT/RECURSIVE constants usage.
  </action>
  <verify>
    <automated>cargo test -p rlm-core quality_loop_parity -- --test-threads=1 2>&1</automated>
  </verify>
  <done>Phase methods compile in orchestrator_phases.rs; quality_loop_parity green</done>
</task>

<task type="auto">
  <name>Task 2: Extract solve_tree; finalize slim orchestrator; verification</name>
  <files>crates/rlm-core/src/domain/recursive_language_model/solve_tree.rs, crates/rlm-core/src/domain/recursive_language_model/mod.rs</files>
  <action>
    Per D-03:

    - Move `solve_inner` → `solve_tree.rs` as `impl RecursiveLanguageModel`.
    - mod.rs retains only struct definition, new(), and run() (+ any minimal private helpers run requires).
    - Delete old `domain/recursive_language_model.rs` single-file if still present.
    - Confirm no file in recursive_language_model/ exceeds ~400 lines.

    Run full RLM targeted test set per D-06. Write 69-VERIFICATION.md summarizing four monolith splits and targeted test evidence (note full check:rust deferred).
  </action>
  <verify>
    <automated>cargo test -p rlm-core recursive_engine_session quality_loop_parity -- --test-threads=1 2>&1 && cargo check -p rlm-core -p rlm-cli 2>&1</automated>
  </verify>
  <done>ARCH-04 complete: all four monoliths split; RLM mod.rs under 400 lines; targeted tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → model | Prompt content sent to LanguageModel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-09 | E | solve_tree.rs | accept | Refactor-only; recursion depth and budget checks unchanged |
| T-69-10 | I | orchestrator_phases.rs | accept | No new prompt injection surface |
</threat_model>

<verification>
- All four ARCH-04 files decomposed
- Targeted test matrix from D-06 executed in 69-01 through 69-05
- 69-VERIFICATION.md documents REG-02 partial (targeted only)
</verification>

<success_criteria>
recursive_language_model monolith eliminated; phase ARCH-04 success criteria 1–3 satisfied with targeted test evidence
</success_criteria>

<output>
After completion, create `.planning/phases/69-large-file-decomposition/69-05-SUMMARY.md` and `69-VERIFICATION.md`
</output>

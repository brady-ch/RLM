---
phase: 62-ui-regression-fixes
plan: 01
type: execute
requirements: [REG-01, REG-02]
wave: 1
---

# Plan 62-01: UI Regression Fixes

## Tasks

1. Verify TopBar pause-auto-approvals control → `/api/pause-future-auto-approvals`
2. Verify ModelLibraryRow HF install → `/api/model-library/download`
3. Fix approval-mode contract test for Phase 61 shell file moves
4. Run `npm run check` green
5. Write 62-VERIFICATION.md

## Verification

- `npm run check` passes
- `npm test -- --test-name-pattern="approval mode contract"` passes

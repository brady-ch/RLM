---
phase: 66
plan: 01
type: execute
autonomous: true
requirements: [CLI-01, CLI-02, REG-02]
---

# Phase 66 Plan 01 — CLI Full Parity

## Objective

Port Node `args.ts` flag surface to Rust clap; implement plan-node, workflow-export/import; extend ask; add integration tests.

## Tasks

1. Create `66-CONTEXT.md`
2. Port Node args flag surface to rlm-cli clap (approval modes, plan-only, workflow, agent, session/memory flags)
3. Implement plan-node, workflow-export, workflow-import (remove not_implemented stubs)
4. Extend ask with full config/execution flags
5. Expand ask_smoke and add per-command integration tests
6. Run `npm run check:rust` and relevant tests
7. Write plan/summary/verification, update ROADMAP STATE REQUIREMENTS, commit via gsd-sdk

## Verification

- `npm run check:rust` green
- `cargo test -p rlm-cli` covers ask, plan-node, workflow-export/import, session flags
- No `not_implemented` exit-2 stubs for plan-node / workflow-export / workflow-import

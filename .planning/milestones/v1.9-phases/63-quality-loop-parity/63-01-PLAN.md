---
phase: 63
plan: 01
type: execute
autonomous: true
requirements: [ENGN-01, ENGN-02, REG-02]
---

# Phase 63 Plan 01 — Quality Loop Parity

## Objective

Port TypeScript `quality-loop.ts` to Rust, wire `run_quality_loop_path` through the full phase loop, align session readiness JSON, and add parity tests.

## Tasks

1. Add QualityLoop types to `crates/rlm-core/src/domain/types.rs`
2. Port `quality_loop.rs` from `src/domain/recursion/quality-loop.ts`
3. Wire `RecursiveLanguageModel::run_quality_loop_path` via `QualityLoopHost`
4. Fix session readiness structured object + golden fixture drift
5. Fix `QueueModel` FIFO ordering for test parity
6. Add `crates/rlm-core/tests/quality_loop_parity.rs`
7. Run `npm run check:rust`

## Verification

- `npm run check:rust` green
- Five phase call counts = 1 per iteration in parity test
- Budget stop with `max_model_calls: 4` before first iteration
- Graph node `loop` metadata mirrors run metadata

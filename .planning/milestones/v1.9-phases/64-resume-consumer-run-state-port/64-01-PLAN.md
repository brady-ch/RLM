---
phase: 64
plan: 01
type: execute
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, ARCH-01, REG-02]
---

# Phase 64 Plan 01 — Resume Consumer + Run-State Port

## Objective

Cross-session resume loader reads `resumeCursor` + `nodeStatuses`; domain uses `RunStateStorePort`; control-server resume requires confirmation; TS cursor parity.

## Tasks

1. Create `64-CONTEXT.md` from ROADMAP + PERS-03-GAP.md
2. Define `RunStateStorePort` in `ports/`; remove domain→concrete store import
3. Resume loader at graph execution entry skips completed nodes
4. Control-server `POST /api/chat/resume-run` with explicit confirmation
5. Align TS `RunStatePersistence.persistResumeCursor` to Rust cursor shape
6. Integration test: partial run → restart → resume → complete
7. Run `npm run check:rust`

## Verification

- `npm run check:rust` green
- Resume test proves single model call on resume (skipped completed node)
- No `domain/` import of `FileRunStateStore` outside test cfg

---
phase: 70-rust-boundary-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - scripts/rust-boundary-rules.toml
requirements:
  - ARCH-05
autonomous: true
must_haves:
  truths:
    - "AGENTS.md documents Rust workspace layers with May import columns matching rlm-core module tree"
    - "AGENTS.md lists named boundary rules referenced by the check script"
    - "Machine-readable rule manifest exists for scripts/check-rust-boundaries.sh"
  artifacts:
    - path: AGENTS.md
      provides: "Rust concern map table, diagram, and boundary rule register"
      contains: "May import"
    - path: scripts/rust-boundary-rules.toml
      provides: "Forbidden layer arcs with rule names and severity"
  key_links:
    - from: scripts/rust-boundary-rules.toml
      to: AGENTS.md
      via: "rule name comments reference concern map rows"
      pattern: "no-domain-to-persistence"
---

<objective>
Publish the Rust workspace concern map and machine-readable boundary rules manifest (D-01, D-02, ARCH-05 documentation half).

Purpose: Contributors and CI share one canonical layering contract before enforcement lands in Plan 02.
Output: AGENTS.md Rust section + `scripts/rust-boundary-rules.toml`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-rust-boundary-enforcement/70-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/todos/pending/rust-structural-architecture-wave2.md
@AGENTS.md
@crates/rlm-core/src/lib.rs
@crates/rlm-cli/src/main.rs

<interfaces>
From crates/rlm-core/src/lib.rs (module tree to document):

```rust
pub mod adapters;
pub mod application;
pub mod control_server;
pub mod domain;
pub mod interop;
pub mod model_library;
pub mod persistence;
pub mod plugins;
pub mod ports;
pub mod server;
```

CLI crate: `crates/rlm-cli/` — transport entry; imports `rlm_core` public facades only (not internal `crate::` paths).

TS analog sections to mirror in AGENTS.md: Concern map table, dependency diagram, Dependency-cruiser rules table → rename to **Rust boundary rules** with rule names matching TOML manifest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rust concern map to AGENTS.md</name>
  <files>AGENTS.md</files>
  <action>
    Per D-01 and D-02, add a **Rust workspace (`crates/`)** section after the existing TypeScript concern map (do not remove TS content).

    Include:
    1. One-paragraph intro: dependency direction flows inward; `rlm-core` layers mirror TS `src/` concerns; `rlm-cli` is the CLI transport crate.
    2. ASCII diagram showing: rlm-cli → application/control_server → domain; ports ◄── adapters/persistence; plugins/interop at composition edge.
    3. Concern map table with columns **Concern | Path | Role | May import** covering at minimum:
       - **cli** — `crates/rlm-cli/src/` — command dispatch — `rlm_core` public re-exports (bootstrap, persistence facades, domain types), not internal modules
       - **application** — `crates/rlm-core/src/application/` — execution, graph, memory, config, bootstrap — domain, ports; persistence/adapters via documented bootstrap exceptions
       - **domain** — `crates/rlm-core/src/domain/` — recursion policy — ports, domain/recursion helpers only
       - **ports** — `crates/rlm-core/src/ports/` — trait contracts — domain types, other ports
       - **adapters** — `crates/rlm-core/src/adapters/` — model hosts — ports, domain types
       - **persistence** — `crates/rlm-core/src/persistence/` — file stores — ports, domain types
       - **plugins** — `crates/rlm-core/src/plugins/` — manifest, registry, builtins — ports, interop, adapters (tool impl)
       - **interop** — `crates/rlm-core/src/interop/` — MCP/skill runtime — ports, plugin paths
       - **control_server** — `crates/rlm-core/src/control_server/` — HTTP transport + handlers — application, ports, persistence (handler wiring)
       - **server** — `crates/rlm-core/src/server/` — server bootstrap — control_server, application
       - **model_library** — `crates/rlm-core/src/model_library/` — HF/catalog service — ports, persistence paths

    4. **Rust boundary rules** table mapping rule name → forbidden arc → rationale (mirror TS depcruise table format).
    5. **Optional follow-on (not enforced)** paragraph documenting transitional exceptions per D-02: application→persistence/adapters direct imports; ports→domain types; plugins/builtin→domain::types for ToolExecutionResult.

    Reference `scripts/check-rust-boundaries.sh` and `npm run check:rust` as the verification command (wired in Plan 02).
  </action>
  <verify>
    <automated>grep -c "May import" AGENTS.md | awk '{exit ($1 >= 2 ? 0 : 1)}'</automated>
  </verify>
  <done>AGENTS.md contains Rust concern map with May import column and named boundary rules table</done>
</task>

<task type="auto">
  <name>Task 2: Create rust-boundary-rules.toml manifest</name>
  <files>scripts/rust-boundary-rules.toml</files>
  <action>
    Per D-02, create TOML manifest consumed by Plan 02 script. Structure:

    - `[scan]` section: roots = ["crates/rlm-core/src", "crates/rlm-cli/src"]; exclude globs for target/ if any.
    - `[[forbidden]]` entries each with: `name`, `from_layer`, `to_layer`, `severity = "error"`, `comment` referencing AGENTS.md.

    Minimum required rules (error severity):
    - no-domain-to-persistence (from domain → persistence)
    - no-domain-to-adapters
    - no-domain-to-application
    - no-domain-to-control_server
    - no-domain-to-plugins
    - no-domain-to-interop
    - no-ports-to-adapters
    - no-ports-to-persistence
    - no-ports-to-control_server
    - no-ports-to-application
    - no-adapters-to-control_server
    - no-adapters-to-application
    - no-persistence-to-control_server
    - no-persistence-to-application
    - no-plugins-to-application
    - no-plugins-to-domain
    - no-plugins-to-persistence

    Layer detection: map file path prefix under rlm-core/src/{layer}/ to layer name; rlm-cli/src → cli layer (cli may import anything via rlm_core:: — no inner forbidden from cli in this manifest).

    Document in file header comment how `use crate::` and `use rlm_core::` map to layers.
  </action>
  <verify>
    <automated>grep -v '^#' scripts/rust-boundary-rules.toml | grep -c 'no-domain-to-persistence' | awk '{exit ($1 >= 1 ? 0 : 1)}'</automated>
  </verify>
  <done>TOML manifest exists with all inner-layer forbidden arcs and no-domain-to-persistence rule present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor → boundary script | Untrusted module imports scanned at CI |
| AGENTS.md → executor | Documentation must match enforced rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | I | AGENTS.md vs TOML drift | mitigate | Rule names identical in both artifacts; Plan 02 meta-test cross-checks |
| T-70-02 | D | Baseline abuse hiding domain violations | mitigate | D-03 requires zero baseline for domain→persistence |
</threat_model>

<verification>
- AGENTS.md Rust section renders concern map with May import columns
- TOML manifest parseable and lists no-domain-to-persistence
</verification>

<success_criteria>
1. Rust concern map published in AGENTS.md with May import columns
2. Rule manifest ready for check-rust-boundaries.sh
</success_criteria>

<output>
After completion, create `.planning/phases/70-rust-boundary-enforcement/70-01-SUMMARY.md`
</output>

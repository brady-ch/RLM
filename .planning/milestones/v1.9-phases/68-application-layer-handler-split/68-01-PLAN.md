---
phase: 68-application-layer-handler-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/application/mod.rs
  - crates/rlm-core/src/application/config/mod.rs
  - crates/rlm-core/src/application/execution/
  - crates/rlm-core/src/application/graph/
  - crates/rlm-core/src/application/memory/
  - crates/rlm-core/src/application/bootstrap/
  - crates/rlm-core/src/lib.rs
  - crates/rlm-core/src/control_server/mod.rs
  - crates/rlm-core/src/control_server/routes.rs
  - crates/rlm-core/src/server.rs
  - crates/rlm-core/src/plugins/runtime.rs
  - crates/rlm-core/src/graph/executor.rs
  - crates/rlm-cli/src/commands/ask.rs
  - crates/rlm-cli/src/commands/plan_node.rs
requirements:
  - ARCH-02
  - REG-02
autonomous: true
must_haves:
  truths:
    - "rlm-core exposes application/ with execution, graph, memory, config, and bootstrap submodules"
    - "Crate-root re-exports preserve rlm_core::execution and rlm_core::graph public paths"
    - "rlm-core and rlm-cli compile after the module move"
  artifacts:
    - path: "crates/rlm-core/src/application/mod.rs"
      provides: "Application layer root with concern submodules"
      contains: "pub mod execution"
    - path: "crates/rlm-core/src/application/config/mod.rs"
      provides: "Config facade re-exporting persistence config API"
      contains: "load_project_config"
    - path: "crates/rlm-core/src/application/execution/mod.rs"
      provides: "Moved execution concern"
    - path: "crates/rlm-core/src/application/graph/mod.rs"
      provides: "Moved graph concern"
    - path: "crates/rlm-core/src/application/memory/mod.rs"
      provides: "Moved memory concern"
    - path: "crates/rlm-core/src/application/bootstrap/mod.rs"
      provides: "Moved bootstrap concern"
  key_links:
    - from: "crates/rlm-core/src/lib.rs"
      to: "crates/rlm-core/src/application/"
      via: "pub mod application and pub use application::* facades"
      pattern: "pub mod application"
    - from: "crates/rlm-core/src/application/config/mod.rs"
      to: "crates/rlm-core/src/persistence/config.rs"
      via: "pub use re-export"
      pattern: "pub use crate::persistence::.*load_project_config"
---

<objective>
Introduce `application/` module grouping for execution, graph, memory, config, and bootstrap — mirroring v1.7 TypeScript Phase 45 concern layout (D-01, D-02).

Purpose: ARCH-02 — separate orchestration concerns from domain/ports/persistence infrastructure before handler split.
Output: Physical `application/` tree with crate-root compatibility re-exports; green compile gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-application-layer-handler-split/68-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.7-phases/45-application-concern-grouping/45-01-PLAN.md
@crates/rlm-core/src/lib.rs
@crates/rlm-core/src/persistence/mod.rs
@crates/rlm-core/src/bootstrap/mod.rs

<interfaces>
From crates/rlm-core/src/lib.rs (current public surface to preserve):
```rust
pub use bootstrap::{apply_config_overrides, prepare_ask_execution, prepare_cli_runtime, CliAskRuntime, CliConfigOverrides, CliRuntime};
pub use execution::InteractiveExecutionSession;
pub use graph::{default_save_variant, execute_graph, export_and_save_graph_workflow, import_sidecar_to_graph, load_graph_workflow, GraphExecutorInput};
pub use memory::{SemanticMemoryIndex, VectorIndexStatus};
pub use persistence::{load_project_config, LoadedProjectConfig, ...};
```

From crates/rlm-core/src/persistence/mod.rs:
```rust
pub use config::{load_project_config, LoadedProjectConfig};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create application/ tree and move concern modules</name>
  <files>crates/rlm-core/src/application/mod.rs, crates/rlm-core/src/application/execution/, crates/rlm-core/src/application/graph/, crates/rlm-core/src/application/memory/, crates/rlm-core/src/application/bootstrap/, crates/rlm-core/src/application/config/mod.rs</files>
  <read_first>
    - crates/rlm-core/src/lib.rs
    - crates/rlm-core/src/execution/mod.rs
    - crates/rlm-core/src/graph/mod.rs
    - crates/rlm-core/src/memory/mod.rs
    - crates/rlm-core/src/bootstrap/mod.rs
    - .planning/milestones/v1.7-phases/45-application-concern-grouping/45-01-PLAN.md
  </read_first>
  <action>
    Per D-01 and D-02 strangler pattern:

    1. Create `crates/rlm-core/src/application/mod.rs` declaring `pub mod bootstrap; pub mod config; pub mod execution; pub mod graph; pub mod memory;`.

    2. Physically move (git mv) these directories under `application/`:
       - `src/execution/` → `src/application/execution/`
       - `src/graph/` → `src/application/graph/`
       - `src/memory/` → `src/application/memory/`
       - `src/bootstrap/` → `src/application/bootstrap/`

    3. Create `application/config/mod.rs` as a thin facade (no implementation move):
       - `pub use crate::persistence::config::*;` or explicit re-exports of `load_project_config`, `LoadedProjectConfig`, and any types consumed by bootstrap/control_server.
       - Do NOT split `persistence/config.rs` implementation (deferred Phase 69).

    4. Fix intra-application imports inside moved modules (`crate::execution` → `crate::application::execution`, etc.). Cross-layer imports stay unchanged: application modules may still use `crate::domain`, `crate::ports`, `crate::persistence`, `crate::adapters`.

    5. Remove empty top-level module directories after move (no stub re-export shims at old paths — update call sites instead per D-01 discretion).

    Do NOT move `model_library/`, `plugins/`, `control_server/`, `domain/`, `ports/`, `persistence/`, or `adapters/`.
  </action>
  <acceptance_criteria>
    - `crates/rlm-core/src/application/mod.rs` exists and declares all five submodules
    - `crates/rlm-core/src/execution/` directory does not exist (moved)
    - `crates/rlm-core/src/application/config/mod.rs` re-exports `load_project_config`
    - `grep -r 'pub mod execution' crates/rlm-core/src/lib.rs crates/rlm-core/src/application/mod.rs` finds execution only under application
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && cargo check -p rlm-core 2>&1 | tail -5</automated>
  </verify>
  <done>application/ tree exists with moved execution, graph, memory, bootstrap modules and config facade; rlm-core compiles</done>
</task>

<task type="auto">
  <name>Task 2: Wire lib.rs facades and fix external imports</name>
  <files>crates/rlm-core/src/lib.rs, crates/rlm-core/src/control_server/mod.rs, crates/rlm-core/src/control_server/routes.rs, crates/rlm-core/src/server.rs, crates/rlm-core/src/plugins/runtime.rs, crates/rlm-core/src/application/graph/executor.rs, crates/rlm-cli/src/commands/ask.rs, crates/rlm-cli/src/commands/plan_node.rs</files>
  <read_first>
    - crates/rlm-core/src/lib.rs
    - crates/rlm-core/src/control_server/mod.rs
    - crates/rlm-cli/src/commands/ask.rs
    - crates/rlm-cli/Cargo.toml
  </read_first>
  <action>
    Per D-01 crate-root compatibility:

    1. Update `lib.rs`:
       - Add `pub mod application;`
       - Replace direct `pub mod execution/graph/memory/bootstrap` with `pub use application::execution; pub use application::graph; pub use application::memory; pub use application::bootstrap;` OR equivalent `pub use application::{execution, graph, memory, bootstrap};` pattern so `rlm_core::execution::*` paths remain valid for external crates.

    2. Update all remaining `crate::execution`, `crate::graph`, `crate::memory`, `crate::bootstrap` imports across rlm-core and rlm-cli to `crate::application::*` (internal) while preserving public `rlm_core::execution` re-exports for tests.

    3. Key files requiring import updates (non-exhaustive — grep to find all):
       - `control_server/mod.rs`, `control_server/routes.rs`, `server.rs`
       - `plugins/runtime.rs`, `application/graph/executor.rs`, `application/memory/semantic_memory_index.rs`, `application/execution/session_graph.rs`
       - `rlm-cli/src/commands/ask.rs`, `plan_node.rs`
       - Integration tests under `crates/rlm-core/tests/` using `rlm_core::execution` (should still work via lib re-exports; fix only if broken)

    4. Optionally add `pub use application::config::{load_project_config, LoadedProjectConfig};` at crate root if bootstrap/control_server should import config via application path — not required if persistence re-exports remain.

    5. Run `cargo check -p rlm-core -p rlm-cli` — fix all compile errors before finishing.

    Do NOT modify handler logic in routes.rs (Phase 68-02 scope).
  </action>
  <acceptance_criteria>
    - `grep -c 'pub mod application' crates/rlm-core/src/lib.rs` equals 1
    - `grep -E 'pub mod (execution|graph|memory|bootstrap)' crates/rlm-core/src/lib.rs | grep -v application | wc -l` equals 0 (no top-level concern modules outside application)
    - `cargo check -p rlm-core -p rlm-cli` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && cargo check -p rlm-core -p rlm-cli</automated>
  </verify>
  <done>Crate-root re-exports preserve public API; rlm-core and rlm-cli compile cleanly after application layer move</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → persistence | Orchestration reaches stores through existing persistence module — no new HTTP surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | I | Module move | accept | Refactor-only; no runtime behavior or auth changes |
| T-68-02 | D | Import graph | accept | Compile-time checks (`cargo check`) verify wiring integrity |
</threat_model>

<verification>
- `cargo check -p rlm-core -p rlm-cli` passes
- `application/` contains execution, graph, memory, config, bootstrap submodules
- ARCH-02 requirement satisfied for module grouping
</verification>

<success_criteria>
- Physical `application/` module tree matches TS concern grouping
- Public `rlm_core::execution`, `rlm_core::graph`, `rlm_core::memory`, `rlm_core::bootstrap` paths still resolve
- No behavior changes — compile-only verification in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/68-application-layer-handler-split/68-01-SUMMARY.md`
</output>

---
phase: 68-application-layer-handler-split
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - crates/rlm-core/src/control_server/handlers/mod.rs
  - crates/rlm-core/src/control_server/handlers/common.rs
  - crates/rlm-core/src/control_server/handlers/graph.rs
  - crates/rlm-core/src/control_server/handlers/nodes.rs
  - crates/rlm-core/src/control_server/handlers/workflows.rs
  - crates/rlm-core/src/control_server/handlers/session.rs
  - crates/rlm-core/src/control_server/handlers/memory.rs
  - crates/rlm-core/src/control_server/handlers/chat.rs
  - crates/rlm-core/src/control_server/handlers/model_library.rs
  - crates/rlm-core/src/control_server/handlers/plugins.rs
  - crates/rlm-core/src/control_server/handlers/events.rs
  - crates/rlm-core/src/control_server/handlers/static_ui.rs
  - crates/rlm-core/src/control_server/routes.rs
  - crates/rlm-core/src/control_server/mod.rs
requirements:
  - ARCH-03
  - REG-02
autonomous: true
must_haves:
  truths:
    - "routes.rs contains only build_router route wiring (no handler function bodies)"
    - "Each handler module is under ~400 lines"
    - "Existing route integration tests pass unchanged"
  artifacts:
    - path: "crates/rlm-core/src/control_server/handlers/mod.rs"
      provides: "Handler module registry"
      contains: "pub mod graph"
    - path: "crates/rlm-core/src/control_server/routes.rs"
      provides: "Transport-only router wiring"
      max_lines: 120
    - path: "crates/rlm-core/src/control_server/handlers/session.rs"
      provides: "Session and saved-session handlers"
    - path: "crates/rlm-core/src/control_server/handlers/plugins.rs"
      provides: "Plugin registry HTTP handlers"
  key_links:
    - from: "crates/rlm-core/src/control_server/routes.rs"
      to: "crates/rlm-core/src/control_server/handlers/"
      via: "handler fn imports in build_router"
      pattern: "handlers::"
    - from: "crates/rlm-core/src/control_server/handlers/nodes.rs"
      to: "crates/rlm-core/src/application/execution/"
      via: "InteractiveExecutionSession mutations"
      pattern: "state\\.session\\."
---

<objective>
Split monolithic `control_server/routes.rs` into concern-based `handlers/` modules; leave `routes.rs` as transport-only router wiring (D-03, ARCH-03).

Purpose: Match v1.6 Phase 41 TS handler split; enable maintainable control-server surface before Phase 69 file decomposition.
Output: Handler modules each ≤~400 lines; route integration tests green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-application-layer-handler-split/68-CONTEXT.md
@.planning/phases/68-application-layer-handler-split/68-01-SUMMARY.md
@src/application/control-server/route-request.ts
@src/application/control-server/handlers/graph.ts
@src/application/control-server/handlers/session.ts
@crates/rlm-core/src/control_server/routes.rs
@crates/rlm-core/src/control_server/mod.rs

<interfaces>
From crates/rlm-core/src/control_server/mod.rs:
```rust
pub struct RouterState { /* session, paths, model_library, plugin_registry, ... */ }
pub fn build_router(state: Arc<RouterState>) -> axum::Router;
```

From crates/rlm-core/src/control_server/routes.rs (route surface to preserve exactly):
- GET `/api/session`, `/api/run-mode`, `/api/graph`, `/api/saved-sessions`, `/api/graph-workflows`, `/api/memory`, `/api/model-library`, `/api/plugins`, `/api/plugins/doctor`, `/api/events`
- POST routes for nodes, chat, clarifications, stop, workflows export/import, memory preferences, model-library mutations, plugin mutations
- UI fallback via ServeDir or placeholder

Handler functions to relocate (names unchanged): session, run_mode, graph_snapshot, graph_layout, graph_viewport, nodes_*, chat_*, clarifications_*, stop_run, pause_auto_approvals, graph_workflows_*, saved_sessions_*, memory_*, model_library_*, plugins_*, events, ui_placeholder; helpers snapshot_with_extra, spawn_graph_execution, parse_replan, plugin_id_mutation, memory_inspect_payload.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract graph, nodes, workflows, and common handlers</name>
  <files>crates/rlm-core/src/control_server/handlers/mod.rs, crates/rlm-core/src/control_server/handlers/common.rs, crates/rlm-core/src/control_server/handlers/graph.rs, crates/rlm-core/src/control_server/handlers/nodes.rs, crates/rlm-core/src/control_server/handlers/workflows.rs, crates/rlm-core/src/control_server/routes.rs, crates/rlm-core/src/control_server/mod.rs</files>
  <read_first>
    - crates/rlm-core/src/control_server/routes.rs
    - src/application/control-server/handlers/graph.ts
    - src/application/control-server/handlers/workflows.ts
    - .planning/milestones/v1.6-phases/41-control-server-boundary/41-CONTEXT.md
  </read_first>
  <action>
    Per D-03 first tranche:

    1. Create `control_server/handlers/mod.rs` declaring submodules: `common`, `graph`, `nodes`, `workflows`, `session`, `memory`, `chat`, `model_library`, `plugins`, `events`, `static_ui`.

    2. Move shared helpers to `handlers/common.rs`:
       - `snapshot_with_extra`
       - `spawn_graph_execution`
       - `parse_replan`
       - Any Deserialize structs shared across handlers (e.g. replan body types)

    3. Move to `handlers/graph.rs`: `graph_snapshot`, `graph_layout`, `graph_viewport` — pub(crate) async fns taking `State&lt;Arc&lt;RouterState&gt;&gt;`.

    4. Move to `handlers/nodes.rs`: all `nodes_*` handlers (add, edit, model, sampling, expert, plan, breakdown, extend_budget, approve, skip, quality_accept, quality_stop, connect, delete). Target ≤~350 lines; if over 400, split quality-loop handlers to a sibling only as last resort.

    5. Move to `handlers/workflows.rs`: `graph_workflows_list`, `graph_workflows_export`, `graph_workflows_import`.

    6. Update `control_server/mod.rs` to `pub mod handlers;` (handlers remain internal to control_server — not crate-public).

    7. Update `routes.rs` `build_router` to import handler fns from `super::handlers::*` for the routes moved so far. Leave unmoved handlers temporarily referenced from routes.rs until Task 2.

    Preserve exact route paths, HTTP methods, and JSON response shapes. Do not rename handler functions (minimizes diff noise).
  </action>
  <acceptance_criteria>
    - `handlers/graph.rs`, `handlers/nodes.rs`, `handlers/workflows.rs`, `handlers/common.rs` exist
    - `wc -l crates/rlm-core/src/control_server/handlers/nodes.rs` is less than 400
    - `grep -c 'async fn graph_snapshot' crates/rlm-core/src/control_server/routes.rs` equals 0
    - `grep -c 'async fn nodes_add' crates/rlm-core/src/control_server/handlers/nodes.rs` equals 1
    - `cargo check -p rlm-core` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && wc -l crates/rlm-core/src/control_server/handlers/*.rs && cargo check -p rlm-core</automated>
  </verify>
  <done>Graph, nodes, and workflows handlers extracted; routes.rs delegates to handlers for those surfaces</done>
</task>

<task type="auto">
  <name>Task 2: Extract remaining handlers and slim routes.rs to wiring only</name>
  <files>crates/rlm-core/src/control_server/handlers/session.rs, crates/rlm-core/src/control_server/handlers/memory.rs, crates/rlm-core/src/control_server/handlers/chat.rs, crates/rlm-core/src/control_server/handlers/model_library.rs, crates/rlm-core/src/control_server/handlers/plugins.rs, crates/rlm-core/src/control_server/handlers/events.rs, crates/rlm-core/src/control_server/handlers/static_ui.rs, crates/rlm-core/src/control_server/routes.rs</files>
  <read_first>
    - crates/rlm-core/src/control_server/routes.rs
    - src/application/control-server/handlers/session.ts
    - src/application/control-server/handlers/model-library.ts
    - src/application/control-server/handlers/plugins.ts
    - crates/rlm-core/tests/chat_routes.rs
    - crates/rlm-core/tests/plugin_routes.rs
  </read_first>
  <action>
    Per D-03 second tranche and ROADMAP success criteria:

    1. Move to `handlers/session.rs`: `session`, `run_mode`, `saved_sessions`, `saved_sessions_save`, `saved_sessions_detail`, `saved_sessions_open`.

    2. Move to `handlers/memory.rs`: `memory`, `memory_inspect_payload`, `memory_preferences_set`, `memory_preferences_delete`.

    3. Move to `handlers/chat.rs`: `chat_message`, `chat_apply`, `chat_cancel`, `chat_confirm_run`, `chat_resume_run`, `clarifications_answer`, `clarifications_abort`, `stop_run`, `pause_auto_approvals`.

    4. Move to `handlers/model_library.rs`: all `model_library_*` handlers.

    5. Move to `handlers/plugins.rs`: all `plugins_*` handlers plus `plugin_id_mutation` helper.

    6. Move to `handlers/events.rs`: `events` SSE handler.

    7. Move to `handlers/static_ui.rs`: `ui_placeholder` and any UI-specific response helpers.

    8. Finalize `routes.rs`:
       - Contains ONLY `build_router`, route `.route(...)` registrations, and imports
       - No `async fn` handler bodies remain in routes.rs
       - Target ≤~120 lines total

    9. Line-count gate (D-03): run `wc -l crates/rlm-core/src/control_server/handlers/*.rs` — every file must be ≤400 lines. If `session.rs` exceeds 400, move saved-session handlers to `handlers/saved_sessions.rs` and update mod.rs.

    10. REG-02 targeted tests (D-04): run route integration test binaries — do NOT run full workspace clippy/test suite.

    Do NOT change endpoint paths or response JSON structures.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' crates/rlm-core/src/control_server/routes.rs | grep -c '^async fn'` equals 0
    - `wc -l crates/rlm-core/src/control_server/routes.rs` is less than 150
    - All files matching `crates/rlm-core/src/control_server/handlers/*.rs` have line count ≤400
    - Route integration tests exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/brady/workspace/RLM && wc -l crates/rlm-core/src/control_server/routes.rs crates/rlm-core/src/control_server/handlers/*.rs && cargo test -p rlm-core --test chat_routes --test plugin_routes --test model_library_routes --test graph_executor_routes --test persistence_control_server</automated>
  </verify>
  <done>routes.rs is transport-only wiring; all handler modules ≤400 lines; route integration tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/* | Unchanged HTTP API — refactor must not alter auth/input validation behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | T | Handler extraction | accept | No new endpoints; existing axum handlers moved verbatim |
| T-68-04 | I | Plugin install routes | accept | handlers/plugins.rs preserves existing validation from routes.rs |
| T-68-05 | D | Route wiring regression | mitigate | Run five route integration test binaries before marking complete |
</threat_model>

<verification>
- `routes.rs` ≤~150 lines, zero handler function bodies
- Each `handlers/*.rs` ≤400 lines
- `cargo test -p rlm-core --test chat_routes --test plugin_routes --test model_library_routes --test graph_executor_routes --test persistence_control_server` passes
- ARCH-03 satisfied
</verification>

<success_criteria>
- Control server handlers live in `handlers/` concern modules mirroring TS layout
- Router file is transport-only wiring
- Route integration tests pass (REG-02 targeted scope)
</success_criteria>

<output>
After completion, create `.planning/phases/68-application-layer-handler-split/68-02-SUMMARY.md` and `68-VERIFICATION.md`
</output>

---
phase: 71-optional-crate-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measure-rust-compile-baseline.sh
  - .planning/phases/71-optional-crate-split/71-BASELINE.md
  - .planning/phases/71-optional-crate-split/71-DECISION.md
autonomous: true
requirements:
  - ARCH-06
  - REG-02
must_haves:
  truths:
    - "Compile/test baseline captured for rlm-core after Phases 69–70 prerequisites"
    - "Seed trigger thresholds evaluated with explicit SPLIT or DEFER recommendation"
    - "71-DECISION.md records which downstream plan (02 split or 03 defer) to execute"
  artifacts:
    - path: "scripts/measure-rust-compile-baseline.sh"
      provides: "Reproducible compile iteration metrics"
      contains: "rlm-core"
    - path: ".planning/phases/71-optional-crate-split/71-BASELINE.md"
      provides: "Pre-decision measurement record"
      contains: "clean build"
    - path: ".planning/phases/71-optional-crate-split/71-DECISION.md"
      provides: "Split vs defer gate outcome"
      contains: "SPLIT|DEFER"
  key_links:
    - from: "scripts/measure-rust-compile-baseline.sh"
      to: "crates/rlm-core"
      via: "cargo build/test timing"
      pattern: "cargo build -p rlm-core"
    - from: ".planning/phases/71-optional-crate-split/71-DECISION.md"
      to: ".planning/seeds/rust-crate-split.md"
      via: "trigger threshold comparison"
      pattern: "trigger"
---

<objective>
Capture post-cleanup compile iteration baseline and produce an evidence-based SPLIT or DEFER gate for ARCH-06 (D-01, D-02).

Purpose: ROADMAP success criterion 1 — measured baseline before split decision; ARCH-06 requires evaluation, not mandatory split.
Output: `measure-rust-compile-baseline.sh`, `71-BASELINE.md`, `71-DECISION.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-optional-crate-split/71-CONTEXT.md
@.planning/seeds/rust-crate-split.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@Cargo.toml
@crates/rlm-core/Cargo.toml

<interfaces>
Current workspace members (root `Cargo.toml`):
```toml
members = ["crates/rlm-core", "crates/rlm-cli"]
```

Seed trigger conditions (`.planning/seeds/rust-crate-split.md`):
- SPLIT when: full rlm-core test suite routinely >2–3 min on dev machines OR single-crate rebuild fan-out blocks unrelated module work
- Prerequisites: A1–A5 complete (Phases 64 + 70)

Pre-planning probe (2026-05-22, Phases 69–70 not yet executed):
- `cargo build -p rlm-core` clean: ~7.5s
- Incremental rebuild after `domain/types.rs` touch: ~1.1s
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compile baseline measurement script</name>
  <files>scripts/measure-rust-compile-baseline.sh</files>
  <action>
Create `scripts/measure-rust-compile-baseline.sh` (executable, bash, set -euo pipefail) per D-01. Script must:

1. Run from repo root; print timestamp, git short SHA, and `rustc --version`.
2. **Clean build:** `cargo clean -p rlm-core` then time `cargo build -p rlm-core` (wall seconds).
3. **Incremental domain:** touch `crates/rlm-core/src/domain/types.rs`, time `cargo build -p rlm-core`.
4. **Incremental ports:** touch `crates/rlm-core/src/ports/language_model.rs`, time rebuild.
5. **Incremental application:** touch `crates/rlm-core/src/application/mod.rs`, time rebuild.
6. **Test iteration:** time `cargo test -p rlm-core --lib --no-fail-fast` (if compile fails, record failure and exit non-zero with message — do not mask).
7. Emit markdown-friendly summary table to stdout AND write same content to path passed as `$1` (default: `.planning/phases/71-optional-crate-split/71-BASELINE.md`).

Use `/usr/bin/time -f '%e'` or `date +%s` delta for wall seconds. Include note that Phases 69–70 must be complete before baseline is authoritative.
  </action>
  <verify>
    <automated>test -x scripts/measure-rust-compile-baseline.sh && bash -n scripts/measure-rust-compile-baseline.sh</automated>
  </verify>
  <done>Script exists, is executable, and documents all five measurement dimensions</done>
</task>

<task type="auto">
  <name>Task 2: Run baseline and evaluate seed triggers</name>
  <files>.planning/phases/71-optional-crate-split/71-BASELINE.md, .planning/phases/71-optional-crate-split/71-DECISION.md</files>
  <action>
**Prerequisite check:** Confirm Phase 69 and Phase 70 SUMMARY/VERIFICATION artifacts exist. If either incomplete, write `71-DECISION.md` with status DEFER, reason "blocked on Phase {N}", and stop — do not treat as split failure.

If prerequisites met:
1. Run `./scripts/measure-rust-compile-baseline.sh` → `71-BASELINE.md`.
2. Evaluate seed triggers per D-02:
   - Compare `cargo test -p rlm-core` wall time to 120–180s threshold.
   - Assess rebuild fan-out: if incremental domain/ports/application rebuilds all exceed 30s OR routinely force full-crate recompile of unrelated layers, note as fan-out evidence.
3. Write `71-DECISION.md` with frontmatter `decision: SPLIT|DEFER`, sections: Summary, Metrics (link 71-BASELINE), Trigger Evaluation (each seed condition PASS/FAIL with numbers), Recommendation, Next Plan (`71-02` if SPLIT, `71-03` if DEFER).

Apply honest threshold logic — sub-threshold metrics → DEFER even if split is technically feasible. Pre-planning probe (~7.5s build, ~1s incremental) strongly suggests DEFER unless post-69/70 metrics differ materially.
  </action>
  <verify>
    <automated>test -f .planning/phases/71-optional-crate-split/71-DECISION.md && grep -E 'decision: (SPLIT|DEFER)' .planning/phases/71-optional-crate-split/71-DECISION.md</automated>
  </verify>
  <done>71-BASELINE.md and 71-DECISION.md exist; decision is SPLIT or DEFER with numeric evidence</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/dev script → cargo | Script invokes local cargo; no network beyond crate fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | T | measure-rust-compile-baseline.sh | accept | Read-only timing; no privileged ops |
| T-71-02 | I | 71-BASELINE.md metrics | accept | Informational artifact; not consumed by runtime |
</threat_model>

<verification>
- `71-DECISION.md` declares SPLIT or DEFER
- Baseline script re-runnable after Phase 70
- ARCH-06 evaluation documented regardless of outcome
</verification>

<success_criteria>
- Compile/test baseline measured and recorded (ROADMAP criterion 1)
- Explicit split/defer gate with seed trigger comparison
</success_criteria>

<output>
After completion, create `.planning/phases/71-optional-crate-split/71-01-SUMMARY.md`
</output>

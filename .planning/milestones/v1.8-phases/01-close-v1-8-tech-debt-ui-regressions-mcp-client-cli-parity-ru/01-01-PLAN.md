---
phase: 01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/app/TopBar.tsx
  - ui/src/styles.css
  - ui/src/legacy/panels.tsx
  - ui/src/nodes/NodeContextMenu.tsx
  - ui/src/nodes/GraphActionModal.tsx
  - ui/src/nodes/ExecutionNodeCard.tsx
autonomous: true
requirements: [ENGN-02, MDLH-03, REG-01]
must_haves:
  truths:
    - "Operator can pause future auto-approvals from TopBar during initial-plan-recursive runs"
    - "HF search results show enabled Install button that calls download endpoint"
    - "Advanced and Back buttons render as neutral secondary style, not primary green"
    - "Run workflow button uses dedicated primary class with readable layout"
    - "Graph add-child, connect-parent, and delete-subtree use in-app modals not window.prompt/confirm"
  artifacts:
    - path: ui/src/app/TopBar.tsx
      provides: "Pause future auto-approvals control + Run button class fix"
      contains: "pause-future-auto-approvals"
    - path: ui/src/styles.css
      provides: ".secondary and .btn-run-primary button variants"
      contains: ".secondary"
    - path: ui/src/nodes/GraphActionModal.tsx
      provides: "Reusable prompt and confirm modals for graph mutations"
    - path: ui/src/legacy/panels.tsx
      provides: "ModelLibraryRow HF download branch"
  key_links:
    - from: ui/src/app/TopBar.tsx
      to: /api/pause-future-auto-approvals
      via: "post on click then refresh()"
      pattern: "pause-future-auto-approvals"
    - from: ui/src/legacy/panels.tsx
      to: /api/model-library/download
      via: "ModelLibraryRow install handler when source is huggingface"
      pattern: "model-library/download"
    - from: ui/src/nodes/NodeContextMenu.tsx
      to: ui/src/nodes/GraphActionModal.tsx
      via: "modal state for add-child, connect-parent, delete-subtree"
      pattern: "GraphActionModal"
---

<objective>
Restore Phase 61 UI regressions and UI-REVIEW priority fixes so Rust-served workflow shell matches pre-rewrite behavior before REG-01 operator sign-off.

Purpose: Unblocks REG-01 human UAT (Wave 2) by fixing ENGN-02 pause control, MDLH-03 HF download wiring, and top visual/interaction regressions per 61-UI-REVIEW.md.
Output: TopBar pause button, HF install path, secondary/Run styles, graph mutation modals, corrected root empty-state copy.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-CONTEXT.md
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-RESEARCH.md
@.planning/milestones/v1.8-phases/61-ui-shell-rewrite/61-UI-REVIEW.md

<interfaces>
From ui/src/shared/types.ts:
```typescript
export type SessionSnapshot = {
  approvalMode: "full" | "initial-plan" | "initial-plan-recursive";
  autoApprovalPaused: boolean;
  status: string;
  // ...
};
```

From ui/src/app/TopBar.tsx (current — missing pause control):
```typescript
export type TopBarProps = {
  snapshot: SessionSnapshot;
  refresh: () => Promise<void>;
  setErrorMessage: (message: string | undefined) => void;
  // ...
};
```

Pause button conditions (per D-01 / RESEARCH Pattern 4):
- Show when snapshot.status === "running" AND snapshot.approvalMode === "initial-plan-recursive"
- Disable or hide when snapshot.autoApprovalPaused === true
- On click: post("/api/pause-future-auto-approvals") then refresh()
- aria-label="Pause future auto-approvals"

HF download branch (per D-01 / RESEARCH Pattern 5):
| source | endpoint | body |
| curated | POST /api/model-library/install | { model: entry.ollamaModel ?? entry.id } |
| huggingface | POST /api/model-library/download | { model: entry.id } |
| installed | disabled | — |

Modal pattern source: ui/src/legacy/panels.tsx lines 230-294 (.modal-overlay / .modal-card)
NodeContextMenu browser dialogs to replace: lines 157-191 in ui/src/nodes/NodeContextMenu.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secondary CSS and TopBar Run/pause controls</name>
  <files>ui/src/styles.css, ui/src/app/TopBar.tsx</files>
  <action>
    Per D-01 Wave 1 / ENGN-02: define `.secondary` button variant in styles.css (outline/neutral surface per 61-UI-REVIEW priority fix #1). Add `.btn-run-primary` for the Run workflow button — do not use `className="icon"` for a text-labeled primary action.
    In TopBar.tsx: change Run workflow button to `className="btn-run-primary"`. Keep Stop as icon danger.
    Add Pause future auto-approvals button per RESEARCH Pattern 4: visible only when snapshot.status === "running" AND snapshot.approvalMode === "initial-plan-recursive"; disabled when snapshot.autoApprovalPaused; wire to post("/api/pause-future-auto-approvals") via runAction + refresh. Include aria-label="Pause future auto-approvals".
    Optional low-cost parity: if Rust route always succeeds for terminal sessions, no backend change required for this plan.
    Do not add command palette or other deferred features.
  </action>
  <verify>
    <automated>npm run build:ui && npm run lint -- ui/src && grep -v '^#' ui/src/app/TopBar.tsx | grep -c 'pause-future-auto-approvals' | grep -qv '^0$' && grep -v '^#' ui/src/styles.css | grep -c '\.secondary' | grep -qv '^0$'</automated>
  </verify>
  <done>TopBar shows pause control under correct conditions; Run uses btn-run-primary; .secondary styles exist in CSS</done>
</task>

<task type="auto">
  <name>Task 2: HF model download wiring in ModelLibraryRow</name>
  <files>ui/src/legacy/panels.tsx</files>
  <action>
    Per D-01 / MDLH-03: update ModelLibraryRow install handler to branch on entry.source.
    curated: keep POST /api/model-library/install with model entry.ollamaModel ?? entry.id.
    huggingface: POST /api/model-library/download with body { model: entry.id } (repo_id).
    installed: button remains disabled.
    Enable Install button when entry.source === "huggingface" AND entry.status !== "unsupported" AND entry.status !== "installing" AND entry.status !== "installed" (mirror curated availability rules).
    Do not change backend routes — wiring only.
  </action>
  <verify>
    <automated>npm run build:ui && grep -v '^#' ui/src/legacy/panels.tsx | grep -c 'model-library/download' | grep -qv '^0$'</automated>
  </verify>
  <done>HF search rows call /api/model-library/download; curated rows still call /install</done>
</task>

<task type="auto">
  <name>Task 3: Graph mutation modals and stale copy fix</name>
  <files>ui/src/nodes/GraphActionModal.tsx, ui/src/nodes/NodeContextMenu.tsx, ui/src/nodes/ExecutionNodeCard.tsx</files>
  <action>
    Per 61-UI-REVIEW priority fixes #2 and #3: create GraphActionModal.tsx reusing .modal-overlay / .modal-card pattern from legacy/panels.tsx. Support three modes: prompt (add-child, connect-parent with labeled input) and confirm (delete-subtree with node label).
    Replace window.prompt and window.confirm in NodeContextMenu.tsx (lines 157-191) with modal state. Each modal needs accessible dialog (role="dialog", aria-labelledby), labeled inputs, Cancel secondary button, primary submit. Focus trap not required beyond existing panel pattern.
    In ExecutionNodeCard.tsx root empty state: change "click Plan children" to "Right-click → Plan children" per UI-REVIEW.
    Do not use dangerouslySetInnerHTML for prompt values — React text nodes only.
  </action>
  <verify>
    <automated>npm run build:ui && npm run lint -- ui/src/nodes && grep -v '^#' ui/src/nodes/NodeContextMenu.tsx | grep -c 'window\.prompt\|window\.confirm' | grep -q '^0$' && test -f ui/src/nodes/GraphActionModal.tsx</automated>
  </verify>
  <done>No browser dialogs in NodeContextMenu; GraphActionModal handles add/connect/delete; root empty-state copy updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Control server API | User-supplied graph prompts and HF repo ids cross to Rust backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | GraphActionModal prompt input | mitigate | Render as React text; no dangerouslySetInnerHTML |
| T-01-02 | Spoofing | HF download request body | transfer | Backend HfRegistry validates repo_id and filenames (existing) |
| T-01-03 | Denial of Service | Pause button spam | accept | Idempotent pause API; low impact |
</threat_model>

<verification>
- npm run build:ui && npm run lint -- ui/src exit 0
- grep confirms pause route wired, HF download route wired, no window.prompt/confirm in NodeContextMenu
</verification>

<success_criteria>
- All five must_have truths observable in built UI against Rust control server
- ENGN-02 and MDLH-03 UI wiring complete; REG-01 UAT prerequisites met for plan 01-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-01-SUMMARY.md`
</output>

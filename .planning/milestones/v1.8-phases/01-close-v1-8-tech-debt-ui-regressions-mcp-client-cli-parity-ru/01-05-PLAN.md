---
phase: 01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - crates/rlm-core/src/domain/run_state_persistence.rs
  - crates/rlm-core/src/graph/executor.rs
  - crates/rlm-core/src/domain/run_state_types.rs
  - .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/PERS-03-GAP.md
autonomous: true
requirements: [PERS-03]
must_haves:
  truths:
    - "Graph executor writes resumeCursor during execution checkpoints"
    - "RunStatePersistence.persist_resume_cursor uses CAS retry like persist_node_status"
    - "Checkpoint records include activeNodeId, completedNodeIds, and variant"
    - "PERS-03-GAP.md documents shared TS/Rust limitations for full cross-session resume"
    - "Neither runtime claims full session replay — gap doc is explicit"
  artifacts:
    - path: crates/rlm-core/src/domain/run_state_persistence.rs
      provides: "persist_resume_cursor method"
    - path: crates/rlm-core/src/graph/executor.rs
      provides: "Checkpoint hooks writing cursor + node statuses"
    - path: .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/PERS-03-GAP.md
      provides: "Explicit remaining debt documentation"
  key_links:
    - from: crates/rlm-core/src/graph/executor.rs
      to: crates/rlm-core/src/domain/run_state_persistence.rs
      via: "persist_resume_cursor at node transitions"
      pattern: "persist_resume_cursor"
    - from: run_state_persistence.rs
      to: FileRunStateStore
      via: "mutate path resumeCursor with capability token"
      pattern: "resumeCursor"
---

<objective>
Extend Rust run-state persistence beyond nodeStatuses to write resumeCursor at graph executor checkpoints, with explicit gap documentation.

Purpose: Closes PERS-03 partial requirement per D-01 Wave 4 — extend forward; document shared TS/Rust gaps (TS also does not write cursor today per RESEARCH).
Output: persist_resume_cursor, executor hooks, integration test, PERS-03-GAP.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-CONTEXT.md
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-RESEARCH.md
@crates/rlm-core/src/domain/run_state_persistence.rs
@crates/rlm-core/src/graph/executor.rs
@src/domain/run-state-persistence.ts

<interfaces>
Existing Rust persist_node_status (mirror for cursor):
```rust
pub fn persist_node_status(&self, node_id: &str, status: &str) -> std::io::Result<()> {
    // CAS retry via store.mutate path nodeStatuses.{nodeId}
}
```

Graph executor already calls persist_node_status:
```rust
// crates/rlm-core/src/graph/executor.rs
let _ = run_state.persist_node_status(node_id, &execution_status_label(status));
```

Proposed ResumeCursor shape (RESEARCH discretion — not in TS today):
```rust
pub struct ResumeCursor {
    pub active_node_id: String,
    pub completed_node_ids: Vec<String>,
    pub variant: String, // "playbook" | "pipeline"
}
```

TS reference: src/domain/run-state-persistence.ts — verify only nodeStatuses written; do NOT claim TS cursor parity.

Store schema already accepts resumeCursor and checkpoints JSON paths via FileRunStateStore.mutate.
Scope limit per RESEARCH Pitfall 5: no cross-session reload/resume consumer — persist for future use only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResumeCursor type and persist_resume_cursor</name>
  <files>crates/rlm-core/src/domain/run_state_types.rs, crates/rlm-core/src/domain/run_state_persistence.rs</files>
  <behavior>
    - Test: persist_resume_cursor writes resumeCursor path via store.mutate
    - Test: CAS retry on version conflict (mirror node status test pattern)
    - Test: mutation rejected without capability token
  </behavior>
  <action>
    Per D-01 Wave 4 / PERS-03: add ResumeCursor struct (active_node_id, completed_node_ids, variant) in run_state_types.rs or inline in persistence module.
    Implement persist_resume_cursor(&self, cursor: &ResumeCursor) using same CAS retry loop as persist_node_status — path "resumeCursor", action "set", capability_token required.
    Optionally add persist_checkpoint for checkpoints array append if store schema supports it — only if GraphExecutor has natural checkpoint points; otherwise cursor-only is sufficient for this phase.
    Extend existing run_state_persistence unit tests in same file or tests/ module.
    Do not implement cross-process resume loader — persistence write only.
  </action>
  <verify>
    <automated>cargo test -p rlm-core run_state_persistence</automated>
  </verify>
  <done>persist_resume_cursor passes unit tests with CAS and auth behavior</done>
</task>

<task type="auto">
  <name>Task 2: GraphExecutor checkpoint hooks</name>
  <files>crates/rlm-core/src/graph/executor.rs</files>
  <action>
    Per PERS-03: at graph executor node transition points (after persist_node_status calls), also call persist_resume_cursor with current active node, accumulated completed node ids, and run variant from execution context.
    Identify natural hook points in executor.rs walk loop — after node starts, after node completes, before advancing to next node. Minimum: write cursor when active node changes.
    Preserve existing persist_node_status behavior — additive only.
    Log errors at warn level but do not fail graph execution on persistence errors (match node status best-effort pattern if applicable).
    Add integration test in crates/rlm-core/tests/ or graph executor tests: run minimal graph with FileRunStateStore temp dir, assert .planning/runs/{id}.json contains resumeCursor and nodeStatuses after execution step.
  </action>
  <verify>
    <automated>cargo test -p rlm-core run_state && cargo test -p rlm-core graph</automated>
  </verify>
  <done>Executor writes resumeCursor alongside nodeStatuses during graph walk</done>
</task>

<task type="auto">
  <name>Task 3: PERS-03 gap documentation</name>
  <files>.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/PERS-03-GAP.md</files>
  <action>
    Per RESEARCH Pitfall 5 and Claude's discretion: write PERS-03-GAP.md documenting:
    1. What Phase 1 delivers: resumeCursor + nodeStatuses written by Rust GraphExecutor
    2. Shared gap: TS RunStatePersistence also only writes nodeStatuses today (cite src/domain/run-state-persistence.ts)
    3. Not delivered: cross-session resume/reload consumer, full checkpoint replay, process restart continuity
    4. Store schema fields (checkpoints, resumeCursor) that exist but lack readers
    5. Recommended follow-on if full resume needed
    Keep doc factual — no placeholder language. This satisfies "document remaining gaps if full parity exceeds phase" from CONTEXT.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/PERS-03-GAP.md && grep -v '^#' .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/PERS-03-GAP.md | grep -c 'nodeStatuses\|resumeCursor' | grep -qv '^0$'</automated>
  </verify>
  <done>PERS-03-GAP.md exists with explicit shared limitations and Phase 1 deliverables</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph executor → FileRunStateStore | Run state mutations require capability token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Spoofing | Run state mutation | mitigate | Existing FileRunStateStore.authorize capability token check |
| T-01-11 | Tampering | resumeCursor JSON | mitigate | Typed ResumeCursor struct; serde serialize only known fields |
</threat_model>

<verification>
- cargo test -p rlm-core run_state_persistence && run_state integration passes
- PERS-03-GAP.md documents gaps explicitly
- npm run check:rust passes
</verification>

<success_criteria>
- PERS-03: Rust writes resumeCursor at executor checkpoints
- Shared TS/Rust resume limitations documented
- No false claim of full cross-session resume parity
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-05-SUMMARY.md`
</output>

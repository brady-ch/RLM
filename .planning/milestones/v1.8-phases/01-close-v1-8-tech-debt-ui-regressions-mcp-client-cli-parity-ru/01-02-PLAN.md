---
phase: 01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md
autonomous: false
requirements: [REG-01]
must_haves:
  truths:
    - "Operator completes REG-01 checklist on Rust-served UI without blocking failures"
    - "Session save and reopen works on Rust path"
    - "Pause future auto-approvals verified during recursive approval run"
    - "HF model download flow verified from Models tab"
    - "Verification doc records pass/fail per checklist item with operator sign-off"
  artifacts:
    - path: .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md
      provides: "REG-01 human UAT evidence for Phase 1"
  key_links:
    - from: 01-02-VERIFICATION.md
      to: .planning/milestones/v1.8-phases/61-ui-shell-rewrite/61-06-VERIFICATION.md
      via: "extends baseline checklist with Phase 1 regression items"
      pattern: "REG-01"
---

<objective>
Execute REG-01 human operator sign-off on Rust-served UI after Phase 1 UI regression fixes, documenting pass/fail evidence.

Purpose: Closes REG-01 partial requirement from v1.8 milestone audit per D-01 Wave 1 priority.
Output: 01-02-VERIFICATION.md with checklist results and operator approval signal.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-CONTEXT.md
@.planning/milestones/v1.8-phases/61-ui-shell-rewrite/61-06-VERIFICATION.md
@.planning/milestones/v1.8-phases/61-ui-shell-rewrite/61-UI-REVIEW.md

<interfaces>
Baseline REG-01 checklist (61-06-VERIFICATION.md):
1. Start Rust control server + built UI static assets
2. Workflow view: top bar + canvas only (no sidebar models/plugins/sessions/memory)
3. Inline prompt edit on card; right-click → Plan children
4. Select node → Run panel (~360px); canvas click → panel hidden
5. Advanced → each tab loads; ← Back to workflow preserves graph
6. Sessions tab: save and reopen session
7. Run workflow / Stop respond without console errors

Phase 1 extensions (per CONTEXT specifics):
8. TopBar: Pause future auto-approvals visible and functional in initial-plan-recursive run
9. Models tab: HF search → Install calls download endpoint successfully (or documents env limitation)
10. Graph: Add child / Connect parent / Delete subtree via in-app modals (no browser dialogs)
11. TopBar: Advanced renders as secondary style; Run workflow uses primary button styling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated preflight and verification doc scaffold</name>
  <files>.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md</files>
  <action>
    Run automated preflight before human UAT: npm run build:ui, npm run lint -- ui/src, npm run check:rust (smoke — no live Ollama required).
    Create 01-02-VERIFICATION.md with frontmatter (status: human_needed, phase, plan, automated: passed/pending, updated timestamp).
    Include table for all 11 checklist items (7 baseline + 4 Phase 1 extensions) with columns: Item, Steps, Result (PASS/FAIL/SKIP), Notes.
    Pre-fill automated checks section with command results.
    Document start command for operator: build UI then serve against Rust control server (cargo run -p rlm-cli or project dev script).
    If any automated preflight fails, fix blockers in 01-01 artifacts before proceeding to checkpoint — do not mark automated passed with failing build.
  </action>
  <verify>
    <automated>npm run build:ui && npm run lint -- ui/src && test -f .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md</automated>
  </verify>
  <done>Verification doc exists; automated preflight recorded; checklist ready for operator</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: REG-01 operator sign-off on Rust-served UI</name>
  <files>.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md</files>
  <what-built>Phase 1 UI regression fixes from plan 01-01 on Rust-served workflow shell</what-built>
  <how-to-verify>
    1. npm run build:ui
    2. Start Rust control server with static UI (project dev workflow or cargo run -p rlm-cli serve)
    3. Open workflow view — confirm canvas + top bar only (no legacy sidebar)
    4. Edit root prompt; right-click node → Plan children — graph updates
    5. Select node → Run panel appears; click canvas → panel hides
    6. Advanced → visit Models, Plugins, Sessions, Memory, Settings; ← Back preserves graph
    7. Sessions tab: save session, reopen — graph restores
    8. If approval mode initial-plan-recursive with active run: click Pause future auto-approvals — snapshot.autoApprovalPaused becomes true
    9. Models tab: search HF model, click Install — network shows POST /api/model-library/download (or SKIP with note if no network/Ollama)
    10. Right-click → Add child / Connect parent / Delete subtree — in-app modals appear, no browser prompt/confirm
    11. Confirm Advanced button is neutral secondary style; Run workflow is primary styled
    12. Run workflow / Stop — no console errors (SKIP full run if no model host configured)
  </how-to-verify>
  <action>
    Human operator executes checklist. After approval, executor updates 01-02-VERIFICATION.md: set status to operator_signed (or human_needed with gap list if failures), fill Result/Notes per item, record approval timestamp.
    File blocking gaps as follow-on items — do not silently pass failed items.
  </action>
  <verify>
    <automated>grep -v '^#' .planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-VERIFICATION.md | grep -c 'operator_signed\|approved' | grep -qv '^0$' || echo "Awaiting human approval"</automated>
  </verify>
  <done>Operator types approved; VERIFICATION.md records REG-01 sign-off or documented gaps</done>
  <resume-signal>Type "approved" or describe issues for gap closure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Local Rust server | Manual UAT on localhost; no production exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | UAT notes in VERIFICATION.md | accept | No secrets in checklist notes; redact tokens if pasted |
</threat_model>

<verification>
- Automated preflight green before checkpoint
- Human checkpoint completed with approved or explicit gap list
</verification>

<success_criteria>
- REG-01 requirement satisfied with operator-signed verification doc
- All baseline Phase 61 workflows plus Phase 1 regression items verified or explicitly skipped with reason
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-02-SUMMARY.md`
</output>

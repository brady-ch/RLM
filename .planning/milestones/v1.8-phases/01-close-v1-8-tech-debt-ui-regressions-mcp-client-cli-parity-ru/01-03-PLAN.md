---
phase: 01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - crates/rlm-core/src/interop/mod.rs
  - crates/rlm-core/src/interop/mcp_stdio_client.rs
  - crates/rlm-core/src/interop/mcp_tools.rs
  - crates/rlm-core/src/lib.rs
  - crates/rlm-core/src/plugins/runtime.rs
  - crates/rlm-core/src/plugins/registry.rs
  - crates/rlm-core/tests/mcp_stdio.rs
  - crates/rlm-core/tests/mcp_doctor_warning.rs
autonomous: true
requirements: [PLUG-03]
must_haves:
  truths:
    - "Configured MCP servers spawn as stdio subprocesses on default Rust runtime path"
    - "MCP tools register on ExtensionHost as {serverId}.{toolName}"
    - "Required MCP server failure fails runtime init; optional servers log and continue"
    - "Doctor or plugins status surfaces warning when MCP configured but connection failed"
    - "MCP child processes cleaned up on runtime shutdown"
  artifacts:
    - path: crates/rlm-core/src/interop/mcp_stdio_client.rs
      provides: "Content-Length framed JSON-RPC 2.0 stdio client"
    - path: crates/rlm-core/src/interop/mcp_tools.rs
      provides: "create_mcp_tools registration on ExtensionHost"
    - path: crates/rlm-core/tests/mcp_stdio.rs
      provides: "Integration test with mock MCP server"
  key_links:
    - from: crates/rlm-core/src/plugins/runtime.rs
      to: crates/rlm-core/src/interop/mcp_tools.rs
      via: "build_runtime_context interop stage replaces stub"
      pattern: "create_mcp_tools|load_mcp"
    - from: crates/rlm-core/src/interop/mcp_tools.rs
      to: crates/rlm-core/src/plugins/extension_host.rs
      via: "register_tool with prefixed names"
      pattern: "register_tool"
---

<objective>
Port TS StdioMcpClient to Rust and wire MCP tool registration into build_runtime_context, replacing the interop no-op stub.

Purpose: Closes PLUG-03 partial requirement per D-01 MCP architecture — RLM remains MCP client via subprocess stdio, complementary to native plugins.
Output: Working MCP stdio client, registered tools, doctor warning, integration tests mirroring TS behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-CONTEXT.md
@.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-RESEARCH.md
@src/runtime/interop/interop-runtime.ts
@tests/runtime/interop/mcp-skill-interoperability.test.ts

<interfaces>
From src/runtime/interop/interop-runtime.ts (behavioral spec):
```typescript
// initialize sequence
await client.request("initialize", {
  protocolVersion: "2024-11-05",
  capabilities: {},
  clientInfo: { name: "rlm", version: "1.0.0" },
});
client.notify("notifications/initialized", {});
// Tool name: `${server.id}.${tool.name}`
// Framing: Content-Length: N\r\n\r\n{json}
// spawn without shell — command + args array only
```

From crates/rlm-core/src/plugins/runtime.rs (stub to replace):
```rust
pub fn build_runtime_context(input: BuildRuntimeContextInput<'_>) -> RuntimeContext {
    load_builtins(&mut extension_host, input.project_root);
    record("plugins", &mut stages);
    // MCP/skill interop stub — replace with real client
    record("interop", &mut stages);
    let tools = extension_host.all_tools();
    // ...
}
```

Config path: interop.mcp.servers in project YAML (merged via persistence/config.rs merge_interop).
Skill tool (createSkillTool) is OUT OF SCOPE per RESEARCH assumption A3 — MCP servers only.
Do NOT use rmcp SDK — hand-port Content-Length framing per locked CONTEXT decision.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StdioMcpClient port and mock server test</name>
  <files>crates/rlm-core/src/interop/mod.rs, crates/rlm-core/src/interop/mcp_stdio_client.rs, crates/rlm-core/src/lib.rs, crates/rlm-core/tests/mcp_stdio.rs</files>
  <behavior>
    - Test: mock stdio server responds to initialize + notifications/initialized + tools/list with one tool
    - Test: client writes Content-Length framed JSON-RPC requests (not newline-only)
    - Test: client.call_tool invokes tools/call and returns parsed result
    - Test: child process killed on client drop/shutdown
  </behavior>
  <action>
    Per D-01 MCP architecture: create crates/rlm-core/src/interop/ module. Port StdioMcpClient from interop-runtime.ts using tokio::process::Command (no shell), AsyncReadExt/AsyncWriteExt for Content-Length framing, pending request map keyed by JSON-RPC id, buffer parser with newline fallback matching TS.
    Implement initialize (protocolVersion "2024-11-05"), notifications/initialized, tools/list, tools/call.
    Export module from lib.rs.
    Write mcp_stdio.rs integration test spawning a mock MCP server script/process (mirror TS test pattern in tests/runtime/interop/mcp-skill-interoperability.test.ts). Use tempfile fixtures if needed.
    Do not conflate with plugin registry — interop is separate from builtin plugins.
  </action>
  <verify>
    <automated>cargo test -p rlm-core mcp_stdio -- --nocapture</automated>
  </verify>
  <done>StdioMcpClient passes integration tests; Content-Length framing verified</done>
</task>

<task type="auto">
  <name>Task 2: MCP tool wrappers and runtime wiring</name>
  <files>crates/rlm-core/src/interop/mcp_tools.rs, crates/rlm-core/src/plugins/runtime.rs</files>
  <action>
    Per D-01: implement create_mcp_tools(servers, extension_host) in mcp_tools.rs. For each configured server: spawn StdioMcpClient, initialize, list tools, register each as Arc<dyn Tool> with name format!("{}.{}", server.id, tool.name) on ExtensionHost.
    Tool execute delegates to client.call_tool(tool.name, args) returning ToolExecutionResult.
    In build_runtime_context: read interop.mcp.servers from project_config Value; call create_mcp_tools between plugins and tools-resolver stages. Append MCP tools to runtime.tools vec. Store Child handles for lifecycle — kill on drop or explicit shutdown hook.
    Required server failure policy: if server.required == true and init fails, propagate error (fail init). Optional servers: record disconnect, continue init.
    Remove or update stale comment "full client deferred to post-v1.8".
    Read server config shape from TS McpServerConfig / config YAML examples in repo.
  </action>
  <verify>
    <automated>cargo test -p rlm-core --lib && cargo test -p rlm-core mcp_stdio</automated>
  </verify>
  <done>build_runtime_context registers MCP tools; required/optional failure policy matches TS</done>
</task>

<task type="auto">
  <name>Task 3: Doctor/interop disconnect warning</name>
  <files>crates/rlm-core/src/interop/mcp_tools.rs, crates/rlm-core/src/plugins/runtime.rs</files>
  <action>
    Per RESEARCH and CONTEXT specifics: when interop.mcp.servers is non-empty but one or more optional servers fail to connect, surface a doctor-visible warning string (e.g. "MCP configured but server '{id}' not connected: {reason}").
    Wire warning into existing plugin doctor or registry status mechanism consumed by UI plugins panel — follow pattern from PluginRegistryService doctor issues if present; otherwise add interop_warnings vec on RuntimeContext or extension host metadata retrievable by GET /api/plugins/doctor or equivalent existing endpoint.
    Do not build new UI in this plan — backend warning payload only.
    Verify warning appears when mock optional server fails in test.
  </action>
  <verify>
    <automated>cargo test -p rlm-core mcp_doctor_warning -- --nocapture</automated>
  </verify>
  <done>Doctor/status API returns MCP disconnect warning when configured servers fail optional connect</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config YAML → MCP subprocess spawn | Untrusted command paths from project config |
| MCP server JSON-RPC → Tool execute | External process returns tool output to agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Elevation | MCP spawn command | mitigate | Use Command with args array; no shell interpolation (match TS spawn) |
| T-01-06 | Tampering | MCP tool args JSON | mitigate | Pass args as serde_json::Value; validate schema at tool boundary |
| T-01-07 | Denial of Service | Zombie MCP children | mitigate | Store Child handles; kill on drop/shutdown; rejectAll on exit |
</threat_model>

<verification>
- cargo test -p rlm-core mcp_stdio passes
- build_runtime_context no longer no-op at interop stage
- npm run check:rust passes
</verification>

<success_criteria>
- PLUG-03: MCP tools available on default Rust runtime when configured
- Tool naming {serverId}.{toolName} matches TS for agent allowlists
- Doctor warning when MCP configured but disconnected
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-v1-8-tech-debt-ui-regressions-mcp-client-cli-parity-ru/01-03-SUMMARY.md`
</output>

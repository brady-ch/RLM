---
phase: 53
plan: 01
wave: 1
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, REG-03]
---

# Plan 53-01: Rust Persistence Ports

## Tasks

1. Rust file stores mirroring TS adapters (session, memory, run-state, preferences via memory scopes)
2. YAML config loader with layered merge and reference validation
3. Wire `/api/saved-sessions` and `/api/memory` read paths in Axum control server
4. Dual-read tests against Node-written `.rlm/` fixtures
5. `npm run check` and `npm run check:rust` green

## Verification

- `cargo test --workspace` — dual-read + control-server wiring tests pass
- `npm run check` — 471 TS tests pass

---
phase: 60-tauri-in-process-packaging
plan: 01
wave: 1
requirements: [PACK-01, PACK-02, PACK-03, REG-01]
autonomous: true
---

# Phase 60 Plan 01: Tauri In-Process + Packaging

## Goal

Embed Rust control server in Tauri without Node child; stage Rust-only release bundle; document remaining packaging limitations.

## Tasks

### 1. Tauri in-process server (type=auto)
- Replace Node child spawn with `rlm_core::start_server`
- Rust Ollama readiness check; graceful shutdown on window close

### 2. Rust-only packaging scripts (type=auto)
- Update `build-release.mjs` and `smoke-release.mjs` for rust-binary layout
- Remove bundled Node from release staging

### 3. Verification + limitations doc (type=auto)
- `npm run check:rust` green
- VERIFICATION notes human_needed items for full deb smoke

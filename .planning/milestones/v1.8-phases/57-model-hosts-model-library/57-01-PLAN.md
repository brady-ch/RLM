---
phase: 57-model-hosts-model-library
plan: 01
wave: 1
requirements: [MDLH-01, MDLH-02, MDLH-03]
---

# Phase 57 Plan 01: Model Hosts + Model Library

## Goal

Complete Rust Ollama adapter and model library routes with v1.7 parity; fix unconfigured fixture gate.

## Tasks

### 1. Fix model library wiring gate
- Wire `ModelLibraryService` only when explicit project config path exists AND `resolve_ollama_host` succeeds
- Matches strangler fixture: bare temp dir → 404 unconfigured

### 2. Ollama language model parity
- Verify streaming, tool-calling policy, health_check on `OllamaLanguageModel`
- Ensure plan/exec models resolve from config tiers when Ollama host present

### 3. Model library route parity
- Verify catalog, search, install, select-tier, download routes match TS shapes
- Add configured-state integration test with golden snapshot fields

### 4. HF registry validation
- Ensure empty repo_id rejected; download writes to `.rlm/models/registry/`

### 5. Verification
- `cargo test -p rlm-core` green including control_server_fixtures
- `npm run check:rust` and `npm run check` green
- Update REQUIREMENTS.md MDLH-01/02/03 complete

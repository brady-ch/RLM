---
phase: 61-ui-shell-rewrite
plan: 03
type: execute
wave: 3
depends_on: [61-02]
files_modified:
  - ui/src/canvas/GraphCanvas.tsx
  - ui/src/nodes/ExecutionNodeCard.tsx
  - ui/src/nodes/NodeContextMenu.tsx
  - ui/src/app/AppShell.tsx
autonomous: true
requirements: [UI-SC-02]
must_haves:
  truths:
    - "Graph canvas is primary workflow surface with React Flow Background, Controls, MiniMap"
    - "Prompt editable inline on node card in editable states"
    - "Plan/graph actions available via context menu — no persistent Plan button row on card"
    - "Protected replan gate (Replace/Merge/Cancel) remains inline on card"
  artifacts:
    - path: ui/src/canvas/GraphCanvas.tsx
      provides: "ReactFlow wrapper with layout/viewport sync"
      exports: ["GraphCanvas"]
    - path: ui/src/nodes/ExecutionNodeCard.tsx
      provides: "Inline prompt edit, status, ports, budget, quality-loop summary"
      exports: ["ExecutionNodeCard"]
    - path: ui/src/nodes/NodeContextMenu.tsx
      provides: "Right-click action menu per UI-SPEC contract"
      exports: ["NodeContextMenu"]
  key_links:
    - from: ui/src/nodes/NodeContextMenu.tsx
      to: /api/nodes/:id/plan
      via: "POST on Plan children"
      pattern: "/api/nodes/.*/plan"
    - from: ui/src/nodes/ExecutionNodeCard.tsx
      to: ui/src/nodes/NodeContextMenu.tsx
      via: "onContextMenu + ⋮ button"
      pattern: "NodeContextMenu"
---

<objective>
Extract GraphCanvas and refactor ExecutionNodeCard for Sketch 002-B: inline prompt editing, context-menu-driven actions, no footer Plan button.

Purpose: Delivers the node-centric authoring surface — the core UX shift from monolith sidebar.
Output: GraphCanvas mounted in AppShell workflow view; NodeContextMenu wired to existing /api/nodes/* routes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md
@.planning/sketches/002-context-menu-node-editing/README.md
@ui/src/main.tsx

<interfaces>
Context menu API mapping (61-UI-SPEC.md — use exact paths):
- Plan children: POST /api/nodes/:id/plan
- Break down: POST /api/nodes/:id/breakdown
- Extend budget: POST /api/nodes/:id/extend-budget
- Approve: POST /api/nodes/:id/approve
- Skip: POST /api/nodes/:id/skip
- Add child: POST /api/nodes/add (modal collects prompt)
- Connect parent: POST /api/nodes/:id/connect (modal collects parentId)
- Delete subtree: POST /api/nodes/:id/delete (confirm dialog)
- Expert overrides: navigate viewMode advanced + settings tab

Disabled rules: Approve/Skip only when node.status === 'awaiting_approval'; Plan items when editable states.
Card selected state: 2px accent outline #2d6cdf per UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract GraphCanvas with React Flow wiring</name>
  <files>ui/src/canvas/GraphCanvas.tsx, ui/src/app/AppShell.tsx</files>
  <read_first>ui/src/main.tsx (return JSX lines 769–795, onNodesChange/onConnect/onViewportMoveEnd handlers lines 717–767), ui/src/app/AppShell.tsx</read_first>
  <action>
    Create ui/src/canvas/GraphCanvas.tsx accepting props: nodes, edges, onNodesChange, onConnect, onNodeClick (sets selectedNodeId), onPaneClick (clears selectedNodeId per Run panel contract), onViewportMoveEnd, rfInstanceRef, nodeTypes.
    Move ReactFlow, Background gap={20} color="#d8ded9", MiniMap, Controls from main.tsx.
    Mount GraphCanvas in AppShell workflow-main region replacing placeholder.
    Move layout flush, viewport flush, draggingRef logic from App into GraphCanvas or keep in AppShell — preserve POST /api/graph/layout and /api/graph/viewport debounce behavior unchanged.
    Register nodeTypes with ExecutionNodeCard from nodes/ (Task 2).
  </action>
  <acceptance_criteria>
    - ui/src/canvas/GraphCanvas.tsx exports GraphCanvas
    - GraphCanvas renders ReactFlow component
    - onPaneClick clears selection (prop callback to AppShell setSelectedNodeId(undefined))
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Graph canvas renders in workflow view with existing drag/connect/viewport behavior</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor ExecutionNodeCard for Variant B</name>
  <files>ui/src/nodes/ExecutionNodeCard.tsx</files>
  <read_first>ui/src/main.tsx (ExecutionNodeCard lines 1019–1238), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Node Card Anatomy + Copywriting</read_first>
  <behavior>
    - Card shows textarea for prompt when status is planned, ready, or awaiting_approval
    - Card does NOT render node-card-footer Plan children button or btn-primary-plan
    - Card shows hint text "Right-click for actions" or ⋮ Actions affordance
    - Protected replan gate (Replace/Merge/Cancel buttons) still renders when planningError includes replan_requires_choice
    - Selected node gets CSS class applying accent border #2d6cdf
  </behavior>
  <action>
    Move ExecutionNodeCard to ui/src/nodes/ExecutionNodeCard.tsx importing types from shared/types and post from shared/api.
    Remove node-card-footer block with Plan children button (lines ~1189–1198 in main.tsx) — planning moves to context menu per locked 002-B decision.
    Remove node-models debug block at card bottom (model trail moves to Advanced Settings in Plan 05).
    Keep: header, inline prompt textarea, quality loop summary, ports, budget strip, pending plan banner, replan gate.
    Add subtle hint "Right-click for actions" and optional ⋮ button with aria-label="Actions" for keyboard users.
    Accept isSelected prop for accent outline styling.
    Prompt save before plan: context menu Plan children must POST /api/nodes/:id/edit with current prompt then /plan — same as removed planFromCard sequence.
  </action>
  <acceptance_criteria>
    - ui/src/nodes/ExecutionNodeCard.tsx exists
    - grep -v '^#' ui/src/nodes/ExecutionNodeCard.tsx | grep -c 'btn-primary-plan' equals 0
    - grep -v '^#' ui/src/nodes/ExecutionNodeCard.tsx | grep -c 'Right-click for actions' equals 1
    - npm run build:ui exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:ui</automated>
  </verify>
  <done>Node card matches 002-B: inline edit, no persistent action buttons, replan gate on card</done>
</task>

<task type="auto">
  <name>Task 3: NodeContextMenu with API wiring and a11y</name>
  <files>ui/src/nodes/NodeContextMenu.tsx, ui/src/nodes/ExecutionNodeCard.tsx</files>
  <read_first>.planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Context Menu Contract table, ui/src/main.tsx (NodeInspector add-child/connect/delete patterns lines 2526+)</read_first>
  <action>
    Create ui/src/nodes/NodeContextMenu.tsx as a positioned menu component (portal or absolute) with sections Plan, Run, Graph, Advanced per UI-SPEC.
    Wire each item to existing POST endpoints via shared/api runAction pattern; disable items based on node.status same as UI-SPEC (Approve/Skip only awaiting_approval).
    Graph Add child and Connect parent: reuse modal/prompt patterns from NodeInspector (window.prompt acceptable if matching current monolith UX — prefer minimal inline modal component).
    Delete subtree: confirm() before POST /api/nodes/:id/delete with deleteStrategy if needed.
    Advanced → Expert overrides: call onNavigateAdvancedSettings callback prop (AppShell sets viewMode advanced + settings tab).
    Integrate on ExecutionNodeCard: onContextMenu preventDefault + open menu; ⋮ button opens same menu; support Shift+F10 and context menu key on focused card (tabIndex=0 on card root).
    Close menu on Escape, outside click, and after action completes.
  </action>
  <acceptance_criteria>
    - NodeContextMenu lists Plan children, Approve, Delete subtree items
    - Menu item Approve disabled when node.status !== 'awaiting_approval'
    - ExecutionNodeCard wires onContextMenu handler
    - npm run lint -- ui/src/nodes exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run lint -- ui/src/nodes ui/src/canvas</automated>
  </verify>
  <done>Context menu fully wired; keyboard and ⋮ affordance reachable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Context menu → /api/nodes/* | User-triggered mutations; same as monolith |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | S | Delete subtree | mitigate | confirm() dialog before POST delete |
| T-61-03-02 | E | Context menu actions | accept | Disabled state mirrors server node.status |
</threat_model>

<verification>
npm run build:ui && npm run lint -- ui/src/nodes ui/src/canvas
</verification>

<success_criteria>
- Workflow canvas primary surface with refactored node cards
- Plan via context menu succeeds (edit + plan sequence preserved)
- No persistent Plan button on card (UI-SC-02)
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-shell-rewrite/61-03-SUMMARY.md`
</output>

---
phase: 61-ui-shell-rewrite
plan: 05
type: execute
wave: 4
depends_on: [61-02]
files_modified:
  - ui/src/advanced/AdvancedHub.tsx
  - ui/src/advanced/ModelsView.tsx
  - ui/src/advanced/PluginsView.tsx
  - ui/src/advanced/SessionsView.tsx
  - ui/src/advanced/MemoryView.tsx
  - ui/src/advanced/SettingsView.tsx
  - ui/src/app/AppShell.tsx
autonomous: true
requirements: [UI-SC-01, UI-SC-04]
must_haves:
  truths:
    - "Advanced hub is full-screen with sub-tabs Models · Plugins · Sessions · Memory · Settings"
    - "Back to workflow restores graph without losing session state"
    - "Domain data fetches only when respective sub-tab mounts"
    - "Workflow view shows no models/plugins/sessions/memory panels"
  artifacts:
    - path: ui/src/advanced/AdvancedHub.tsx
      provides: "Full-screen takeover + sub-tab routing"
      exports: ["AdvancedHub"]
    - path: ui/src/advanced/SettingsView.tsx
      provides: "Expert overrides, sampling, workflows, quality-loop inspector"
      exports: ["SettingsView"]
  key_links:
    - from: ui/src/app/AppShell.tsx
      to: ui/src/advanced/AdvancedHub.tsx
      via: "viewMode === advanced"
      pattern: "AdvancedHub"
    - from: ui/src/advanced/ModelsView.tsx
      to: /api/model-library
      via: "fetch on mount"
      pattern: "/api/model-library"
---

<objective>
Relocate all monolith sidebar domain panels into the full-screen Advanced hub (Sketch 001-C).

Purpose: Removes models/plugins/sessions/memory from workflow view (UI-SC-01) while preserving all existing panel functionality.
Output: AdvancedHub with five sub-views; lazy fetch per tab; Settings absorbs NodeInspector edit surfaces.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md
@.planning/notes/ui-shell-architecture.md
@ui/src/main.tsx

<interfaces>
Panel functions to relocate from main.tsx (move, do not rewrite logic):
- ModelLibraryPanel + ModelLibraryRow → ModelsView.tsx (~1813)
- PluginPanel + PluginRow → PluginsView.tsx (~1994)
- SavedSessionPanel → SessionsView.tsx (~1547)
- MemoryPanel → MemoryView.tsx (~1685)
- GraphWorkflowPanel + RefineGraphPanel + NodeInspector expert/sampling/quality-loop → SettingsView.tsx (~1241, ~1345, ~2331, ~2780)

AdvancedHub props:
- onBack: () => setViewMode('workflow')
- initialTab?: 'models' | 'plugins' | 'sessions' | 'memory' | 'settings' (for context menu Expert overrides navigation)
- selectedNodeId, snapshot, refresh, setErrorMessage for SettingsView

Copy: Back button label "← Back to workflow"; tab labels exactly "Models", "Plugins", "Sessions", "Memory", "Settings".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdvancedHub shell and domain view extraction (Models, Plugins, Sessions, Memory)</name>
  <files>ui/src/advanced/AdvancedHub.tsx, ui/src/advanced/ModelsView.tsx, ui/src/advanced/PluginsView.tsx, ui/src/advanced/SessionsView.tsx, ui/src/advanced/MemoryView.tsx, ui/src/app/AppShell.tsx</files>
  <read_first>ui/src/main.tsx (ModelLibraryPanel, PluginPanel, SavedSessionPanel, MemoryPanel functions), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Advanced Hub Contract</read_first>
  <action>
    Create ui/src/advanced/AdvancedHub.tsx: full-screen layout with header "← Back to workflow" button calling onBack; horizontal sub-tab buttons; render active view only.
    Extract ModelsView from ModelLibraryPanel — fetch /api/model-library on mount via useEffect (not on AppShell mount).
    Extract PluginsView from PluginPanel — fetch /api/plugins and /api/plugins/doctor on mount.
    Extract SessionsView from SavedSessionPanel — fetch /api/saved-sessions on mount.
    Extract MemoryView from MemoryPanel — fetch /api/memory on mount.
    Each view owns its local search/state (modelSearch, savedSessionDetail, etc.) — move state from AppShell into views.
    Wire AppShell: when viewMode==='advanced', render AdvancedHub; TopBar hidden on advanced view.
    Support initialTab prop so NodeContextMenu Expert overrides opens Settings tab (set in Plan 03 callback).
  </action>
  <acceptance_criteria>
    - AdvancedHub.tsx exports AdvancedHub with onBack prop
    - ModelsView fetches /api/model-library inside useEffect on mount
    - grep -v '^#' ui/src/app/AppShell.tsx | grep -c 'ModelLibraryPanel' equals 0
    - Back button text contains "Back to workflow"
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Four domain tabs relocated with lazy fetch; Advanced reachable from TopBar</done>
</task>

<task type="auto">
  <name>Task 2: SettingsView with inspector and workflow panels</name>
  <files>ui/src/advanced/SettingsView.tsx</files>
  <read_first>ui/src/main.tsx (NodeInspector, GraphWorkflowPanel, RefineGraphPanel, QualityLoopInspector), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Settings bullet list</read_first>
  <action>
    Create ui/src/advanced/SettingsView.tsx combining:
    - GraphWorkflowPanel (workflows, run variant, pipeline input)
    - RefineGraphPanel (chat refine secondary path)
    - NodeInspector content for expert overrides, sampling, model override, quality-loop inspector — keyed to selectedNodeId from AppShell (show picker or "Select a node on canvas" empty state when none selected)
    Move NodeInspector, QualityLoopInspector, PortRows, PolicyRows helper components into SettingsView.tsx or ui/src/advanced/settings/ subfolder.
    Settings tab does NOT include Approve/Skip or clarification (those stay in RunPanel per boundary rules).
    Preserve all existing POST endpoints from NodeInspector (edit, expert binding updates, etc.).
  </action>
  <acceptance_criteria>
    - SettingsView.tsx exists and exports SettingsView
    - SettingsView contains GraphWorkflowPanel logic (grep workflow or graph-workflows)
    - SettingsView does NOT contain POST /approve handler
    - npm run build:ui exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:ui</automated>
  </verify>
  <done>Settings tab holds power-user surfaces removed from workflow view</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Advanced views → /api/* | Same endpoints as monolith panels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | I | Session reopen | accept | Same SavedSessionPanel behavior |
| T-61-05-02 | T | Plugin install | accept | No new plugin APIs; relocate UI only |
</threat_model>

<verification>
npm run build:ui && npm run lint -- ui/src/advanced
</verification>

<success_criteria>
- Advanced hub full-screen with all five tabs (UI-SC-04)
- Workflow view free of domain panels (UI-SC-01)
- Back restores workflow view with graph state intact (client routing only; SSE unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-shell-rewrite/61-05-SUMMARY.md`
</output>

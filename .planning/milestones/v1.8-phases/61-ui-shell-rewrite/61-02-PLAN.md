---
phase: 61-ui-shell-rewrite
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - ui/src/app/AppShell.tsx
  - ui/src/app/TopBar.tsx
  - ui/src/main.tsx
autonomous: true
requirements: [UI-SC-01, UI-SC-04]
must_haves:
  truths:
    - "App renders AppShell instead of inline App with monolith sidebar layout"
    - "Top bar shows run status, Run workflow, Stop, and Advanced entry on workflow view"
    - "Session snapshot loads via /api/session + SSE; domain panels do NOT fetch on global mount"
  artifacts:
    - path: ui/src/app/AppShell.tsx
      provides: "workflow | advanced view routing and session state owner"
      exports: ["AppShell"]
    - path: ui/src/app/TopBar.tsx
      provides: "Thin top bar per UI-SPEC"
      exports: ["TopBar"]
    - path: ui/src/main.tsx
      provides: "Thin entry mounting AppShell"
      min_lines: 1
      contains: "createRoot"
  key_links:
    - from: ui/src/main.tsx
      to: ui/src/app/AppShell.tsx
      via: "render AppShell"
      pattern: "AppShell"
    - from: ui/src/app/AppShell.tsx
      to: /api/events
      via: "EventSource snapshot listener"
      pattern: "EventSource.*api/events"
---

<objective>
Introduce AppShell + TopBar with workflow/advanced routing and lift session/SSE state out of the monolith App function.

Purpose: Establishes the shell frame (Sketch 001-A) before canvas, run panel, and Advanced hub land in later plans.
Output: Thin main.tsx entry; AppShell owns snapshot, selection, error, and view mode state.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ui-shell-rewrite/61-CONTEXT.md
@.planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md
@.planning/sketches/001-workflow-advanced-shell/index.html
@ui/src/main.tsx

<interfaces>
From Plan 01 outputs:
- import { SessionSnapshot, ExecutionNode } from '../shared/types'
- import { post, runAction } from '../shared/api'

AppShell state contract for downstream plans:
- viewMode: 'workflow' | 'advanced'
- selectedNodeId: string | undefined
- snapshot: SessionSnapshot (from /api/session + SSE)
- setSelectedNodeId, setViewMode, refresh, setErrorMessage passed as props to children (stub regions OK this plan)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppShell with session state and view routing</name>
  <files>ui/src/app/AppShell.tsx</files>
  <read_first>ui/src/main.tsx (App function lines 515–649 for state + SSE), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Layout & Regions table</read_first>
  <action>
    Create ui/src/app/AppShell.tsx as the root layout component.
    Lift from main.tsx App(): snapshot state, refresh callback, EventSource on /api/events for snapshot events, errorMessage, selectedNodeId, planningNodeId/planningError, runVariant/activeRunVariant, clarificationAnswer state.
    Add viewMode state: 'workflow' | 'advanced' (default 'workflow').
    On mount: call refresh() only — remove refreshModelLibrary, refreshSavedSessions, refreshGraphWorkflows, refreshMemory, refreshPlugins from global useEffect (per UI-SPEC Advanced hub: domain fetches on sub-tab mount only; deferred to Plan 05).
    execution event listener may still call refresh() for session snapshot — do NOT re-add domain refreshes on execution events until Advanced views own them.
    Render layout: when viewMode==='workflow', show TopBar + main content area with placeholder div data-testid="workflow-main"; when 'advanced', show placeholder data-testid="advanced-main" (AdvancedHub wired in Plan 05).
    Preserve graph sync useEffects (nodes/edges from snapshot) — may stay in AppShell or move to GraphCanvas in Plan 03; if kept here, import graph helpers from shared/types.
  </action>
  <acceptance_criteria>
    - ui/src/app/AppShell.tsx exports function AppShell
    - grep -v '^#' ui/src/app/AppShell.tsx | grep -c 'refreshModelLibrary' equals 0
    - grep -v '^#' ui/src/app/AppShell.tsx | grep -c "EventSource" equals 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>AppShell owns session/SSE state; no domain panel fetches on mount</done>
</task>

<task type="auto">
  <name>Task 2: TopBar and thin main entry</name>
  <files>ui/src/app/TopBar.tsx, ui/src/main.tsx</files>
  <read_first>ui/src/main.tsx (aside header lines 798–876), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Copywriting table</read_first>
  <action>
    Create ui/src/app/TopBar.tsx per UI-SPEC: run status pill (uiRunStatusLabels), active node hint when snapshot.activeNodeId set, Run workflow button (POST /api/chat/confirm-run with runVariant), Stop button (POST /api/stop), Advanced button label exactly "Advanced" setting viewMode to 'advanced'.
    Wire TopBar only on workflow view inside AppShell.
    Replace main.tsx body: keep ReactDOM createRoot, import AppShell, render AppShell — target under 30 lines excluding imports.
    Keep legacy App function and sidebar in main.tsx temporarily (imported nowhere) until Plan 06 deletes it — OR move App to ui/src/_legacy/App.monolith.tsx if needed for typecheck; AppShell is the active tree.
    Advanced back navigation ("← Back to workflow") is Plan 05 — not this task.
  </action>
  <acceptance_criteria>
    - ui/src/main.tsx line count under 40
    - ui/src/app/TopBar.tsx contains aria-label="Run workflow"
    - ui/src/app/TopBar.tsx button text includes Advanced
    - npm run build:ui exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:ui</automated>
  </verify>
  <done>TopBar visible on workflow view; main.tsx is thin entry; UI builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TopBar → /api/chat/confirm-run, /api/stop | Same endpoints as monolith header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-01 | D | Stop / Run buttons | accept | Same POST bodies as existing sidebar header |
| T-61-02-02 | I | SSE snapshot | accept | Server-authoritative; no client mutation of graph via SSE |
</threat_model>

<verification>
npm run build:ui && npm run typecheck
</verification>

<success_criteria>
- AppShell + TopBar render; session loads
- Global domain fetches removed from mount (UI-SC-01 partial — workflow view has no models/plugins panels)
- main.tsx under 40 lines
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-shell-rewrite/61-02-SUMMARY.md`
</output>

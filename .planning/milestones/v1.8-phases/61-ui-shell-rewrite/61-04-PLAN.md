---
phase: 61-ui-shell-rewrite
plan: 04
type: execute
wave: 4
depends_on: [61-03]
files_modified:
  - ui/src/run-panel/RunPanel.tsx
  - ui/src/app/AppShell.tsx
  - ui/src/styles.css
autonomous: true
requirements: [UI-SC-03]
must_haves:
  truths:
    - "Run panel hidden when no node selected — no empty placeholder rail"
    - "Run panel shows Approve/Skip only when selected node is awaiting_approval"
    - "Clarification form appears when pendingClarification targets selected node"
    - "Run panel excludes prompt edit, model trail, expert forms, memory, plugins"
  artifacts:
    - path: ui/src/run-panel/RunPanel.tsx
      provides: "Slim ~360px approval/clarification rail"
      exports: ["RunPanel"]
  key_links:
    - from: ui/src/app/AppShell.tsx
      to: ui/src/run-panel/RunPanel.tsx
      via: "conditional render when selectedNodeId"
      pattern: "selectedNodeId.*RunPanel"
    - from: ui/src/run-panel/RunPanel.tsx
      to: /api/nodes/:id/approve
      via: "Approve button"
      pattern: "/approve"
---

<objective>
Build the slim Run panel (Sketch 002-B) — visible only on node select, approve/clarify only.

Purpose: Replaces run control + clarification blocks from monolith sidebar without resurrecting the inspector junk drawer.
Output: RunPanel mounted in AppShell workflow grid; responsive bottom sheet at max-width 800px.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md
@.planning/sketches/002-context-menu-node-editing/README.md
@ui/src/main.tsx

<interfaces>
Run panel content (explicitly excluded per UI-SPEC):
- INCLUDE: node label, id, status header; Approve/Skip; clarification textarea + Submit/Abort; global run-disabled readiness.reason
- EXCLUDE: prompt textarea, Plan/Save, model trail, expert/sampling editors, memory, plugins, sessions

Extract from main.tsx aside bottom (lines ~938–1000): run control, clarification timeline, pendingClarification handling with POST /api/clarifications/answer and abort paths — grep for clarification endpoints in main.tsx before implementing.

Clarification state: clarificationAnswer lives in AppShell (Plan 02); pass as controlled props.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunPanel component</name>
  <files>ui/src/run-panel/RunPanel.tsx</files>
  <read_first>ui/src/main.tsx (Run control + Clarification blocks ~938–1010), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Run Panel Contract</read_first>
  <action>
    Create ui/src/run-panel/RunPanel.tsx.
    Props: selectedNode (ExecutionNode | undefined), snapshot (for pendingClarification, readiness), clarificationAnswer, setClarificationAnswer, refresh, setErrorMessage, runDisabled, readiness.
    Return null when selectedNode is undefined — no placeholder rail (UI-SC-03 copy: "Hidden — no placeholder rail").
    Header: {selectedNode.label} · {selectedNode.id} · {selectedNode.status}.
    When selectedNode.status === 'awaiting_approval': render Approve (POST /api/nodes/:id/approve) and Skip (POST /api/nodes/:id/skip) buttons using runAction.
    When snapshot.chat.pendingClarification?.nodeId === selectedNode.id: show prompt text, answer textarea, Submit (existing clarification answer endpoint from monolith), Abort run button.
    When runDisabled globally: show readiness.reason in Readiness block.
    Do NOT import from advanced/ (boundary rule per architecture note).
    Do NOT include prompt textarea or any NodeInspector fields.
  </action>
  <acceptance_criteria>
    - RunPanel returns null when selectedNode prop is undefined
    - grep -v '^#' ui/src/run-panel/RunPanel.tsx | grep -c 'node-inspector' equals 0
    - grep -v '^#' ui/src/run-panel/RunPanel.tsx | grep -c '/approve' equals 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>RunPanel component implements approve/clarify/readiness only</done>
</task>

<task type="auto">
  <name>Task 2: Wire workflow grid layout and responsive sheet</name>
  <files>ui/src/app/AppShell.tsx, ui/src/styles.css</files>
  <read_first>ui/src/styles.css (.shell grid lines 64–83, media query ~1044), .planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md Layout responsive rule</read_first>
  <action>
    Update AppShell workflow layout to CSS grid: top bar full width; below — canvas column + optional 360px run panel column when selectedNodeId set (match UI-SPEC shell-grid / no-dock pattern from sketch 001).
    Mount RunPanel in right column; pass selectedNode derived from snapshot.graph.nodes.find(id === selectedNodeId).
    Replace old .shell grid-template-columns minmax(0,1fr) 380px inspector with workflow-shell class: 1fr when no selection, 1fr 360px when selection active.
    Add @media (max-width: 800px): run panel position fixed bottom 0 full width max-height 50vh overlay (bottom sheet); canvas remains full width.
    Remove run/clarification duplicate blocks from legacy sidebar if still rendered — AppShell must not render old aside.inspector content (defer full aside removal to Plan 06 if legacy App still exists).
  </action>
  <acceptance_criteria>
    - ui/src/styles.css contains workflow-shell class
    - ui/src/styles.css contains @media (max-width: 800px) rule for run panel
    - AppShell conditionally renders RunPanel when selectedNodeId is truthy
    - npm run build:ui exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:ui</automated>
  </verify>
  <done>Run panel mounts on select only; responsive bottom sheet at 800px</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPanel → approve/skip/clarification | Same POST contracts as monolith |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04-01 | E | Approve/Skip | mitigate | Buttons disabled unless awaiting_approval |
| T-61-04-02 | I | Clarification answer | accept | Same endpoint as monolith; no new validation layer |
</threat_model>

<verification>
npm run build:ui && npm run typecheck
</verification>

<success_criteria>
- Run panel visible on node select only (UI-SC-03)
- Approve/clarify flows relocated from sidebar
- No edit fields in run panel
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-shell-rewrite/61-04-SUMMARY.md`
</output>

---
phase: 61-ui-shell-rewrite
plan: 06
type: execute
wave: 5
depends_on: [61-03, 61-04, 61-05]
files_modified:
  - ui/src/main.tsx
  - ui/src/styles.css
  - ui/src/app/AppShell.tsx
autonomous: false
requirements: [UI-SC-01, UI-SC-02, UI-SC-03, UI-SC-04, UI-SC-05]
must_haves:
  truths:
    - "main.tsx is thin entry only (~50 lines or fewer)"
    - "No monolith aside.inspector sidebar remains in rendered tree"
    - "npm run check passes (typecheck, lint, build:ui)"
    - "REG-01 workflows pass on Rust-served UI"
  artifacts:
    - path: ui/src/main.tsx
      provides: "Entry only"
      max_lines: 50
    - path: ui/src/styles.css
      provides: "Workflow shell layout without 380px inspector default"
  key_links:
    - from: ui/src/main.tsx
      to: ui/src/app/AppShell.tsx
      via: "sole render path"
      pattern: "createRoot.*AppShell"
---

<objective>
Delete the monolith sidebar, finalize shell CSS, verify build, and human-sign-off REG-01 on Rust-served UI.

Purpose: Closes the phase — no duplicate code paths, no scrollable junk drawer.
Output: Clean component tree; check green; UAT evidence for UI-SC-05.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ui-shell-rewrite/61-UI-SPEC.md
@.planning/phases/61-ui-shell-rewrite/61-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
REG-01 definition (REQUIREMENTS.md): graph authoring, execution, session save/reopen, model library, plugin panel behave as before on Rust runtime.

Verification commands:
- npm run typecheck
- npm run lint -- ui/src
- npm run build:ui
- npm run check:rust (optional smoke if control server needed for manual UAT)

Manual UAT script (checkpoint):
1. npm run dev:ui with Rust control server (cargo run -p rlm-cli -- serve or project dev script)
2. Workflow view: confirm no sidebar panels; canvas + top bar only
3. Edit prompt on card; right-click Plan children
4. Select node → Run panel approve/clarify; deselect → panel hidden
5. Advanced → each tab loads; Back to workflow preserves graph
6. Session save/reopen from Sessions tab
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove monolith and finalize styles</name>
  <files>ui/src/main.tsx, ui/src/styles.css, ui/src/app/AppShell.tsx</files>
  <read_first>ui/src/main.tsx (entire file), ui/src/styles.css (.shell and .inspector rules)</read_first>
  <action>
    Delete legacy App function and all orphaned panel components still in main.tsx (ModelLibraryPanel, NodeInspector, etc.) — functionality must already live in advanced/ and nodes/ from Plans 03–05.
    Remove temporary re-exports from Plan 01 if still present.
    main.tsx final state: imports, createRoot, render AppShell, under 50 lines.
    Remove .inspector sidebar styles or gate behind deleted class; ensure .workflow-shell layout is default.
    Remove duplicate SSE/domain logic if any remains outside AppShell.
    Run eslint --fix on ui/src if needed for unused imports after deletion.
  </action>
  <acceptance_criteria>
    - wc -l ui/src/main.tsx outputs a number less than or equal to 50
    - grep -v '^#' ui/src/main.tsx | grep -c 'function App(' equals 0
    - grep -v '^#' ui/src/main.tsx | grep -c 'aside className="inspector"' equals 0
    - npm run check exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint -- ui/src && npm run build:ui</automated>
  </verify>
  <done>Monolith deleted; main.tsx entry-only; automated checks pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: REG-01 Rust UI smoke verification</name>
  <files>.planning/phases/61-ui-shell-rewrite/61-06-VERIFICATION.md</files>
  <action>
    Human verifies REG-01 workflows on Rust-served UI after automated checks pass. Executor creates 61-06-VERIFICATION.md with UAT checklist results after user approves. No code changes in this task unless user reports failures.
  </action>
  <what-built>Complete UI shell rewrite — workflow canvas, context menus, Run panel, Advanced hub</what-built>
  <how-to-verify>
    1. Start Rust control server and UI: `npm run build:ui` then serve UI against Rust backend (project dev workflow or `cargo run -p rlm-cli` with static UI)
    2. Open workflow view — confirm NO models/plugins/sessions/memory panels visible (only top bar + canvas)
    3. Edit root node prompt on card; right-click → Plan children — graph updates
    4. Select a node — Run panel appears (~360px); click canvas background — panel hides
    5. Click Advanced — full-screen hub; visit Models, Plugins, Sessions, Memory, Settings tabs
    6. Click "← Back to workflow" — same graph state restored
    7. Sessions tab: save and reopen a session — graph restores
    8. Run workflow end-to-end if API keys configured; otherwise confirm Run/Stop buttons respond without console errors
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/61-ui-shell-rewrite/61-06-VERIFICATION.md || echo "Awaiting human approval"</automated>
  </verify>
  <done>User types "approved" and VERIFICATION.md records REG-01 UAT pass</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full UI → Rust /api/* | Regression surface for REG-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-06-01 | D | Session reopen | mitigate | Human UAT step 7 verifies save/reopen |
| T-61-06-02 | T | Broken mutation wiring | mitigate | Human UAT steps 3–8 cover plan/run/advanced flows |
</threat_model>

<verification>
npm run check && npm run build:ui
Human checkpoint Task 2 for UI-SC-05 / REG-01
</verification>

<success_criteria>
- main.tsx ≤50 lines; no monolith sidebar in tree
- All five UI-SPEC success criteria met
- REG-01 human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-shell-rewrite/61-06-SUMMARY.md`
</output>

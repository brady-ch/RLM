---
phase: 54-recursive-engine-execution-controller
plan: 01
type: execute
requirements: [ENGN-01, ENGN-02]
---

# Phase 54 Plan 01: Recursive Engine + ExecutionController

## Objective

Port `RecursiveLanguageModel` orchestrator and `InteractiveExecutionSession` session authority to Rust with behavioral parity to TypeScript, wiring live session state into control-server routes.

## Tasks

1. Domain recursion helpers (budget_guard, prompt_utilities, execution_graph_sync, tool_round_loop)
2. RecursiveLanguageModel core solve path + quality-loop entry
3. InteractiveExecutionSession with approval/clarification/stop + SSE broadcast
4. Wire `/api/session`, `/api/run-mode`, `/api/events` to live session
5. Rust integration tests mirroring TS behavioral spec

## Verification

- `cargo test --workspace`
- `npm run check`
- `npm run check:rust`

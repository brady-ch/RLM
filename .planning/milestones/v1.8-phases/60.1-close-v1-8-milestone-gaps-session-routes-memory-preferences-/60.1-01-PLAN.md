---
phase: 60.1-close-v1-8-milestone-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/memory/session_memory_bridge.rs
  - crates/rlm-core/src/memory/mod.rs
  - crates/rlm-core/src/persistence/memory_store.rs
  - crates/rlm-core/tests/session_memory_bridge.rs
autonomous: true
gap_closure: true
requirements: [VIDX-02, PERS-01]
must_haves:
  truths:
    - "Rust can build a full SavedSessionPayload with memory, preferences, and vectorIndex sections from live runtime state"
    - "Rust can restore session memory scopes/episodic and merge vector records on reopen"
  artifacts:
    - path: crates/rlm-core/src/memory/session_memory_bridge.rs
      provides: build_saved_session_payload, restore_session_memory, restore_graph_workflow_metadata
      contains: build_saved_session_payload
    - path: crates/rlm-core/src/persistence/memory_store.rs
      provides: restore_session_data bulk import
      contains: restore_session_data
    - path: crates/rlm-core/tests/session_memory_bridge.rs
      provides: bridge unit tests
  key_links:
    - from: crates/rlm-core/src/memory/session_memory_bridge.rs
      to: crates/rlm-core/src/persistence/memory_store.rs
      via: restore_session_data on reopen
      pattern: restore_session_data
    - from: crates/rlm-core/src/memory/session_memory_bridge.rs
      to: crates/rlm-core/src/persistence/file_vector_index.rs
      via: export_session_vector_records / merge_session_vector_records
      pattern: merge_session_vector_records
---

<objective>
Complete the Rust session-memory bridge and FileMemoryStore restore API so save/reopen route handlers can mirror TS `session-memory-bridge.ts` semantics.

Purpose: Closes the orphaned-bridge gap (VIDX-02) and unblocks session route wiring (PERS-01, REG-01).
Output: Full payload build/restore helpers with unit tests; no route changes in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-CONTEXT.md
@.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md
@src/application/memory/session-memory-bridge.ts
@src/application/control-server/handlers/session.ts
@crates/rlm-core/src/memory/session_memory_bridge.rs
@crates/rlm-core/src/persistence/memory_store.rs
@crates/rlm-core/src/persistence/session_store.rs

<interfaces>
From crates/rlm-core/src/persistence/session_store.rs:
```rust
pub struct SavedSessionPayload {
    pub session: Value,
    pub run_state: Option<Value>,
    pub artifacts: Value,
    pub memory: Value,
    pub preferences: Value,
    pub vector_index: Value,
    pub graph_workflow_metadata: Option<Value>,
}
```

From crates/rlm-core/src/memory/session_memory_bridge.rs (existing):
```rust
pub fn read_run_id_from_payload(payload: &Value, fallback: &str) -> String;
pub fn build_saved_vector_index_section(...) -> Value;
pub fn merge_session_vector_records(ann: &mut AnnVectorIndex, run_id: &str, vector_section: &Value) -> Result<(), io::Error>;
pub fn export_session_vector_records(json_index: &FileVectorIndex, run_id: &str) -> Result<Vec<VectorIndexRecord>, io::Error>;
```

From src/application/memory/session-memory-bridge.ts (target behavior):
- `buildSavedSessionPayload` exports memory v2 section (scopes, episodic, audit, packets), preferences v2, vectorIndex v2, artifacts refs-only, graphWorkflowMetadata v1
- `restoreSessionMemory` calls `restoreSessionData`, merges vector records, returns saved runId
- `restoreGraphWorkflowMetadata` returns degraded note when metadata missing
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FileMemoryStore.restore_session_data</name>
  <files>crates/rlm-core/src/persistence/memory_store.rs</files>
  <read_first>src/adapters/persistence/file-memory-store.ts, crates/rlm-core/src/persistence/memory_store.rs</read_first>
  <behavior>
    - restore_session_data rewrites scopes with target session_id, caps episodic at 500 and packets at 200
    - Existing list_scopes/read_scope/patch_scope behavior unchanged
  </behavior>
  <action>
    Add pub fn restore_session_data matching TS restoreSessionData: accept session_id plus scopes, episodic, optional audit and packets vectors; write each scope via scope_path_for_lifetime, write episodic/audit/packet JSON atomically. Per D-04 session save/reopen decision — do not reimplement store semantics, mirror TS adapter.
  </action>
  <acceptance_criteria>
    - `grep -n 'pub fn restore_session_data' crates/rlm-core/src/persistence/memory_store.rs` returns a match
    - Unit test in memory_store tests or session_memory_bridge tests restores scopes under a new run_id
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core restore_session -- --nocapture</automated>
  </verify>
  <done>FileMemoryStore can bulk-import saved memory sections for session reopen</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete session_memory_bridge build/restore helpers</name>
  <files>crates/rlm-core/src/memory/session_memory_bridge.rs, crates/rlm-core/src/memory/mod.rs</files>
  <read_first>src/application/memory/session-memory-bridge.ts, crates/rlm-core/src/memory/session_memory_bridge.rs, crates/rlm-core/src/persistence/session_store.rs</read_first>
  <behavior>
    - build_saved_session_payload includes non-empty memory and vectorIndex when runtime has data
    - restore_session_memory returns runId from payload memory section and merges vectors into AnnVectorIndex
    - restore_graph_workflow_metadata returns degraded:true when metadata version != 1
  </behavior>
  <action>
    Add build_saved_session_payload (inputs: session snapshot Value, run_id, FileMemoryStore, FileVectorIndex, optional embed provider and graph workflow metadata) producing SavedSessionPayload-compatible sections. Add restore_session_memory (payload, memory_store, ann index) calling restore_session_data and merge_session_vector_records. Add restore_graph_workflow_metadata returning (metadata, degraded, optional note). Re-export new symbols from memory/mod.rs. Per D-04 and D-01 — mirror TS buildSavedSessionPayload and restoreSessionMemory exactly.
  </action>
  <acceptance_criteria>
    - `grep -n 'pub fn build_saved_session_payload' crates/rlm-core/src/memory/session_memory_bridge.rs` returns a match
    - `grep -n 'pub fn restore_session_memory' crates/rlm-core/src/memory/session_memory_bridge.rs` returns a match
    - Exported from crates/rlm-core/src/memory/mod.rs
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core session_memory_bridge -- --nocapture</automated>
  </verify>
  <done>Bridge exports full save payload and restores memory+vectors on reopen</done>
</task>

<task type="auto">
  <name>Task 3: Bridge unit test suite</name>
  <files>crates/rlm-core/tests/session_memory_bridge.rs</files>
  <read_first>crates/rlm-core/src/memory/session_memory_bridge.rs, src/application/memory/session-memory-bridge.ts</read_first>
  <action>
    Create integration-style unit tests: save exports live scopes/episodic and vector records; restore rebinds under saved runId; vector merge preserves other sessions' records; read_run_id_from_payload fallback behavior. Use temp dirs pattern from existing bridge tests.
  </action>
  <acceptance_criteria>
    - File crates/rlm-core/tests/session_memory_bridge.rs exists with at least 4 test functions
    - `cargo test -p rlm-core session_memory_bridge` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core session_memory_bridge -- --nocapture</automated>
  </verify>
  <done>Automated evidence that bridge closes VIDX-02 unit-test-only orphan status</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Saved session payload → memory restore | Untrusted persisted JSON crosses into live memory store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.1-01 | I | restore_session_data | mitigate | Validate section version/status fields before write; cap episodic/packet array lengths |
| T-60.1-02 | T | merge_session_vector_records | mitigate | Reject malformed vector sections (version != 2); use existing sanitize_id on scope paths |
</threat_model>

<verification>
- `cargo test -p rlm-core session_memory_bridge` passes
- `npm run check:rust` passes
</verification>

<success_criteria>
- build_saved_session_payload and restore_session_memory exist and are tested
- No control-server route changes in this plan (reserved for 60.1-02)
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-01-SUMMARY.md`
</output>

---
phase: 60.1-close-v1-8-milestone-gaps
plan: 02
type: execute
wave: 2
depends_on: ["60.1-01"]
files_modified:
  - crates/rlm-core/src/control_server/routes.rs
  - crates/rlm-core/src/control_server/mod.rs
  - crates/rlm-core/src/persistence/memory_store.rs
  - crates/rlm-core/tests/persistence_control_server.rs
autonomous: true
gap_closure: true
requirements: [REG-01, PERS-01, PERS-02, VIDX-02]
must_haves:
  truths:
    - "POST /api/saved-sessions/save persists session with memory and vectorIndex sections on Rust runtime"
    - "GET /api/saved-sessions/{id} returns saved session detail"
    - "POST /api/saved-sessions/{id}/open restores snapshot and memory; returns 409 when verification.status !== complete"
    - "POST /api/memory/preferences and DELETE /api/memory/preferences/{key} mutate preferences and return inspect payload"
  artifacts:
    - path: crates/rlm-core/src/control_server/routes.rs
      provides: save/open/detail session routes and memory preference routes
    - path: crates/rlm-core/tests/persistence_control_server.rs
      provides: route integration tests
  key_links:
    - from: crates/rlm-core/src/control_server/routes.rs
      to: crates/rlm-core/src/memory/session_memory_bridge.rs
      via: build_saved_session_payload on save, restore_session_memory on open
      pattern: build_saved_session_payload
    - from: crates/rlm-core/src/control_server/routes.rs
      to: crates/rlm-core/src/persistence/session_store.rs
      via: FileSessionStore.save/load
      pattern: FileSessionStore::new
---

<objective>
Wire missing Axum session save/reopen/detail routes and memory preference mutation routes, closing REG-01, PERS-01, PERS-02, and VIDX-02 handler integration gaps.

Purpose: Fixes broken UI flows (save/open 404, preferences 404) identified in v1.8 milestone audit.
Output: Working routes with integration tests mirroring TS handler status codes and error vocabulary.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-CONTEXT.md
@src/application/control-server/handlers/session.ts
@crates/rlm-core/src/control_server/routes.rs
@crates/rlm-core/src/control_server/mod.rs
@tests/domain/recursion/recursive-language-model.test.ts

<interfaces>
TS handler semantics (session.ts):
- POST /api/saved-sessions/save → 404 if store unconfigured; 200 + SavedSessionRecord
- GET /api/saved-sessions/{id} → 404 unconfigured; 200 + record
- POST /api/saved-sessions/{id}/open → 409 + { error, savedSession } when verification.status !== "complete"; else restore snapshot + memory, rebind runId
- POST /api/memory/preferences body { key, value, lifetime? } → inspect payload
- DELETE /api/memory/preferences/{key} → inspect payload

Rust RouterState fields for wiring:
- paths.sessions_dir, paths.memory_dir, memory_session_id, session, memory_index(session_id)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold persistence_control_server integration tests</name>
  <files>crates/rlm-core/tests/persistence_control_server.rs</files>
  <read_first>tests/domain/recursion/recursive-language-model.test.ts (lines ~3393-3482), crates/rlm-core/tests/persistence_control_server.rs</read_first>
  <behavior>
    - save_open_round_trip test: POST save, mutate graph, POST open, assert node restored and verification.status complete
    - memory_preference_round_trip test: POST preference "tone", DELETE preference "tone", both return 200 with inspect payload
    - Tests compile and fail (404 or unimplemented) until Tasks 2–3 wire routes
  </behavior>
  <action>
    Add failing integration tests to persistence_control_server.rs before route implementation. Port TS patterns from recursive-language-model.test.ts: (1) save/open round-trip with graph mutation reversal, asserting vectorIndex section present; (2) preference create/delete round-trip on configured memory fixture. Use tests/fixtures/persistence/node-written/ or mkdtemp with .rlm/sessions and .rlm/memory dirs. Name tests with prefixes save_open_ and memory_preference_ so downstream tasks can filter. Per D-01 testing decision — tests first, routes second.
  </action>
  <acceptance_criteria>
    - At least two new async tests in persistence_control_server.rs (save/open and preferences)
    - `cargo test -p rlm-core persistence_control_server -- --nocapture` compiles (may fail until routes wired)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core persistence_control_server -- --nocapture 2>&1 | grep -qE 'test result:|running [0-9]+ test'</automated>
  </verify>
  <done>Failing integration test scaffold exists before route wiring</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire session save, detail, and open routes</name>
  <files>crates/rlm-core/src/control_server/routes.rs, crates/rlm-core/src/control_server/mod.rs</files>
  <read_first>src/application/control-server/handlers/session.ts, crates/rlm-core/src/control_server/routes.rs, crates/rlm-core/src/memory/session_memory_bridge.rs</read_first>
  <behavior>
    - POST save returns 404 with saved_sessions_unconfigured() when sessions dir not configured
    - Saved payload includes vectorIndex section when memory configured
    - POST open returns 409 when verification.status != "complete"
    - POST open restores graph nodes removed after save (mirrors TS integration test)
  </behavior>
  <action>
    Register routes: POST /api/saved-sessions/save, GET /api/saved-sessions/{id}, POST /api/saved-sessions/{id}/open. On save: build payload via build_saved_session_payload using state.session.snapshot(), state.memory_session_id, memory store, vector index from state.memory_index(). On open: load saved record; if verification.status != "complete" return 409 with savedSession body; else restore_session_memory, update RouterState memory_session_id to restored runId, session.restore_snapshot, patch graph workflow metadata from restore_graph_workflow_metadata. On detail: return FileSessionStore.load. Per D-01 and D-04 — match TS status codes and 409 unsafe reopen semantics.
  </action>
  <acceptance_criteria>
    - `grep -n 'saved-sessions/save' crates/rlm-core/src/control_server/routes.rs` returns a match
    - `grep -n 'saved-sessions/{id}/open' crates/rlm-core/src/control_server/routes.rs` returns a match
    - save_open_* integration tests from Task 1 pass
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core save_open -- --nocapture</automated>
  </verify>
  <done>Session save/reopen/detail routes return non-404 on configured Rust runtime</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire memory preference POST and DELETE routes</name>
  <files>crates/rlm-core/src/control_server/routes.rs, crates/rlm-core/src/persistence/memory_store.rs</files>
  <read_first>src/application/memory/memory-resolver.ts (setPreference/deletePreference), src/application/control-server/handlers/session.ts (tryMemoryRoutes)</read_first>
  <behavior>
    - POST creates preference in project-preferences scope with lifetime project default
    - DELETE removes key from project-preferences scope
    - Both return full inspect payload including vectorIndex like GET /api/memory
  </behavior>
  <action>
    Add FileMemoryStore helpers set_preference and delete_preference using patch_scope on project-preferences scope (mirror MemoryResolver: source "ui", lifetime project|permanent from body). Register POST /api/memory/preferences and DELETE /api/memory/preferences/{key}. Return same inspect shape as existing memory GET handler (scopes + vectorIndex). Per D-02 — return updated inspect payload after mutation.
  </action>
  <acceptance_criteria>
    - `grep -n 'memory/preferences' crates/rlm-core/src/control_server/routes.rs` shows POST and DELETE route registrations
    - memory_preference_* integration tests from Task 1 pass
    - Full persistence_control_server suite exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core persistence_control_server -- --nocapture</automated>
  </verify>
  <done>Memory preferences editable via UI routes on Rust runtime</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → session save/open | Untrusted JSON body and session IDs |
| HTTP client → memory preferences | Untrusted key/value strings written to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.1-03 | I | saved-sessions/{id}/open | mitigate | Block reopen when verification.unsafe_to_continue or status != complete (409) |
| T-60.1-04 | I | memory/preferences | mitigate | sanitize_id on preference keys; reject empty keys with 400 |
| T-60.1-05 | D | session save | mitigate | Use existing FileSessionStore atomic writes; return 500 on IO errors without partial files |
</threat_model>

<verification>
- `cargo test -p rlm-core persistence_control_server` passes
- `npm run check:rust` passes
</verification>

<success_criteria>
- All four session routes and two preference routes respond correctly on configured fixture
- 409 unsafe reopen behavior matches TS
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-02-SUMMARY.md`
</output>

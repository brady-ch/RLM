---
phase: 60.1-close-v1-8-milestone-gaps
plan: 04
type: execute
wave: 3
depends_on: ["60.1-01"]
files_modified:
  - crates/rlm-core/src/domain/run_state_persistence.rs
  - crates/rlm-core/src/domain/mod.rs
  - crates/rlm-core/src/domain/recursive_language_model.rs
  - crates/rlm-core/src/graph/executor.rs
  - crates/rlm-core/src/control_server/routes.rs
  - crates/rlm-core/tests/run_state_execution.rs
  - .planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [PERS-03]
must_haves:
  truths:
    - "Graph execution seeds FileRunStateStore snapshot at run start OR explicit deferral documented with rationale"
    - "Run-state mutations flush during node execution when wired"
  artifacts:
    - path: crates/rlm-core/src/domain/run_state_persistence.rs
      provides: RunStatePersistence helper mirroring TS
      min_lines: 40
    - path: .planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md
      provides: PERS-03 disposition (wired or deferred)
  key_links:
    - from: crates/rlm-core/src/control_server/routes.rs
      to: crates/rlm-core/src/persistence/run_state_store.rs
      via: spawn_graph_execution passes run_state_store + run_id
      pattern: FileRunStateStore
    - from: crates/rlm-core/src/graph/executor.rs
      to: crates/rlm-core/src/domain/run_state_persistence.rs
      via: flush on node status changes
      pattern: RunStatePersistence
---

<objective>
Wire FileRunStateStore into Rust graph execution checkpoint path, or document explicit deferral per phase success criterion #6.

Purpose: Closes PERS-03 unsatisfied gap — store exists but is orphaned from execution.
Output: Minimal run-state persistence during graph execution OR documented deferral in 60.1-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-CONTEXT.md
@.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md
@src/domain/run-state-persistence.ts
@src/domain/recursive-language-model.ts (initializeRunState, flushRunStateWrites)
@crates/rlm-core/src/persistence/run_state_store.rs
@crates/rlm-core/src/graph/executor.rs
@crates/rlm-core/src/control_server/routes.rs (spawn_graph_execution)

<interfaces>
From crates/rlm-core/src/persistence/run_state_store.rs:
```rust
pub struct FileRunStateStore { ... }
impl FileRunStateStore {
    pub fn get_snapshot(&self, run_id: &str) -> io::Result<Option<RunStateSnapshot>>;
    pub fn create_snapshot(&self, run_id: &str, seed: ...) -> io::Result<RunStateSnapshot>;
    pub fn mutate(&self, run_id: &str, request: RunStateMutationRequest) -> io::Result<RunStateMutationResult>;
}
```

From spawn_graph_execution — GraphExecutorInput currently has no run_state field.
TS RuntimeRunState: { runId, store: RunStateStorePort, flush interval }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold run_state_execution integration tests</name>
  <files>crates/rlm-core/tests/run_state_execution.rs</files>
  <read_first>.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md, crates/rlm-core/tests/persistence_dual_read.rs</read_first>
  <behavior>
    - run_state_snapshot_after_execution: after confirm-run graph execution, FileRunStateStore.get_snapshot returns Some for run_id with mutation log entry
    - Test compiles and fails until Tasks 2–3 wire run-state into executor and spawn path
  </behavior>
  <action>
    Create crates/rlm-core/tests/run_state_execution.rs with failing integration test before run-state wiring. Use temp dir pattern from persistence_dual_read.rs. Per D-06 — test scaffold first, wiring follows.
  </action>
  <acceptance_criteria>
    - File crates/rlm-core/tests/run_state_execution.rs exists with at least one async test
    - `cargo test -p rlm-core run_state_execution -- --nocapture` compiles (may fail until wiring complete)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core run_state_execution -- --nocapture 2>&1 | grep -qE 'test result:|running [0-9]+ test'</automated>
  </verify>
  <done>Failing run-state integration test scaffold exists before wiring</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RunStatePersistence and extend GraphExecutorInput</name>
  <files>crates/rlm-core/src/domain/run_state_persistence.rs, crates/rlm-core/src/domain/mod.rs, crates/rlm-core/src/graph/executor.rs</files>
  <read_first>src/domain/run-state-persistence.ts, crates/rlm-core/src/persistence/run_state_store.rs, crates/rlm-core/src/graph/executor.rs</read_first>
  <behavior>
    - RunStatePersistence create_snapshot seeds prompt on init
    - GraphExecutor mutates run state on at least one node status transition
  </behavior>
  <action>
    Create run_state_persistence.rs mirroring TS RunStatePersistence: hold run_id + Arc FileRunStateStore, create_snapshot on init with prompt seed, mutate on execution status events (node started/completed/failed paths in GraphExecutor). Add optional run_state field to GraphExecutorInput. Export module from domain/mod.rs. Add unit tests with run_state_persistence prefix in domain module or dedicated test. Per D-06 — wire if feasible; do not stub with no-op without documenting.
  </action>
  <acceptance_criteria>
    - File crates/rlm-core/src/domain/run_state_persistence.rs exists
    - GraphExecutorInput includes optional run_state: Option Arc FileRunStateStore or RunStatePersistence wrapper
    - GraphExecutor calls run_state init before first node and mutate on at least one status transition
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core run_state_persistence -- --nocapture</automated>
  </verify>
  <done>Run-state store connected to graph executor lifecycle</done>
</task>

<task type="auto">
  <name>Task 3: Pass run state from spawn_graph_execution and finalize or defer</name>
  <files>crates/rlm-core/src/control_server/routes.rs, crates/rlm-core/tests/run_state_execution.rs, .planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md</files>
  <read_first>crates/rlm-core/src/control_server/routes.rs (spawn_graph_execution), crates/rlm-core/src/persistence/paths.rs, .planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md</read_first>
  <action>
    When paths.run_state_dir parent exists (or always when project_root configured), construct FileRunStateStore from state.paths.run_state_dir and pass to GraphExecutorInput with run_id = state.memory_session_id. Seed snapshot with graph root prompt on execution start. IF wiring succeeds: run_state_execution integration test from Task 1 passes. IF full checkpoint/resume parity with TS RLM is not achievable in this scope: update 60.1-VERIFICATION.md PERS-03 section with explicit deferral rationale (what is wired vs what remains for follow-on), list remaining gaps, and mark disposition deferred — do NOT silently skip. Per D-06 and phase success criterion #6.
  </action>
  <acceptance_criteria>
    - spawn_graph_execution passes run_state into GraphExecutorInput when store dir accessible
    - Either `cargo test -p rlm-core run_state_execution` passes OR 60.1-VERIFICATION.md contains a "PERS-03 Disposition" section with deferral rationale
    - No silent skip — one of wired or documented deferral must be present
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rlm-core run_state_execution -- --nocapture 2>/dev/null || grep -q 'PERS-03 Disposition' .planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-VERIFICATION.md</automated>
  </verify>
  <done>PERS-03 gap closed by wiring or explicit documented deferral</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph executor → run state store | Internal execution events written to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.1-09 | T | run_state mutate | mitigate | Validate mutation paths; use existing FileRunStateStore seq/version checks |
| T-60.1-10 | I | run state JSON on disk | accept | Local .planning/runs only; same trust model as TS adapter |
</threat_model>

<verification>
- run_state_execution test passes OR deferral documented in 60.1-VERIFICATION.md
- `npm run check:rust` passes
</verification>

<success_criteria>
- FileRunStateStore no longer orphaned from execution path OR deferral explicitly recorded
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-close-v1-8-milestone-gaps-session-routes-memory-preferences-/60.1-04-SUMMARY.md`
</output>

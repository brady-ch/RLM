---
phase: 75-skill-interop-depth
plan: 02
type: execute
wave: 2
depends_on:
  - "75-01"
files_modified:
  - crates/rlm-core/src/ports/skill_loader.rs
  - crates/rlm-core/src/plugins/extension_host.rs
  - crates/rlm-core/src/plugins/builtin/mod.rs
  - crates/rlm-core/src/plugins/registry/doctor.rs
  - crates/rlm-core/src/interop/skill_runtime.rs
  - crates/rlm-core/tests/skill_interop.rs
autonomous: true
requirements:
  - PLUG-05
user_setup: []
must_haves:
  truths:
    - "ManifestSkillLoader.load() validates root_path and discovers skill candidates asynchronously"
    - "register_manifest_skill_loaders awaits load() before registering loader"
    - "Doctor reports skill loader load failures with path and structured reason"
  artifacts:
    - path: crates/rlm-core/src/ports/skill_loader.rs
      provides: "SkillLoader async load trait method"
      contains: "async fn load"
    - path: crates/rlm-core/src/plugins/extension_host.rs
      provides: "Async manifest loader registration"
      contains: "load"
  key_links:
    - from: register_manifest_skill_loaders
      to: ManifestSkillLoader.load
      via: "await before register_skill_loader"
      pattern: "load\\(\\)"
    - from: collect_skill_loader_issues
      to: loader load errors
      via: "structured PluginDoctorIssue message"
      pattern: "skill_loader"
---

<objective>
Implement ManifestSkillLoader async load() and wire declarative manifest skill paths into runtime (PLUG-05).

Purpose: Phase 65 registered loader paths without loading; skill tool only reads YAML searchPaths, ignoring manifest declarative paths.
Output: SkillLoader.load(), async registration, merged search paths, doctor load failure context.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-skill-interop-depth/75-CONTEXT.md
@.planning/phases/75-skill-interop-depth/75-01-SUMMARY.md
@src/ports/skill-loader-port.ts
@crates/rlm-core/tests/skill_interop.rs

<interfaces>
TS SkillLoaderPort:
```typescript
export interface SkillLoaderPort {
  name: string;
  load(): Promise<void>;
}
```

Rust ManifestSkillLoader today: name() + root_path() only; register_manifest_skill_loaders sync register.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SkillLoader.load trait + ManifestSkillLoader impl</name>
  <files>crates/rlm-core/src/ports/skill_loader.rs, crates/rlm-core/tests/skill_interop.rs</files>
  <behavior>
    - load() on valid directory with SKILL.md succeeds
    - load() on missing directory returns Err with path in message
    - ManifestSkillLoader exposes discovered search path(s) after successful load for merge into SkillRuntime
  </behavior>
  <action>
    Per D-02, extend `SkillLoader` trait with `async fn load(&self) -> Result<(), String>` using async_trait (already in workspace).

    Implement on `ManifestSkillLoader`:
    1. Verify root_path.is_dir() else Err with manifest path context.
    2. Call discover_skill_candidates(&[root_path]) — store discovered paths internally (add `loaded_paths: Mutex<Vec<PathBuf>>` or return via `fn search_paths(&self) -> &[PathBuf]` after load).
    3. Optionally validate at least one valid candidate or allow empty with warn — match TS lenient registration behavior from extension host.

    Add unit test in skill_interop.rs: temp fixture plugin with manifest skillLoaders pointing at skills/demo dir; loader.load().await ok; get search paths non-empty.

    RED then GREEN.
  </action>
  <verify>
    <automated>cargo test -p rlm-core manifest_skill_loader_load -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <done>ManifestSkillLoader.load() discovers skills from declarative path; tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Async registration, runtime merge, doctor context</name>
  <files>crates/rlm-core/src/plugins/extension_host.rs, crates/rlm-core/src/plugins/builtin/mod.rs, crates/rlm-core/src/interop/skill_runtime.rs, crates/rlm-core/src/plugins/registry/doctor.rs</files>
  <action>
    Per D-02/D-03:

    1. Change `register_manifest_skill_loaders` to async `register_manifest_skill_loaders_async` (keep sync wrapper only if needed for non-async callers — prefer updating call sites in builtin/mod.rs to await in async context or use `tokio::runtime::Handle::current().block_on` in sync build path with comment).

    2. For each manifest path: construct ManifestSkillLoader, await load(). On Err: emit SKILL_PARSE_ERROR lifecycle event via sink if available AND collect warning string for interop_warnings; still register loader or skip per strict policy — prefer skip registration with doctor issue on hard failure.

    3. Merge all loaded loader search paths into SkillInteropConfig.search_paths in load_skill_interop (dedupe paths). Manifest-declared paths must be resolvable by skill tool after init.

    4. Extend `collect_skill_loader_issues` to attempt load (sync block_on) during doctor scan; push PluginDoctorIssue with code `skill_loader_load_failed`, message including path + error reason, severity warn.

    5. Update manifest_skill_loaders_register_on_extension_host test to assert load ran (e.g. skill tool resolves skill from manifest loader path fixture).

    Use discretion per CONTEXT: merge paths into SkillRuntime config so skill tool finds declarative skills.
  </action>
  <verify>
    <automated>cargo test -p rlm-core manifest_skill_loaders skill_loader -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Declarative manifest skill paths loaded async; doctor surfaces load failures; skill tool resolves manifest-loaded skills</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin manifest skillLoaders paths → filesystem | Declarative paths read skill files from plugin dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03 | I | ManifestSkillLoader root_path | mitigate | Resolve relative paths against plugin_root only; reject path traversal outside plugin root in register_manifest_skill_loaders |
| T-75-04 | D | load() failure | mitigate | Doctor warn + optional strict fail; do not panic runtime init |
</threat_model>

<verification>
Scoped: `cargo test -p rlm-core manifest_skill skill_loader`. Full suite deferred.
</verification>

<success_criteria>
- PLUG-05: async load() removes stub registration-only behavior
- Manifest loader paths discoverable by skill tool
- Doctor issues include structured load failure context
</success_criteria>

<output>
After completion, create `.planning/phases/75-skill-interop-depth/75-02-SUMMARY.md`
</output>

---
phase: 75-skill-interop-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/application/execution/runtime_events.rs
  - crates/rlm-core/src/application/execution/mod.rs
  - crates/rlm-core/src/interop/skill_runtime.rs
  - crates/rlm-core/src/interop/mod.rs
  - crates/rlm-core/tests/skill_interop.rs
autonomous: true
requirements:
  - PLUG-04
user_setup: []
must_haves:
  truths:
    - "Skill parse failures emit structured lifecycle events with code SKILL_PARSE_ERROR"
    - "Event severity reflects strict vs lenient path policy"
    - "ResolvedSkill warnings carry RuntimeEvent values not plain SKILL_PARSE_ERROR strings only"
  artifacts:
    - path: crates/rlm-core/src/application/execution/runtime_events.rs
      provides: "RuntimeEvent types and create helper"
      contains: "SKILL_PARSE_ERROR"
    - path: crates/rlm-core/src/interop/skill_runtime.rs
      provides: "Event sink integration in resolve_skill"
      contains: "emit"
  key_links:
    - from: SkillRuntime.resolve_skill
      to: RuntimeEventSink
      via: "emit_lifecycle on invalid candidate"
      pattern: "SKILL_PARSE_ERROR"
    - from: skill_interop.rs tests
      to: InMemoryEventStore or test sink
      via: "assert event code"
      pattern: "SKILL_PARSE_ERROR"
---

<objective>
Emit structured SKILL_PARSE_ERROR lifecycle events from Rust skill runtime (PLUG-04), matching TS McpSkillRuntime semantics.

Purpose: Phase 65 deferred lifecycle events to warning strings; doctor and observability lack structured parse failure context.
Output: RuntimeEvent module, SkillRuntime event sink, updated tests asserting event code/severity.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-skill-interop-depth/75-CONTEXT.md
@src/runtime/interop/mcp-skill-runtime.ts
@src/application/execution/runtime-events.ts
@tests/runtime/interop/mcp-skill-interoperability.test.ts
@crates/rlm-core/src/interop/skill_runtime.rs

<interfaces>
TS RuntimeEvent fields (runtime-events.ts):
- eventId, fingerprint, runId, code, severity, source, subject, occurredAt, seq, message, metrics?

TS resolveSkill invalid candidate:
- code: "SKILL_PARSE_ERROR", source: "skills", subject: absolutePath, severity warn|error

Rust ResolvedSkill currently:
- warnings: Vec<String> with "SKILL_PARSE_ERROR [...]" prefix
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RuntimeEvent types + skill_interop event test</name>
  <files>crates/rlm-core/src/application/execution/runtime_events.rs, crates/rlm-core/src/application/execution/mod.rs, crates/rlm-core/tests/skill_interop.rs</files>
  <behavior>
    - Lenient path: invalid candidate at /a emits SKILL_PARSE_ERROR with severity warn; valid candidate at /b wins
    - Strict path: invalid candidate emits severity error and resolve returns Err
    - Test asserts event.code == "SKILL_PARSE_ERROR" and subject is absolute path (not string prefix only)
  </behavior>
  <action>
    Per D-01, create `runtime_events.rs` under application/execution with:

    - `RuntimeEventSeverity` enum (Info, Warn, Error) serialized lowercase to match TS
    - `RuntimeEvent` struct with fields matching TS contract (use uuid for event_id, chrono or time crate for occurred_at ISO string)
    - `RuntimeEventSink` trait with `fn emit(&self, event: RuntimeEvent) -> Result<(), String>` (sync ok for tests; async not required if callers use block_on in tests)
    - `InMemoryRuntimeEventStore` for tests
    - `create_runtime_event` helper accepting run_id, code, severity, source, subject, seq, message

    Export module from `application/execution/mod.rs`.

    Add failing test in skill_interop.rs mirroring TS mcp-skill-interoperability lenient/strict cases using InMemoryRuntimeEventStore wired into SkillRuntime (RED first).

    Do not remove string warnings yet — Task 2 replaces emission path.
  </action>
  <verify>
    <automated>cargo test -p rlm-core skill_parse_error_emits_lifecycle_event -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <done>RuntimeEvent module exists; failing test documents expected SKILL_PARSE_ERROR lifecycle behavior</done>
</task>

<task type="auto">
  <name>Task 2: Wire SkillRuntime event sink</name>
  <files>crates/rlm-core/src/interop/skill_runtime.rs, crates/rlm-core/src/interop/mod.rs</files>
  <action>
    Per D-01/D-03:

    1. Add optional `Arc<dyn RuntimeEventSink>` + `run_id: String` + sequence counter to `SkillRuntime` (constructor params with defaults for existing call sites — use empty run_id and NoopSink when unset).

    2. Change `ResolvedSkill.warnings` from `Vec<String>` to `Vec<RuntimeEvent>` (update all Rust callers in skill_runtime.rs tests and skill tool execute path).

    3. In `resolve_skill` invalid candidate branch, call sink.emit with code SKILL_PARSE_ERROR, source "skills", subject candidate.absolute_path display, severity from strictness, message from reason.

    4. Update `load_skill_interop` to accept optional sink/run_id from future wiring — for now pass NoopSink; tests construct SkillRuntime directly with InMemory store.

    5. Update skill_interop.rs tests to GREEN. Remove reliance on warnings[0].contains("SKILL_PARSE_ERROR") string match — assert structured event fields.

    6. Re-export RuntimeEvent types from interop mod only if needed by tests; prefer importing from application::execution.
  </action>
  <verify>
    <automated>cargo test -p rlm-core skill_parse_error -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <done>SKILL_PARSE_ERROR lifecycle events emitted; skill_interop tests pass with structured assertions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill files → SkillRuntime | Untrusted SKILL.md content parsed for frontmatter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | I | RuntimeEvent.message | mitigate | Use candidate.reason only; do not embed full file contents in events |
| T-75-02 | I | Event subject path | accept | Absolute paths aid debugging; local dev tool surface |
</threat_model>

<verification>
Scoped: `cargo test -p rlm-core skill_parse_error skill_interop` filters. Full workspace test deferred.
</verification>

<success_criteria>
- PLUG-04: structured SKILL_PARSE_ERROR events, not warning strings only
- Lenient warn + strict error severities match TS tests
- Existing skill_interop fixture load tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/75-skill-interop-depth/75-01-SUMMARY.md`
</output>

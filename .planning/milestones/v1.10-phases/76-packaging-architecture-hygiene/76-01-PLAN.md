---
phase: 76-packaging-architecture-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - PACK-04
user_setup: []
must_haves:
  truths:
    - "npm test runs deb-smoke-lib unit tests via test:packaging"
    - "Standalone test:packaging script still works"
  artifacts:
    - path: package.json
      provides: "Default test gate includes packaging"
      contains: "test:packaging"
  key_links:
    - from: package.json scripts.test
      to: scripts/packaging/deb-smoke-lib.test.mjs
      via: "npm run test:packaging chain"
      pattern: "test:packaging"
---

<objective>
Wire `test:packaging` into default `npm test` developer gate (PACK-04).

Purpose: PACK-03 CI smoke exists as standalone script but v1.9 audit flagged packaging tests excluded from default npm test loop.
Output: Updated package.json test script chaining packaging smoke after dist tests (or parallel-safe sequence).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-packaging-architecture-hygiene/76-CONTEXT.md
@package.json
@scripts/packaging/deb-smoke-lib.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain test:packaging into npm test</name>
  <files>package.json</files>
  <action>
    Per D-01, update `"test"` script from:
    `npm run build && node --test dist/tests`
    to include packaging gate, e.g.:
    `npm run build && node --test dist/tests && npm run test:packaging`

    Keep `"test:packaging"` unchanged. Do not add packaging to `"check"` separately unless already implied via npm test — check script already runs npm test.

    Avoid duplicating build in test:packaging chain (test:packaging is node --test only today — correct).
  </action>
  <verify>
    <automated>npm run test:packaging 2>&1 | tail -10</automated>
  </verify>
  <done>test:packaging passes standalone</done>
</task>

<task type="auto">
  <name>Task 2: Verify test script references packaging</name>
  <files>package.json</files>
  <action>
    Confirm `"test"` script string contains `test:packaging`. Document in 76-01-SUMMARY that full `npm test` run is optional for phase verify (deferred) but script wiring satisfies PACK-04 contract.

    Optional: run `node -e "const p=require('./package.json'); if(!p.scripts.test.includes('test:packaging')) process.exit(1)"` as automated gate.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.test.includes('test:packaging')){console.error('missing');process.exit(1)}; console.log('ok')"</automated>
  </verify>
  <done>npm test script includes test:packaging chain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaging test → filesystem | deb-smoke-lib reads fixture paths only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | D | deb-smoke-lib.test.mjs | accept | Unit test uses fixtures; no network in default packaging test |
</threat_model>

<verification>
Scoped: `npm run test:packaging` + package.json assertion. Full npm test deferred in phase close notes.
</verification>

<success_criteria>
- PACK-04: default npm test script invokes test:packaging
- test:packaging still passes in isolation
</success_criteria>

<output>
After completion, create `.planning/phases/76-packaging-architecture-hygiene/76-01-SUMMARY.md`
</output>

---
phase: 76-packaging-architecture-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .planning/milestones/v1.9-phases/71-optional-crate-split/71-DECISION.md
  - scripts/measure-rust-compile-baseline.sh
  - scripts/measure-rust-compile-baseline.test.mjs
autonomous: true
requirements:
  - ARCH-07
  - ARCH-08
user_setup: []
must_haves:
  truths:
    - "71-DECISION.md reflects Phase 70 complete; no stale prerequisite denial"
    - "measure-rust-compile-baseline.sh aborts on cargo failure"
    - "AGENTS.md documents each baselined Rust boundary arc with ratchet plan"
    - "Default check:rust:boundaries behavior unchanged unless arcs eliminated"
  artifacts:
    - path: .planning/milestones/v1.9-phases/71-optional-crate-split/71-DECISION.md
      provides: "Accurate defer decision post-Phase 70"
      contains: "Phase 70"
    - path: AGENTS.md
      provides: "Transitional boundary ratchet table"
      contains: "rust-boundary-baseline"
  key_links:
    - from: measure-rust-compile-baseline.sh
      to: run_timed_step exit 1 on cargo fail
      via: "if ! elapsed= time_build"
      pattern: "exit 1"
    - from: AGENTS.md
      to: scripts/rust-boundary-baseline.json
      via: "documented arc list"
      pattern: "baseline"
---

<objective>
Refresh architecture decision docs and document transitional Rust boundary baseline arcs (ARCH-07, ARCH-08).

Purpose: 71-DECISION stale language misleads contributors; 7 baseline arcs need explicit ratchet plan without flipping CI to strict mode.
Output: Updated 71-DECISION.md, verified baseline script, AGENTS.md ratchet table, optional script regression test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-packaging-architecture-hygiene/76-CONTEXT.md
@.planning/milestones/v1.9-phases/71-optional-crate-split/71-DECISION.md
@.planning/milestones/v1.9-phases/71-optional-crate-split/71-REVIEW.md
@scripts/measure-rust-compile-baseline.sh
@scripts/rust-boundary-baseline.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh 71-DECISION.md</name>
  <files>.planning/milestones/v1.9-phases/71-optional-crate-split/71-DECISION.md</files>
  <action>
    Per D-02, update 71-DECISION.md:

    1. Change Phase 70 prerequisite row from **not met** to **met** — cite Phase 70 summaries/verification and `scripts/check-rust-boundaries.sh` existence.
    2. Update Summary blocked_on / prerequisite prose: Phase 70 A5 complete; baseline is authoritative for defer gate (metrics unchanged — still DEFER).
    3. Remove or revise footnote "Phase 70 still pending — re-run baseline" to optional refresh note, not blocker.
    4. Keep decision DEFER and compile metrics table; do not reopen crate split.

    Do not change ARCH-06 evaluated defer outcome.
  </action>
  <verify>
    <automated>grep -v '^#' .planning/milestones/v1.9-phases/71-optional-crate-split/71-DECISION.md | grep -c 'Phase 70.*pending\|Phase 70 prerequisites are \*\*not\*\* met' || true; test $(grep -v '^#' .planning/milestones/v1.9-phases/71-optional-crate-split/71-DECISION.md | grep -c 'Phase 70.*pending\|not\*\* met') -eq 0</automated>
  </verify>
  <done>71-DECISION accurately states Phase 70 complete; stale denial language removed</done>
</task>

<task type="auto">
  <name>Task 2: Baseline script guard + AGENTS ratchet table</name>
  <files>scripts/measure-rust-compile-baseline.sh, scripts/measure-rust-compile-baseline.test.mjs, AGENTS.md</files>
  <action>
    Per D-02/D-03:

    **Script (ARCH-07):** Verify `run_timed_step` propagates cargo failure (already present at lines 34-37). Add `scripts/measure-rust-compile-baseline.test.mjs` node test that sources/parses script or runs `bash -n` plus asserts `run_timed_step` contains `exit 1` on failure and `time_build` captures `rc=$?`. If script already correct, test documents regression guard per 71-REVIEW.

    **AGENTS.md (ARCH-08):** Add subsection "Transitional boundary baseline (ratchet plan)" under Rust boundary rules listing all 7 entries from `scripts/rust-boundary-baseline.json` with columns: Rule | From module | Rationale for defer | Removal condition. Examples:
    - no-plugins-to-domain: ToolExecutionResult in domain types until ports consolidation
    - no-persistence-to-application: transitional config re-export
    - no-plugins-to-application: agent_registry filter in plugin runtime

    State explicitly: default CI uses baseline mode; strict mode only when arc count reaches zero. Do NOT remove baseline entries unless code moves eliminate arc in same task (optional only if trivial).

    Run `npm run check:rust:boundaries` to confirm unchanged pass behavior.
  </action>
  <verify>
    <automated>node --test scripts/measure-rust-compile-baseline.test.mjs && npm run check:rust:boundaries 2>&1 | tail -8</automated>
  </verify>
  <done>Baseline script guarded by test; AGENTS documents all 7 arcs; boundary check still passes in default mode</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → contributor behavior | Misdocumented prerequisites could trigger unnecessary crate split work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02 | I | AGENTS ratchet table | accept | Documentation only; no runtime security impact |
| T-76-03 | E | baseline script | mitigate | run_timed_step exit 1 prevents bogus baseline markdown on cargo failure |
</threat_model>

<verification>
Scoped: node script test + check:rust:boundaries. Full check:rust deferred.
</verification>

<success_criteria>
- ARCH-07: 71-DECISION refreshed; baseline script failure propagation verified
- ARCH-08: all baseline arcs documented with ratchet plan; default boundary check unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/76-packaging-architecture-hygiene/76-02-SUMMARY.md`
</output>

---
phase: 74-ts-resume-cursor-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/graph/graph-executor.ts
  - src/domain/run-state-persistence.ts
autonomous: true
requirements:
  - PERS-04
user_setup: []
must_haves:
  truths:
    - "TypeScript graph executor persists node status to run state when runState is wired"
    - "TypeScript graph executor calls persistResumeCursor before running each node and after successful completion"
    - "Persisted resumeCursor JSON uses camelCase activeNodeId, completedNodeIds, variant playbook matching Rust RunStateStorePort"
  artifacts:
    - path: src/application/graph/graph-executor.ts
      provides: "Run-state writes at node transitions"
      contains: "persistResumeCursor"
    - path: src/domain/run-state-persistence.ts
      provides: "ResumeCursor type and persist helpers"
      contains: "variant"
  key_links:
    - from: src/application/graph/graph-executor.ts
      to: src/domain/run-state-persistence.ts
      via: "RunStatePersistence instance from input.runState"
      pattern: "persistResumeCursor"
    - from: persistResumeCursorNow
      to: FileRunStateStore.mutate
      via: 'path: "resumeCursor"'
      pattern: "resumeCursor"
---

<objective>
Wire TypeScript graph executor run-state persistence at node transitions so Node runtime writes the same resume cursor shape as Rust (PERS-04 write path).

Purpose: `RunStatePersistence.persistResumeCursor` exists but `graph-executor.ts` never invokes it — resume cursor never updates during TS graph runs.
Output: Graph executor persists node status + resume cursor mirroring Rust `persist_run_state_status` / `persist_resume_cursor` call sites.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-ts-resume-cursor-parity/74-CONTEXT.md
@src/application/graph/graph-executor.ts
@src/domain/run-state-persistence.ts
@crates/rlm-core/src/application/graph/executor.rs

<interfaces>
From src/domain/run-state-persistence.ts:
```typescript
export interface ResumeCursor {
  activeNodeId: string;
  completedNodeIds: string[];
  variant: string;
}
export class RunStatePersistence {
  async persistNodeStatus(nodeId: string, status: string): Promise<void>;
  async persistResumeCursor(cursor: ResumeCursor): Promise<void>;
}
```

From GraphExecutorInput (add runState usage only in this plan — resume flag deferred to 74-02):
```typescript
runState?: RuntimeRunState | undefined;
```

Rust mirror call sites (executor.rs):
- Before model: persist_node_status(running) + persist_resume_cursor(active_node_id, completed_so_far)
- After success: persist_node_status(completed) + push to completed_node_ids + persist_resume_cursor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run-state helper module in graph executor</name>
  <files>src/application/graph/graph-executor.ts</files>
  <action>
    Per D-01/D-02, add private helpers alongside existing graph executor functions (mirror Rust `persist_run_state_status` / `persist_resume_cursor`):

    1. `createRunStatePersistence(input: GraphExecutorInput): RunStatePersistence | undefined` — return undefined when `input.runState` absent; else `new RunStatePersistence(input.runState, () => {})` (no-op emit unless session already emits run-state events elsewhere).

    2. `persistRunStateStatus(persistence, nodeId, status)` — await `persistNodeStatus` when persistence defined; swallow errors to match Rust `_ =` behavior (do not fail graph run on store rejection).

    3. `persistResumeCursorTransition(persistence, activeNodeId, completedNodeIds)` — await `persistResumeCursor({ activeNodeId, completedNodeIds, variant: "playbook" })`.

    4. Track `completedNodeIds: string[]` in `executeGraph` loop (mutated only on successful node completion in Task 2).

    Import `RunStatePersistence` from `../../domain/run-state-persistence.js`. Do NOT add resume skip logic yet (74-02).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Helpers compile; graph-executor imports RunStatePersistence without resume consumption yet</done>
</task>

<task type="auto">
  <name>Task 2: Invoke persistence at node transitions</name>
  <files>src/application/graph/graph-executor.ts</files>
  <action>
    In `executeGraph`, after approval gate and when setting node to running (per D-01):

    1. Call `persistRunStateStatus(persistence, nodeId, "running")`.
    2. Call `persistResumeCursorTransition(persistence, nodeId, completedNodeIds)` — active node is current node about to execute.

    On successful node completion (before `updateNodeStatus completed`):

    3. Push `nodeId` to `completedNodeIds`.
    4. Call `persistRunStateStatus(persistence, nodeId, "completed")`.
    5. Call `persistResumeCursorTransition(persistence, nodeId, completedNodeIds)`.

    On failure paths that set `failed`/`skipped`/`cancelled`, call `persistRunStateStatus` with that status (parity with Rust status persistence on terminal states). Do not add `resume: true` skip logic.

    On first graph run when `runState` present and store has no snapshot, call `persistence.initialize(prompt, agentId)` once before loop — use root prompt from first root node or `"graph run"` mirroring Rust `prepare_run_state` fresh-run branch.
  </action>
  <verify>
    <automated>npm run build && node --test dist/tests/application/graph/graph-executor.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>Existing graph-executor tests pass; executor invokes persistResumeCursor at running + completed transitions when runState wired</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph-executor → run-state store | Persisted cursor influences resume skip behavior on next run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | T | persistResumeCursor | accept | Resume cursor is execution checkpoint metadata, not auth secret; store already enforces capability tokens on mutate |
| T-74-02 | I | completedNodeIds array | mitigate | Only push nodeId after successful completion; use store etag retry already in RunStatePersistence |
</threat_model>

<verification>
Scoped: `npm run build && node --test dist/tests/application/graph/graph-executor.test.js`. Full suite deferred.
</verification>

<success_criteria>
- Graph executor source contains persistResumeCursor calls at running and post-complete transitions
- ResumeCursor persisted with variant `"playbook"` and camelCase fields
- Existing graph-executor unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/74-ts-resume-cursor-parity/74-01-SUMMARY.md`
</output>

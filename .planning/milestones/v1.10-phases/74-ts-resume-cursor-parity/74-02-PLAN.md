---
phase: 74-ts-resume-cursor-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "74-01"
files_modified:
  - src/application/graph/graph-executor.ts
  - src/domain/run-state-persistence.ts
  - tests/application/graph/graph-executor-resume.test.ts
autonomous: true
requirements:
  - PERS-04
user_setup: []
must_haves:
  truths:
    - "GraphExecutorInput.resume true loads persisted resumeCursor and skips completed nodes"
    - "Resume after partial Node execution invokes model only for incomplete nodes"
    - "Persisted cursor shape round-trips through FileRunStateStore matching Rust expectations"
  artifacts:
    - path: tests/application/graph/graph-executor-resume.test.ts
      provides: "Partial run resume skip test"
      contains: "resume"
    - path: src/domain/run-state-persistence.ts
      provides: "loadResumeState helper"
      contains: "loadResumeState"
  key_links:
    - from: executeGraph
      to: loadResumeState
      via: "prepare run when input.resume true"
      pattern: "loadResumeState"
    - from: graph-executor-resume.test.ts
      to: FileRunStateStore
      via: "seed root completed + cursor on child"
      pattern: "resumeCursor"
---

<objective>
Add TS graph executor resume consumption and targeted test proving partial Node run skips completed nodes (PERS-04 read path).

Purpose: Write path alone does not satisfy PERS-04 — resume must skip completed nodes using persisted cursor.
Output: `resume: boolean` on GraphExecutorInput, `loadResumeState()` helper, passing scoped resume test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-ts-resume-cursor-parity/74-CONTEXT.md
@.planning/phases/74-ts-resume-cursor-parity/74-01-SUMMARY.md
@crates/rlm-core/tests/run_state_resume.rs
@tests/application/graph/graph-executor.test.ts

<interfaces>
Seed pattern from run_state_resume.rs:
- run_id fixed string
- root node status completed in nodeStatuses
- resumeCursor: { activeNodeId: "child", completedNodeIds: ["root"], variant: "playbook" }
- Queue/counting model expects exactly 1 complete() call after resume
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loadResumeState + resume skip test</name>
  <files>tests/application/graph/graph-executor-resume.test.ts, src/domain/run-state-persistence.ts</files>
  <behavior>
    - Test: seed FileRunStateStore with root completed + cursor active on child; executeGraph with resume:true skips root and completes child with CountingModel.completeCount === 1
    - Test: loadResumeState returns completedNodeIds ["root"] and activeNodeId "child" from seeded snapshot
  </behavior>
  <action>
    Per D-03/D-04, add to `run-state-persistence.ts`:

    1. Export `LoadedResumeState` interface `{ completedNodeIds: string[]; activeNodeId?: string; variant?: string }`.
    2. Add `loadResumeState(): Promise<LoadedResumeState | undefined>` on `RunStatePersistence` — read snapshot via store.getSnapshot, parse `resumeCursor` and merge completed ids from cursor + nodeStatuses with status completed (mirror Rust `parse_loaded_resume_state` / `load_resume_state`).

    Create `tests/application/graph/graph-executor-resume.test.ts` with CountingModel pattern from graph-executor.test.ts. Seed temp FileRunStateStore dir, build RuntimeRunState, seed via RunStatePersistence.initialize + persistNodeStatus + persistResumeCursor. Register root + child single-pass nodes; call executeGraph with `{ ...input, runState, resume: true }`. Assert model call count 1 and both nodes completed in session snapshot.

    Write test first (RED), then implement loadResumeState (GREEN).
  </action>
  <verify>
    <automated>npm run build && node --test dist/tests/application/graph/graph-executor-resume.test.js</automated>
  </verify>
  <done>loadResumeState parses seeded cursor; resume test passes with single model invocation</done>
</task>

<task type="auto">
  <name>Task 2: Wire resume flag in executeGraph</name>
  <files>src/application/graph/graph-executor.ts</files>
  <action>
    Per D-03, extend `GraphExecutorInput` with optional `resume?: boolean`.

    At start of `executeGraph` (after topological order, before loop), when `input.resume && input.runState`:

    1. Load resume state via RunStatePersistence.loadResumeState().
    2. Build `skipCompleted: Set<string>` from `completedNodeIds`.
    3. For each completed id present in graph, call `session.control.updateNodeStatus?.(id, "completed")` (mirror Rust `apply_resume_to_session`).
    4. Initialize `completedNodeIds` tracker from loaded state.

    In loop, if `skipCompleted.has(nodeId)` continue without model call (before approval wait if node already completed).

    Fresh run branch: when `!input.resume` and no snapshot, keep initialize from 74-01.

    Do not change Rust code. Ensure variant remains `"playbook"` on subsequent persist calls.
  </action>
  <verify>
    <automated>npm run build && node --test dist/tests/application/graph/graph-executor-resume.test.js && node --test dist/tests/application/graph/graph-executor.test.js</automated>
  </verify>
  <done>executeGraph skips completed nodes when resume:true; resume + existing executor tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted resumeCursor → executeGraph | Tampered store could skip nodes incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03 | T | skipCompleted set | mitigate | Only honor cursor when resume:true explicitly set; store mutations require capability token |
| T-74-04 | D | loadResumeState | accept | Resume is opt-in via caller flag; Node CLI path uses same store ACL as Rust |
</threat_model>

<verification>
Scoped: `node --test dist/tests/application/graph/graph-executor-resume.test.js` + existing graph-executor tests. Full suite deferred.
</verification>

<success_criteria>
- PERS-04 satisfied: persist at transitions (74-01) + skip completed on resume (74-02)
- Targeted resume test green
- No regressions in graph-executor.test.ts
</success_criteria>

<output>
After completion, create `.planning/phases/74-ts-resume-cursor-parity/74-02-SUMMARY.md`
</output>

---
phase: 13-rubric-and-evaluator-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - RUBR-01
  - RUBR-02
  - RUBR-03
must_haves:
  truths:
    - "CLI compact output exposes selected rubric and terminal gate state without requiring JSON parsing."
    - "JSON output continues to expose the full canonical qualityLoop object."
    - "The full Phase 13 target test set passes after renderer changes."
  artifacts:
    - path: "src/cli/render.ts"
      provides: "Compact quality-loop rubric and gate/evaluator metadata projection"
      contains: "qualityLoopRubric:"
    - path: "tests/recursive-language-model.test.ts"
      provides: "Renderer assertions for rubric and structured gate metadata"
      contains: "renders compact quality loop rubric and gate metadata"
  key_links:
    - from: "src/cli/render.ts"
      to: "src/domain/types.ts"
      via: "QualityLoopMetadata.rubric and QualityLoopMetadata.gate"
      pattern: "loop\\.rubric"
---

<objective>
Expose the Phase 13 rubric/evaluator contract through CLI rendering and run final targeted verification.

Purpose: Users and downstream tools need to inspect selected rubric and gate state through existing CLI compact/JSON output surfaces.
Output: Compact renderer lines, JSON contract assertions, and final targeted quality-loop verification.
</objective>

<execution_context>
@/home/brady/.codex/get-shit-done/workflows/execute-plan.md
@/home/brady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-rubric-and-evaluator-contract/13-CONTEXT.md
@.planning/phases/13-rubric-and-evaluator-contract/13-RESEARCH.md
@.planning/phases/13-rubric-and-evaluator-contract/13-VALIDATION.md
@.planning/phases/13-rubric-and-evaluator-contract/13-PATTERNS.md
@.planning/phases/13-rubric-and-evaluator-contract/13-01-PLAN.md
@.planning/phases/13-rubric-and-evaluator-contract/13-02-PLAN.md
@src/cli/render.ts
@src/domain/types.ts
@tests/recursive-language-model.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compact rubric and gate renderer lines</name>
  <files>src/cli/render.ts</files>
  <read_first>
    - src/cli/render.ts
    - src/domain/types.ts
    - .planning/phases/13-rubric-and-evaluator-contract/13-CONTEXT.md
  </read_first>
  <behavior>
    - Compact output shows selected rubric id, confidence, and matched signal count when present.
    - Compact output shows terminal gate decision, score, pass threshold, and failed condition count when present.
    - JSON output remains unchanged except for naturally including new `qualityLoop` fields.
  </behavior>
  <action>
    In `src/cli/render.ts`, inside the existing `if (result.metadata.qualityLoop)` block:
    Add a compact line exactly shaped like `qualityLoopRubric: id=${loop.rubric.id} confidence=${loop.rubric.confidence} signals=${loop.rubric.matchedSignals.length}` when `loop.rubric` exists.
    Add a compact line exactly shaped like `qualityLoopGate: decision=${loop.gate.decision} score=${loop.gate.score} threshold=${loop.gate.passThreshold} failedConditions=${loop.gate.failedConditions.length}` when `loop.gate` exists.
    Keep the existing `qualityLoop:` and `qualityLoopUsage:` lines unchanged.
    Do not duplicate full criteria, issue text, rationale, or comparison notes in compact output.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'qualityLoopRubric:' src/cli/render.ts` returns one match.
    - `rg -n 'qualityLoopGate:' src/cli/render.ts` returns one match.
    - `rg -n 'qualityLoopUsage:' src/cli/render.ts` still returns one match.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Compact output exposes the selected rubric and terminal gate summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend renderer tests for rubric and gate metadata</name>
  <files>tests/recursive-language-model.test.ts</files>
  <read_first>
    - tests/recursive-language-model.test.ts
    - src/cli/render.ts
    - src/domain/types.ts
    - .planning/phases/13-rubric-and-evaluator-contract/13-VALIDATION.md
  </read_first>
  <behavior>
    - Compact renderer tests assert the new rubric and gate lines.
    - JSON renderer tests assert full structured metadata remains present under `qualityLoop`.
  </behavior>
  <action>
    In `tests/recursive-language-model.test.ts`, update the existing quality-loop render fixture or add tests named exactly:
    - `renders compact quality loop rubric and gate metadata`
    - `renders json quality loop rubric and evaluator metadata`
    Build a `QualityLoopMetadata` fixture with `rubric`, `gate`, and an iteration containing `critiqueEvaluation`, `gateEvaluation`, and `bestOfProgressEvaluation`.
    Assert compact output matches `qualityLoopRubric: id=code_engineering confidence=` and `qualityLoopGate: decision=pass score=`.
    Assert JSON parse result includes `parsed.qualityLoop.rubric.id === "code_engineering"`, `parsed.qualityLoop.gate.decision === "pass"`, and `parsed.qualityLoop.iterations[0].gateEvaluation.decision === "pass"`.
  </action>
  <verify>
    <automated>npm run build && node --test --test-name-pattern='renders .*quality loop.*(rubric|evaluator|gate|metadata)' dist/tests/recursive-language-model.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'renders compact quality loop rubric and gate metadata|renders json quality loop rubric and evaluator metadata' tests/recursive-language-model.test.ts` returns exactly two matches.
    - `rg -n 'qualityLoopRubric: id=code_engineering|qualityLoopGate: decision=pass' tests/recursive-language-model.test.ts` returns at least two matches.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Renderer tests prove compact and JSON surfaces expose Phase 13 metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run Phase 13 targeted verification</name>
  <files>tests/recursive-language-model.test.ts, src/domain/types.ts, src/domain/recursive-language-model.ts, src/cli/render.ts</files>
  <read_first>
    - .planning/phases/13-rubric-and-evaluator-contract/13-VALIDATION.md
    - tests/recursive-language-model.test.ts
    - src/domain/types.ts
    - src/domain/recursive-language-model.ts
    - src/cli/render.ts
  </read_first>
  <behavior>
    - All Phase 13 quality-loop tests pass together.
    - Build passes after all type, runtime, parser, gate, and renderer changes.
  </behavior>
  <action>
    Run the Phase 13 quick command exactly:
    `npm run build && node --test --test-name-pattern='quality loop.*rubric|quality loop.*evaluator|quality loop.*gate|renders .*quality loop' dist/tests/recursive-language-model.test.js`
    If this command fails because a test-name pattern does not include all added tests, first run the narrower failing pattern from the error output, fix the code, then re-run the exact Phase 13 quick command.
    Do not run browser automation, UI dev servers, MCP servers, or localhost control-server tests in this plan.
  </action>
  <verify>
    <automated>npm run build && node --test --test-name-pattern='quality loop.*rubric|quality loop.*evaluator|quality loop.*gate|renders .*quality loop' dist/tests/recursive-language-model.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && node --test --test-name-pattern='quality loop.*rubric|quality loop.*evaluator|quality loop.*gate|renders .*quality loop' dist/tests/recursive-language-model.test.js` exits 0.
    - `rg -n 'qualityLoopRubric:' src/cli/render.ts` returns one match.
    - `rg -n 'QualityLoopGateEvaluation' src/domain/types.ts` returns at least one match.
    - `rg -n 'parseQualityLoopGate' src/domain/recursive-language-model.ts` returns at least one match.
  </acceptance_criteria>
  <done>Phase 13 targeted verification is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain metadata -> CLI compact output | Compact rendering must show useful status without leaking verbose evaluator raw text. |
| Domain metadata -> JSON output | JSON output must preserve full canonical typed metadata for tools and UI consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | I | `src/cli/render.ts` | mitigate | Render compact rubric/gate summaries while leaving full evaluator detail in JSON metadata. |
</threat_model>

<verification>
Run `npm run build && node --test --test-name-pattern='renders .*quality loop.*(rubric|evaluator|gate|metadata)' dist/tests/recursive-language-model.test.js`.
Run `npm run build && node --test --test-name-pattern='quality loop.*rubric|quality loop.*evaluator|quality loop.*gate|renders .*quality loop' dist/tests/recursive-language-model.test.js`.
</verification>

<success_criteria>
- Compact output includes `qualityLoopRubric:` and `qualityLoopGate:` when metadata exists.
- JSON output preserves full `qualityLoop.rubric`, `qualityLoop.gate`, and iteration evaluator details.
- Phase 13 targeted quality-loop verification passes.
</success_criteria>

---
phase: 13-rubric-and-evaluator-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/domain/types.ts
  - src/domain/recursive-language-model.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - RUBR-02
  - RUBR-03
must_haves:
  truths:
    - "Critique, gate, and best-of-progress evaluator outputs either parse into structured loop state or surface explicit degraded/failed states."
    - "Gate decisions visibly account for rubric fit, critique resolution, and meaningful improvement before passing or continuing."
    - "Parse failures never silently fall back to free-text success."
  artifacts:
    - path: "src/domain/types.ts"
      provides: "Structured critique, gate, and best-of-progress evaluator contracts"
      contains: "QualityLoopGateEvaluation"
    - path: "src/domain/recursive-language-model.ts"
      provides: "JSON parser helpers and structured gate stop routing"
      contains: "parseQualityLoopGate"
    - path: "tests/recursive-language-model.test.ts"
      provides: "Fake-model coverage for parse success, degraded/failure, and gate stop reasons"
      contains: "quality loop parses structured evaluator outputs"
  key_links:
    - from: "src/domain/recursive-language-model.ts"
      to: "src/domain/types.ts"
      via: "QualityLoopGateEvaluation"
      pattern: "gateEvaluation"
---

<objective>
Replace placeholder free-text evaluator handling with structured critique, gate, and best-of-progress parsing required by RUBR-02 and RUBR-03.

Purpose: Users need explicit evaluator state and gate reasoning instead of hidden string markers.
Output: Evaluator types, parser helpers, schema-constrained loop prompts, structured gate stop routing, and fake-model tests.
</objective>

<execution_context>
@/home/brady/.codex/get-shit-done/workflows/execute-plan.md
@/home/brady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-rubric-and-evaluator-contract/13-CONTEXT.md
@.planning/phases/13-rubric-and-evaluator-contract/13-RESEARCH.md
@.planning/phases/13-rubric-and-evaluator-contract/13-VALIDATION.md
@.planning/phases/13-rubric-and-evaluator-contract/13-PATTERNS.md
@.planning/phases/13-rubric-and-evaluator-contract/13-01-PLAN.md
@src/domain/types.ts
@src/domain/recursive-language-model.ts
@tests/recursive-language-model.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured evaluator metadata types</name>
  <files>src/domain/types.ts</files>
  <read_first>
    - src/domain/types.ts
    - .planning/phases/13-rubric-and-evaluator-contract/13-CONTEXT.md
    - .planning/phases/13-rubric-and-evaluator-contract/13-RESEARCH.md
  </read_first>
  <behavior>
    - Critique, gate, and best-of-progress evaluations have typed metadata fields.
    - Per-phase parse status and parse errors are explicit.
    - Optional fields preserve compatibility with Phase 12 metadata objects.
  </behavior>
  <action>
    In `src/domain/types.ts`, add:
    `export interface QualityLoopCritiqueEvaluation { summary: string; issues: QualityLoopIssue[]; resolved: boolean; suggestedImprovements: string[]; }`.
    `export interface QualityLoopGateEvaluation { decision: "pass" | "continue"; score: number; passThreshold: number; rubricFit: boolean; critiqueResolved: boolean; meaningfulImprovement: boolean; rationale: string; failedConditions: string[]; unresolvedIssues: QualityLoopIssue[]; }`.
    `export interface QualityLoopBestOfProgressEvaluation { selectedCandidateId: string; rationale: string; score: number; comparisonNotes: string[]; }`.
    `export type QualityLoopEvaluatorParseStatus = "parsed" | "degraded" | "failed";`.
    Add optional `parseStatus?: QualityLoopEvaluatorParseStatus | undefined;` and `parseError?: string | undefined;` to `QualityLoopPhaseRecord`.
    Add optional `critiqueEvaluation?: QualityLoopCritiqueEvaluation | undefined;`, `gateEvaluation?: QualityLoopGateEvaluation | undefined;`, and `bestOfProgressEvaluation?: QualityLoopBestOfProgressEvaluation | undefined;` to `QualityLoopIterationRecord`.
    Add optional `gate?: QualityLoopGateEvaluation | undefined;` to `QualityLoopMetadata`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'interface QualityLoopCritiqueEvaluation|interface QualityLoopGateEvaluation|interface QualityLoopBestOfProgressEvaluation' src/domain/types.ts` returns exactly three matches.
    - `rg -n 'QualityLoopEvaluatorParseStatus' src/domain/types.ts` returns at least two matches.
    - `rg -n 'critiqueEvaluation\\?:|gateEvaluation\\?:|bestOfProgressEvaluation\\?:' src/domain/types.ts` returns exactly three matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Evaluator outputs have typed slots in canonical loop metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse evaluator JSON and route explicit failures</name>
  <files>src/domain/recursive-language-model.ts</files>
  <read_first>
    - src/domain/recursive-language-model.ts
    - src/domain/types.ts
    - .planning/phases/13-rubric-and-evaluator-contract/13-PATTERNS.md
  </read_first>
  <behavior>
    - Critique, gate, and best-of-progress phase outputs are parsed from JSON.
    - Malformed evaluator output produces degraded state when a candidate exists and failed state when no candidate exists.
    - Parser failures create `QualityLoopIssue` records tied to the evaluator phase.
  </behavior>
  <action>
    In `src/domain/recursive-language-model.ts`, add parser helpers:
    `extractJsonObject(value: string): unknown`;
    `parseQualityLoopCritique(value: string, phase: QualityLoopPhaseName): QualityLoopCritiqueEvaluation`;
    `parseQualityLoopGate(value: string): QualityLoopGateEvaluation`;
    `parseQualityLoopBestOfProgress(value: string, fallbackCandidateId: string): QualityLoopBestOfProgressEvaluation`.
    Use runtime type guards, not ad hoc unchecked casts. Required critique JSON shape:
    `{ "summary": string, "resolved": boolean, "issues": [{ "id"?: string, "severity": "info"|"warning"|"error", "text": string }], "suggestedImprovements": string[] }`.
    Required gate JSON shape:
    `{ "decision": "pass"|"continue", "score": number, "passThreshold": number, "rubricFit": boolean, "critiqueResolved": boolean, "meaningfulImprovement": boolean, "rationale": string, "failedConditions": string[], "unresolvedIssues": [{ "id"?: string, "severity": "info"|"warning"|"error", "text": string }] }`.
    Required best-of-progress JSON shape:
    `{ "selectedCandidateId"?: string, "rationale": string, "score": number, "comparisonNotes": string[] }`.
    Replace placeholder `DEGRADED` text handling so parse errors call one shared helper that marks the phase `parseStatus`, writes `parseError`, creates a `QualityLoopIssue`, updates metadata, and returns `finish("degraded", "degraded", ...)` if `selectedCandidateId` exists or `finish("failed", "failed", ...)` otherwise.
    Preserve candidate full-text storage behavior and do not add model routing changes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'function extractJsonObject|function parseQualityLoopCritique|function parseQualityLoopGate|function parseQualityLoopBestOfProgress' src/domain/recursive-language-model.ts` returns exactly four matches.
    - `rg -n 'parseStatus = "parsed"|parseStatus = "degraded"|parseStatus = "failed"' src/domain/recursive-language-model.ts` returns at least three matches.
    - `rg -n 'DEGRADED' src/domain/recursive-language-model.ts` returns no matches in gate routing logic.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Evaluator parse failures are explicit degraded or failed loop states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement structured gate stop semantics</name>
  <files>src/domain/recursive-language-model.ts</files>
  <read_first>
    - src/domain/recursive-language-model.ts
    - src/domain/types.ts
    - .planning/phases/13-rubric-and-evaluator-contract/13-CONTEXT.md
  </read_first>
  <behavior>
    - Passing gate requires rubric fit, critique resolution, meaningful improvement, score threshold, and no error unresolved issues.
    - `critique_resolved` and `no_meaningful_improvement` are reachable stop reasons.
    - Continuing loops still respect existing max-iteration and budget behavior.
  </behavior>
  <action>
    In `runQualityLoop()`, after all five phase calls parse structured evaluator outputs for critique, gate, and best-of-progress and attach them to the current `QualityLoopIterationRecord`.
    Add helper `gatePasses(evaluation: QualityLoopGateEvaluation): boolean` that returns true only when `decision === "pass"`, `score >= passThreshold`, `rubricFit`, `critiqueResolved`, `meaningfulImprovement`, and `unresolvedIssues` has no `severity === "error"`.
    Add helper `critiqueResolved(evaluation: QualityLoopGateEvaluation): boolean` that returns true when `score >= passThreshold`, `critiqueResolved === true`, and unresolved issues are empty or all `severity === "info"`.
    Add helper `hasMeaningfulImprovement(current: QualityLoopGateEvaluation, previous: QualityLoopGateEvaluation | undefined): boolean` that returns true for the first iteration, for score increases of at least `0.05`, or for a lower count of warning/error unresolved issues. If false, stop with `no_meaningful_improvement`.
    Stop routing order after parse success must be:
    1. `passed` when `gatePasses(gateEvaluation)` is true.
    2. `critique_resolved` when `critiqueResolved(gateEvaluation)` is true.
    3. `no_meaningful_improvement` when `hasMeaningfulImprovement(gateEvaluation, previousGateEvaluation)` is false.
    4. Continue to next iteration when budget and max iterations allow.
    5. Existing `max_iterations` terminal state after loop exhaustion.
    Store the latest terminal gate evaluation in `metadata.gate`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'function gatePasses|function critiqueResolved|function hasMeaningfulImprovement' src/domain/recursive-language-model.ts` returns exactly three matches.
    - `rg -n 'finish\\(\"completed\", \"passed\"|finish\\(\"completed\", \"critique_resolved\"|finish\\(\"completed\", \"no_meaningful_improvement\"' src/domain/recursive-language-model.ts` returns at least three matches.
    - `rg -n 'metadata\\.gate =' src/domain/recursive-language-model.ts` returns at least one match.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Gate decisions visibly combine rubric fit, critique resolution, and improvement before passing or stopping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add evaluator and gate tests</name>
  <files>tests/recursive-language-model.test.ts</files>
  <read_first>
    - tests/recursive-language-model.test.ts
    - src/domain/recursive-language-model.ts
    - src/domain/types.ts
    - .planning/phases/13-rubric-and-evaluator-contract/13-VALIDATION.md
  </read_first>
  <behavior>
    - Tests prove structured evaluator outputs parse into loop state.
    - Tests prove malformed evaluator outputs do not silently pass.
    - Tests prove gate pass, critique resolved, and no meaningful improvement stop reasons.
  </behavior>
  <action>
    In `tests/recursive-language-model.test.ts`, add tests named exactly:
    - `quality loop parses structured evaluator outputs`
    - `quality loop degraded on malformed evaluator output with candidate`
    - `quality loop fails on malformed evaluator output before candidate`
    - `quality loop gate stops with passed`
    - `quality loop gate stops with critique resolved`
    - `quality loop gate stops with no meaningful improvement`
    Use `QueueModel` responses with JSON strings for critique, gate, and best-of-progress. Assert `iteration.critiqueEvaluation`, `iteration.gateEvaluation`, `iteration.bestOfProgressEvaluation`, `phase.parseStatus`, `metadata.gate`, `metadata.stopReason`, and `metadata.unresolvedIssues` as appropriate.
  </action>
  <verify>
    <automated>npm run build && node --test --test-name-pattern='quality loop (parses structured evaluator outputs|degraded on malformed evaluator output with candidate|fails on malformed evaluator output before candidate|gate stops with passed|gate stops with critique resolved|gate stops with no meaningful improvement)' dist/tests/recursive-language-model.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'quality loop parses structured evaluator outputs|quality loop degraded on malformed evaluator output with candidate|quality loop fails on malformed evaluator output before candidate|quality loop gate stops with passed|quality loop gate stops with critique resolved|quality loop gate stops with no meaningful improvement' tests/recursive-language-model.test.ts` returns exactly six matches.
    - `rg -n 'critiqueEvaluation|gateEvaluation|bestOfProgressEvaluation|parseStatus' tests/recursive-language-model.test.ts` returns at least eight matches.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>RUBR-02 and RUBR-03 have focused fake-model coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model evaluator output -> parser | Untrusted model text becomes typed loop metadata only after validation. |
| Gate metadata -> loop control | Parsed gate fields determine whether a loop passes, continues, degrades, or fails. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | T/D | evaluator parser helpers | mitigate | Validate required JSON fields and fail/degrade explicitly instead of accepting malformed prose. |
| T-13-04 | R/I | gate routing | mitigate | Store gate rationale, failed conditions, rubric fit, critique resolution, and improvement flags in typed metadata. |
| T-13-06 | D | loop control | mitigate | Preserve existing budget and max-iteration checks while adding gate stop semantics. |
</threat_model>

<verification>
Run `npm run build && node --test --test-name-pattern='quality loop (parses structured evaluator outputs|degraded on malformed evaluator output with candidate|fails on malformed evaluator output before candidate|gate stops with passed|gate stops with critique resolved|gate stops with no meaningful improvement)' dist/tests/recursive-language-model.test.js`.
</verification>

<success_criteria>
- RUBR-02 is covered by structured evaluator parser tests and explicit parse failure routing.
- RUBR-03 is covered by gate stop semantics and tests for `passed`, `critique_resolved`, and `no_meaningful_improvement`.
- Placeholder `DEGRADED` text marker no longer drives gate decisions.
</success_criteria>

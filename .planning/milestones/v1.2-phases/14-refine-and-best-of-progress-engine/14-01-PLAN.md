---
phase: 14-refine-and-best-of-progress-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/recursive-language-model.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - REFN-01
  - REFN-02
  - REFN-03
must_haves:
  truths:
    - "Refined candidates remain available for comparison after later refinements are generated."
    - "The loop final answer is selected from the best candidate seen so far, not automatically from the last iteration."
    - "Malformed best-of-progress selection produces explicit degraded or failed state."
---

<objective>
Implement deterministic best-of-progress selection across accumulated quality-loop candidates.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add selection metadata and candidate ranking</name>
  <files>src/domain/types.ts, src/domain/recursive-language-model.ts, tests/recursive-language-model.test.ts</files>
  <read_first>
    - src/domain/types.ts
    - src/domain/recursive-language-model.ts
    - tests/recursive-language-model.test.ts
    - .planning/phases/14-refine-and-best-of-progress-engine/14-CONTEXT.md
  </read_first>
  <action>
    Extend candidate metadata and loop metadata with selection rationale. Select final answers by validating parsed best-of-progress candidate ids against accumulated candidates, ranking valid fallback candidates when needed, and recording invalid selections as degraded issues.
  </action>
  <acceptance_criteria>
    - `rg -n 'QualityLoopSelectionMetadata' src/domain/types.ts` returns at least one match.
    - `rg -n 'selectBestQualityLoopCandidate' src/domain/recursive-language-model.ts` returns one match.
    - `rg -n 'quality loop can select earlier candidate as final answer' tests/recursive-language-model.test.ts` returns one match.
    - `npm run build && node --test --test-name-pattern='quality loop' dist/tests/recursive-language-model.test.js` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `npm run build && node --test --test-name-pattern='quality loop' dist/tests/recursive-language-model.test.js`.
Run `npm test`.
</verification>

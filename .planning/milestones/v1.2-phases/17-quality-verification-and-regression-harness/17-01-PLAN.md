---
phase: 17-quality-verification-and-regression-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/execution-controller.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - VERF-01
  - VERF-02
  - VERF-03
must_haves:
  truths:
    - "Loop tests assert explicit status, stop reason, and diagnostic text for success, degraded, stopped, failed, budget, cancellation, and routing failure paths."
    - "Prompt/model edits invalidate stale quality-loop metadata."
    - "Regression coverage spans graph metadata, lifecycle events, CLI text/JSON, UI/API source, and run-state records."
---

<objective>
Harden the quality-loop regression harness and add stale-loop invalidation.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Invalidate stale loop metadata on edits</name>
  <files>src/application/execution-controller.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Clear `node.loop` on quality-loop prompt/model edits and add session tests proving invalidation.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cross-surface observability regressions</name>
  <files>tests/recursive-language-model.test.ts</files>
  <action>
    Add assertions for lifecycle events, graph metadata, compact CLI, JSON output, and run-state replay.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strengthen strict failure assertions</name>
  <files>tests/recursive-language-model.test.ts</files>
  <action>
    Add helper assertions and cover parse failure, budget exhaustion, manual stop/cancellation, and selected-model failure diagnostics.
  </action>
</task>
</tasks>

<verification>
Run `npm run build`.
Run `npm run build:ui`.
Run `node --test --test-name-pattern='quality loop .*regression|stale quality loop|run-state.*quality loop|quality loop .*explicit' dist/tests/recursive-language-model.test.js`.
Run `npm test`.
</verification>

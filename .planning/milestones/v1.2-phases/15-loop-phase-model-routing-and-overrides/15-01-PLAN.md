---
phase: 15-loop-phase-model-routing-and-overrides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ports/language-model-port.ts
  - src/application/project-config.ts
  - src/application/model-provider.ts
  - src/domain/types.ts
  - src/domain/recursive-language-model.ts
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - MODL-04
  - MODL-05
  - MODL-06
must_haves:
  truths:
    - "Draft, critique, refine, gate, and best-of-progress phases route through distinct model purposes."
    - "A runtime qualityLoop.phaseModels override can select one loop phase model independently."
    - "Loop metadata and CLI/JSON output expose planned and effective model assignment per phase."
    - "A selected unavailable or invalid phase model fails explicitly."
---

<objective>
Implement per-phase quality-loop model routing, overrides, and audit metadata.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend model purpose and config contracts</name>
  <files>src/ports/language-model-port.ts, src/application/project-config.ts, src/domain/types.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Add quality-loop purpose names, default agent model selections, runtime qualityLoop.phaseModels schema, and typed metadata contracts for loop phase model assignments.
  </action>
  <acceptance_criteria>
    - Existing configs without quality-loop purpose keys still load.
    - Configs with qualityLoop.phaseModels parse and resolve into runtime config.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route loop phases and record assignment metadata</name>
  <files>src/domain/recursive-language-model.ts, src/application/model-provider.ts, src/cli/render.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Route each loop phase with its distinct purpose, apply phase-specific overrides before task-level overrides, record planned/effective model assignment on phase records and loop summary metadata, and render compact output.
  </action>
  <acceptance_criteria>
    - A fake model test observes distinct completion purposes for all five loop phases.
    - A fake model test proves one phase override changes only that phase's effective model.
    - Compact render includes qualityLoopModels summary.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail explicit invalid selected phase models</name>
  <files>src/domain/recursive-language-model.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Validate selected phase override values before invoking the phase model and fail the loop with a diagnostic that names the phase, requested selection, and available configured routes.
  </action>
  <acceptance_criteria>
    - Invalid phase model test fails with explicit message and failed loop status.
    - No silent fallback path exists for a selected invalid phase model.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `npm run build && node --test --test-name-pattern='quality loop .*model|purpose routing model|project config' dist/tests/recursive-language-model.test.js`.
Run `npm test`.
</verification>

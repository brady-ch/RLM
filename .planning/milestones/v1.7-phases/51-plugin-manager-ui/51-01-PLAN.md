---
phase: 51-plugin-manager-ui
plan: 01
type: execute
autonomous: true
wave: 1
depends_on: [50-01]
requirements: [UI-01, UI-02, UI-03, UI-04]
---

# Phase 51 Plan 01: Plugin Manager UI

## Objective

UI plugin panel exposes same plugin management as CLI with aligned vocabulary, remote install trust confirm, and restart semantics.

## Tasks

### Task 1: UI design contract (type=auto)

Create `51-UI-SPEC.md` with copy, layout, and API alignment.

### Task 2: Plugin panel component (type=auto)

Add `PluginPanel` to `ui/src/main.tsx` with list, install, enable/disable/uninstall, doctor, remote confirm modal, restart banner. Styles in `ui/src/styles.css`.

### Task 3: Verification (type=auto)

Run `npm run check`. Create `51-UI-REVIEW.md` and `51-01-SUMMARY.md`.

## Verification

```bash
npm run check
```

## Threat Model

No new server surface; UI consumes existing `/api/plugins/*` handlers.

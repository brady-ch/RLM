---
phase: 45
plan: 01
wave: 1
requirements: [TAXN-02]
---

# Plan 45-01: Application Concern Grouping

## Objective

Group flat `application/` modules by domain concern with root facade re-exports; preserve CLI, control-server, and session flows.

## Tasks

1. **Create concern folders** — Add `execution/`, `graph/`, `memory/`, `plugins/` under `src/application/`; `control-server/` already exists.
2. **Move modules** — Relocate flat root implementations into concern folders; fix cross-concern relative imports.
3. **Root facades** — Replace former flat files with re-exports at original paths for backward compatibility.
4. **Plugins concern** — Add `plugins/index.ts` extension registry facade (Phase 46 expands to manager).
5. **Regression gate** — `npm run check` green.

## Verification

- Concern folders exist: execution, graph, memory, plugins, control-server
- Former flat files live under concern folders or root facades
- `npm run check` passes

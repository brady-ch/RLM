---
phase: 48-dependency-cruiser-ratchet
plan: 01
type: execute
autonomous: true
wave: 1
depends_on: [47-01]
requirements: [DEPS-01, DEPS-03, DEPS-04]
---

# Phase 48 Plan 01: Dependency-Cruiser Ratchet

## Objective

Ratchet boundary enforcement to error severity with empty baseline; fix runtime→cli violations via bootstrap injection.

## Context

Phase 47 added plugin/runtime depcruise rules at warn severity. Two `no-runtime-to-cli` warnings remain in `build-runtime-context.ts` (logger + shutdown). Baseline is already empty from Phase 43.

## Tasks

### Task 1: Bootstrap CLI injection (type=auto)

Remove `src/cli/*` imports from `src/runtime/composition/build-runtime-context.ts`. Add `RuntimeCliWiring` to composition options; bootstrap facade injects `createStderrRuntimeLogger` and `installShutdownHandlers`.

**Done when:** `dependency-cruise src/runtime/composition/build-runtime-context.ts` reports zero `no-runtime-to-cli` violations.

### Task 2: Ratchet severity and strict CI (type=auto)

- Change all `.dependency-cruiser.js` rules to `severity: "error"`
- Add `depcruise:strict` script; point `npm run check` at strict (no `--ignore-known`)
- Exclude `src/plugins/__depcruise-fixtures__/` from production scan
- Update depcruise meta-tests for error severity and violation naming
- Document optional `application→adapters` rule and bootstrap exceptions in AGENTS.md

**Done when:** `npm run check` passes with strict depcruise; forbidden import probe fails with named rule.

## Success Criteria

1. Baseline empty; v1.6 violations remain fixed
2. `npm run check` uses strict depcruise at error severity
3. Forbidden plugins→application import fails CI with clear rule name
4. Optional application→adapters documented in AGENTS.md

## Verification

```bash
npm run depcruise:strict
npm run check
```

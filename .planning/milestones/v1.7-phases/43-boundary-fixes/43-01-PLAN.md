---
phase: 43
plan: 01
wave: 1
requirements: [REG-01, REG-02, RUNT-01, RUNT-02]
---

# Plan 43-01: ARCH-02 Boundary Fixes

## Objective

Eliminate three dependency-cruiser baseline violations by correcting import directions and colocating content-tree with its adapter consumer.

## Tasks

1. **AgentConfig → domain** — Add `src/domain/agent-config.ts`; re-export from `application/config/types.ts`; update `domain/agents.ts`.
2. **ExtensionHostPort → ports** — Add `src/ports/extension-host-port.ts`; update `extension-port.ts`, `ExtensionHost`, and builtin extension `register()` signatures.
3. **content-tree relocation** — Move to `src/adapters/tools/content-tree.ts`; update web-fetch-tool and tests; delete application copy.
4. **Baseline shrink** — Set `dependency-cruiser-baseline.json` to `[]`.
5. **Regression gate** — `npm run check` green (359 tests).

## Verification

- Zero entries in dependency-cruiser baseline
- `npm run check` passes
- No behavior change to CLI/config/graph flows

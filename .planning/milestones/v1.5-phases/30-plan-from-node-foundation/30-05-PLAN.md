---
phase: 30-plan-from-node-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
  - "04"
files_modified:
  - tests/recursive-language-model.test.ts
  - tests/graph-planner.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-07
must_haves:
  truths:
    - "All phase 30 plan behaviors covered by automated tests"
    - "Existing heuristic-dependent tests updated to mock planner"
    - "Budget exhaustion and recursive planning tests still pass"
  artifacts:
    - path: tests/graph-planner.test.ts
      provides: "Unit tests for graph planner module"
    - path: tests/recursive-language-model.test.ts
      provides: "Integration tests for plan-from-node flows"
  key_links:
    - from: tests/recursive-language-model.test.ts
      to: src/application/execution-controller.ts
      via: "createInteractiveExecutionSession({ planModel })"
      pattern: "planModel"
---

<objective>
Consolidate Phase 30 test coverage: mock planner fixtures, replan semantics, failure codes, budget behavior, and CLI/API parity.

Purpose: Nyquist validation and regression guard for PLAN-01 through PLAN-07 (phase scope).
Output: Passing test suite with no heuristic assumptions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/30-plan-from-node-foundation/30-VALIDATION.md
</execution_context>

<context>
@.planning/phases/30-plan-from-node-foundation/30-VALIDATION.md
@tests/recursive-language-model.test.ts
@tests/graph-planner.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared mock planner fixtures and update legacy plan tests</name>
  <files>tests/recursive-language-model.test.ts, tests/graph-planner.test.ts</files>
  <read_first>
    - tests/recursive-language-model.test.ts (lines 1761-1842)
    - src/application/graph-planner.ts (PlannedChildSpec)
  </read_first>
  <action>
    Create shared test helper (top of recursive-language-model.test.ts or `tests/helpers/mock-plan-model.ts`):

    ```typescript
    export function createMockPlanModel(specs: PlannedChildSpec[] | "invalid" | "throw"): LanguageModelPort
    ```

    Update tests:
    - `interactive session seeds a typed root composer` — unchanged (no plan call)
    - `node-local plan creates pending typed child graph` — use mock returning 5-child audiobook fixture OR 3-child generic fixture; assert pendingPlan set, chat readiness draft
    - `plan budget exhaustion pauses expansion` — pass mock (unused when exhausted before planner)
    - `recursive planning shares root budget` — mock returns high-complexity children for first plan; subsequent plans return 1 child each

    Ensure every `createInteractiveExecutionSession` used in plan tests includes `planModel: createMockPlanModel(...)`.

    Add test `root-composer seeds empty prompt focus state` (PLAN-01 backend): session with no seedRootPrompt → root prompt is default placeholder but plan rejects empty with invalid_prompt when prompt cleared.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/recursive-language-model.test.ts | grep -c 'planModel'` >= 3
    - `npm test -- tests/recursive-language-model.test.ts -t "plan"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/recursive-language-model.test.ts -t "plan"</automated>
  </verify>
  <done>Legacy plan tests use mock planner; no dependency on keyword heuristics</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end failure, replan, and CLI coverage</name>
  <files>tests/recursive-language-model.test.ts, tests/graph-planner.test.ts</files>
  <read_first>
    - .planning/phases/30-plan-from-node-foundation/30-VALIDATION.md
    - tests/recursive-language-model.test.ts (control server tests ~1887)
  </read_first>
  <action>
    Add integration tests:

    1. `parent replan removes pristine model-planned children` (PLAN-04) — plan root twice with different mock fixtures; assert first batch ids absent, second batch present, total child count equals second fixture length

    2. `manual child survives parent replan` (PLAN-04) — addNode then replan root; manual child id still in graph

    3. `child plan receives ancestor context` (PLAN-03) — plan root then plan child; mock records last context/messages including root label

    4. `API returns invalid_planner_output` (PLAN-07) — control server test with invalid mock → 409 + code

    5. `CLI plan-node parses and runs` — parseArgs test + optional handler smoke with mock

    6. `graph-planner unit tests complete` — ensure graph-planner.test.ts covers all V-01/V-02 cases from VALIDATION.md

    Run full suite: `npm test` must pass.
  </action>
  <acceptance_criteria>
    - `npm test` exits 0
    - `npm run build` exits 0
    - Test file contains test name matching /replan|ancestor|invalid_planner/
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>Phase 30 behaviors verified; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→production | Mock models only in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | T | test fixtures | accept | Tests use deterministic mocks; no live model in CI |
</threat_model>

<verification>
```bash
npm test
npm run build
```
</verification>

<success_criteria>
- npm test and npm run build pass
- All PLAN-01–04 and PLAN-07 behaviors have named tests
</success_criteria>

<output>
After completion, create `.planning/phases/30-plan-from-node-foundation/30-05-SUMMARY.md`
</output>

---
phase: 30-plan-from-node-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/application/execution-controller.ts
  - src/domain/types.ts
autonomous: true
requirements:
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-07
must_haves:
  truths:
    - "planNode invokes model planner instead of keyword heuristics"
    - "Child-node plan receives ancestor context"
    - "Parent replan removes pristine auto-planned descendants only"
    - "Planner failures throw MutationError with no heuristic fallback"
  artifacts:
    - path: src/application/execution-controller.ts
      provides: "async planNode with graph planner integration"
      contains: "planChildren"
  key_links:
    - from: src/application/execution-controller.ts
      to: src/application/graph-planner.ts
      via: "await planChildren(this.planModel, context)"
      pattern: "planChildren\\("
    - from: src/application/execution-controller.ts
      to: src/application/graph-planner.ts
      via: "GraphPlannerError → MutationError"
      pattern: "invalid_planner_output|planning_failed"
---

<objective>
Replace `plannedChildrenFor` keyword heuristics in `planNode()` with async model-driven planning, replan semantics, and explicit failure codes.

Purpose: Core PLAN-02/03/04/07 behavior lives in execution-controller (per CONTEXT).
Output: Async `planNode`, removed heuristic function, replan helper.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-plan-from-node-foundation/30-01-SUMMARY.md
@.planning/phases/30-plan-from-node-foundation/30-CONTEXT.md
@src/application/graph-planner.ts
@src/application/execution-controller.ts

<interfaces>
From plan 01 `graph-planner.ts`:
```typescript
export async function planChildren(model: LanguageModelPort, context: GraphPlannerContext): Promise<PlanChildrenResult>;
export class GraphPlannerError extends Error { code: "planning_failed" | "invalid_planner_output"; }
```

Existing `planNode` budget logic (preserve lines 319-343, 374-397):
- Budget root via `findBudgetRoot`
- Exhausted → `awaiting_approval`, return `{ exhausted: true }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject plan model and make planNode async</name>
  <files>src/application/execution-controller.ts, tests/recursive-language-model.test.ts</files>
  <read_first>
    - src/application/execution-controller.ts (planNode ~319, createInteractiveExecutionSession ~1425)
    - src/application/graph-planner.ts
    - tests/recursive-language-model.test.ts (plan tests ~1774)
  </read_first>
  <behavior>
    - Session without planModel → MutationError planning_failed with message about missing planner model
    - Session with mock planModel → planNode resolves with plannedNodeIds
  </behavior>
  <action>
    Extend `InteractiveExecutionSession` constructor / `createInteractiveExecutionSession` input:
    ```typescript
    { approvalMode?, seedRootPrompt?, planModel?: LanguageModelPort }
    ```
    Store as private readonly `planModel?: LanguageModelPort`.

    Change signature: `async planNode(nodeId: string): Promise<{ plannedNodeIds: string[]; budget: ComposerPlanBudget; exhausted: boolean }>`.

    At start of planNode (after requireEditableNode):
    1. Validate non-empty prompt: `(node.prompt ?? node.label).trim()` — if empty throw `MutationError("invalid_prompt", "Node prompt cannot be empty.", [nodeId], undefined, "Enter a non-empty prompt before planning.")` per UI-SPEC.

    2. If `!this.planModel` throw `MutationError("planning_failed", "Graph planner model is not configured.", [nodeId], undefined, "Configure a plan purpose model tier.")`.

    Remove `plannedChildrenFor` function entirely (lines ~1653-1675).

    Temporarily wire `planChildren` call replacing `plannedChildrenFor` line — full replan in task 2.

    Update tests that call `session.planNode` to `await session.planNode` and pass mock `planModel` returning fixed JSON `{ children: [...] }` matching prior test expectations (3 generic children OR audiobook-specific fixture per test).

    Export a test helper `createMockPlanModel(children: PlannedChildSpec[])` in test file or graph-planner.test.ts for reuse.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/application/execution-controller.ts | grep -c 'plannedChildrenFor'` equals 0
    - `grep -v '^#' src/application/execution-controller.ts | grep -c 'async planNode'` >= 1
    - `grep -v '^#' src/application/execution-controller.ts | grep -c 'invalid_prompt'` >= 1
    - `npm test -- tests/recursive-language-model.test.ts -t "node-local plan"` exits 0 (may need mock wiring)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/recursive-language-model.test.ts -t "node-local plan"</automated>
  </verify>
  <done>planNode is async, uses injected model, rejects empty prompt, heuristic removed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ancestor context, replan pristine descendants, error mapping</name>
  <files>src/application/execution-controller.ts, tests/recursive-language-model.test.ts</files>
  <read_first>
    - src/application/execution-controller.ts (collectDescendants, registerNode, planNode)
    - .planning/phases/30-plan-from-node-foundation/30-CONTEXT.md (Subtree & Replan Semantics)
  </read_first>
  <behavior>
    - Child plan: ancestor chain from node to budget root included in GraphPlannerContext
    - Parent replan: removes direct children where composer.plannedBy === "model" and no modelOverrideSource === "user"
    - Manual addNode children (plannedBy user or no plannedBy) remain
    - GraphPlannerError → MutationError with same code and redacted details
  </behavior>
  <action>
    Per PLAN-03: build `ancestors` array walking `parentId` from node up to (but excluding) budget root; each entry `{ id, label, prompt }`.

    Per PLAN-04: before creating children, call private `removePristinePlannedDescendants(nodeId)`:
    - Collect direct children of `nodeId`
    - Remove child (and its subtree via existing delete logic or recursive remove) when ALL true:
      - `child.status === "planned"`
      - `child.composer?.plannedBy === "model"` (set when registering planner children)
      - `child.modelOverrideSource !== "user"`
    - Do NOT remove manual children (`addNode` sets `plannedBy: "user"` or omit plannedBy on manual adds)

    When registering planner-created children, set `composer.plannedBy = "model"`.

    Wrap `planChildren` in try/catch:
    ```typescript
    catch (e) {
      if (e instanceof GraphPlannerError)
        throw new MutationError(e.code, e.message, [nodeId], e.details, suggestedFixByCode(e.code));
    }
    ```
    Suggested fixes per UI-SPEC: planning_failed → "Check the planner model tier and prompt, then try Plan children again."; invalid_planner_output → "Fix configuration or retry. No fallback was applied."

    Add tests:
    1. `child node plan includes ancestor context` — mock captures messages, assert ancestor label present
    2. `parent replan replaces pristine planned descendants` — plan twice on root, second plan removes first batch plan-* ids
    3. `manual child survives parent replan` — addNode then replan, manual child id still exists
    4. `invalid planner output surfaces MutationError` — mock bad JSON → code invalid_planner_output
  </action>
  <acceptance_criteria>
    - `grep -v '^#' src/application/execution-controller.ts | grep -c 'removePristinePlannedDescendants\|plannedBy'` >= 2
    - `grep -v '^#' src/application/execution-controller.ts | grep -c 'invalid_planner_output'` >= 1
    - `npm test -- tests/recursive-language-model.test.ts -t "plan"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/recursive-language-model.test.ts -t "plan"</automated>
  </verify>
  <done>Subtree planning, silent pristine replan, and explicit planner failures implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→graph | Validated child specs become execution graph nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | E | removePristinePlannedDescendants | mitigate | Only remove plannedBy model + no user override |
| T-30-05 | I | MutationError details | mitigate | Redact validation errors; no raw model dumps in user message |
| T-30-06 | D | planning_failed path | mitigate | No children registered; graph unchanged except failure publish event optional |
</threat_model>

<verification>
```bash
npm test -- tests/recursive-language-model.test.ts -t "plan"
npm run build
```
</verification>

<success_criteria>
- No reference to plannedChildrenFor remains
- Replan and ancestor context tests pass
- MutationError codes planning_failed and invalid_planner_output thrown correctly
</success_criteria>

<output>
After completion, create `.planning/phases/30-plan-from-node-foundation/30-02-SUMMARY.md`
</output>

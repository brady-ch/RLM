---
phase: 30-plan-from-node-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - ui/src/main.tsx
  - ui/src/styles.css
autonomous: true
requirements:
  - PLAN-01
  - PLAN-07
must_haves:
  truths:
    - "Empty root-composer loads focused with prompt ready and no required chat step"
    - "Plan children is primary CTA on node card footer"
    - "Planning failures show UI-SPEC copy with no silent fallback messaging"
    - "Chat panel demoted below node inspector as Refine graph (optional)"
  artifacts:
    - path: ui/src/main.tsx
      provides: "Graph-primary authoring UX"
      contains: "Plan children"
    - path: ui/src/styles.css
      provides: "Root focus ring and planning failure styles"
      contains: "root-composer-focus"
  key_links:
    - from: ui/src/main.tsx
      to: "/api/nodes/:id/plan"
      via: "post plan from node card footer"
      pattern: "/plan"
---

<objective>
Implement graph-primary authoring UX per UI-SPEC: root-composer first-run, node-card Plan children CTA, demoted chat, and explicit planning failure surfaces.

Purpose: PLAN-01 root authoring and PLAN-07 visible failure states in UI.
Output: Updated React UI and CSS matching 30-UI-SPEC.md copywriting contract.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-plan-from-node-foundation/30-UI-SPEC.md
@.planning/phases/30-plan-from-node-foundation/30-CONTEXT.md
@ui/src/main.tsx
@ui/src/styles.css

<interfaces>
MutationError JSON from API (409):
```typescript
{ code: string; message: string; nodeIds: string[]; details?: string; suggestedFix?: string }
```

Existing `runAction(setErrorMessage, () => post(...), refresh)` pattern in main.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-composer first-run and node-card Plan children CTA</name>
  <files>ui/src/main.tsx, ui/src/styles.css</files>
  <read_first>
    - ui/src/main.tsx (ExecutionNodeCard ~692, selectedNodeId ~341, NodeInspector ~1280)
    - .planning/phases/30-plan-from-node-foundation/30-UI-SPEC.md (First-Run & Root Composer, Node Card Anatomy)
  </read_first>
  <action>
    Per PLAN-01 / UI-SPEC:

    1. On initial load when graph has only `root-composer`: auto-select it (`useEffect` setSelectedNodeId("root-composer") when nodes.length === 1).

    2. Extend `ExecutionNodeCard` for editable nodes (root-composer always; selected child when editable):
       - Inline compact textarea (min-height 80px) bound to local prompt state synced via `/api/nodes/:id/edit` on Plan click (atomic save-then-plan acceptable: POST edit then POST plan in sequence)
       - Footer with primary button **Plan children** (`#1f3d32` fill, min-height 44px, `GitBranchPlus` icon)
       - `aria-label="Plan children for {node.label || node.id}"`
       - Show empty canvas hint when only root with no children: heading **Start with a task**, body per UI-SPEC

    3. Add `planningNodeId` state: set during plan POST, clear on refresh; while set, card gets `aria-busy="true"`, accent border class, disabled Plan button, copy **Planning children…**

    4. On success show meta row **Plan ready** — `{N} child nodes drafted. Review on canvas, then approve or run.`

    5. Rename inspector Plan button label to **Plan children** (keep duplicate for keyboard users per UI-SPEC).

    6. CSS in `styles.css`:
       - `.node-card.root-composer-focus` or `.node-card.focused-root` — 2px accent outline offset 2px (`#2d6cdf`)
       - `.node-card.planning-in-progress` — accent border
       - `.node-card-footer .btn-primary-plan` — min-height 44px

    7. Center viewport on root-composer on first load (use existing rfInstanceRef setViewport when only root exists).

    Do NOT remove chat API wiring.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' ui/src/main.tsx | grep -c 'Plan children'` >= 2
    - `grep -v '^#' ui/src/main.tsx | grep -c 'Start with a task'` >= 1
    - `grep -v '^#' ui/src/main.tsx | grep -c 'Planning children'` >= 1
    - `grep -v '^#' ui/src/main.tsx | grep -c 'aria-busy'` >= 1
    - `npm run build` exits 0 (includes UI build if part of monorepo script)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Root-composer focused empty state and node-card Plan children primary path implemented</done>
</task>

<task type="auto">
  <name>Task 2: Demote chat panel and surface planning failure states</name>
  <files>ui/src/main.tsx, ui/src/styles.css</files>
  <read_first>
    - ui/src/main.tsx (Chat mutation section ~580, errorMessage handling)
    - .planning/phases/30-plan-from-node-foundation/30-UI-SPEC.md (Copywriting Contract, Planning & Failure States, Chat Demotion)
  </read_first>
  <action>
    Chat demotion (UI-SPEC):
    - Rename label **Chat mutation** → **Refine graph (optional)**
    - Add helper: **Use chat to preview edits after planning. Graph submit is the default authoring path.**
    - Move chat section below NodeInspector in DOM order (relocate JSX block)
    - Collapsed by default on empty graph: add `chatExpanded` state default false when child count === 0; toggle button to expand

    Failure surfaces (PLAN-07):
    - Parse API errors in `runAction`: when response JSON has `code` in (`planning_failed`, `invalid_planner_output`, `invalid_prompt`), set node-scoped warning on planning node
    - Inspector `.error` banner with `role="alert"`: show `message`, `details`, `suggestedFix`
    - Copy templates:
      - planning_failed: **Planning failed:** {message}. **Check the planner model tier and prompt, then try Plan children again.**
      - invalid_planner_output: **Planner returned invalid output.** {details}. **No fallback was applied. Fix configuration or retry.**
    - Node card `.meta-row.warning` with `AlertTriangle` for same errors
    - Never display heuristic fallback children on failure (graph unchanged server-side; UI must not synthesize children)

    Show `composer.pendingPlan` on card: **Draft plan:** {summary} · {child count} nodes

    CSS: `.meta-row.warning` uses `#fff3f0` bg, `#e4b0a8` border, `#8f2f2f` text per UI-SPEC (verify or add rules).
  </action>
  <acceptance_criteria>
    - `grep -v '^#' ui/src/main.tsx | grep -c 'Refine graph (optional)'` >= 1
    - `grep -v '^#' ui/src/main.tsx | grep -c 'No fallback was applied'` >= 1
    - `grep -v '^#' ui/src/main.tsx | grep -c 'planning_failed'` >= 1
    - `grep -v '^#' ui/src/main.tsx | grep -c 'role="alert"'` >= 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Chat demoted; planning errors visible on node card and inspector with UI-SPEC copy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Error messages rendered in DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | I | error details display | mitigate | Use API-redacted details only; no stack traces in UI |
| T-30-10 | D | planning UI state | mitigate | aria-busy during in-flight plan; disable double-submit |
</threat_model>

<verification>
```bash
npm run build
grep -v '^#' ui/src/main.tsx | grep -c 'Plan children'
```
</verification>

<success_criteria>
- UI-SPEC copy strings present in main.tsx
- Node card is default Plan children path; chat demoted
- Failure banners use role="alert"
</success_criteria>

<output>
After completion, create `.planning/phases/30-plan-from-node-foundation/30-04-SUMMARY.md`
</output>

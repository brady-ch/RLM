# 34-01 Summary

Implemented graph workflow sidecar types, serializer (playbook/pipeline/both), store under `.rlm/workflows/`, and `kind: graph` workflow config union. Expert fields round-trip in sidecar nodes. Tests cover serializer round-trip and store I/O.

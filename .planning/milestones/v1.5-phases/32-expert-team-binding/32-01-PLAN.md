---
phase: 32-expert-team-binding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/application/graph-planner.ts
  - src/application/execution-controller.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - TEAM-01
  - TEAM-05
must_haves:
  truths:
    - "Planner output accepts and validates expert assignment fields"
    - "Planned graph nodes persist agent id, assignment mode, runtime, tool allowlist, and purpose tiers"
    - "Invalid planner expert assignments fail as invalid planner output with no fallback"
---

<objective>
Add the planner and graph-state contract for expert-assigned nodes.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend planned child schema</name>
  <files>src/application/graph-planner.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Add optional `agentId`, `runtime`, `toolAllowlist`, and `purposeTiers` fields to planned child output, constrained to known expert presets and runtime modes.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist planner assignments</name>
  <files>src/domain/types.ts, src/application/execution-controller.ts</files>
  <action>
    Add expert metadata to execution graph nodes and populate planner-created children with planner assignment mode and default runtime.
  </action>
</task>

</tasks>

<verification>
```bash
npm run build
npm test
```
</verification>


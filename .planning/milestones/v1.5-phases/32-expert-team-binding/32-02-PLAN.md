---
phase: 32-expert-team-binding
plan: 02
type: execute
wave: 1
depends_on:
  - 32-01-PLAN.md
files_modified:
  - src/domain/types.ts
  - src/application/execution-controller.ts
  - src/application/control-server.ts
  - ui/src/main.tsx
autonomous: true
requirements:
  - TEAM-02
  - TEAM-06
must_haves:
  truths:
    - "Users can set expert preset, runtime, tool allowlist, and purpose tiers before execution"
    - "Custom expert overrides mark nodes protected for parent replan"
    - "Node cards and inspector show effective expert assignment metadata"
---

<objective>
Expose expert overrides across API and UI, and integrate them with protected replan semantics.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add expert override mutation</name>
  <files>src/application/execution-controller.ts, src/application/control-server.ts</files>
  <action>
    Add a node expert override setter and API route that validate known expert presets/runtime modes, normalize allowlists, mark assignment mode custom, and add `expert_override` to protection reasons.
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add UI controls</name>
  <files>ui/src/main.tsx</files>
  <action>
    Show expert/runtime/custom state on cards and add inspector controls for expert preset, runtime, allowlist, and purpose-tier JSON.
  </action>
</task>

</tasks>

<verification>
```bash
npm run build
npm run build:ui
```
</verification>


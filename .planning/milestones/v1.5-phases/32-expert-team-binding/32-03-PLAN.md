---
phase: 32-expert-team-binding
plan: 03
type: execute
wave: 2
depends_on:
  - 32-01-PLAN.md
  - 32-02-PLAN.md
files_modified:
  - src/domain/types.ts
  - src/domain/recursive-language-model.ts
  - src/application/execution-controller.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - TEAM-03
  - TEAM-04
  - TEAM-07
must_haves:
  truths:
    - "Approved node expert fields bind into execution tasks"
    - "Tool allowlists filter shared registered tools per node"
    - "Purpose tier maps route matching model purposes via overrideModelSelection"
---

<objective>
Bind expert metadata at execution checkpoint time so Phase 33 graph traversal can consume the same per-node contract.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry expert metadata through approval</name>
  <files>src/domain/types.ts, src/domain/recursive-language-model.ts, src/application/execution-controller.ts</files>
  <action>
    Extend task nodes and approval decisions with expert fields, preserve existing overrides when runtime nodes re-register, and copy approved expert fields into execution tasks.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce allowlists and tier routing</name>
  <files>src/domain/recursive-language-model.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Filter the shared tool registry by node allowlist for tool-enabled completions and apply purpose tier overrides to model calls unless an explicit model override is present.
  </action>
</task>

</tasks>

<verification>
```bash
npm run build
npm test
```
</verification>


---
phase: 31-protected-replan-ux
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/application/control-server.ts
  - src/cli/args.ts
  - src/index.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - PLAN-05
  - PLAN-06
  - SURF-01
must_haves:
  truths:
    - "HTTP plan/breakdown routes accept replan choices"
    - "CLI plan-node supports --replan replace|merge|cancel"
    - "CLI and API return the same structured replan_requires_choice code when choice is missing"
---

<objective>
Wire protected replan choice through API and CLI surfaces.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API replan choice passthrough</name>
  <files>src/application/control-server.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Parse `{ replan }` from POST `/api/nodes/:id/plan` and `/breakdown` bodies. Pass valid choices to `session.planNode`.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI --replan support</name>
  <files>src/cli/args.ts, src/index.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Parse `--replan replace|merge|cancel` for `plan-node`. Pass it into `session.planNode`. Reject invalid values during argument parsing.
  </action>
</task>

</tasks>

<verification>
```bash
npm run build
npm test
```
</verification>

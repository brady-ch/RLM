---
phase: 31-protected-replan-ux
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - ui/src/main.tsx
  - ui/src/styles.css
autonomous: true
requirements:
  - PLAN-05
  - PLAN-06
must_haves:
  truths:
    - "UI shows a node-scoped protected replan gate"
    - "UI offers Replace subtree, Merge, and Cancel"
    - "Merge copy says protected edits are kept and replaceable drafts are replanned"
---

<objective>
Expose protected replan choice in the UI when the backend returns `replan_requires_choice`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Node-scoped gate panel</name>
  <files>ui/src/main.tsx, ui/src/styles.css</files>
  <action>
    Detect `replan_requires_choice` planning failures and display a panel for the affected node with Replace subtree, Merge, and Cancel actions. Send the selected choice back to `/api/nodes/:id/plan`.
  </action>
</task>

</tasks>

<verification>
```bash
npm run build
npm run build:ui
npm test
```
</verification>

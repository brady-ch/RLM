---
phase: 31-protected-replan-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/application/graph-planner.ts
  - src/application/execution-controller.ts
  - tests/recursive-language-model.test.ts
autonomous: true
requirements:
  - PLAN-05
  - PLAN-06
must_haves:
  truths:
    - "Protected descendants trigger an explicit replan_requires_choice gate"
    - "Replace deletes protected and pristine descendants before applying fresh planner output"
    - "Merge preserves protected descendants and removes only pristine planned descendants"
    - "Cancel makes no planner call and leaves the graph unchanged"
---

<objective>
Implement core protected replan semantics inside `InteractiveExecutionSession.planNode`.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add protection metadata and detection</name>
  <files>src/domain/types.ts, src/application/execution-controller.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Add node/composer protection metadata where needed. Treat manual child adds, manual prompt edits, and user model overrides as protected. Add helper detection that scans descendants of the replanned node.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement replace merge cancel</name>
  <files>src/application/execution-controller.ts, src/application/graph-planner.ts, tests/recursive-language-model.test.ts</files>
  <action>
    Extend `planNode` with optional `replan` choice. No choice plus protected descendants throws `MutationError("replan_requires_choice")`. `replace` deletes all descendants. `merge` preserves protected descendants and includes their context in planner messages. `cancel` returns without mutation or planner call.
  </action>
</task>

</tasks>

<verification>
```bash
npm run build
npm test
```
</verification>

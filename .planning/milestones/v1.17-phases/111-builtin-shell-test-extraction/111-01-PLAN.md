---
phase: 111-builtin-shell-test-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/plugins/builtin/shell.rs
  - crates/rlm-core/tests/plugins/builtin/shell.rs
autonomous: true
requirements:
  - PLUG-111-01
  - PLUG-111-02
  - PLUG-111-03
  - PLUG-111-04
must_haves:
  truths:
    - "shell.rs contains no inline test bodies — only a #[path] stub module"
    - "rejects_blocked_operators and allows_allowlisted_command run from crates/rlm-core/tests/plugins/builtin/shell.rs"
    - "cargo test -p rlm-core shell_tests passes without regression"
    - "tests/plugins/builtin/ mirror tree extended with shell.rs"
    - "No new rust-boundary-baseline.json entries introduced"
  artifacts:
    - path: crates/rlm-core/tests/plugins/builtin/shell.rs
      provides: "Extracted builtin shell unit tests"
      contains: "rejects_blocked_operators"
    - path: crates/rlm-core/src/plugins/builtin/shell.rs
      provides: "Production shell logic with path stub only"
      contains: "#[path = \"../../../tests/plugins/builtin/shell.rs\"]"
  key_links:
    - from: crates/rlm-core/src/plugins/builtin/shell.rs
      to: crates/rlm-core/tests/plugins/builtin/shell.rs
      via: "#[cfg(test)] #[path = ...] mod shell_tests"
      pattern: "#\\[path = \"\\.\\./\\.\\./\\.\\./tests/plugins/builtin/shell\\.rs\"\\]"
    - from: crates/rlm-core/tests/plugins/builtin/shell.rs
      to: crates/rlm-core/src/plugins/builtin/shell.rs
      via: "use super::* imports GuardedShellTool"
      pattern: "use super::\\*"
---

<objective>
Extract inline tests from `plugins/builtin/shell.rs` to mirrored `tests/plugins/builtin/shell.rs` using the established Phase 110 builtin #[path] stub pattern.

Purpose: Continue v1.17 plugin test mirror chain into builtin shell tool; keep source modules free of inline test bodies.
Output: Thin stub in `shell.rs`, full test bodies in `tests/plugins/builtin/shell.rs`; no module split (177 lines post-extraction, well under 300-line threshold).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-builtin-shell-test-extraction/111-CONTEXT.md
@.planning/phases/110-builtin-write-file-test-extraction/110-01-SUMMARY.md
@.planning/phases/110-builtin-write-file-test-extraction/110-01-PLAN.md
@.planning/notes/rust-infrastructure-layer-decomposition.md
@.planning/ROADMAP.md
@AGENTS.md

<interfaces>
From `crates/rlm-core/src/plugins/builtin/shell.rs` (production surface — unchanged):

```rust
pub struct GuardedShellTool {
    workspace_root: PathBuf,
    allowed_commands: HashSet<String>,
}

impl GuardedShellTool {
    pub fn new(workspace_root: &Path) -> Self;
    fn validate_command(&self, command: &str) -> Result<(), String>;
}
```

Inline tests to extract (lines 183–193):

```rust
#[test]
fn rejects_blocked_operators() {
    let tool = GuardedShellTool::new(Path::new("."));
    assert!(tool.validate_command("ls | cat").is_err());
}

#[test]
fn allows_allowlisted_command() {
    let tool = GuardedShellTool::new(Path::new("."));
    assert!(tool.validate_command("pwd").is_ok());
}
```

Path depth: `shell.rs` lives at `src/plugins/builtin/` — 3 levels below crate root:

```rust
#[cfg(test)]
#[path = "../../../tests/plugins/builtin/shell.rs"]
mod shell_tests;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mirrored tests/plugins/builtin/shell.rs with extracted test bodies</name>
  <files>crates/rlm-core/tests/plugins/builtin/shell.rs</files>
  <read_first>
    crates/rlm-core/src/plugins/builtin/shell.rs
    crates/rlm-core/tests/plugins/builtin/write_file.rs
  </read_first>
  <action>
    1. Create `crates/rlm-core/tests/plugins/builtin/shell.rs`.
    2. Add `use super::*;` at top (matches Phase 110 convention).
    3. Move test functions verbatim from `shell.rs` lines 183–193:
       - `rejects_blocked_operators`
       - `allows_allowlisted_command`
    4. Do NOT modify `shell.rs` yet — stub wiring is Task 2.
    5. Do NOT add new tests or change assertions — extraction only.
    6. Do NOT split `shell.rs` — 194 lines pre-extraction, no split needed.
  </action>
  <verify>
    <automated>test -f crates/rlm-core/tests/plugins/builtin/shell.rs && grep -q 'use super::\*' crates/rlm-core/tests/plugins/builtin/shell.rs && grep -q 'rejects_blocked_operators' crates/rlm-core/tests/plugins/builtin/shell.rs && grep -q 'allows_allowlisted_command' crates/rlm-core/tests/plugins/builtin/shell.rs</automated>
  </verify>
  <done>Mirrored test file exists with both test fns; source still has inline body temporarily</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline tests with #[path] stub and verify</name>
  <files>crates/rlm-core/src/plugins/builtin/shell.rs</files>
  <read_first>
    crates/rlm-core/src/plugins/builtin/shell.rs
    crates/rlm-core/src/plugins/builtin/write_file.rs
    scripts/rust-boundary-baseline.json
  </read_first>
  <action>
    1. Delete the entire inline `#[cfg(test)] mod tests { ... }` block (lines 179–194) from `shell.rs`.
    2. Replace with thin stub using 3-level path:
       ```rust
       #[cfg(test)]
       #[path = "../../../tests/plugins/builtin/shell.rs"]
       mod shell_tests;
       ```
    3. Confirm `shell.rs` has zero `#[test]` attributes and zero test function bodies.
    4. Confirm boundary baseline entry count unchanged (expect 0 entries).
    5. Do NOT modify `plugins/builtin/mod.rs` or `plugins/mod.rs`.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core shell_tests -- --test-threads=1 2>&1</automated>
    <automated>grep -v '^[[:space:]]*//' crates/rlm-core/src/plugins/builtin/shell.rs | grep -E '#\[(tokio::)?test\]' | wc -l | xargs test 0 -eq</automated>
    <automated>grep -q '#\[path = "../../../tests/plugins/builtin/shell.rs"\]' crates/rlm-core/src/plugins/builtin/shell.rs</automated>
  </verify>
  <done>shell.rs is stub-only; shell_tests pass; no new boundary baseline entries</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test stub → source module | Test module inherits module item visibility via `use super::*` child module |
| shell command validation | Production allowlist/blocklist logic unchanged — unit tests use validate_command only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | I | shell.rs command validation | accept | Refactor-only; no production logic changes |
| T-111-02 | I | test file | accept | Tests moved verbatim; same assertions |
| T-111-03 | D | boundary baseline | mitigate | Verify baseline JSON entry count unchanged after extraction |
</threat_model>

<verification>
- `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core shell_tests -- --test-threads=1`
- `grep -v '^[[:space:]]*//' crates/rlm-core/src/plugins/builtin/shell.rs | grep -E '#\[(tokio::)?test\]'` produces no matches
- `bash scripts/check-rust-boundaries.sh` — confirm no NEW violations beyond current baseline
- `test -f crates/rlm-core/tests/plugins/builtin/shell.rs`
</verification>

<success_criteria>
- PLUG-111-01: Zero inline test bodies in `shell.rs`
- PLUG-111-02: Tests mirrored under `crates/rlm-core/tests/plugins/builtin/shell.rs`
- PLUG-111-03: `#[path]` stub wired with correct 3-level relative path
- PLUG-111-04: `cargo test -p rlm-core shell_tests` passes
</success_criteria>

<output>
After completion, create `.planning/phases/111-builtin-shell-test-extraction/111-01-SUMMARY.md`
</output>

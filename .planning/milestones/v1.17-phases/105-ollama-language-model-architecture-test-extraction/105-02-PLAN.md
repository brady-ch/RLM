---
phase: 105-ollama-language-model-architecture-test-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - "105-01"
files_modified:
  - crates/rlm-core/src/adapters/ollama_language_model.rs
  - crates/rlm-core/src/adapters/ollama_language_model/mod.rs
  - crates/rlm-core/src/adapters/ollama_language_model/request.rs
  - crates/rlm-core/src/adapters/ollama_language_model/response.rs
  - crates/rlm-core/src/adapters/mod.rs
requirements:
  - ADAPT-105-06
  - ADAPT-105-07
autonomous: true
must_haves:
  truths:
    - "Post-extraction ollama_language_model source exceeded 300-line threshold (~320 lines) and is split into request/response submodules"
    - "Each submodule is scannable (target ≤ ~200 non-blank lines; mod.rs holds struct, constructors, health_check, chat_stream orchestration, LanguageModel impl)"
    - "adapters/mod.rs still exports OllamaLanguageModel and OllamaLanguageModelError with unchanged public API"
    - "cargo test -p rlm-core ollama_language_model_tests passes after split"
    - "No new rust-boundary-baseline.json entries introduced"
  artifacts:
    - path: crates/rlm-core/src/adapters/ollama_language_model/mod.rs
      provides: "OllamaLanguageModel facade, chat_stream orchestration, LanguageModel impl, #[path] stub"
      contains: "#[path = \"../../../tests/adapters/ollama_language_model.rs\"]"
    - path: crates/rlm-core/src/adapters/ollama_language_model/request.rs
      provides: "Chat request body construction and tool definition building"
      contains: "fn chat_message_to_ollama"
    - path: crates/rlm-core/src/adapters/ollama_language_model/response.rs
      provides: "Stream chunk deserialization and tool-call merge helpers"
      contains: "fn merge_tool_calls"
  key_links:
    - from: crates/rlm-core/src/adapters/ollama_language_model/mod.rs
      to: crates/rlm-core/src/adapters/ollama_language_model/request.rs
      via: "mod request; build_chat_body delegates to request helpers"
      pattern: "mod request"
    - from: crates/rlm-core/src/adapters/ollama_language_model/mod.rs
      to: crates/rlm-core/src/adapters/ollama_language_model/response.rs
      via: "mod response; chat_stream uses response stream parsing"
      pattern: "mod response"
    - from: crates/rlm-core/src/adapters/ollama_language_model/mod.rs
      to: crates/rlm-core/tests/adapters/ollama_language_model.rs
      via: "#[cfg(test)] #[path = ...] mod ollama_language_model_tests (3-level path from subdirectory)"
      pattern: "#\\[path = \"\\.\\./\\.\\./\\.\\./tests/adapters/ollama_language_model\\.rs\"\\]"
---

<objective>
Split post-extraction `ollama_language_model.rs` (~320 lines) into request vs response submodules per decomposition note and CONTEXT discretion — source exceeds the 300-line readability threshold after test extraction in 105-01.

Purpose: Complete v1.17 Phase 105 paired pass; make the language model adapter scannable while preserving private-helper access for extracted tests and unchanged public API.
Output: Directory module `ollama_language_model/` with `request.rs`, `response.rs`, `mod.rs`; flat file removed; #[path] stub updated to 3-level path.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-ollama-language-model-architecture-test-extraction/105-CONTEXT.md
@.planning/phases/105-ollama-language-model-architecture-test-extraction/105-01-SUMMARY.md
@.planning/phases/101-run-state-store-architecture-test-extraction/101-01-PLAN.md
@.planning/phases/103-memory-store-architecture-test-extraction/103-02-PLAN.md
@.planning/notes/rust-infrastructure-layer-decomposition.md
@AGENTS.md

<interfaces>
Prerequisite: 105-01 complete — flat `ollama_language_model.rs` with #[path] stub at `../../tests/adapters/...`, ~320 non-blank lines, zero inline tests, ports import for CancellationController.

Split boundary (per decomposition note — request vs response):

| Submodule | Contents | ~Lines |
|-----------|----------|--------|
| `request.rs` | `chat_message_to_ollama`; `build_chat_body` as `impl OllamaLanguageModel` block; `build_tool_definitions` as associated fn | ~80 |
| `response.rs` | `StreamChunk`, `StreamMessage`, `OllamaToolCall`, `OllamaFunctionCall`; `merge_tool_calls`; stream-line parsing helper extracted from `chat_stream` loop | ~90 |
| `mod.rs` | `OllamaLanguageModelError`, struct, `Default`/`new`/`with_cancellation`, `model_name`/`base_url`, `health_check`, `chat_stream` orchestration (calls response parser), `LanguageModel` impl, #[path] stub | ~150 |

Path depth after split (subdirectory module — matches memory_store/run_state_store pattern):
`#[path = "../../../tests/adapters/ollama_language_model.rs"]` from `src/adapters/ollama_language_model/mod.rs`.

Public API preserved via `adapters/mod.rs`:
```rust
pub mod ollama_language_model;
pub use ollama_language_model::{OllamaLanguageModel, OllamaLanguageModelError};
```
(Rust resolves `pub mod ollama_language_model;` to directory module automatically — no export path change.)

Tests in `tests/adapters/ollama_language_model.rs` use `use super::*` — after split, `super` is `mod.rs` which re-exports or owns `build_tool_definitions`/`build_chat_body` so existing test assertions compile unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ollama_language_model/ directory and move request/response code</name>
  <files>crates/rlm-core/src/adapters/ollama_language_model/mod.rs, crates/rlm-core/src/adapters/ollama_language_model/request.rs, crates/rlm-core/src/adapters/ollama_language_model/response.rs, crates/rlm-core/src/adapters/ollama_language_model.rs</files>
  <read_first>
    crates/rlm-core/src/adapters/ollama_language_model.rs
    crates/rlm-core/src/persistence/run_state_store/mod.rs
    crates/rlm-core/src/persistence/run_state_store/persist.rs
  </read_first>
  <action>
    Per decomposition note (request vs response split):

    1. Create directory `crates/rlm-core/src/adapters/ollama_language_model/`.
    2. Create `request.rs`:
       - Move `chat_message_to_ollama` free function.
       - Move `build_tool_definitions` associated fn (keep `impl OllamaLanguageModel` or `impl OllamaLanguageModel` in request.rs with `use super::OllamaLanguageModel`).
       - Move `build_chat_body` method into `impl OllamaLanguageModel` block in request.rs.
    3. Create `response.rs`:
       - Move `StreamChunk`, `StreamMessage`, `OllamaToolCall`, `OllamaFunctionCall` structs.
       - Move `merge_tool_calls`.
       - Extract stream buffer/line parsing from `chat_stream` into a helper (e.g. `apply_stream_line`) callable from mod.rs — keep HTTP send + cancellation check in mod.rs `chat_stream`.
    4. Create `mod.rs`:
       - Move remaining items: imports, `OllamaLanguageModelError`, `OllamaLanguageModel` struct, constructors, `health_check`, slimmed `chat_stream`, `LanguageModel` impl.
       - Declare `mod request; mod response;` (use `pub(super)` or `pub(crate)` on moved items only if tests require — prefer keeping test-accessible methods on `impl OllamaLanguageModel` in request.rs with child-module visibility via `super`).
       - Copy #[path] stub from flat file but update path to 3-level: `../../../tests/adapters/ollama_language_model.rs`.
    5. Delete flat `crates/rlm-core/src/adapters/ollama_language_model.rs` after mod.rs compiles.
    6. Do NOT change production behavior, error messages, or defaults — move-only refactor.
    7. Do NOT modify `tests/adapters/ollama_language_model.rs` unless required for import path fixes.
  </action>
  <acceptance_criteria>
    - `test ! -f crates/rlm-core/src/adapters/ollama_language_model.rs`
    - `test -f crates/rlm-core/src/adapters/ollama_language_model/mod.rs`
    - `test -f crates/rlm-core/src/adapters/ollama_language_model/request.rs`
    - `test -f crates/rlm-core/src/adapters/ollama_language_model/response.rs`
    - `grep -q 'fn chat_message_to_ollama' crates/rlm-core/src/adapters/ollama_language_model/request.rs`
    - `grep -q 'fn merge_tool_calls' crates/rlm-core/src/adapters/ollama_language_model/response.rs`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-core 2>&1</automated>
  </verify>
  <done>Directory module compiles; flat file removed; request/response concerns separated</done>
</task>

<task type="auto">
  <name>Task 2: Verify exports, tests, boundaries, and line counts</name>
  <files>crates/rlm-core/src/adapters/mod.rs, crates/rlm-core/src/adapters/ollama_language_model/mod.rs</files>
  <read_first>
    crates/rlm-core/src/adapters/mod.rs
    scripts/rust-boundary-baseline.json
    crates/rlm-core/tests/adapters/ollama_language_model.rs
  </read_first>
  <action>
    1. Confirm `adapters/mod.rs` still exports `OllamaLanguageModel` and `OllamaLanguageModelError` — adjust only if module path resolution broke (directory module should work unchanged).
    2. Confirm #[path] stub in mod.rs uses `../../../tests/adapters/ollama_language_model.rs`.
    3. Run full ollama_language_model_tests — all 3 tests must pass without modifying test assertions.
    4. Count non-blank lines per submodule; each should be ≤ ~200 (mod.rs may be ~150–180).
    5. Confirm baseline JSON entry count still 6 — no new violations.
    6. Grep gate: zero `#[test]` in production source files under `ollama_language_model/`.
  </action>
  <acceptance_criteria>
    - `grep -q 'pub use ollama_language_model::' crates/rlm-core/src/adapters/mod.rs`
    - `grep -q '#\[path = "../../../tests/adapters/ollama_language_model.rs"\]' crates/rlm-core/src/adapters/ollama_language_model/mod.rs`
    - `grep -r '#\[test\]' crates/rlm-core/src/adapters/ollama_language_model/ | grep -v '^[[:space:]]*//' | wc -l | grep -qx '0'`
    - Baseline JSON array length equals 6
  </acceptance_criteria>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core ollama_language_model_tests -- --test-threads=1 2>&1</automated>
  </verify>
  <done>Public API unchanged; 3 tests pass; submodules within readability target; no new boundary baseline entries</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module split → test stub | `use super::*` must still reach `build_chat_body` and `build_tool_definitions` after move |
| Ollama streaming | Stream parsing refactor must preserve tool-call merge and cancellation behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | I | chat_stream refactor | mitigate | Move-only split; run ollama_language_model_tests (3 cases) before marking done |
| T-105-05 | I | public API | accept | adapters/mod.rs re-exports unchanged; no signature changes |
| T-105-06 | D | boundary baseline | mitigate | Verify baseline count unchanged; ollama still imports ports::CancellationController |
</threat_model>

<verification>
- `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core ollama_language_model_tests -- --test-threads=1`
- `node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-core`
- `bash scripts/check-rust-boundaries.sh`
- Per-submodule non-blank line count ≤ ~200
</verification>

<success_criteria>
- ADAPT-105-06: Source split into request/response submodules because post-extraction line count exceeded 300
- ADAPT-105-07: Public API and all three unit tests pass after split
</success_criteria>

<output>
After completion, create `.planning/phases/105-ollama-language-model-architecture-test-extraction/105-02-SUMMARY.md`
</output>

---
phase: 107-plugin-runtime-registry-boundary-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/ports/agent.rs
  - crates/rlm-core/src/ports/mod.rs
  - crates/rlm-core/src/application/execution/agent_registry.rs
  - crates/rlm-core/src/application/execution/mod.rs
  - crates/rlm-core/src/application/graph/executor.rs
  - crates/rlm-core/src/plugins/runtime.rs
  - crates/rlm-core/tests/plugins/runtime.rs
  - scripts/rust-boundary-baseline.json
  - AGENTS.md
requirements:
  - PLUG-107-01
  - PLUG-107-03
autonomous: true
must_haves:
  truths:
    - "AgentProfile and filter_agent_tools live in ports/agent.rs, not application"
    - "plugins/runtime.rs imports AgentProfile and filter_agent_tools from ports only"
    - "runtime.rs contains no inline test bodies — only a #[path] stub module"
    - "Two runtime unit tests run from crates/rlm-core/tests/plugins/runtime.rs"
    - "no-plugins-to-application baseline entry removed (count drops from 2 to 1)"
  artifacts:
    - path: crates/rlm-core/src/ports/agent.rs
      provides: "AgentProfile DTO and filter_agent_tools helper for cross-layer tool filtering"
      contains: "pub fn filter_agent_tools"
    - path: crates/rlm-core/tests/plugins/runtime.rs
      provides: "Extracted plugin runtime unit tests"
      contains: "init_order_matches_v17_pipeline"
    - path: crates/rlm-core/src/plugins/runtime.rs
      provides: "Runtime composition without application imports"
      contains: "#[path = \"../../tests/plugins/runtime.rs\"]"
  key_links:
    - from: crates/rlm-core/src/plugins/runtime.rs
      to: crates/rlm-core/src/ports/agent.rs
      via: "use crate::ports::{filter_agent_tools, AgentProfile}"
      pattern: "use crate::ports::\\{[^}]*filter_agent_tools"
    - from: crates/rlm-core/src/application/execution/agent_registry.rs
      to: crates/rlm-core/src/ports/agent.rs
      via: "resolve_agent returns ports::AgentProfile"
      pattern: "use crate::ports::AgentProfile"
    - from: crates/rlm-core/src/plugins/runtime.rs
      to: crates/rlm-core/tests/plugins/runtime.rs
      via: "#[cfg(test)] #[path = ...] mod runtime_tests"
      pattern: "#\\[path = \"\\.\\./\\.\\./tests/plugins/runtime\\.rs\"\\]"
---

<objective>
Move `AgentProfile` and `filter_agent_tools` from application to `ports/agent.rs`, repoint all consumers (plugins runtime, graph executor), extract inline tests from `plugins/runtime.rs` to mirrored `tests/plugins/runtime.rs`, and remove the `no-plugins-to-application` baseline suppression.

Purpose: Resolve the first of two remaining plugins-block boundary deferrals per v1.17 decomposition; follow Phase 105/106 ports consolidation precedent.
Output: Ports-owned agent tool filter API, extracted runtime tests, baseline count 2→1.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-plugin-runtime-registry-boundary-cleanup/107-CONTEXT.md
@.planning/phases/106-tool-result-type-ports-consolidation/106-01-SUMMARY.md
@.planning/notes/rust-infrastructure-layer-decomposition.md
@.planning/ROADMAP.md
@AGENTS.md

<interfaces>
From `crates/rlm-core/src/application/execution/agent_registry.rs` (move verbatim to ports/agent.rs):

```rust
#[derive(Debug, Clone)]
pub struct AgentProfile {
    pub id: String,
    pub system_prompt: String,
    pub tool_names: Vec<String>,
}

pub fn filter_agent_tools(agent: &AgentProfile, allowlist: Option<&[String]>) -> AgentProfile {
    // ... HashSet allowlist intersection logic unchanged ...
}
```

From `crates/rlm-core/src/plugins/runtime.rs` (consumer to repoint):

```rust
pub fn resolve_tools_for_agent(
    runtime: &RuntimeContext,
    agent: &AgentProfile,
    allowlist: Option<&[String]>,
) -> Vec<Arc<dyn Tool>> {
    let filtered = filter_agent_tools(agent, allowlist);
    // ... tool name HashSet filter unchanged ...
}
```

Phase 105 precedent (`application/execution/cancellation.rs`): thin re-export shim allowed for backward compat:

```rust
pub use crate::ports::CancellationController;
```

Phase 104 #[path] stub pattern (from `src/plugins/runtime.rs`, 2 levels up to crate root):

```rust
#[cfg(test)]
#[path = "../../tests/plugins/runtime.rs"]
mod runtime_tests;
```

Inline tests to extract (lines 112–144 of runtime.rs): `init_order_matches_v17_pipeline`, `loads_builtin_tools`.

Baseline entry to remove:

```json
{ "rule": "no-plugins-to-application", "from": "crates/rlm-core/src/plugins/runtime.rs", ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move AgentProfile and filter_agent_tools to ports</name>
  <files>
    crates/rlm-core/src/ports/agent.rs,
    crates/rlm-core/src/ports/mod.rs,
    crates/rlm-core/src/application/execution/agent_registry.rs,
    crates/rlm-core/src/application/execution/mod.rs,
    crates/rlm-core/src/application/graph/executor.rs,
    crates/rlm-core/src/plugins/runtime.rs
  </files>
  <action>
    Per CONTEXT Claude's discretion — consolidate under `ports/` following Phase 105/106 pattern:

    1. Create `ports/agent.rs` with `AgentProfile` struct and `filter_agent_tools` fn copied verbatim from `agent_registry.rs` (lines 7–12 and 57–78). Add `use std::collections::HashSet;` at top.

    2. In `ports/mod.rs`, add `pub mod agent;` and `pub use agent::*;`.

    3. In `application/execution/agent_registry.rs`:
       - Remove `AgentProfile` struct and `filter_agent_tools` fn definitions.
       - Add `use crate::ports::AgentProfile;`.
       - Keep `resolve_agent`, `is_known_expert_agent`, `KNOWN_AGENTS` unchanged — they still construct/return `AgentProfile`.
       - Add thin re-exports at file bottom: `pub use crate::ports::{filter_agent_tools, AgentProfile};` (matches cancellation shim pattern).

    4. In `application/execution/mod.rs`, keep existing `pub use agent_registry::{resolve_agent, AgentProfile};` — add `filter_agent_tools` to the re-export list OR re-export via `pub use agent_registry::*` if cleaner. Ensure external callers of `filter_agent_tools` from application path still compile (graph executor currently imports from agent_registry directly).

    5. In `application/graph/executor.rs`, change import from `crate::application::execution::agent_registry::{filter_agent_tools, resolve_agent}` to import `filter_agent_tools` from `crate::ports` and `resolve_agent` from agent_registry (or consolidate into existing import blocks — behavior unchanged).

    6. In `plugins/runtime.rs`, replace `use crate::application::execution::agent_registry::{filter_agent_tools, AgentProfile};` with `use crate::ports::{filter_agent_tools, AgentProfile};`. Do **not** change `resolve_tools_for_agent` logic.

    Do **not** move `resolve_agent` to ports (orchestration stays in application). Do **not** touch registry/service.rs — that is Plan 02.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-core 2>&1</automated>
    <automated>rg 'application::execution::agent_registry' crates/rlm-core/src/plugins/runtime.rs | wc -l | xargs test 0 -eq</automated>
    <automated>rg 'pub fn filter_agent_tools' crates/rlm-core/src/ports/agent.rs | wc -l | xargs test 1 -eq</automated>
  </verify>
  <done>AgentProfile and filter_agent_tools defined in ports/agent.rs; plugins/runtime.rs has zero application imports; rlm-core compiles</done>
</task>

<task type="auto">
  <name>Task 2: Extract runtime tests and ratchet application baseline entry</name>
  <files>
    crates/rlm-core/src/plugins/runtime.rs,
    crates/rlm-core/tests/plugins/runtime.rs,
    scripts/rust-boundary-baseline.json,
    AGENTS.md
  </files>
  <action>
    1. Create `crates/rlm-core/tests/plugins/runtime.rs` — move both test fns verbatim from runtime.rs `mod tests` block (`init_order_matches_v17_pipeline`, `loads_builtin_tools`). Use `use super::*;` to access `build_runtime_context`, `BuildRuntimeContextInput`, `COMPOSITION_INIT_ORDER`, etc.

    2. Replace inline `#[cfg(test)] mod tests { ... }` in `plugins/runtime.rs` with Phase 104 stub:

       ```rust
       #[cfg(test)]
       #[path = "../../tests/plugins/runtime.rs"]
       mod runtime_tests;
       ```

    3. Remove the `no-plugins-to-application` entry from `scripts/rust-boundary-baseline.json`. Remaining baseline must be exactly one entry: `no-plugins-to-persistence` (registry/service.rs).

    4. In `AGENTS.md` transitional baseline table, delete the row for `no-plugins-to-application` / `plugins/runtime.rs`. Leave the `no-plugins-to-persistence` row intact for Plan 02.

    5. Run boundary check — must pass with 1 baseline entry.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core runtime_tests 2>&1</automated>
    <automated>bash scripts/check-rust-boundaries.sh 2>&1</automated>
    <automated>python3 -c "import json; b=json.load(open('scripts/rust-boundary-baseline.json')); assert len(b)==1; assert b[0]['rule']=='no-plugins-to-persistence'"</automated>
    <automated>rg '#\[test\]' crates/rlm-core/src/plugins/runtime.rs | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>Runtime tests extracted to tests/plugins/; zero inline #[test] in runtime.rs; baseline count is 1; boundaries pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent tool filter → runtime | Allowlist intersection determines which tools an agent may invoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | E | filter_agent_tools allowlist | accept | Type move only — allowlist intersection logic unchanged; no new tool exposure surface |
| T-107-02 | I | Baseline partial removal | mitigate | Verify check-rust-boundaries.sh passes before committing baseline shrink |
</threat_model>

<verification>
- `rg 'application::execution::agent_registry' crates/rlm-core/src/plugins/` returns no matches
- `rg 'pub struct AgentProfile' crates/rlm-core/src/ports/agent.rs` returns match
- `rg '#\[test\]' crates/rlm-core/src/plugins/runtime.rs` returns no matches
- `python3 -c "import json; print(len(json.load(open('scripts/rust-boundary-baseline.json'))))"` prints `1`
- `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core runtime_tests` exits 0
</verification>

<success_criteria>
- PLUG-107-01: `filter_agent_tools` and `AgentProfile` exposed through `ports/agent.rs`; plugins/runtime.rs imports from ports
- PLUG-107-03: Runtime inline tests extracted to `tests/plugins/runtime.rs` with #[path] stub
</success_criteria>

<output>
After completion, create `.planning/phases/107-plugin-runtime-registry-boundary-cleanup/107-01-SUMMARY.md`
</output>

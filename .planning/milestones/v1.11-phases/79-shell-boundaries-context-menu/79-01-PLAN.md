---
phase: 79-shell-boundaries-context-menu
plan: 01
type: execute
wave: 1
autonomous: true
requirements:
  - SHEL-02
  - SHEL-03
  - SHEL-04
---

# Plan 79-01: Shell Boundaries & Context Menu

## Objective

Verify Phase 61 shell implementation, ratchet workflow vs Advanced separation with ESLint + contract tests, confirm context menu Variant B and Run panel scope.

## Tasks

### T1 — Shell boundary contract test

Create `tests/ui/shell-boundaries.test.ts`:

- `run-panel/` has no imports from `advanced/`
- `RunPanel.tsx` excludes prompt edit, plan buttons, domain panel patterns
- `NodeContextMenu.tsx` includes Plan/Run/Graph/Advanced sections and key `/api/nodes/*` paths
- `ExecutionNodeCard.tsx` exposes ⋮ button and Shift+F10 / ContextMenu key handlers
- `AppShell.tsx` renders `AdvancedHub` only in advanced mode; workflow uses TopBar + canvas + RunPanel
- `main.tsx` mounts `<AppShell />` only

**Verify:** `npm test` (shell-boundaries subset passes)

### T2 — ESLint run-panel boundary rule

Add `no-restricted-imports` for `ui/src/run-panel/**` blocking `advanced/` imports.

**Verify:** `npm run lint -- ui/src/run-panel`

### T3 — Run panel test id

Add `data-testid="run-panel"` to RunPanel aside for contract/UAT selectors.

**Verify:** `npm run build:ui`

### T4 — Phase verification

Write `79-VERIFICATION.md` with `status: passed`.

## Success Criteria

1. Workflow view: top bar + canvas only — no domain panels in workflow branch
2. Run panel on select: approve/clarify/readiness — no edit fields
3. Context menu Variant B wired with keyboard access
4. `run-panel/` cannot import from `advanced/` (lint + test)

---
phase: 77-interaction-polish
plan: 01
type: execute
wave: 1
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
---

# Plan 77-01: Verify Interaction Polish

## Goal

Ratchet UX-01–04 with automated tests and operator runbook updates. Implementation largely landed in commits `6cb5abb`–`1b1046d`.

## Tasks

### T1 — Tier refresh integration test

Add Rust test proving `POST /api/model-library/select-tier` updates router plan model (not just persisted config).

**Files:** `crates/rlm-core/tests/model_library_routes.rs` or new `control_server_tier_refresh.rs`

**Verify:** `cargo test tier_refresh` passes

### T2 — Session bootstrap golden alignment

Confirm `session-idle.json` fixture matches seeded session; add test if missing.

**Verify:** `cargo test session-idle` or control-server fixture test passes

### T3 — Operator runbook updates

Update `72-UAT.md` prerequisites and `docs/UI.md` with:
- Rust serve command
- `rlm ui --stop` / `--replace`
- Tier assignment before Plan/Run
- WSL memory note referencing `scripts/wslconfig.example`

**Verify:** docs readable; no stale Node-only-only paths

### T4 — Phase verification artifact

Write `77-VERIFICATION.md` with automated status `passed`; note browser UAT deferred to Phase 81.

**Verify:** frontmatter `status: passed`

## Success Criteria

1. Tier select refreshes live plan model (test)
2. UI build passes
3. ui_lock tests pass
4. Runbook documents Rust UI lifecycle

---
phase: "01"
plan: "01-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["PLAN-01", "APRV-01"]
files_modified:
  - src/application/execution-controller.ts
  - src/application/run-recursive-prompt.ts
  - src/domain/recursive-language-model.ts
  - src/domain/types.ts
  - src/application/agent-runner.ts
  - src/application/workflow-runner.ts
autonomous: true
must_haves:
  truths:
    - "Execution cannot run child work before a plan graph exists."
    - "Approval gating is backend-authoritative and blocks progress until approved/skipped/cancelled."
    - "Approval transitions are idempotent and reject stale actions explicitly."
---

<objective>
Enforce backend-authoritative hard-gate semantics for planning and approval so node execution cannot proceed without valid checkpoint decisions.
</objective>

<tasks>
  <task id="1" type="analysis">
    <action>Audit current approval and plan-before-run transitions across `execution-controller`, `recursive-language-model`, and orchestration entry points.</action>
    <files>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/run-recursive-prompt.ts</file>
      <file>src/application/workflow-runner.ts</file>
    </files>
    <verify>Document concrete transition invariants and missing guards in code comments or nearby constants.</verify>
    <acceptance_criteria>Transition invariants are explicit and directly reference `PLAN-01`/`APRV-01` behaviors.</acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <action>Refactor/extend execution controller to be the only approval authority, with first-write-wins token/version checks and explicit stale/duplicate rejection results.</action>
    <files>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/types.ts</file>
    </files>
    <verify>Typecheck passes and new controller behavior is covered by tests for duplicate + stale actions.</verify>
    <acceptance_criteria>No non-controller approval path can advance an `awaiting_approval` node; stale actions return structured errors.</acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <action>Enforce plan-graph-before-child-execution guardrails in recursive/orchestration flow and ensure node registration/status sequence honors hard stop semantics.</action>
    <files>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/run-recursive-prompt.ts</file>
      <file>src/application/agent-runner.ts</file>
      <file>src/application/workflow-runner.ts</file>
    </files>
    <verify>Integration tests validate that child execution does not begin prior to plan graph availability and explicit approval transitions.</verify>
    <acceptance_criteria>Runs without approval remain paused at checkpoints; `planOnly` behavior remains intact and explicit.</acceptance_criteria>
  </task>

  <task id="4" type="testing">
    <action>Add/expand tests for hard-gate semantics: paused-until-approval, stale/duplicate action handling, and no pre-plan child execution.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` passes with new assertions covering `PLAN-01` and `APRV-01` in backend flow.</verify>
    <acceptance_criteria>Regression tests fail if approval bypasses occur or plan-before-run invariant breaks.</acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`
- Run `npm test`
- Manually validate a run in `--require-approval` mode pauses at checkpoints and rejects stale approval attempts with visible error output.
</verification>

<success_criteria>
- Backend controller is sole approval authority.
- Approval gate is truly blocking and race-safe.
- Plan graph exists before child execution starts.
- Backend tests cover new invariants.
</success_criteria>

---
phase: "05"
plan: "05-02"
type: "standard"
wave: 2
depends_on: ["05-01"]
requirements_addressed: ["ERRO-03"]
files_modified:
  - src/application/workflow-runner.ts
  - src/application/model-provider.ts
  - src/application/agent-runner.ts
autonomous: true
must_haves:
  truths:
    - "D-03: Workflow failures use the same structured error posture as other runtime paths where results already carry codes/messages."
    - "D-08: Workflow-runner paths do not downgrade failure visibility versus single-agent runs."
    - "D-09: combineWorkflowResults marks workflow graph nodes failed when that agent rejected or threw; executionStatus reflects errors[]."
    - "continueOnError still surfaces per-agent failure in metadata and node status (not silent success)."
threat_model:
  threats:
    - id: "T-05-02"
      severity: "low"
      risk: "Workflow continues after error but UI shows all-green nodes."
      mitigation: "Set per-workflow-agent node status to failed when that agent's promise rejects or result.errors indicate failure."
---

<objective>
Harden workflow and model routing layers so model/tool/workflow errors propagate into `RecursivePromptResult.metadata` and workflow execution graphs with failed node statuses, matching single-agent and recursive-domain bars per D-08 and roadmap SC1/SC2.
</objective>

<tasks>
  <task id="1" type="implementation">
    <read_first>
      <file>.planning/phases/05-reliability-and-no-silent-failure-hardening/05-CONTEXT.md</file>
      <file>src/application/workflow-runner.ts</file>
      <file>src/domain/types.ts</file>
    </read_first>
    <action>Refactor `combineWorkflowResults` to accept terminal `executionStatus` including `\"failed\"` (extend union in the helper). For each workflow agent id, set `executionGraph.nodes[index].status` to `\"failed\"` when that agent's index was rejected in `Promise.allSettled` or its `RecursivePromptResult.metadata.errors` is non-empty after merge — per D-08 document the exact rule in a short comment. When `errors[]` is non-empty after combining, set top-level metadata `executionStatus` to `\"failed\"` unless the workflow explicitly completed with only soft warnings (if no soft-warning distinction exists, any collected error string implies failed). For `continueOnError: true`, still return a combined result with `executionStatus: \"failed\"` if any agent failed (partial success must be visibly distinct per D-09 — use `failed` at run level). Ensure QA agent failure paths append to errors and mark the QA node failed when QA throws.</action>
    <files>
      <file>src/application/workflow-runner.ts</file>
    </files>
    <verify>`npm run build` exits 0 and `rg -n "combineWorkflowResults|executionStatus.*failed|workflow-agent" src/application/workflow-runner.ts` reflects failure-aware graph nodes.</verify>
    <acceptance_criteria>
      <criterion>Workflow with one rejected agent produces metadata.executionStatus `failed` and at least one node with status `failed`.</criterion>
      <criterion>Workflow graph nodes are not all `completed` when `errors` array has entries from agent failures.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <read_first>
      <file>src/application/model-provider.ts</file>
      <file>src/application/agent-runner.ts</file>
    </read_first>
    <action>Audit `PurposeRoutingLanguageModel` and `runConfiguredAgent`: ensure model `complete()` rejections are not caught and converted into empty success. If a wrapper swallows errors, rethrow or convert to a failed `RecursivePromptResult` with `metadata.errors` and `executionStatus: \"failed\"` per D-01. Do not add silent model fallback (D-13). Touch `agent-runner.ts` only if required to propagate failures; keep changes minimal.</action>
    <files>
      <file>src/application/model-provider.ts</file>
      <file>src/application/agent-runner.ts</file>
    </files>
    <verify>`npm test` exits 0.</verify>
    <acceptance_criteria>
      <criterion>Throwing model surfaces as failed metadata or thrown error consistent with existing tests.</criterion>
      <criterion>No new retry-or-fallback logic for model selection.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build` and `npm test`.
- Spot-check `runWorkflow` with mocked failing agent in tests (05-04) if not covered here.
</verification>

<success_criteria>
- ERRO-03: Workflow and model paths do not fail silently or present all-success graphs when agents failed.
- Partial workflow failure is visible at run level and on per-agent nodes.
</success_criteria>

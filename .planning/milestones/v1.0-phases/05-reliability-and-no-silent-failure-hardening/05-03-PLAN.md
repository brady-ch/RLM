---
phase: "05"
plan: "05-03"
type: "standard"
wave: 3
depends_on: ["05-02"]
requirements_addressed: ["ERRO-01"]
files_modified:
  - src/application/control-server.ts
  - src/cli/render.ts
  - src/cli/runtime-logger.ts
  - src/index.ts
autonomous: true
must_haves:
  truths:
    - "D-04: Interactive CLI users see failures in rendered output and stderr, not only in debug logs."
    - "D-04: `--json-stream` lines include the same terminal failure facts as snapshot (status + optional code/category)."
    - "D-07: Compact and JSON render modes use vocabulary aligned with `labelForCategory` / status strings."
    - "Soft-failed runs exit non-zero when execution failed (model/tool/workflow) per actionable CLI convention."
threat_model:
  threats:
    - id: "T-05-03"
      severity: "medium"
      risk: "Error messages from tools/models reflected in UI could contain malicious HTML/scripts."
      mitigation: "React escapes text; avoid `dangerouslySetInnerHTML` for error strings; keep server JSON as plain strings only."
---

<objective>
Project the domain failure vocabulary through HTTP control server, human-readable CLI rendering, and process exit semantics so ERRO-01 is satisfied for both JSON and interactive users per D-04/D-05.
</objective>

<tasks>
  <task id="1" type="implementation">
    <read_first>
      <file>src/application/control-server.ts</file>
      <file>src/domain/execution-failure.ts</file>
      <file>src/domain/types.ts</file>
    </read_first>
    <action>Ensure `GET /api/session` (or equivalent snapshot handler) returns JSON that includes enough information for the UI header to show run-level failure consistent with `InteractiveExecutionSession.snapshot()` after 05-01 (same `status` enum). If snapshot already exposes `graph.nodes[].status`, add optional `runSummary?: { code?: string; category?: ExecutionFailureCategory; message?: string }` only if needed to satisfy D-07 parity — prefer reusing node statuses to avoid drift. Ensure SSE `snapshot` / `execution` payloads serialize new `ExecutionEvent` fields (`failureCategory`, `code`, `message`) without breaking existing clients.</action>
    <files>
      <file>src/application/control-server.ts</file>
    </files>
    <verify>`npm run build` exits 0 and `rg -n "snapshot|EventSource|/api/session" src/application/control-server.ts` shows events carry updated shapes.</verify>
    <acceptance_criteria>
      <criterion>UI can distinguish failed run from completed run using only API payload.</criterion>
      <criterion>No uncaught exception leaks stack traces into JSON responses — use safe messages.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <read_first>
      <file>src/cli/render.ts</file>
      <file>src/domain/execution-failure.ts</file>
    </read_first>
    <action>Enhance `renderResult` for non-compact, non-json mode: when `metadata.errors.length > 0` or `metadata.executionStatus === \"failed\"`, prepend or append a clear `Errors:` section listing errors and the run-level status (use `labelForCategory` or status label helper for consistency per D-07). For `renderCompact`, add a single line `errors: N` or first error preview when failed. For `renderJson`, include `executionStatus`, `errors`, and optional failure summary fields mirroring types. Update `createStderrRuntimeLogger` usage only if needed so failure stages are logged at `error` severity for interactive users (D-04).</action>
    <files>
      <file>src/cli/render.ts</file>
      <file>src/cli/runtime-logger.ts</file>
    </files>
    <verify>`npm run build` exits 0.</verify>
    <acceptance_criteria>
      <criterion>Default text output is not a bare `Answer:` when the run failed — user sees explicit error section.</criterion>
      <criterion>JSON output includes structured failure fields suitable for scripting.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <read_first>
      <file>src/index.ts</file>
      <file>src/cli/args.ts</file>
    </read_first>
    <action>After successful `main()` resolution, set `process.exitCode = 1` when the final `RecursivePromptResult.metadata.executionStatus === \"failed\"` or when `metadata.errors.length > 0` and the run is not an explicit partial-success mode (if the product defines partial success — otherwise treat any errors as failure). Do not exit 1 for clean completions with empty errors. Ensure `--json` / `--json-stream` consumers still receive full payloads before exit. Document the exit-code contract in `helpText` or `args.ts` help string in one line.</action>
    <files>
      <file>src/index.ts</file>
      <file>src/cli/args.ts</file>
    </files>
    <verify>`npm test` exits 0.</verify>
    <acceptance_criteria>
      <criterion>Failed run yields non-zero exit code without relying on uncaught exceptions.</criterion>
      <criterion>Successful run with no errors remains exit 0.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build` and `npm test`.
- Manual smoke (optional): `rlm` with a forced failure returns non-zero and prints errors section.
</verification>

<success_criteria>
- ERRO-01: CLI surfaces runtime failures explicitly in normal interactive and JSON modes.
- Exit codes align with failure for shell scripting.
</success_criteria>

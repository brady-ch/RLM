---
phase: "05"
plan: "05-04"
type: "standard"
wave: 4
depends_on: ["05-03"]
requirements_addressed: ["ERRO-01", "ERRO-03"]
files_modified:
  - ui/src/main.tsx
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "D-05: UI run header and node cards show failed state when backend reports failed (consistent with CLI labels)."
    - "D-10: Regression tests exist for model failure, tool failure, workflow failure, and one approval/edit/run loop failure path."
    - "D-11: Tests assert structured fields (status, errors, exitCode via helpers, event payloads) not sleep-based timing."
    - "Roadmap SC4: Critical failure surfacing flows are test-covered."
threat_model:
  threats:
    - id: "T-05-04"
      severity: "low"
      risk: "Tests become flaky if tied to real Ollama/network."
      mitigation: "Use QueueModel, ThrowingModel, and mocked session/control paths already in `tests/recursive-language-model.test.ts`."
---

<objective>
Complete UI parity for failure visibility and add deterministic regression tests for model, tool, workflow, and checkpoint-loop failures per D-05, D-10, D-11 and roadmap success criteria 2–4.
</objective>

<tasks>
  <task id="1" type="implementation">
    <read_first>
      <file>ui/src/main.tsx</file>
      <file>src/domain/execution-failure.ts</file>
    </read_first>
    <action>Update `App` shell in `ui/src/main.tsx` per D-05: run header (`snapshot.status`) uses the same user-visible semantics as CLI — when `status === \"failed\"`, show a prominent failed state (existing `.status.failed` CSS or add class) and optional one-line message from the selected node's failure context if API provides it. Ensure each node card shows `failed` styling when `execution.status === \"failed\"` (badge or border). Keep accessibility: status text remains readable, focus not trapped. If UI cannot import `../src/domain` due to Vite root, duplicate only the **label strings** in a `const uiStatusLabels` block with a comment pointing to `execution-failure.ts` as source of truth (D-06/D-12).</action>
    <files>
      <file>ui/src/main.tsx</file>
    </files>
    <verify>`npm run build:ui` exits 0 (or `npm run build` if monolithic script exists).</verify>
    <acceptance_criteria>
      <criterion>Failed run is visually distinct from `completed` in the header.</criterion>
      <criterion>Failed nodes are visually distinct on the canvas/inspector.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="testing">
    <read_first>
      <file>tests/recursive-language-model.test.ts</file>
      <file>.planning/phases/05-reliability-and-no-silent-failure-hardening/05-CONTEXT.md</file>
    </read_first>
    <action>Add four regression tests with descriptive names: (1) **model failure** — `ThrowingModel` or queue empty causing throw — assert `metadata.executionStatus === \"failed\"` and `errors` or thrown path. (2) **tool failure** — tool returns `status: \"error\"` — assert run records failure and trace contains error. (3) **workflow failure** — `runWorkflow` with a workflow whose agent map includes a throwing model or configurable failure — assert `metadata.executionStatus` and workflow graph node shows `failed`. (4) **approval/edit/run loop** — use `createInteractiveExecutionSession`, pause at approval, inject failure on resume or mutate graph then run — assert session snapshot or events show `failed` and no silent `completed`. Use existing helpers (`QueueModel`, `GuardedShellTool`, session + control server patterns in file). Per D-11, assert on synchronous metadata and snapshot fields only.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` exits 0 and `rg -n "model failure|tool failure|workflow failure|approval|failed" tests/recursive-language-model.test.ts` finds the four scenario titles or nearby comments.</verify>
    <acceptance_criteria>
      <criterion>Four distinct behaviors are covered as named in 05-CONTEXT D-10.</criterion>
      <criterion>Removing failure-surfacing code should turn at least one test red.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`, `npm run build:ui` (if separate), `npm test`.
- Quick grep: `executionStatus.*failed` in tests for assertions.
</verification>

<success_criteria>
- ERRO-01 and ERRO-03 have automated regression coverage for critical paths.
- UI reflects backend failed states without contradictions.
- Roadmap SC2–SC4 addressed for this phase scope.
</success_criteria>

---
phase: "05"
plan: "05-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["ERRO-01", "ERRO-03"]
files_modified:
  - src/domain/types.ts
  - src/domain/execution-failure.ts
  - src/application/execution-controller.ts
  - src/domain/recursive-language-model.ts
autonomous: true
must_haves:
  truths:
    - "D-01: Run-level snapshot status never reports completed when any node failed (per ERRO-01)."
    - "D-06: A documented failure/run vocabulary exists in code for CLI and UI to share (codes + human labels)."
    - "D-07: Node and run summaries derive from the same status category for a given failure."
    - "D-09: Recursive execution does not leave executionStatus as completed when outcome is failure."
    - "D-12: Exact code strings are implementation-chosen but mapping is explicit in src/domain/execution-failure.ts."
threat_model:
  threats:
    - id: "T-05-01"
      severity: "low"
      risk: "Over-broad failure classification could mark successful runs as failed."
      mitigation: "Derive run terminal status from explicit node statuses and metadata.errors only when spec-defined."
---

<objective>
Establish a single failure vocabulary in the domain layer and fix authoritative session/run status so failed work is never summarized as completed. Align recursive engine terminal status with actual outcome per 05-CONTEXT.md D-01, D-06, D-07, D-09.
</objective>

<tasks>
  <task id="1" type="implementation">
    <read_first>
      <file>.planning/phases/05-reliability-and-no-silent-failure-hardening/05-CONTEXT.md</file>
      <file>src/domain/types.ts</file>
    </read_first>
    <action>Add `src/domain/execution-failure.ts` exporting (per D-12): a small `ExecutionFailureCategory` union (e.g. `model` | `tool` | `workflow` | `validation` | `cancelled` | `internal`), stable string `code` values for each case the product surfaces, `labelForCategory()`, and `summarizeRunFromNodes(nodes: Pick<ExecutionGraphNode, \"status\" | \"id\" | \"label\">[]): { terminal: ExecutionStatus; primaryMessage?: string }` documenting the mapping table in a file header comment (D-06). Extend `ExecutionEvent` in `src/domain/types.ts` with optional `failureCategory?: ExecutionFailureCategory` and `code?: string` so JSON streams and logs can carry the same facts as UI (D-04). Re-export types from `types.ts` or keep imports from `execution-failure.ts` — pick one pattern and use it consistently in later plans.</action>
    <files>
      <file>src/domain/types.ts</file>
      <file>src/domain/execution-failure.ts</file>
    </files>
    <verify>`npm run build` exits 0 and `rg -n "ExecutionFailureCategory|labelForCategory|summarizeRunFromNodes" src/domain/execution-failure.ts src/domain/types.ts` finds the new contracts.</verify>
    <acceptance_criteria>
      <criterion>New module is imported by at least one consumer in a later task or Phase 5 plan (execution-controller or CLI) without circular imports.</criterion>
      <criterion>`ExecutionEvent` optionally carries failure category and code for streaming consumers.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <read_first>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/execution-failure.ts</file>
    </read_first>
    <action>Fix `InteractiveExecutionSession.snapshot()` per D-01 and roadmap SC3: when `terminal` is true, set top-level `status` to `failed` if any node has `status === \"failed\"`; use `cancelled` if `cancellation.isCancelled()` (precedence over failed if both — document chosen rule in a one-line comment); otherwise if all non-failed terminals are `completed` or `skipped`, use `completed`. Never return `completed` when any node is `failed`. Optionally publish an `execution` event when transitioning to failed/cancelled terminal states so `--json-stream` observers see run-level alignment (D-04). Wire `failureCategory`/`code` on events when controller marks a node failed from a known cause.</action>
    <files>
      <file>src/application/execution-controller.ts</file>
    </files>
    <verify>`npm run build` exits 0 and `rg -n "status:.*failed|snapshot\\(" src/application/execution-controller.ts` shows revised aggregation logic.</verify>
    <acceptance_criteria>
      <criterion>Snapshot with at least one `failed` node yields session `status` === `failed`.</criterion>
      <criterion>Cancelled session still reports `cancelled` at run level when cancellation was requested.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <read_first>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/domain/types.ts</file>
    </read_first>
    <action>Audit `RecursiveLanguageModel.run` and inner paths for D-02/D-09: ensure `metadata.executionStatus` is set to `\"failed\"` when execution stops due to model throw, budget exhaustion treated as failure, or persistent tool/model errors that invalidate the answer — not `\"completed\"` with only `metadata.errors` populated. Ensure child/recursive failures propagate: if a sub-task fails, parent terminal state and `executionControl.updateNodeStatus` reflect `failed`. Push tool error strings to `metadata.errors` (existing) and align `executionStatus` with failure when the engine returns an answer but marks degraded state — follow one coherent rule documented in code comments (avoid contradicting D-01).</action>
    <files>
      <file>src/domain/recursive-language-model.ts</file>
    </files>
    <verify>`npm test` exits 0; extend or add assertions in `tests/recursive-language-model.test.ts` only if this task needs new unit coverage — otherwise leave dedicated regressions to 05-04-PLAN.md.</verify>
    <acceptance_criteria>
      <criterion>No code path returns `executionStatus: \"completed\"` when the engine recorded a hard failure path for that run.</criterion>
      <criterion>Recursive children that fail lead to parent run failure visibility (not hidden \"completed\").</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build` and `npm test`.
- Grep for `summarizeRunFromNodes` / updated `snapshot` / `executionStatus` in domain and controller.
</verification>

<success_criteria>
- ERRO-03: Domain and session state do not mask failures as success.
- ERRO-01: Authoritative backend status is correct for downstream UI/API/CLI (05-02/05-03 consume this).
- D-06 mapping table exists in `execution-failure.ts` header comment.
</success_criteria>

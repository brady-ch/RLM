---
phase: "03"
plan: "03-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["PLAN-02", "MODL-02", "MODL-03"]
files_modified:
  - src/domain/types.ts
  - src/application/execution-controller.ts
  - src/domain/recursive-language-model.ts
  - src/application/model-provider.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "Planned model is persisted on node at creation."
    - "Node override applies to current node only."
    - "Selected model failure is strict-fail with explicit error (no silent fallback)."
---

<objective>
Implement backend model metadata and per-node override mechanics so execution uses planned/overridden model deterministically with strict failure semantics.
</objective>

<tasks>
  <task id="1" type="implementation">
    <action>Extend execution node/domain metadata for planned and effective model fields plus override source trail.</action>
    <files>
      <file>src/domain/types.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
    </files>
    <verify>Typecheck confirms node metadata fields are available through graph snapshots and events.</verify>
    <acceptance_criteria>Each node can carry planned/effective model values and override provenance.</acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <action>Add controller-level per-node model override API (current node only) at checkpoint boundaries.</action>
    <files>
      <file>src/application/execution-controller.ts</file>
    </files>
    <verify>Attempting override outside editable statuses returns explicit structured error.</verify>
    <acceptance_criteria>Override mutates only targeted node model assignment; no descendant/global propagation.
</acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <action>Wire execution/model-provider flow to apply node model override for that node and strict-fail on selected model failure without automatic fallback.</action>
    <files>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/model-provider.ts</file>
    </files>
    <verify>Tests capture strict-fail behavior and no hidden model fallback on explicit selection failure.</verify>
    <acceptance_criteria>Final effective model used per node is deterministic and auditable.
</acceptance_criteria>
  </task>

  <task id="4" type="testing">
    <action>Add tests for planned model persistence, per-node override scope, and strict-fail routing behavior.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` passes with new model-routing assertions.</verify>
    <acceptance_criteria>Regression tests fail if override leaks scope or fallback occurs silently.
</acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`
- Run `npm test`
- Confirm strict fail path is visible for invalid/failed explicit node model.
</verification>

<success_criteria>
- Backend supports node-level planned/effective model contract.
- Override scope is current-node only.
- Strict fail semantics enforced.
- Model routing trail is captured for later display/logging.
</success_criteria>

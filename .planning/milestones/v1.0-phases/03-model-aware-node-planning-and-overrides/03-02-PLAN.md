---
phase: "03"
plan: "03-02"
type: "standard"
wave: 2
depends_on: ["03-01"]
requirements_addressed: ["MODL-01", "MODL-02", "MODL-03"]
files_modified:
  - src/application/control-server.ts
  - ui/src/main.tsx
  - ui/src/styles.css
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "Node cards show planned + effective model."
    - "Checkpoint UI supports per-node override action."
    - "Model routing audit trail is visible and explicit in outputs/logs."
---

<objective>
Expose model planning/override/audit data to UI and CLI so developers can see and control per-node model behavior transparently.
</objective>

<tasks>
  <task id="1" type="implementation">
    <action>Add control-server endpoint(s) for per-node model override and return updated node model metadata.</action>
    <files>
      <file>src/application/control-server.ts</file>
    </files>
    <verify>Override API returns structured errors when override is invalid for node status or model selection.</verify>
    <acceptance_criteria>Transport remains proxy-only over controller override API.
</acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <action>Update UI node card and inspector to display planned + effective model and provide per-node override input/action during checkpoints.</action>
    <files>
      <file>ui/src/main.tsx</file>
      <file>ui/src/styles.css</file>
    </files>
    <verify>Manual UI flow shows planned/effective divergence clearly after override.</verify>
    <acceptance_criteria>Model metadata is visible on cards and editable via checkpoint action.
</acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <action>Update CLI rendering/audit output to include per-node full model trail (planned, override source, fallback reason if present, final effective model).</action>
    <files>
      <file>src/cli/render.ts</file>
    </files>
    <verify>CLI output/JSON includes full trail fields for debugging model-routing decisions.</verify>
    <acceptance_criteria>No ambiguity remains about which model was planned vs executed.
</acceptance_criteria>
  </task>

  <task id="4" type="testing">
    <action>Add integration assertions for override endpoint behavior and planned/effective model visibility contracts.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` confirms UI/API-level model metadata and override semantics.
</verify>
    <acceptance_criteria>Failures in display/override trail behavior are caught by automated tests.
</acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`
- Run `npm test`
- Validate UI displays planned+effective model and strict-fail visibility.
</verification>

<success_criteria>
- Planned+effective model shown per node.
- Per-node override available and scoped correctly.
- Full model routing trail appears in logs/output.
- Phase 3 requirement coverage complete.
</success_criteria>

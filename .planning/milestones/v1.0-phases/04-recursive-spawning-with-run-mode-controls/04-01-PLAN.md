---
phase: "04"
plan: "04-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["APRV-05", "RECR-01", "RECR-02"]
files_modified:
  - src/domain/types.ts
  - src/application/execution-controller.ts
  - src/domain/recursive-language-model.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "D-01: approval modes are a first-class runtime concept."
    - "D-02: --require-approval preserves full checkpoint approval."
    - "D-06: machine values are full, initial-plan, and initial-plan-recursive."
    - "D-07: spawned nodes remain visible in the execution graph."
    - "D-08: auto-approved spawned nodes have explicit approved/auto-approved metadata before running."
    - "D-09: initial-plan pauses when recursion creates a new unseen branch."
    - "D-10: initial-plan-recursive auto-approves newly spawned recursive branches unless hard risk occurs."
    - "D-11: pause future auto-approvals affects future nodes only."
    - "D-15: hard system risks pause or fail visibly."
    - "D-16: hard risks are not silently converted into auto-approval."
threat_model:
  threats:
    - id: "T-04-01"
      severity: "high"
      risk: "Invalid approval mode silently falls back to full or auto behavior."
      mitigation: "Validate mode values at type/parse boundaries and throw explicit errors for unknown values."
    - id: "T-04-02"
      severity: "high"
      risk: "initial-plan auto-approves new recursive branches the user has not seen."
      mitigation: "Use spawnedAfterInitialApproval metadata and require manual approval for those nodes in initial-plan mode."
    - id: "T-04-03"
      severity: "medium"
      risk: "Auto-approved nodes run invisibly without graph or event evidence."
      mitigation: "Set approvalSource=auto, approvalReason, and publish an approved event before running."
---

<objective>
Implement the backend approval-mode policy for full checkpoints, initial-plan, and initial-plan-recursive while preserving recursive child spawning and explicit hard-risk failure behavior.
</objective>

<tasks>
  <task id="1" type="implementation">
    <read_first>
      <file>.planning/phases/04-recursive-spawning-with-run-mode-controls/04-CONTEXT.md</file>
      <file>src/domain/types.ts</file>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
    </read_first>
    <action>Add `export type ApprovalMode = "full" | "initial-plan" | "initial-plan-recursive";` to `src/domain/types.ts`. Add `approvalMode?: ApprovalMode`, `approvalSource?: "manual" | "auto" | "none"`, `approvalReason?: string`, `spawnedAfterInitialApproval?: boolean`, and `autoApprovalPaused?: boolean` to `ExecutionGraphNode`. Add `approvalMode?: ApprovalMode`, `pauseFutureAutoApprovals?: (() => void) | undefined`, and `autoApprovalPaused?: (() => boolean) | undefined` to `ExecutionControl`. Extend `ExecutionEvent` with optional `approvalMode?: ApprovalMode`, `approvalSource?: "manual" | "auto" | "none"`, and `message?: string` already exists; use message text `node auto-approved` for auto approvals.</action>
    <files>
      <file>src/domain/types.ts</file>
    </files>
    <verify>`npm run build` exits 0 and `rg -n "type ApprovalMode|approvalSource|spawnedAfterInitialApproval|pauseFutureAutoApprovals" src/domain/types.ts` finds the new contract fields.</verify>
    <acceptance_criteria>
      <criterion>`src/domain/types.ts` contains `export type ApprovalMode = "full" | "initial-plan" | "initial-plan-recursive";`.</criterion>
      <criterion>`ExecutionGraphNode` contains `approvalSource?: "manual" | "auto" | "none"`.</criterion>
      <criterion>`ExecutionControl` contains `pauseFutureAutoApprovals?: (() => void) | undefined`.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <read_first>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/types.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
    </read_first>
    <action>Update `InteractiveExecutionSession` to accept an optional constructor input `{ approvalMode?: ApprovalMode }`, defaulting to `"full"`. Add private fields `approvalMode: ApprovalMode`, `initialPlanAccepted = false`, and `futureAutoApprovalsPaused = false`. In `registerNode`, default node metadata to `approvalMode: this.approvalMode`, `approvalSource: input.approvalSource ?? "none"`, and `autoApprovalPaused: this.futureAutoApprovalsPaused`. Add public methods `pauseFutureAutoApprovals()` and `autoApprovalPaused()`; the pause method sets `futureAutoApprovalsPaused = true` and publishes `{ type: "execution", status: "ready", message: "future auto-approvals paused", approvalMode: this.approvalMode }` without changing the current running node.</action>
    <files>
      <file>src/application/execution-controller.ts</file>
    </files>
    <verify>`npm run build` exits 0 and `rg -n "initialPlanAccepted|futureAutoApprovalsPaused|pauseFutureAutoApprovals|autoApprovalPaused" src/application/execution-controller.ts` finds the session fields and methods.</verify>
    <acceptance_criteria>
      <criterion>`createInteractiveExecutionSession({ approvalMode: "initial-plan" })` is a valid call.</criterion>
      <criterion>`pauseFutureAutoApprovals()` publishes `future auto-approvals paused` and does not set any running node to `cancelled` or `awaiting_approval`.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <read_first>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/types.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
    </read_first>
    <action>Refactor `waitForNodeApprovalInternal` to call a new private helper `shouldAutoApprove(node: ExecutionGraphNode): { auto: boolean; reason: string }`. Return `{ auto: false, reason: "full checkpoint approval required" }` when mode is `"full"` or `futureAutoApprovalsPaused` is true. For `"initial-plan"`, return auto true only when `initialPlanAccepted` is true and `node.spawnedAfterInitialApproval !== true`; return auto false with reason `new recursive branch requires approval` when `spawnedAfterInitialApproval === true`. For `"initial-plan-recursive"`, return auto true when `initialPlanAccepted` is true regardless of `spawnedAfterInitialApproval`. Manual approval through `approveNode` must set `node.approvalSource = "manual"` and `initialPlanAccepted = true`. Auto approval must set `node.approvalSource = "auto"`, `node.approvalReason = reason`, clear `approvalToken`, update status to `"approved"`, publish `node auto-approved`, resolve immediately with status `"approved"`, and then allow normal execution to mark the node `"running"`.</action>
    <files>
      <file>src/application/execution-controller.ts</file>
    </files>
    <verify>`npm test` exits 0 with tests for manual first approval, initial-plan branch pause, and initial-plan-recursive auto approval.</verify>
    <acceptance_criteria>
      <criterion>`approveNode` sets `approvalSource` to `"manual"`.</criterion>
      <criterion>Auto-approved nodes have status `"approved"` before the model marks them `"running"`.</criterion>
      <criterion>`initial-plan` does not auto-approve a node whose graph metadata has `spawnedAfterInitialApproval: true`.</criterion>
      <criterion>`initial-plan-recursive` auto-approves a node whose graph metadata has `spawnedAfterInitialApproval: true`.</criterion>
    </acceptance_criteria>
  </task>

  <task id="4" type="implementation">
    <read_first>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/types.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
    </read_first>
    <action>In `RecursiveLanguageModel`, add a private boolean field `initialApprovalBoundaryPassed = false`. Set it to true immediately after the first non-skipped approval decision returns from `waitForNodeApproval`. In `ensureExecutionNode` and the local `node` object inside `waitForNodeApproval`, set `spawnedAfterInitialApproval: this.initialApprovalBoundaryPassed && task.parentId !== undefined`. Preserve existing `parentId`, `depth`, model metadata, and status behavior. Do not catch model/tool/budget errors in the approval-policy path.</action>
    <files>
      <file>src/domain/recursive-language-model.ts</file>
    </files>
    <verify>`npm run build` exits 0 and `rg -n "initialApprovalBoundaryPassed|spawnedAfterInitialApproval" src/domain/recursive-language-model.ts` finds the boundary logic.</verify>
    <acceptance_criteria>
      <criterion>Root/depth-selector nodes created before first approval have `spawnedAfterInitialApproval` unset or false.</criterion>
      <criterion>Children created by `decompose` after first approval have `spawnedAfterInitialApproval: true`.</criterion>
      <criterion>Existing model override metadata remains unchanged for child nodes.</criterion>
    </acceptance_criteria>
  </task>

  <task id="5" type="testing">
    <read_first>
      <file>tests/recursive-language-model.test.ts</file>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
    </read_first>
    <action>Add tests named exactly `initial-plan mode pauses on newly spawned recursive branches`, `initial-plan-recursive mode auto-approves newly spawned recursive branches`, `pause future auto approvals affects future nodes only`, and `auto-approved nodes are emitted before running`. Use the existing queue-model and session helpers. Assert graph snapshots and execution events include `approvalSource: "auto"` for auto approvals and that `awaiting_approval` appears for new recursive branches in conservative `initial-plan` mode.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` exits 0 and `rg -n "initial-plan mode pauses|initial-plan-recursive mode auto-approves|pause future auto approvals|auto-approved nodes are emitted" tests/recursive-language-model.test.ts` finds the new regression tests.</verify>
    <acceptance_criteria>
      <criterion>Tests fail if spawned nodes disappear from `session.snapshot().graph.nodes`.</criterion>
      <criterion>Tests fail if `initial-plan` auto-approves `spawnedAfterInitialApproval: true` nodes.</criterion>
      <criterion>Tests fail if `pauseFutureAutoApprovals` cancels the current running node.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`.
- Run `npm test`.
- Confirm `rg -n "approvalSource|spawnedAfterInitialApproval|pauseFutureAutoApprovals" src tests` shows implementation and tests.
</verification>

<success_criteria>
- `APRV-05` has a first-class backend approval-mode contract.
- `RECR-01` recursive child spawning remains intact.
- `RECR-02` spawned nodes inherit the active run-mode behavior deterministically.
- Auto-approved nodes are observable before running.
- Hard-risk error paths remain explicit and are not converted into approval success.
</success_criteria>


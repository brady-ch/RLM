---
phase: "04"
plan: "04-03"
type: "standard"
wave: 3
depends_on: ["04-01", "04-02"]
requirements_addressed: ["APRV-05", "RECR-01", "RECR-02"]
files_modified:
  - src/domain/recursive-language-model.ts
  - src/application/execution-controller.ts
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "D-12: CLI emits event stream plus final summary for spawned auto-approved nodes."
    - "D-15: hard system risks must pause or fail visibly in both initial-plan modes."
    - "D-16: hard risks are not silently converted into auto-approval or fallback behavior."
    - "D-17: initial-plan and initial-plan-recursive differ only on new recursive branch auto-approval."
    - "D-21: prioritize domain/controller tests for approval policy inheritance, auto-approval, pause-future, and branch policy."
    - "D-22: require cross-surface behavior coverage across domain/controller, CLI, and UI for the mode contract."
threat_model:
  threats:
    - id: "T-04-06"
      severity: "high"
      risk: "A hard model/tool/budget/cancellation failure is hidden by auto-approval logic."
      mitigation: "Add regression tests that inject failures under initial-plan-recursive and assert failed/cancelled/error metadata is visible."
    - id: "T-04-07"
      severity: "medium"
      risk: "initial-plan and initial-plan-recursive drift beyond the intended branch-policy difference."
      mitigation: "Shared tests assert identical hard-risk behavior and different spawned-branch approval behavior only."
---

<objective>
Harden recursive run-mode behavior with cross-surface regression coverage, explicit final summaries, and no-silent-failure checks for hard-risk paths.
</objective>

<tasks>
  <task id="1" type="testing">
    <read_first>
      <file>tests/recursive-language-model.test.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/execution-controller.ts</file>
      <file>.planning/phases/04-recursive-spawning-with-run-mode-controls/04-VALIDATION.md</file>
    </read_first>
    <action>Add an end-to-end recursive execution test named exactly `recursive spawning remains observable under initial-plan-recursive`. Configure a queue model that selects recursive depth, decomposes into at least two child tasks, answers children, summarizes, and synthesizes. Run with `createInteractiveExecutionSession({ approvalMode: "initial-plan-recursive" })`. Manually approve the first checkpoint, then assert all spawned child nodes appear in `session.snapshot().graph.nodes`, have parent edges, and include `approvalSource: "auto"` before completion.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` exits 0 and `rg -n "recursive spawning remains observable under initial-plan-recursive" tests/recursive-language-model.test.ts` finds the test.</verify>
    <acceptance_criteria>
      <criterion>Test fails if recursive children are not registered in the graph.</criterion>
      <criterion>Test fails if auto-approved child nodes lack `approvalSource: "auto"`.</criterion>
      <criterion>Test fails if parent-child edges are missing for spawned nodes.</criterion>
    </acceptance_criteria>
  </task>

  <task id="2" type="testing">
    <read_first>
      <file>tests/recursive-language-model.test.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/execution-controller.ts</file>
    </read_first>
    <action>Add tests named exactly `model errors remain visible in initial-plan-recursive mode`, `tool errors remain visible in initial-plan-recursive mode`, `budget exhaustion remains visible in initial-plan-recursive mode`, and `cancellation remains visible in initial-plan-recursive mode`. Use existing queue model/tool test helpers. Assert `result.metadata.errors` or thrown error text contains the explicit failure text, graph status becomes `failed` or `cancelled` as appropriate, and no assertion relies on silent fallback output.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` exits 0 and `rg -n "model errors remain visible|tool errors remain visible|budget exhaustion remains visible|cancellation remains visible" tests/recursive-language-model.test.ts` finds the tests.</verify>
    <acceptance_criteria>
      <criterion>Tests fail if auto-approval catches and suppresses model errors.</criterion>
      <criterion>Tests fail if tool error output is omitted from `metadata.errors`.</criterion>
      <criterion>Tests fail if cancellation does not produce `cancelled` execution status.</criterion>
    </acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <read_first>
      <file>src/cli/render.ts</file>
      <file>src/domain/types.ts</file>
      <file>tests/recursive-language-model.test.ts</file>
    </read_first>
    <action>Update final CLI graph summary rendering to include approval mode, approval source, and branch policy per node. Use exact compact fragments `approvalMode=<value>`, `approvalSource=<manual|auto|none>`, and `spawnedAfterInitialApproval=<true|false>`. Keep existing planned/effective model trail output. For final summaries, count auto-approved nodes and include exact text `autoApprovedNodes=<n>` when executionGraph exists.</action>
    <files>
      <file>src/cli/render.ts</file>
    </files>
    <verify>`npm test` exits 0 and `rg -n "autoApprovedNodes|approvalMode=|approvalSource=|spawnedAfterInitialApproval=" src/cli/render.ts tests/recursive-language-model.test.ts` finds renderer support and tests.</verify>
    <acceptance_criteria>
      <criterion>Compact output includes `autoApprovedNodes=0` when no nodes were auto-approved.</criterion>
      <criterion>Compact output includes `approvalSource=auto` for auto-approved nodes.</criterion>
      <criterion>Existing model trail fragments remain present.</criterion>
    </acceptance_criteria>
  </task>

  <task id="4" type="testing">
    <read_first>
      <file>tests/recursive-language-model.test.ts</file>
      <file>ui/src/main.tsx</file>
      <file>src/application/control-server.ts</file>
      <file>src/cli/args.ts</file>
      <file>src/cli/render.ts</file>
    </read_first>
    <action>Add a cross-surface contract test named exactly `approval mode contract is consistent across cli api and ui labels`. Assert CLI parser accepts machine values `full`, `initial-plan`, `initial-plan-recursive`; control-server/session snapshot exposes `approvalMode`; UI source contains labels `Full checkpoints`, `Initial plan`, and `Initial plan + recursive`; CLI render contains the active machine value. This test may read UI source text directly rather than launching a browser.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` exits 0 and `rg -n "approval mode contract is consistent across cli api and ui labels" tests/recursive-language-model.test.ts` finds the test.</verify>
    <acceptance_criteria>
      <criterion>Test fails if any UI label diverges from D-05.</criterion>
      <criterion>Test fails if any CLI machine value diverges from D-06.</criterion>
      <criterion>Test fails if API snapshot omits `approvalMode`.</criterion>
    </acceptance_criteria>
  </task>

  <task id="5" type="testing">
    <read_first>
      <file>tests/recursive-language-model.test.ts</file>
      <file>src/application/execution-controller.ts</file>
      <file>src/domain/recursive-language-model.ts</file>
    </read_first>
    <action>Add a branch-policy comparison test named exactly `initial-plan modes differ only on spawned branch auto approval`. Run the same recursive prompt twice, once with `approvalMode: "initial-plan"` and once with `"initial-plan-recursive"`. Assert both modes manually approve the initial checkpoint, both expose hard-risk handling through the same error path, and the only approval policy difference is that the spawned child reaches `awaiting_approval` in `"initial-plan"` while the equivalent spawned child receives `approvalSource: "auto"` in `"initial-plan-recursive"`.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` exits 0 and `rg -n "initial-plan modes differ only on spawned branch auto approval" tests/recursive-language-model.test.ts` finds the comparison test.</verify>
    <acceptance_criteria>
      <criterion>Test fails if initial-plan-recursive changes hard-risk handling.</criterion>
      <criterion>Test fails if initial-plan auto-approves newly spawned child branches.</criterion>
      <criterion>Test fails if initial-plan-recursive pauses newly spawned child branches without a hard-risk condition.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`.
- Run `npm run build:ui`.
- Run `npm test`.
- Confirm Phase 4 requirement IDs `APRV-05`, `RECR-01`, and `RECR-02` appear in PLAN frontmatter across the phase.
</verification>

<success_criteria>
- Recursive spawning is preserved under all run modes.
- `initial-plan` and `initial-plan-recursive` differ only on new recursive branch auto-approval.
- CLI/API/UI all expose the same mode contract and auto-approval metadata.
- Hard-risk failures remain visible and test-covered.
- Phase 4 is ready for execution with no uncovered requirements or decisions.
</success_criteria>


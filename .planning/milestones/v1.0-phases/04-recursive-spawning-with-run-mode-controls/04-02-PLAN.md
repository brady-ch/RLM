---
phase: "04"
plan: "04-02"
type: "standard"
wave: 2
depends_on: ["04-01"]
requirements: ["APRV-05", "RECR-02"]
requirements_addressed: ["APRV-05", "RECR-02"]
files_modified:
  - src/application/control-server.ts
  - ui/src/main.tsx
  - ui/src/styles.css
  - src/cli/render.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "User can select and inspect active run mode from CLI/UI flows before and during execution."
    - "Spawned nodes are observable with auto-approved/approved status and branch policy behavior."
    - "User can stop and pause future auto-approvals across API/UI/CLI surfaces."
    - "Auto-run still ends with explicit summary of spawned-node behavior."
  artifacts:
    - path: "src/application/control-server.ts"
      provides: "Run-mode and pause-future controls for UI/API."
    - path: "ui/src/main.tsx"
      provides: "Mode labels, spawned-node badges/status, and pause-future control UX."
    - path: "src/cli/render.ts"
      provides: "Event-stream/final-summary observability for spawned auto-approved nodes."
  key_links:
    - from: "src/application/control-server.ts"
      to: "ui/src/main.tsx"
      via: "control endpoints for mode selection and pause-future actions"
    - from: "src/application/execution-controller.ts"
      to: "src/cli/render.ts"
      via: "event/status payloads rendered for CLI observability"
    - from: "ui/src/main.tsx"
      to: "tests/recursive-language-model.test.ts"
      via: "cross-surface contract assertions for mode/state display"
---

<objective>
Expose run-mode controls and spawned-node observability consistently across API, UI, and CLI so users can operate recursive auto-run safely and transparently.

Purpose: Complete cross-surface Phase 4 behavior and evidence for APRV-05 and RECR-02 after backend policy contracts are in place.
Output: Control-server/UI/CLI updates plus cross-surface regression coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-recursive-spawning-with-run-mode-controls/04-CONTEXT.md
@.planning/phases/04-recursive-spawning-with-run-mode-controls/04-01-PLAN.md
@src/application/control-server.ts
@src/cli/render.ts
@ui/src/main.tsx
@ui/src/styles.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend control-server run controls and validation</name>
  <files>src/application/control-server.ts</files>
  <behavior>
    - API exposes selected approval mode using stable machine values and user labels (per D-05, D-06).
    - API supports stop and pause-future-auto-approvals actions with explicit result payloads (per D-14).
    - Invalid transitions/settings return structured actionable errors, never silent no-ops (per D-16, D-20).
  </behavior>
  <action>Add transport endpoints/handlers required for pre-approval mode selection and in-run pause-future controls, keeping control-server as a thin validation and controller-proxy layer consistent with prior phases. Ensure response payloads include run-mode and spawned-node status data needed by UI/CLI displays.</action>
  <verify>
    <automated>npm test -- recursive-language-model.test.js -t "control server run mode"</automated>
  </verify>
  <done>Control API provides run-mode and pause-future controls with explicit success/error semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UI run-mode controls and spawned-node visibility</name>
  <files>ui/src/main.tsx, ui/src/styles.css</files>
  <behavior>
    - UI shows `Full checkpoints`, `Initial plan`, and `Initial plan + recursive` labels (per D-05).
    - Spawned auto-approved nodes remain visible in graph with explicit badges/statuses (per D-07, D-08, D-13).
    - UI allows pause-future-auto-approvals and stop actions while preserving keyboard and focus access.
    - Hard-risk pauses/failures remain clearly visible and not masked as auto-run success (per D-15, D-16).
  </behavior>
  <action>Wire mode selector and pause-future controls to control-server endpoints, update node card/inspector rendering for auto-approved and branch-policy states, and preserve existing backend-authoritative graph rendering pattern. Keep visual and interaction behavior explicit so users can distinguish conservative vs recursive auto-run behavior.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>UI exposes mode contract and spawned-node run state clearly, with actionable controls for stop and pause-future behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI observability and cross-surface regression tests</name>
  <files>src/cli/render.ts, tests/recursive-language-model.test.ts</files>
  <behavior>
    - CLI stream/final summary includes spawned auto-approved node events/statuses (per D-12).
    - Tests verify mode contract parity across controller/API/UI/CLI surfaces (per D-22).
    - Tests verify pause-future semantics affect only future nodes and preserve current-node completion behavior (per D-11, D-14).
  </behavior>
  <action>Update CLI rendering for run-mode and spawned-node event narratives and add integration assertions that exercise cross-surface consistency, especially mode labels/values mapping and pause-future behavior. Keep test assertions specific to user-visible outcomes rather than implementation internals.</action>
  <verify>
    <automated>npm test -- recursive-language-model.test.js</automated>
  </verify>
  <done>Cross-surface behavior is automated and fails fast on any mismatch in mode semantics or spawned-node observability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI actions -> control API | User-triggered mode/control actions enter backend state transitions. |
| Controller events -> CLI/UI rendering | Incorrect mapping can hide safety state or show inaccurate approval status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `src/application/control-server.ts` | mitigate | Enforce status-aware transition validation for pause/stop/mode actions and return structured errors on invalid transitions. |
| T-04-05 | Information Disclosure | `src/cli/render.ts`, `ui/src/main.tsx` | mitigate | Render only execution state needed for observability; avoid leaking internals while preserving risk visibility and actionability. |
| T-04-06 | Repudiation | `src/cli/render.ts`, `ui/src/main.tsx` | mitigate | Emit and render explicit auto-approved/approved transition markers and final summary lines for spawned nodes (D-12, D-13). |
</threat_model>

<verification>
- Run `npm run build`
- Run `npm test -- recursive-language-model.test.js`
- In interactive mode, verify mode labels, pause-future control, and spawned-node badge/state updates are visible in UI and CLI output.
</verification>

<success_criteria>
- User can select run mode pre-approval and control pause-future/stop during execution.
- Spawned nodes remain observable with explicit approval/auto-approval states.
- CLI and UI expose consistent mode and run-state semantics.
- APRV-05 and RECR-02 cross-surface expectations are covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-recursive-spawning-with-run-mode-controls/04-02-SUMMARY.md`
</output>

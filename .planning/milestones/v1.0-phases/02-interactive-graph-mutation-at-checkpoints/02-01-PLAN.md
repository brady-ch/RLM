---
phase: "02"
plan: "02-01"
type: "standard"
wave: 1
depends_on: []
requirements_addressed: ["APRV-02", "APRV-03", "APRV-04", "ERRO-02"]
files_modified:
  - src/application/execution-controller.ts
  - src/domain/types.ts
  - src/domain/recursive-language-model.ts
  - tests/recursive-language-model.test.ts
autonomous: true
must_haves:
  truths:
    - "All graph mutations are applied through execution-controller only."
    - "Delete cascades descendants and keeps graph/node maps coherent."
    - "Mutation validation failures return structured, testable error data."
---

<objective>
Implement controller-level graph mutation primitives (edit/add/delete/connect) with locked validation semantics and subtree re-evaluation readiness.
</objective>

<tasks>
  <task id="1" type="implementation">
    <action>Define typed mutation request/result and structured error model for checkpoint graph changes.</action>
    <files>
      <file>src/domain/types.ts</file>
      <file>src/application/execution-controller.ts</file>
    </files>
    <verify>Typecheck confirms mutation payloads and error contracts are consumed without `any`-style escapes.</verify>
    <acceptance_criteria>Structured errors include `code`, `message`, `nodeIds`, optional `details`, optional `suggestedFix`.</acceptance_criteria>
  </task>

  <task id="2" type="implementation">
    <action>Add controller methods for add/delete/connect node mutations with validation: valid parent for non-root, configured max-depth enforcement, cascade delete descendants, cycle allowance.</action>
    <files>
      <file>src/application/execution-controller.ts</file>
    </files>
    <verify>Controller tests confirm mutation behaviors and invariant preservation after each operation.</verify>
    <acceptance_criteria>Mutation APIs enforce locked rules and return structured error payloads for invalid requests.</acceptance_criteria>
  </task>

  <task id="3" type="implementation">
    <action>Implement affected-subtree re-evaluation contract after checkpoint mutation so resumed execution does not use stale downstream graph state.</action>
    <files>
      <file>src/domain/recursive-language-model.ts</file>
      <file>src/application/execution-controller.ts</file>
    </files>
    <verify>Integration tests prove checkpoint mutations alter subsequent execution path as expected.</verify>
    <acceptance_criteria>Resume behavior re-evaluates affected subtree rather than blindly continuing unchanged descendants.</acceptance_criteria>
  </task>

  <task id="4" type="testing">
    <action>Add/extend tests for add/delete/connect, cascade delete, invalid parent/depth, and structured mutation errors.</action>
    <files>
      <file>tests/recursive-language-model.test.ts</file>
    </files>
    <verify>`npm test` covers mutation operations and validation failures.
</verify>
    <acceptance_criteria>Regression tests fail if mutation invariants are broken or errors become message-only.</acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `npm run build`
- Run `npm test`
- Confirm controller mutation API returns structured errors for invalid graph mutations.
</verification>

<success_criteria>
- Controller mutation API exists and is authoritative.
- Locked mutation rules are enforced.
- Resume semantics account for subtree re-evaluation.
- Tests cover mutation success + failure paths.
</success_criteria>

---
phase: 123-workflow-view-simplification
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - ui/src/app/TopBar.tsx
  - ui/src/advanced/SettingsView.tsx
  - ui/src/run-panel/RunPanel.tsx
  - ui/src/app/AppShell.tsx
  - tests/ui/shell-boundaries.test.ts
  - tests/ui/reg01-static-wiring.test.ts
requirements: []
---

<objective>
Thin TopBar chrome, selection-gate RunPanel (approve/clarify only), remove WorkflowOverview from default workflow view, preserve first-run launcher path.
</objective>

<must_haves>
<truths>
- TopBar shows status, run/stop, Advanced only — no approval pill, run-variant pill, or theme toggle
- RunPanel renders only when a node is selected
- Unselected workflow view is canvas-only (no WorkflowOverview)
- FirstRunLauncher → graph path unchanged
</truths>
</must_haves>

<tasks>

<task id="1" type="auto">
<name>Trim TopBar and move theme to Advanced</name>
<read_first>
- ui/src/app/TopBar.tsx
- ui/src/advanced/SettingsView.tsx
- .planning/phases/123-workflow-view-simplification/123-CONTEXT.md
</read_first>
<action>
In TopBar.tsx: remove ThemeToggle import/usage, remove approval-mode-pill span, remove run-variant-pill span (Running playbook/pipeline pill). Keep status block, run/stop/resume/pause-auto-approval, Advanced button.

In SettingsView.tsx: import ThemeToggle and render it in an appearance section at top of advanced-settings-view.
</action>
<acceptance_criteria>
- `grep -q approval-mode-pill ui/src/app/TopBar.tsx` exits 1
- `grep -q ThemeToggle ui/src/app/TopBar.tsx` exits 1
- `grep -q ThemeToggle ui/src/advanced/SettingsView.tsx` exits 0
- TopBar still contains `/api/chat/confirm-run` and `Advanced`
</acceptance_criteria>
</task>

<task id="2" type="auto">
<name>Selection-gate RunPanel and slim approve/clarify</name>
<read_first>
- ui/src/run-panel/RunPanel.tsx
- ui/src/app/AppShell.tsx
- ui/src/run-panel/WorkflowOverview.tsx
- tests/ui/shell-boundaries.test.ts
</read_first>
<action>
In RunPanel.tsx: when `!selectedNode` return null (remove WorkflowOverview import). Remove prompt, approvalReason, readiness sections. Change back button label to "Close panel". Keep header, approve/skip, clarification. Drop unused props: onFitGraph, runDisabled, readinessReason.

In AppShell.tsx: stop passing removed RunPanel props.

In tests/ui/shell-boundaries.test.ts: assert RunPanel returns null when unselected (`return null`), does not import WorkflowOverview, AppShell workflow block does not reference workflow-overview testid when unselected path removed from RunPanel.

Add test: TopBar does not contain approval-mode-pill or run-variant-pill.
</action>
<acceptance_criteria>
- `grep -q WorkflowOverview ui/src/run-panel/RunPanel.tsx` exits 1
- `grep -q 'return null' ui/src/run-panel/RunPanel.tsx` exits 0
- RunPanel still matches `/approve/` and `/clarification/i`
- `npm run test:agent:verify:light` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
- npm run test:agent:verify:light
</verification>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| First-run path regression | mitigate | Existing first-run-launcher tests unchanged |
| Theme unreachable | mitigate | ThemeToggle in Advanced Settings |
</threat_model>

---
phase: 127-lazy-routes-and-bundle-lightening
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - ui/src/app/AppShell.tsx
  - ui/src/advanced/AdvancedHub.tsx
  - ui/src/advanced/AdvancedLoadingFallback.tsx
  - tests/ui/shell-boundaries.test.ts
requirements: []
---

<objective>
Code-split Advanced hub with React.lazy at AppShell and per-tab level; document bundle before/after vs Phase 121 baseline (522.60 kB JS / 45.52 kB CSS).
</objective>

<must_haves>
<truths>
- AdvancedHub loaded via React.lazy in AppShell (not static import)
- Advanced tab views lazy-loaded in AdvancedHub (not static imports)
- Workflow-first load: main chunk excludes Advanced tab modules
- Before/after bundle sizes recorded in SUMMARY vs 121 baseline
- npm run test:agent:verify:light passes
</truths>
</must_haves>

<tasks>

<task id="1" type="auto">
<name>Lazy-load AdvancedHub in AppShell with Suspense fallback</name>
<read_first>
- ui/src/app/AppShell.tsx
- ui/src/advanced/AdvancedHub.tsx
- .planning/phases/127-lazy-routes-and-bundle-lightening/127-CONTEXT.md
- tests/ui/shell-boundaries.test.ts
</read_first>
<action>
Remove static `import { AdvancedHub } from "../advanced/AdvancedHub"`. Add `React.lazy(() => import("../advanced/AdvancedHub").then(m => ({ default: m.AdvancedHub })))` as LazyAdvancedHub. Wrap advanced branch in Suspense with minimal loading fallback (create ui/src/advanced/AdvancedLoadingFallback.tsx with data-testid="advanced-loading"). Keep AdvancedTab type import as `import type` from AdvancedHub path in useViewRouter (unchanged).
</action>
<acceptance_criteria>
- `grep -q 'React.lazy' ui/src/app/AppShell.tsx` exits 0
- `grep -q 'import { AdvancedHub }' ui/src/app/AppShell.tsx` exits 1
- AppShell advanced branch uses Suspense with LazyAdvancedHub
- shell-boundaries workflow block still excludes AdvancedHub JSX
</acceptance_criteria>
</task>

<task id="2" type="auto">
<name>Lazy-load Advanced tab views in AdvancedHub</name>
<read_first>
- ui/src/advanced/AdvancedHub.tsx
- ui/src/advanced/ModelsView.tsx
</read_first>
<action>
Replace static imports of ModelsView, PluginsView, SessionsView, MemoryView, SettingsView with React.lazy per module. Wrap each active tab render in Suspense using AdvancedLoadingFallback (or tab-scoped variant). Remove all five static view imports from AdvancedHub.tsx top level.
</action>
<acceptance_criteria>
- `grep -E 'import \{ (ModelsView|PluginsView|SessionsView|MemoryView|SettingsView)' ui/src/advanced/AdvancedHub.tsx` exits 1
- AdvancedHub.tsx contains React.lazy for each tab view
- AdvancedHub still exports AdvancedTab type and AdvancedHub component
</acceptance_criteria>
</task>

<task id="3" type="auto">
<name>Bundle measurement, static tests, and verification</name>
<read_first>
- .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
- tests/ui/shell-boundaries.test.ts
</read_first>
<action>
Run npm run build:ui; capture JS/CSS chunk names and sizes. Add shell-boundaries tests asserting AppShell uses React.lazy for AdvancedHub and AdvancedHub uses lazy for tab views. Run npm run test:agent:verify:light. Record bundle comparison in SUMMARY (baseline 522.60 kB JS from Phase 121).
</action>
<acceptance_criteria>
- npm run build:ui exits 0
- npm run test:agent:verify:light exits 0
- Build output shows separate Advanced chunk(s) beyond main index
- tests/ui/shell-boundaries.test.ts contains lazy-route assertions
</acceptance_criteria>
</task>

</tasks>

<verification>
- npm run test:agent:verify:light
- npm run build:ui
</verification>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| Flash of empty Advanced on slow network | mitigate | Suspense fallback with aria-busy |
| Type import pulls Advanced chunk early | mitigate | import type only in useViewRouter |
| Static tests break on lazy pattern | mitigate | Update shell-boundaries assertions |
</threat_model>

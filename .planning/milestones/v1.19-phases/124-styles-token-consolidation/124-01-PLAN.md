---
phase: 124-styles-token-consolidation
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - ui/src/styles.css
  - ui/src/styles/base.css
  - ui/src/styles/canvas.css
  - ui/src/styles/nodes.css
  - ui/src/styles/workflow.css
  - ui/src/styles/advanced.css
  - tests/ui/styles-modularity.test.ts
requirements: []
---

<objective>
Split monolithic styles.css by concern, remove dead CSS from Phases 122–123, preserve canvas dot grid and node card polish. Single Vite bundle via import barrel.
</objective>

<must_haves>
<truths>
- styles.css is import barrel only (@import tokens + concern modules)
- Canvas dot grid and node-card rules unchanged in output
- Dead rules removed: approval-mode-pill, run-variant-pill, legacy shell/inspector, run-panel-prompt/detail, workflow-overview cluster
- npm run build:ui and npm run test:agent:verify:light pass
</truths>
</must_haves>

<tasks>

<task id="1" type="auto">
<name>Split styles.css by concern</name>
<read_first>
- ui/src/styles.css
- ui/src/shared/tokens.css
- .planning/phases/124-styles-token-consolidation/124-CONTEXT.md
</read_first>
<action>
Create ui/src/styles/ with:
- base.css — reset, body, buttons, forms, modals, status/meta pills, shared utilities
- canvas.css — react-flow canvas, edges, handles, minimap (preserve dot grid exactly)
- nodes.css — node cards, chips, ports, loop summary, context menus
- workflow.css — workflow shell, topbar, run-panel, first-run overlay
- advanced.css — advanced hub, sessions, plugins, models, memory, node-inspector, graph-workflow-panel

Replace styles.css body with @import chain: tokens.css then the five modules. Preserve all live rules; no visual redesign.
</action>
<acceptance_criteria>
- test -f ui/src/styles/base.css && test -f ui/src/styles/canvas.css
- grep -q '@import "./styles/' ui/src/styles.css
- grep -q 'radial-gradient' ui/src/styles/canvas.css
- grep -q 'node-card' ui/src/styles/nodes.css
</acceptance_criteria>
</task>

<task id="2" type="auto">
<name>Remove dead CSS from Phases 122–123</name>
<read_first>
- ui/src/styles.css
- ui/src/app/TopBar.tsx
- ui/src/run-panel/RunPanel.tsx
- ui/src/run-panel/WorkflowOverview.tsx
</read_first>
<action>
Remove orphaned selectors not referenced by mounted components:
- .shell, .inspector and related media query
- .run-variant-pill, .approval-mode-pill
- .run-panel-prompt, .run-panel-detail blocks
- .workflow-overview*, .memory-budget-*, .workflow-status-counts, .workflow-node-list*, .node-list-* (WorkflowOverview unmounted)
Keep .run-variant-controls (GraphWorkflowPanel), .run-success-hint (TopBar), .theme-toggle-label (ThemeToggle in Advanced).
</action>
<acceptance_criteria>
- grep -q approval-mode-pill ui/src/styles/ ; test $? -eq 1
- grep -q run-variant-pill ui/src/styles/ ; test $? -eq 1
- grep -q workflow-overview ui/src/styles/ ; test $? -eq 1
- grep -q run-variant-controls ui/src/styles/advanced.css
</acceptance_criteria>
</task>

<task id="3" type="auto">
<name>Add modularity test and verify build</name>
<files>tests/ui/styles-modularity.test.ts</files>
<action>
Add tests/ui/styles-modularity.test.ts asserting:
- styles.css imports all five modules
- canvas.css contains dot grid background-image
- dead selectors absent from ui/src/styles/
Run npm run build:ui and npm run test:agent:verify:light
</action>
<acceptance_criteria>
- npm run build:ui exits 0
- npm run test:agent:verify:light exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
- npm run build:ui
- npm run test:agent:verify:light
</verification>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| Canvas visual regression | mitigate | Preserve canvas.css rules verbatim; dot grid test |
| Broken import chain | mitigate | styles-modularity.test.ts + build:ui |
</threat_model>

---
phase: 122-advanced-hub-pruning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/advanced/settings/RefineGraphPanel.tsx
  - ui/src/advanced/settings/QualityLoopInspector.tsx
  - ui/src/advanced/SettingsView.tsx
  - ui/src/advanced/settings/NodeInspector.tsx
  - ui/src/advanced/AdvancedHub.tsx
  - ui/src/app/AppShell.tsx
  - ui/src/styles.css
  - tests/ui/cut-list-completeness.test.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "RefineGraphPanel.tsx and QualityLoopInspector.tsx deleted from ui/src"
    - "SettingsView no longer imports or renders deleted panels"
    - "NodeInspector no longer imports QualityLoopInspector"
    - "Advanced hub tab order is Models, Sessions, Plugins, Memory, Settings"
    - "Orphaned chatMessage, deleteStrategy, graphHasPlannedNodes props removed from AppShell → AdvancedHub → SettingsView chain"
    - "CSS rules exclusive to deleted panels removed from styles.css"
    - "npm run build:ui passes"
    - "npm run test:agent:verify:light passes"
  artifacts:
    - path: ui/src/advanced/AdvancedHub.tsx
      provides: "Reordered tab nav with Models and Sessions first"
      contains: '["models", "Models"], ["sessions", "Sessions"]'
    - path: ui/src/advanced/SettingsView.tsx
      provides: "Settings without deleted panels"
      min_lines: 50
  key_links:
    - from: ui/src/advanced/SettingsView.tsx
      to: ui/src/advanced/settings/GraphWorkflowPanel.tsx
      via: "retained settings panel"
      pattern: "GraphWorkflowPanel"
    - from: ui/src/nodes/QualityLoopCardSummary.tsx
      to: node cards
      via: "quality loop status remains on canvas"
      pattern: "QualityLoopCardSummary"
---

<objective>
Execute Phase 121 Delete verdicts for RefineGraphPanel and QualityLoopInspector; reorder Advanced hub tabs (Models, Sessions first); remove dead wiring and exclusive CSS.

Purpose: Simplify Advanced landing per v1.19 UI Product Simplification cut list.
Output: Deleted panel files, cleaned SettingsView/NodeInspector, reordered AdvancedHub tabs, passing build and agent verify.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-advanced-hub-pruning/122-CONTEXT.md
@.planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
@ui/src/advanced/AdvancedHub.tsx
@ui/src/advanced/SettingsView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete panel components and remove wiring</name>
  <files>
    ui/src/advanced/settings/RefineGraphPanel.tsx
    ui/src/advanced/settings/QualityLoopInspector.tsx
    ui/src/advanced/SettingsView.tsx
    ui/src/advanced/settings/NodeInspector.tsx
    ui/src/advanced/AdvancedHub.tsx
    ui/src/app/AppShell.tsx
  </files>
  <read_first>
    - ui/src/advanced/SettingsView.tsx
    - ui/src/advanced/settings/NodeInspector.tsx
    - ui/src/advanced/AdvancedHub.tsx
    - ui/src/app/AppShell.tsx
    - .planning/phases/122-advanced-hub-pruning/122-CONTEXT.md
  </read_first>
  <action>
1. Delete `ui/src/advanced/settings/RefineGraphPanel.tsx`
2. Delete `ui/src/advanced/settings/QualityLoopInspector.tsx`
3. In SettingsView.tsx: remove RefineGraphPanel import and JSX; remove props chatMessage, setChatMessage, deleteStrategy, setDeleteStrategy, graphHasPlannedNodes from SettingsViewProps and destructuring; remove pendingMutation variable if unused
4. In NodeInspector.tsx: remove QualityLoopInspector import and the `{node.loop ? <QualityLoopInspector .../> : null}` block (quality loop status remains on node cards via QualityLoopCardSummary)
5. In AdvancedHub.tsx: remove chatMessage, setChatMessage, deleteStrategy, setDeleteStrategy, graphHasPlannedNodes from AdvancedHubProps, destructuring, and SettingsView pass-through
6. In AppShell.tsx: remove useState for chatMessage and deleteStrategy; remove graphHasPlannedNodes computed value if only used for AdvancedHub; remove corresponding props passed to AdvancedHub
Do NOT modify NodeInspector slim-down beyond QualityLoopInspector removal (Phase 126).
Do NOT modify GraphWorkflowPanel or NodeInspector other sections.
  </action>
  <acceptance_criteria>
    - `test ! -f ui/src/advanced/settings/RefineGraphPanel.tsx` exits 0
    - `test ! -f ui/src/advanced/settings/QualityLoopInspector.tsx` exits 0
    - `grep -q RefineGraphPanel ui/src/advanced/SettingsView.tsx` exits 1
    - `grep -q QualityLoopInspector ui/src/advanced/settings/NodeInspector.tsx` exits 1
    - `grep -q chatMessage ui/src/app/AppShell.tsx` exits 1
    - `grep -q deleteStrategy ui/src/app/AppShell.tsx` exits 1
  </acceptance_criteria>
  <verify>grep -rE 'RefineGraphPanel|QualityLoopInspector' ui/src/ ; test $? -eq 1</verify>
  <done>Deleted panels and all imports/props wiring removed from ui/src chain</done>
</task>

<task type="auto">
  <name>Task 2: Reorder Advanced tabs and remove exclusive CSS</name>
  <files>
    ui/src/advanced/AdvancedHub.tsx
    ui/src/styles.css
  </files>
  <read_first>
    - ui/src/advanced/AdvancedHub.tsx
    - ui/src/styles.css
    - .planning/phases/122-advanced-hub-pruning/122-CONTEXT.md
  </read_first>
  <action>
1. In AdvancedHub.tsx tab array, reorder to: models, sessions, plugins, memory, settings (labels unchanged)
2. In styles.css remove CSS blocks used ONLY by deleted components:
   - `.chat-refine-panel` rules (lines ~270-278)
   - `.quality-loop-panel` and nested loop inspector rules: `.loop-inspector-grid`, `.loop-detail-block`, `.loop-iteration`, `.phase-row`, `.loop-issue` (lines ~912-978)
Do NOT split or refactor styles.css structure (Phase 124).
  </action>
  <acceptance_criteria>
    - AdvancedHub.tsx tab tuple order starts with `["models", "Models"]` then `["sessions", "Sessions"]` then `["plugins", "Plugins"]`
    - `grep -q chat-refine-panel ui/src/styles.css` exits 1
    - `grep -q quality-loop-panel ui/src/styles.css` exits 1
  </acceptance_criteria>
  <verify>grep -A6 'advanced-tabs' ui/src/advanced/AdvancedHub.tsx | head -15</verify>
  <done>Tabs reordered Models/Sessions first; dead CSS removed</done>
</task>

<task type="auto">
  <name>Task 3: Update cut-list test and verify build</name>
  <files>tests/ui/cut-list-completeness.test.ts</files>
  <read_first>
    - tests/ui/cut-list-completeness.test.ts
    - .planning/phases/121-ui-vision-audit-and-cut-list/121-CUT-LIST.md
  </read_first>
  <action>
Update cut-list-completeness.test.ts for post-deletion inventory:
1. Keep MANDATORY_SURFACES including RefineGraphPanel and QualityLoopInspector (121 audit doc still references them)
2. Change verdict row count assertion: use `assert.ok(verdictRows.length >= expectedRows)` instead of strict equality, since 121-CUT-LIST retains Delete rows for removed files while current auditable inventory is smaller
3. Run `npm run build:ui` and `npm run test:agent:verify:light`
  </action>
  <acceptance_criteria>
    - `node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/cut-list-completeness.test.ts` exits 0
    - `npm run build:ui` exits 0
    - `npm run test:agent:verify:light` exits 0
  </acceptance_criteria>
  <verify>npm run test:agent:verify:light</verify>
  <done>Build and agent verify pass; cut-list test updated for smaller live inventory</done>
</task>

</tasks>

<verification>
```bash
npm run build:ui
npm run test:agent:verify:light
node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/cut-list-completeness.test.ts
grep -rE 'RefineGraphPanel|QualityLoopInspector' ui/src/ ; test $? -eq 1
```
</verification>

<success_criteria>
- RefineGraphPanel and QualityLoopInspector deleted; no ui/src references remain
- Advanced tabs ordered Models, Sessions, Plugins, Memory, Settings
- npm run build:ui and npm run test:agent:verify:light pass
</success_criteria>

<threat_model>
| Threat | Severity | Mitigation | Disposition |
|--------|----------|------------|-------------|
| Orphaned props cause TypeScript errors | low | Remove full prop chain AppShell→AdvancedHub→SettingsView | mitigate |
| Quality loop actions lost | low | QualityLoopCardSummary on node cards retains inline status | accept |
| Cut-list test false failure | low | Relax row count for historical Delete rows | mitigate |
</threat_model>

# Phase 83 Plan 01: Canvas & Node Card Polish

## Tasks

1. Dot-grid canvas background in `styles.css` + `GraphCanvas.tsx` Background color
2. Refactor node card header to light design + status chips in `ExecutionNodeCard.tsx`
3. Theme MiniMap, handles, card hover/selected/running states
4. Verify build

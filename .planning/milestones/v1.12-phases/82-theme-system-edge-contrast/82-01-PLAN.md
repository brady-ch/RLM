# Phase 82 Plan 01: Theme System & Edge Contrast

**Phase:** 82  
**Requirements:** THEME-01, THEME-02, THEME-03, EDGE-01, EDGE-02, EDGE-03

## Tasks

1. Add `theme.ts` + `ThemeToggle.tsx`; wire TopBar and `main.tsx` init
2. Expand `tokens.css` with light/dark semantic tokens and edge variables
3. Add FOUC-prevention inline script to `index.html`
4. Update `graph-utils.ts` + `AppShell.tsx` for edge status classes
5. Style edges in `styles.css` using `--edge-*` tokens

## Verification

- `npm run build:ui` passes
- Theme toggle persists after refresh
- Edges visible in both themes (manual browser check in Phase 85)

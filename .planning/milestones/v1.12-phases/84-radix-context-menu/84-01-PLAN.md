# Phase 84 Plan 01: Radix Context Menu

## Tasks

1. Install `@radix-ui/react-context-menu`
2. Rewrite `NodeContextMenu.tsx` as `NodeContextMenuShell` with Radix primitives
3. Wrap `ExecutionNodeCard` in shell; wire ⋮ + keyboard
4. Add `.radix-context-menu*` CSS
5. Update shell-boundaries test

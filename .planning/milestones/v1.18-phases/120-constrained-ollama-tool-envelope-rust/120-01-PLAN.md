---
phase: 120-constrained-ollama-tool-envelope-rust
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/rlm-core/src/ports/language_model.rs
  - crates/rlm-core/src/domain/recursion/tool_envelope.rs
  - crates/rlm-core/src/domain/recursion/mod.rs
  - crates/rlm-core/tests/domain/recursion/tool_envelope.rs
autonomous: true
requirements:
  - RETIRE-120-01
  - RETIRE-120-02
user_setup: []
must_haves:
  truths:
    - "LanguageModelCompleteOptions exposes optional response_format JSON Schema"
    - "build_tool_envelope_schema produces closed tool name enum from registered tools"
    - "Envelope schema includes final and tool_call branches per Option A research doc"
  artifacts:
    - path: crates/rlm-core/src/domain/recursion/tool_envelope.rs
      provides: "JSON Schema envelope builder and parse mapper"
      exports: ["build_tool_envelope_schema", "parse_envelope_response"]
    - path: crates/rlm-core/src/ports/language_model.rs
      provides: "response_format field on complete options"
      contains: "response_format"
  key_links:
    - from: build_tool_envelope_schema
      to: LanguageModelToolDefinition.schema
      via: "per-tool args branch in oneOf"
      pattern: "oneOf"
---

<objective>
Add `response_format` to the language-model port and implement the Option A JSON envelope builder with tests.

Purpose: ROADMAP criteria 1–2 and TOOL-CALLING-CONSTRAINED-DECODING.md §0.6 Option A require a schema the Ollama adapter can pass as `format` without `tools`.
Output: `tool_envelope` module + port field + unit tests for schema shape and parse behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-constrained-ollama-tool-envelope-rust/120-CONTEXT.md
@.planning/research/TOOL-CALLING-CONSTRAINED-DECODING.md
@crates/rlm-core/src/ports/language_model.rs

<interfaces>
From `crates/rlm-core/src/ports/language_model.rs`:

```rust
pub struct LanguageModelToolDefinition {
    pub name: String,
    pub description: String,
    pub schema: serde_json::Value,
}

pub struct LanguageModelCompleteOptions<'a> {
    pub purpose: Option<&'a str>,
    pub tools_enabled: bool,
    pub tools: Vec<LanguageModelToolDefinition>,
    pub constrained_tool_calling: bool,
}
```

Add field (ROADMAP criterion 1):
```rust
pub response_format: Option<serde_json::Value>,
```

Update `simple()` to set `response_format: None`.

Envelope shape per TOOL-CALLING doc §0.6 Option A:

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["choice"],
  "properties": {
    "choice": {
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "final"],
          "properties": {
            "kind": { "const": "final" },
            "final": { "type": "string" }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "tool", "args"],
          "properties": {
            "kind": { "const": "tool_call" },
            "tool": { "enum": ["shell", "..."] },
            "args": { /* per-tool oneOf or merged schema */ }
          }
        }
      ]
    }
  }
}
```

`parse_envelope_response(content: &str, allowed_tools: &[LanguageModelToolDefinition]) -> Result<ParsedEnvelope, EnvelopeError>`:
- `ParsedEnvelope::Final(String)` or `ParsedEnvelope::ToolCall { name, args }`
- Reject unknown tool names not in `allowed_tools` (ROADMAP criterion 5)
- Reject malformed JSON with typed error (do not silently coerce per research §0.8)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Add response_format field and failing envelope tests</name>
  <files>crates/rlm-core/src/ports/language_model.rs, crates/rlm-core/tests/domain/recursion/tool_envelope.rs</files>
  <behavior>
    - build_tool_envelope_schema with tools [shell, write_file] produces choice.properties.tool.enum containing exactly those names
    - parse valid final envelope → Final("done")
    - parse valid tool_call → ToolCall shell
    - parse tool_call with unknown tool "evil" → Err
    - parse invalid JSON → Err
  </behavior>
  <action>
1. Add `response_format: Option<serde_json::Value>` to `LanguageModelCompleteOptions`; update `simple()` and grep all struct literals.
2. Create test module `crates/rlm-core/tests/domain/recursion/tool_envelope.rs` with #[path] stub from `tool_envelope.rs` mod (follow ollama_language_model test pattern).
3. Write failing tests for `build_tool_envelope_schema` and `parse_envelope_response` per behavior block.
4. Run tests — must fail (module not implemented).
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core tool_envelope -- --nocapture 2>&1 | grep -q "test result" && exit 1 || exit 0</automated>
  </verify>
  <done>Failing tests exist; response_format on port struct</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement tool_envelope module</name>
  <files>crates/rlm-core/src/domain/recursion/tool_envelope.rs, crates/rlm-core/src/domain/recursion/mod.rs</files>
  <action>
Implement `tool_envelope.rs`:
- `build_tool_envelope_schema(tools: &[LanguageModelToolDefinition]) -> Value` per Option A envelope in TOOL-CALLING doc
- `parse_envelope_response(content: &str, allowed_tools: &[LanguageModelToolDefinition]) -> Result<ParsedEnvelope, EnvelopeError>`
- For args discrimination: use oneOf entries per tool `{ kind: const tool_call, tool: const &lt;name&gt;, args: tool.schema }`
- Export from `domain/recursion/mod.rs`
- All tool_envelope tests pass
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core tool_envelope -- --nocapture</automated>
  </verify>
  <done>Envelope builder and parser implemented; all unit tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model→parse_envelope | Untrusted model JSON crosses into tool execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | T | parse_envelope_response | mitigate | Reject unknown tool names against closed allowlist before returning ToolCall |
| T-120-02 | I | envelope JSON | mitigate | Validate structure; typed EnvelopeError on parse failure — no silent fallback to free-text tool_calls |
</threat_model>

<verification>
`node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core tool_envelope -- --nocapture`
</verification>

<success_criteria>
response_format on port; envelope builder and parser tested; unknown tool rejected at parse time.
</success_criteria>

<output>
After completion, create `.planning/phases/120-constrained-ollama-tool-envelope-rust/120-01-SUMMARY.md`
</output>

---
phase: 116-application-layer-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/
  - AGENTS.md
autonomous: true
requirements:
  - RETIRE-116-01
user_setup: []
must_haves:
  truths:
    - "src/application/ directory no longer exists"
    - "Rust application layer remains sole implementation under crates/rlm-core/src/application/"
    - "AGENTS.md documents TS application layer removed Phase 116"
  artifacts:
    - path: crates/rlm-core/src/application/
      provides: "Canonical application layer (execution, graph, memory, config, bootstrap)"
      contains: "mod.rs"
  key_links:
    - from: crates/rlm-cli
      to: crates/rlm-core/src/application/
      via: "rlm_core public re-exports"
      pattern: "rlm_core::"
---

<objective>
Delete the entire TypeScript application layer per 113-AUDIT.md Phase 116 inventory and update AGENTS.md concern map.

Purpose: Rust `crates/rlm-core/src/application/` is sole application implementation; TS duplicate orchestration must not remain after Phase 115 CLI/runtime removal.
Output: Removed `src/application/` (~60 files); AGENTS.md reflects Phase 116 boundary.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-application-layer-removal/116-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/phases/115-cli-entry-and-runtime-composition-removal/115-VERIFICATION.md
@AGENTS.md

<interfaces>
113-AUDIT.md Phase 116 delete inventory — **application (remainder)** after control-server removed in Phase 114:

Subdirectories under `src/application/`:
```
bootstrap/     (4 files)
config/        (11 files)
execution/     (11 files)
graph/         (6 files)
memory/        (5 files)
plugins/       (3 files)
*.ts           (legacy root re-exports, ~20 files)
```

Rust counterpart (KEEP): `crates/rlm-core/src/application/`

113-GATES Phase 116 gate (partial — full suite in Plan 03):
- `test ! -d src/application` — exit 0

Scope guard (116-CONTEXT):
- Do NOT delete `src/domain/`, `src/ports/`, `src/adapters/`, `src/plugins/` — Phase 117+
- Do NOT modify remaining `src/` files to fix import errors in this plan — Plan 03 handles orphan pruning
- Do NOT delete `tests/application/` — Plan 02 scope

AGENTS.md updates required:
- Opening paragraph: change "TypeScript retains application/domain/adapters/plugins layers until Phase 116+" → "domain/adapters/plugins until Phase 117+"
- TS concern map: remove or strike `application` row; note **Removed Phase 116**
- Layout table: remove `src/application/*` path rows or mark removed; point to `crates/rlm-core/src/application/`
- Tests mirror: remove `tests/application/` rows (deleted in Plan 02)
- Extending section: remove references to `src/application/config/`, `agent-registry.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/application/ tree</name>
  <files>src/application/</files>
  <action>
    Delete entire TypeScript application layer per 113-AUDIT.md Phase 116:

    1. `git rm -r src/application/`

    Verify inventory matches audit — no control-server subtree (removed Phase 114). Expected subdirs: `bootstrap/`, `config/`, `execution/`, `graph/`, `memory/`, `plugins/`, plus legacy root re-export `.ts` files.

    Scope guard:
    - Do NOT touch `crates/rlm-core/src/application/` — Rust canonical layer
    - Do NOT delete or edit `tests/application/` in this task — Plan 02
    - Do NOT fix broken imports in `src/plugins/`, `src/domain/`, etc. — Plan 03

    After deletion, confirm zero files remain: `find src/application -type f 2>/dev/null | wc -l` must be 0.
  </action>
  <verify>
    <automated>test ! -d src/application</automated>
  </verify>
  <done>`src/application/` fully removed; Rust application layer untouched</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md for Phase 116 boundary</name>
  <files>AGENTS.md</files>
  <action>
    Update AGENTS.md to document TS application layer removal (mirror Phase 115 cli/runtime pattern):

    1. **Opening paragraph:** State Rust `rlm-cli` is sole CLI (Phase 115); TS retains `domain/`, `ports/`, `adapters/`, `plugins/` until Phase 117+; application orchestration is Rust-only in `crates/rlm-core/src/application/`.

    2. **TS concern map diagram:** Remove `application` node from the ASCII diagram or annotate `~~application~~` **Removed Phase 116**. Keep Rust diagram unchanged.

    3. **TS concern table:** Remove the `application` row or replace with strikethrough note pointing to `crates/rlm-core/src/application/`.

    4. **Layout section:** Remove all `src/application/*` bullet paths. Add note: application use cases live in `crates/rlm-core/src/application/`.

    5. **Tests mirror table:** Remove `tests/application/*` rows (Plan 02 deletes them; document "Removed Phase 116").

    6. **Extending section:** Replace `src/application/config/` and `agent-registry.ts` guidance with Rust paths under `crates/rlm-core/src/application/` and `rlm.config.yaml` agents block.

    Do NOT remove domain/ports/adapters/plugins TS sections — those are Phase 117–118 scope.
  </action>
  <verify>
    <automated>grep -v '^#' AGENTS.md | grep -c 'Removed Phase 116' | grep -qv '^0$' && grep -v '^#' AGENTS.md | grep -c 'src/application/' | grep -qv '^0$' || (grep -v '^#' AGENTS.md | grep -c 'Removed Phase 116' | grep -qv '^0$' && ! grep -q 'src/application/config' AGENTS.md)</automated>
  </verify>
  <done>AGENTS.md reflects Rust-only application layer; no active `src/application/` path references in Layout/Extending</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS src/ → deleted application | Remaining TS layers must not import deleted modules (verified Plan 03) |
| Rust application → domain | Unchanged; Rust boundary rules still enforce inward deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | I | Orphan TS imports to deleted application | mitigate | Plan 03 grep gate: no `src/application` imports in kept files |
| T-116-02 | D | Accidental deletion of Rust application layer | mitigate | Scope guard: only `git rm -r src/application/`; never touch `crates/rlm-core/src/application/` |
</threat_model>

<verification>
- `test ! -d src/application` — exit 0
- AGENTS.md documents Phase 116 removal
</verification>

<success_criteria>
- `src/application/` deleted entirely
- AGENTS.md updated for Rust-only application orchestration
</success_criteria>

<output>
After completion, create `.planning/phases/116-application-layer-removal/116-01-SUMMARY.md`
</output>

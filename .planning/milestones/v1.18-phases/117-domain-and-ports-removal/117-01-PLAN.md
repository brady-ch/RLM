---
phase: 117-domain-and-ports-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/
  - src/ports/
  - AGENTS.md
autonomous: true
requirements:
  - RETIRE-117-01
user_setup: []
must_haves:
  truths:
    - "src/domain/ directory no longer exists"
    - "src/ports/ directory no longer exists"
    - "Rust domain and ports remain sole implementation under crates/rlm-core/src/domain/ and crates/rlm-core/src/ports/"
    - "AGENTS.md documents TS domain/ports removed Phase 117"
  artifacts:
    - path: crates/rlm-core/src/domain/
      provides: "Canonical recursion policy and orchestrator"
      contains: "mod.rs"
    - path: crates/rlm-core/src/ports/
      provides: "Canonical port trait contracts"
      contains: "mod.rs"
  key_links:
    - from: crates/rlm-core/src/application/
      to: crates/rlm-core/src/domain/
      via: "Rust inward dependency"
      pattern: "crate::domain::"
---

<objective>
Delete the TypeScript domain orchestrator and port interfaces per 113-AUDIT.md Phase 117 inventory and update AGENTS.md concern map.

Purpose: Rust `crates/rlm-core/src/domain/` and `crates/rlm-core/src/ports/` are canonical; TS duplicates must not remain after Phase 116 application removal.
Output: Removed `src/domain/` (11 files) and `src/ports/` (11 files); AGENTS.md reflects Phase 117 boundary.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-domain-and-ports-removal/117-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/phases/116-application-layer-removal/116-03-SUMMARY.md
@AGENTS.md

<interfaces>
113-AUDIT.md Phase 117 delete inventory — **domain**:

```
src/domain/agent-config.ts
src/domain/agents.ts
src/domain/execution-failure.ts
src/domain/recursion/budget-guard.ts
src/domain/recursion/execution-graph-sync.ts
src/domain/recursion/prompt-utilities.ts
src/domain/recursion/quality-loop.ts
src/domain/recursion/tool-round-loop.ts
src/domain/recursive-language-model.ts
src/domain/run-state-persistence.ts
src/domain/types.ts
```

113-AUDIT.md Phase 117 delete inventory — **ports** (flat):

```
src/ports/embedding-port.ts
src/ports/extension-host-port.ts
src/ports/extension-port.ts
src/ports/language-model-port.ts
src/ports/memory-store-port.ts
src/ports/run-state-store-port.ts
src/ports/runtime-logger-port.ts
src/ports/session-store-port.ts
src/ports/skill-loader-port.ts
src/ports/tool-port.ts
src/ports/trace-port.ts
```

Rust counterparts (KEEP):
- `crates/rlm-core/src/domain/`
- `crates/rlm-core/src/ports/`
- `crates/rlm-core/tests/domain/` (Rust mirrored domain tests)

113-GATES Phase 117 gate (partial — full suite in Plan 03):
- `test ! -d src/domain && test ! -d src/ports` — exit 0

Scope guard (117-CONTEXT):
- Do NOT delete `src/adapters/`, `src/plugins/` — Phase 118
- Do NOT delete `tests/domain/` — Plan 02 scope
- Do NOT fix broken imports in remaining `src/adapters/`, `src/plugins/` — Phase 118 deletes those trees; transitional `npm run build` / `typecheck` failure expected until Phase 118–119
- Do NOT touch `crates/rlm-core/src/domain/` or `crates/rlm-core/src/ports/`

AGENTS.md updates required:
- Opening paragraph: change "TypeScript retains domain/ports/adapters/plugins until Phase 117+" → "adapters/plugins until Phase 118+"; domain/ports Rust-only
- TS concern map diagram: annotate `~~domain~~` and `~~ports~~` **Removed Phase 117**
- TS concern table: remove or strike domain/ports rows; point to `crates/rlm-core/src/domain/` and `crates/rlm-core/src/ports/`
- Layout section: remove `src/domain/*` and `src/ports/*` bullet paths; note Rust canonical paths
- Tests mirror: mark `tests/domain/` **Removed Phase 117** (Plan 02 deletes)
- Extending section: replace `src/domain/recursive-language-model.ts` and recursion helper paths with Rust equivalents under `crates/rlm-core/src/domain/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/domain/ and src/ports/ trees</name>
  <files>src/domain/, src/ports/</files>
  <action>
    Delete TypeScript domain and ports layers per 113-AUDIT.md Phase 117:

    1. `git rm -r src/domain/`
    2. `git rm -r src/ports/`

    Verify inventory matches audit (11 files each). Expected domain subdirs: `recursion/` plus root `.ts` files.

    Scope guard:
    - Do NOT touch `crates/rlm-core/src/domain/` or `crates/rlm-core/src/ports/` — Rust canonical layers
    - Do NOT delete or edit `tests/domain/` in this task — Plan 02
    - Do NOT fix broken imports in `src/adapters/`, `src/plugins/` — Phase 118 deletes those trees

    After deletion, confirm zero files remain:
    - `find src/domain -type f 2>/dev/null | wc -l` must be 0
    - `find src/ports -type f 2>/dev/null | wc -l` must be 0
  </action>
  <verify>
    <automated>test ! -d src/domain && test ! -d src/ports</automated>
  </verify>
  <done>`src/domain/` and `src/ports/` fully removed; Rust domain/ports layers untouched</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md for Phase 117 boundary</name>
  <files>AGENTS.md</files>
  <action>
    Update AGENTS.md to document TS domain/ports removal (mirror Phase 116 application pattern):

    1. **Opening paragraph:** State Rust `rlm-cli` is sole CLI (Phase 115); TS retains `adapters/`, `plugins/` until Phase 118+; domain/ports and application orchestration are Rust-only in `crates/rlm-core/`.

    2. **TS concern map diagram:** Annotate `~~domain~~` and `~~ports~~` **Removed Phase 117**. Keep adapters/plugins nodes until Phase 118.

    3. **TS concern table:** Remove or strike domain/ports rows; point to `crates/rlm-core/src/domain/` and `crates/rlm-core/src/ports/`.

    4. **Layout section:** Remove all `src/domain/*` and `src/ports/*` bullet paths. Add note: recursion policy and port contracts live in `crates/rlm-core/src/domain/` and `crates/rlm-core/src/ports/`.

    5. **Tests mirror table:** Mark `tests/domain/` **Removed Phase 117** (deleted in Plan 02).

    6. **Extending section:** Replace `src/domain/recursive-language-model.ts`, `src/domain/recursion/`, and port interface guidance with Rust paths. New agents/workflows guidance unchanged (already Rust in Phase 116).

    Do NOT remove adapters/plugins TS sections — Phase 118 scope.
  </action>
  <verify>
    <automated>grep -v '^#' AGENTS.md | grep -c 'Removed Phase 117' | grep -qv '^0$' && ! grep -q 'src/domain/recursive-language-model' AGENTS.md</automated>
  </verify>
  <done>AGENTS.md reflects Rust-only domain/ports; no active `src/domain/` or `src/ports/` path references in Layout/Extending</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS src/ → deleted domain/ports | Remaining TS adapters/plugins still import ports until Phase 118 (transitional) |
| Rust domain → ports | Unchanged; Rust boundary rules still enforce inward deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | I | Orphan TS imports to deleted domain/ports in kept files | mitigate | Plan 03 grep gates for ui/, scripts/, tests/helpers |
| T-117-02 | D | Accidental deletion of Rust domain/ports | mitigate | Scope guard: only `git rm -r src/domain/ src/ports/`; never touch `crates/rlm-core/src/domain/` or `ports/` |
</threat_model>

<verification>
- `test ! -d src/domain && test ! -d src/ports` — exit 0
- AGENTS.md documents Phase 117 removal
</verification>

<success_criteria>
- `src/domain/` and `src/ports/` deleted entirely
- AGENTS.md updated for Rust-only domain/ports
</success_criteria>

<output>
After completion, create `.planning/phases/117-domain-and-ports-removal/117-01-SUMMARY.md`
</output>

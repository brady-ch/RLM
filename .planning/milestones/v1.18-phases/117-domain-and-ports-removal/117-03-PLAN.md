---
phase: 117-domain-and-ports-removal
plan: 03
type: execute
wave: 2
depends_on:
  - "117-01"
  - "117-02"
files_modified:
  - tests/helpers/quality-loop-helpers.ts
  - tests/helpers/mock-language-model.ts
  - tests/helpers/simple-tools.ts
  - scripts/phase40-peel-source.ts
  - scripts/patch-rlm-phase40-core.mjs
  - scripts/stitch-quality-loop.mjs
  - ui/src/shared/labels.ts
  - tsconfig.json
autonomous: true
requirements:
  - RETIRE-117-03
  - RETIRE-117-04
user_setup: []
must_haves:
  truths:
    - "No kept file under ui/ or scripts/ imports from deleted src/domain/ or src/ports/ paths"
    - "No kept test helper imports from deleted domain/ports modules"
    - "cargo test -p rlm-core passes"
    - "Legacy Phase 40 peel/stitch scripts removed (they referenced deleted src/domain/)"
  artifacts:
    - path: ui/src/shared/labels.ts
      provides: "UI status labels with Rust source comment (not TS domain path)"
  key_links:
    - from: ui/src/shared/labels.ts
      to: crates/rlm-core/src/domain/
      via: "comment reference only — UI does not import Rust"
      pattern: "execution.failure|execution_failure"
---

<objective>
Prune orphaned TS files that imported deleted domain/ports modules, clean ui/scripts references, then run all 113-GATES Phase 117 verification commands.

Purpose: ROADMAP success criterion 3 requires no domain/ports imports in `ui/` or `scripts/`; 117-CONTEXT requires pruning tests that import deleted modules; Rust gate proves runtime unaffected.
Output: Clean import graph in kept paths; Phase 117 gates green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-domain-and-ports-removal/117-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/phases/116-application-layer-removal/116-03-SUMMARY.md

<interfaces>
113-GATES Phase 117 gate commands (ALL must pass):

1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — exit 0
2. `test ! -d src/domain && test ! -d src/ports` — exit 0
3. `test ! -d tests/domain` — exit 0

ROADMAP success criterion 3 — import grep gates:

```bash
# ui/ — no imports from deleted paths (comments OK if updated)
grep -r "src/domain\|src/ports\|from ['\"].*\/domain\/\|from ['\"].*\/ports\/" ui/ --include='*.ts' --include='*.tsx' \
  | grep -v '^[^:]*:.*\/\/' && exit 1 || exit 0

# scripts/ — no imports from deleted paths
grep -r "src/domain\|src/ports\|from ['\"].*\/domain\/\|from ['\"].*\/ports\/" scripts/ --include='*.ts' --include='*.mjs' \
  && exit 1 || exit 0
```

Test helpers to delete (orphaned — zero importers after Phase 116-03):

| File | Imports deleted | Action |
|------|-----------------|--------|
| `tests/helpers/quality-loop-helpers.ts` | `src/domain/types.js` | **Delete** |
| `tests/helpers/mock-language-model.ts` | `src/ports/language-model-port.js` | **Delete** |
| `tests/helpers/simple-tools.ts` | `src/ports/runtime-logger-port.js`, `tool-port.js` | **Delete** |

Legacy Phase 40 scripts (reference deleted src/domain/ — historical migration only):

| File | Action |
|------|--------|
| `scripts/phase40-peel-source.ts` | **Delete** — imports `../ports/*`; excluded from compile but violates scripts gate |
| `scripts/patch-rlm-phase40-core.mjs` | **Delete** — reads `src/domain/recursive-language-model.ts` |
| `scripts/stitch-quality-loop.mjs` | **Delete** — reads `src/domain/recursion/` snippets |

UI comment update:

| File | Action |
|------|--------|
| `ui/src/shared/labels.ts` line 3 | Update comment from `src/domain/execution-failure.ts` → `crates/rlm-core/src/domain/execution_failure.rs` (or nearest Rust equivalent after grep) |

tsconfig.json cleanup:

Remove obsolete excludes now that domain peel scripts and `_*.ts` stubs are gone:
- `"**/src/domain/recursion/_*.ts"`
- `"scripts/phase40-peel-source.ts"`

Transitional exceptions (document in SUMMARY, do NOT block gate):
- `src/adapters/` and `src/plugins/` still import deleted `ports/` — Phase 118 deletes those trees
- `npm run build` / `npm run typecheck` may still fail until Phase 118–119 — not Phase 117 gates
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete domain/ports-coupled test helpers and legacy peel scripts</name>
  <files>tests/helpers/quality-loop-helpers.ts, tests/helpers/mock-language-model.ts, tests/helpers/simple-tools.ts, scripts/phase40-peel-source.ts, scripts/patch-rlm-phase40-core.mjs, scripts/stitch-quality-loop.mjs</files>
  <action>
    Delete orphaned files that cannot load without deleted domain/ports modules:

    1. `git rm tests/helpers/quality-loop-helpers.ts`
    2. `git rm tests/helpers/mock-language-model.ts`
    3. `git rm tests/helpers/simple-tools.ts`
       - Rationale: zero remaining importers (verified Phase 116-03 removed sole consumers)

    4. `git rm scripts/phase40-peel-source.ts`
    5. `git rm scripts/patch-rlm-phase40-core.mjs`
    6. `git rm scripts/stitch-quality-loop.mjs`
       - Rationale: Phase 40 historical migration scripts; target deleted `src/domain/` and `src/ports/`; quality loop already lives in Rust

    Scope guard:
    - Do NOT delete `tests/adapters/` or `tests/plugins/` — Phase 118
    - Do NOT delete `scripts/rust-boundary-check.test.mjs` or `scripts/measure-rust-compile-baseline.sh` — they reference Rust `crates/rlm-core/src/domain/` paths (valid)
  </action>
  <verify>
    <automated>test ! -f tests/helpers/mock-language-model.ts && test ! -f scripts/phase40-peel-source.ts && test ! -f scripts/stitch-quality-loop.mjs</automated>
  </verify>
  <done>Orphan test helpers and legacy Phase 40 scripts removed</done>
</task>

<task type="auto">
  <name>Task 2: Update ui comment and tsconfig excludes</name>
  <files>ui/src/shared/labels.ts, tsconfig.json</files>
  <action>
    1. **ui/src/shared/labels.ts:** Replace line 3 comment:
       - From: `Mirror labelForCategory / status strings in src/domain/execution-failure.ts for header copy.`
       - To: `Mirror labelForCategory / status strings in crates/rlm-core/src/domain/execution_failure.rs for header copy.`
       - If Rust module name differs, grep `crates/rlm-core/src/domain/` for `labelForCategory` or equivalent and cite actual path.

    2. **tsconfig.json:** Remove obsolete exclude entries:
       - Remove `"**/src/domain/recursion/_*.ts"`
       - Remove `"scripts/phase40-peel-source.ts"`
       - Keep `"dist"` and `"ui"` excludes

    3. Run import grep gates for ui/ and scripts/ (see interfaces block). Fix any remaining hits beyond updated comment.
  </action>
  <verify>
    <automated>grep -r "src/domain\|src/ports" ui/ scripts/ --include='*.ts' --include='*.tsx' --include='*.mjs' | grep -v 'crates/rlm-core' | grep -v '^[^:]*:.*\/\/' && exit 1 || exit 0</automated>
  </verify>
  <done>No ui/ or scripts/ references to deleted TS domain/ports paths; tsconfig excludes cleaned</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 117 verification gates</name>
  <files></files>
  <action>
    Run all 113-GATES Phase 117 commands sequentially:

    1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — must exit 0
    2. `test ! -d src/domain && test ! -d src/ports` — must exit 0
    3. `test ! -d tests/domain` — must exit 0
    4. Import grep gate — ui/ and scripts/ (Task 2 verify command)
    5. Test helper grep — confirm no domain/ports imports in kept tests/:
       ```bash
       grep -r "src/domain\|src/ports" tests/ --include='*.ts' && exit 1 || exit 0
       ```
       Expected: may still match zero (helpers deleted); adapters tests use word "reports" not path — gate should pass
    6. `npm run test:agent:verify:light` — confirm still green

    Document in SUMMARY:
    - Gate results table (pass/fail per command)
    - Transitional: `npm run build` / `typecheck` status (expected fail until Phase 118–119 due to adapters/plugins importing deleted ports)
    - Coverage transfer notes for deleted constrained-tool-calling and peel scripts
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core && test ! -d src/domain && test ! -d src/ports && test ! -d tests/domain</automated>
  </verify>
  <done>All 113-GATES Phase 117 commands pass; test:agent:verify:light green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ui/ → deleted TS domain | UI must not import deleted modules; comment-only Rust reference OK |
| scripts/ → deleted TS domain | Legacy peel scripts removed; remaining scripts use Rust paths only |
| Remaining src/adapters, src/plugins → deleted ports | Transitional until Phase 118 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-05 | T | Deleted constrained-tool-calling coverage | transfer | Rust `recursive_engine_session.rs` + Phase 120 envelope tests |
| T-117-06 | I | Stale ui comment referencing deleted TS path | mitigate | Update labels.ts comment to Rust path |
| T-117-07 | D | cargo test gate failure | mitigate | Run RAM-gated full suite; fix in-scope failures before marking complete |
</threat_model>

<verification>
113-GATES Phase 117 full checklist:
1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core` — exit 0
2. `test ! -d src/domain && test ! -d src/ports` — exit 0
3. `test ! -d tests/domain` — exit 0
4. ui/scripts import grep gates — exit 0
5. `npm run test:agent:verify:light` — exit 0
</verification>

<success_criteria>
- No domain/ports imports in ui/, scripts/, or kept tests/helpers
- All Phase 117 gates pass
- Legacy Phase 40 scripts removed
</success_criteria>

<output>
After completion, create `.planning/phases/117-domain-and-ports-removal/117-03-SUMMARY.md`
</output>

---
phase: 117-domain-and-ports-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/domain/
autonomous: true
requirements:
  - RETIRE-117-02
user_setup: []
must_haves:
  truths:
    - "tests/domain/ directory no longer exists"
    - "Rust domain tests under crates/rlm-core/tests/domain/ remain"
  artifacts:
    - path: crates/rlm-core/tests/domain/
      provides: "Mirrored Rust domain and recursion tests"
  key_links:
    - from: crates/rlm-core/tests/domain/recursion/
      to: crates/rlm-core/src/domain/recursion/
      via: "Rust unit/integration tests"
      pattern: "budget_guard|prompt_utilities|execution_graph_sync"
---

<objective>
Delete mirrored TypeScript domain tests per 113-AUDIT.md Phase 117 inventory.

Purpose: TS domain tests duplicate Rust coverage in `crates/rlm-core/tests/domain/`; keeping them blocks clean deletion and references deleted `src/domain/` modules.
Output: Removed `tests/domain/` (4 test files under recursion/ + constrained-tool-calling).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-domain-and-ports-removal/117-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md

<interfaces>
113-AUDIT.md mirrored tests to delete (5 files under `tests/domain/`):

```
tests/domain/constrained-tool-calling.test.ts
tests/domain/recursion/budget-guard.unit.test.ts
tests/domain/recursion/execution-graph-sync.unit.test.ts
tests/domain/recursion/prompt-utilities.unit.test.ts
```

Note: `recursive-language-model.test.ts` was already deleted in Phase 116-03.

Rust counterparts KEEP:
```
crates/rlm-core/tests/domain/recursion/budget_guard.rs
crates/rlm-core/tests/domain/recursion/prompt_utilities.rs
crates/rlm-core/tests/domain/recursion/execution_graph_sync.rs
crates/rlm-core/tests/domain/recursion/quality_loop.rs
crates/rlm-core/tests/domain/recursion/tool_round_loop.rs
```

113-GATES Phase 117 gate (partial):
- `test ! -d tests/domain` — exit 0

Scope guard:
- Do NOT delete `crates/rlm-core/tests/domain/`
- Do NOT delete `tests/helpers/` in this plan — Plan 03 handles domain/ports-coupled helpers
- Do NOT delete `tests/adapters/` — Phase 118
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tests/domain/ tree</name>
  <files>tests/domain/</files>
  <action>
    Delete entire mirrored TypeScript domain test tree per 113-AUDIT.md Phase 117:

    1. `git rm -r tests/domain/`

    Expected files removed:
    - `tests/domain/constrained-tool-calling.test.ts`
    - `tests/domain/recursion/budget-guard.unit.test.ts`
    - `tests/domain/recursion/execution-graph-sync.unit.test.ts`
    - `tests/domain/recursion/prompt-utilities.unit.test.ts`

    Coverage transfer rationale:
    - `constrained-tool-calling.test.ts` → Rust tool envelope tests (Phase 120) and existing `crates/rlm-core/tests/recursive_engine_session.rs`
    - Recursion unit tests → `crates/rlm-core/tests/domain/recursion/*.rs`

    After deletion: `find tests/domain -type f 2>/dev/null | wc -l` must be 0.
  </action>
  <verify>
    <automated>test ! -d tests/domain</automated>
  </verify>
  <done>`tests/domain/` fully removed; Rust domain tests untouched</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no npm scripts reference tests/domain</name>
  <files>package.json, scripts/agent-safe-verify.mjs</files>
  <action>
    Verify no kept test runner or verify profile still targets deleted `tests/domain/` paths:

    1. `grep -r "tests/domain" package.json scripts/ --include='*.json' --include='*.mjs' --include='*.ts'` — must return zero matches (excluding `.planning/` if accidentally searched).

    2. If any match found in `scripts/agent-safe-verify.mjs` or `package.json` scripts, remove or replace with Rust equivalent (`cargo test -p rlm-core domain` or specific test file filter).

    3. If no matches (expected — Phase 116 already removed recursive-language-model.test.ts and agent-safe-verify does not reference domain tests), document "no changes required" in SUMMARY.

    Do NOT modify `scripts/run-test-suite.mjs` build chain — full npm test may still fail until Phase 118–119 (transitional).
  </action>
  <verify>
    <automated>! grep -rq "tests/domain" package.json scripts/agent-safe-verify.mjs 2>/dev/null</automated>
  </verify>
  <done>No test runner or verify profile references deleted tests/domain/ paths</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deleted TS domain tests → Rust tests | Coverage must exist in crates/rlm-core/tests/domain/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03 | T | Deleted constrained-tool-calling.test.ts coverage gap | transfer | Rust `recursive_engine_session.rs` + Phase 120 tool envelope tests |
| T-117-04 | D | Accidental deletion of Rust domain tests | mitigate | Scope guard: only `git rm -r tests/domain/`; never touch `crates/rlm-core/tests/domain/` |
</threat_model>

<verification>
- `test ! -d tests/domain` — exit 0
- No package.json or agent-safe-verify references to tests/domain
</verification>

<success_criteria>
- `tests/domain/` deleted entirely
- Rust domain test mirror preserved
</success_criteria>

<output>
After completion, create `.planning/phases/117-domain-and-ports-removal/117-02-SUMMARY.md`
</output>

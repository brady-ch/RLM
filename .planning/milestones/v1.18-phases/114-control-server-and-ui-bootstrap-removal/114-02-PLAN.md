---
phase: 114-control-server-and-ui-bootstrap-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/control-server/control-server-deps.ts
  - src/application/control-server/handlers/graph.ts
  - src/application/control-server/handlers/model-library.ts
  - src/application/control-server/handlers/plugins.ts
  - src/application/control-server/handlers/session.ts
  - src/application/control-server/handlers/static-ui.ts
  - src/application/control-server/handlers/workflows.ts
  - src/application/control-server/http-utils.ts
  - src/application/control-server/index.ts
  - src/application/control-server/route-request.ts
  - src/application/control-server/start-input-from-bootstrap.ts
  - src/application/control-server/types.ts
  - src/cli/run-modes/ui.ts
  - src/index.ts
  - tests/domain/recursion/recursive-language-model.test.ts
autonomous: true
requirements:
  - RETIRE-114-01
  - RETIRE-114-04
user_setup: []
must_haves:
  truths:
    - "src/application/control-server/ directory no longer exists"
    - "src/cli/run-modes/ui.ts no longer exists"
    - "Node CLI index.ts does not boot a TS control server for ui command"
    - "No src/ or tests/ file imports from application/control-server"
  artifacts:
    - path: src/index.ts
      provides: "ui command rejects TS bootstrap with Rust redirect message"
      min_lines: 100
  key_links:
    - from: src/index.ts
      to: crates/rlm-cli
      via: "ui command directs operator to npm run rlm -- ui (Rust dispatcher default)"
      pattern: "RLM_RUNTIME=rust|npm run rlm"
---

<objective>
Delete the TypeScript control server and UI bootstrap; remove all remaining consumers so the codebase compiles without TS HTTP transport.

Purpose: Rust Axum in `crates/rlm-core/src/control_server/` is the sole HTTP implementation per phase boundary in 114-CONTEXT.md.
Output: Deleted `src/application/control-server/` and `src/cli/run-modes/ui.ts`; cleaned imports and HTTP integration tests in domain test file.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-control-server-and-ui-bootstrap-removal/114-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@src/index.ts
@src/cli/run-modes/ui.ts
@crates/rlm-core/src/control_server/mod.rs

<interfaces>
Files to delete (12 under control-server + ui bootstrap):

```
src/application/control-server/control-server-deps.ts
src/application/control-server/handlers/graph.ts
src/application/control-server/handlers/model-library.ts
src/application/control-server/handlers/plugins.ts
src/application/control-server/handlers/session.ts
src/application/control-server/handlers/static-ui.ts
src/application/control-server/handlers/workflows.ts
src/application/control-server/http-utils.ts
src/application/control-server/index.ts
src/application/control-server/route-request.ts
src/application/control-server/start-input-from-bootstrap.ts
src/application/control-server/types.ts
src/cli/run-modes/ui.ts
```

Rust replacement (already shipped — do not modify in this plan):

```rust
// crates/rlm-cli/src/commands/ui.rs
pub async fn run(port: u16, ui_dist: Option<PathBuf>, project_root: PathBuf, stop: bool, replace: bool)
// crates/rlm-core — start_server(ServerConfig { port, ui_dist_dir, project_root, ... })
```

index.ts ui branch today (remove/replace):

```typescript
import { runUiMode } from "./cli/run-modes/ui.js";
// ...
if (options.command === "ui") {
  await runUiMode(ctx, CLI_ENTRY_PATH);
  return;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove HTTP integration tests that boot TS control server</name>
  <files>tests/domain/recursion/recursive-language-model.test.ts</files>
  <action>
    In `tests/domain/recursion/recursive-language-model.test.ts`, remove all test cases that import or call `startControlServer`. Delete these entire tests (HTTP coverage moves to Rust golden fixtures):

    1. `"control server exposes plan and budget endpoints with explicit exhaustion gate"`
    2. `"quality loop control api records loop scoped accept and stop decisions"`
    3. `"ui confirm run executes quality loop in the shared session"`
    4. `"control server exposes model library catalog install search and tier selection"`
    5. `"control server saves and opens interactive session snapshots"`
    6. `"control server exposes memory inspection and preference mutation"`
    7. `"control server refuses unsafe saved-session open with verification details"`

    Refactor (do not delete) `"approval mode contract is consistent across cli api and ui labels"`:
    - Remove `startControlServer` session setup and `/api/run-mode` fetch block
    - Keep `parseArgs` assertion and static UI source file assertions (`ui/src/shared/api.ts`, `QualityLoopInspector.tsx`)

    Remove unused imports after edits:
    - `startControlServer` from control-server
    - `ModelLibraryService`, `createUiExecutionRunner`, `InteractiveExecutionSession` if no longer referenced

    Do NOT delete or weaken non-HTTP domain tests in this file.
  </action>
  <verify>
    <automated>grep -r "startControlServer\|application/control-server" tests/domain/recursion/recursive-language-model.test.ts && exit 1 || exit 0</automated>
  </verify>
  <done>recursive-language-model.test.ts has zero startControlServer references; approval mode test retains CLI/UI static checks only</done>
</task>

<task type="auto">
  <name>Task 2: Delete TS control server and UI bootstrap; update index.ts</name>
  <files>src/application/control-server/, src/cli/run-modes/ui.ts, src/index.ts</files>
  <action>
    Delete entire directories/files per 113-AUDIT.md:
    - `src/application/control-server/` (all 12 files)
    - `src/cli/run-modes/ui.ts`

    Update `src/index.ts`:
    - Remove `import { runUiMode } from "./cli/run-modes/ui.js"`
    - Replace the `options.command === "ui"` branch with a clear stderr message and non-zero exit, e.g.:
      - Inform operator that TS UI bootstrap was removed in Phase 114
      - Direct to `npm run rlm -- ui` or `RLM_UI_DIST=ui/dist cargo run -p rlm-cli -- ui`
      - Set `process.exitCode = 1` and return (do not attempt to spawn Rust from index.ts — Phase 115 owns CLI retirement)

    Scope guard: Do NOT delete `src/cli/` other files, `src/application/` other modules, or `src/index.ts` itself — those are Phase 115+.

    Final grep gate — must return no matches in src/ and tests/:
    `grep -r "application/control-server\|startControlServer\|runUiMode" src/ tests/ --include='*.ts'`
  </action>
  <verify>
    <automated>test ! -d src/application/control-server && test ! -f src/cli/run-modes/ui.ts && grep -r "application/control-server\|startControlServer\|runUiMode" src/ tests/ --include='*.ts' && exit 1 || exit 0</automated>
  </verify>
  <done>control-server directory and ui.ts deleted; index.ts ui command exits with Rust redirect; zero TS control-server imports remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node CLI → HTTP | TS server removed; accidental TS boot paths must not remain |
| Domain tests → HTTP | Deleted tests must not leave orphaned imports that break build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03 | E | src/index.ts ui branch | mitigate | Explicit exit + message; no silent fallback to deleted TS server |
| T-114-04 | I | Deleted HTTP tests | accept | Coverage transferred to Rust golden fixtures + existing domain unit tests |
| T-114-05 | D | grep gate | mitigate | Automated verify confirms no control-server imports in src/ and tests/ |
</threat_model>

<verification>
```bash
test ! -d src/application/control-server
test ! -f src/cli/run-modes/ui.ts
grep -r "application/control-server" src/ tests/ --include='*.ts' && exit 1 || true
npm run build
```
</verification>

<success_criteria>
- All 12 control-server files and ui.ts deleted
- index.ts no longer imports or calls runUiMode
- npm run build succeeds (TS compile without control-server)
- No startControlServer or control-server imports in src/ or tests/
</success_criteria>

<output>
After completion, create `.planning/phases/114-control-server-and-ui-bootstrap-removal/114-02-SUMMARY.md`
</output>

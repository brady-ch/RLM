---
phase: 115-cli-entry-and-runtime-composition-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/rlm-runtime.mjs
  - scripts/rlm-runtime.test.mjs
  - package.json
autonomous: true
requirements:
  - RETIRE-115-02
  - RETIRE-115-04
user_setup: []
must_haves:
  truths:
    - "npm run rlm dispatches only to Rust rlm binary — no Node CLI escape hatch"
    - "package.json no longer exposes dev/start/rlm:node scripts pointing at dist/src/index.js"
    - "test:rlm-runtime confirms Rust-only dispatcher behavior"
  artifacts:
    - path: scripts/rlm-runtime.mjs
      provides: "Rust-only CLI dispatcher"
      contains: "resolveRustBinary"
    - path: package.json
      provides: "npm rlm entry via scripts/rlm-runtime.mjs"
      contains: "\"rlm\": \"node scripts/rlm-runtime.mjs\""
  key_links:
    - from: package.json
      to: scripts/rlm-runtime.mjs
      via: "bin.rlm and scripts.rlm"
      pattern: "rlm-runtime\\.mjs"
    - from: scripts/rlm-runtime.mjs
      to: target/release/rlm or target/debug/rlm
      via: "spawnSync(binary, forwarded)"
      pattern: "spawnSync\\(binary"
---

<objective>
Simplify the npm CLI dispatcher to Rust-only and remove package.json Node CLI entry scripts.

Purpose: Phase 115 retires the Node escape hatch (`RLM_RUNTIME=node`) per 113-GATES.md; `npm rlm` must always invoke `rlm-cli`.
Output: Rust-only `scripts/rlm-runtime.mjs`, updated tests, cleaned `package.json` scripts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-cli-entry-and-runtime-composition-removal/115-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@.planning/notes/rust-only-runtime-migration-decisions.md
@scripts/rlm-runtime.mjs
@scripts/rlm-runtime.test.mjs
@package.json

<interfaces>
Current dispatcher (to be replaced):

```javascript
// scripts/rlm-runtime.mjs — remove runNode(), RLM_RUNTIME branching
const runtime = (rawRuntime || "rust").toLowerCase();
if (runtime === "rust") runRust();
else if (runtime === "node") runNode(); // DELETE
```

package.json scripts to remove (113-AUDIT Phase 115 table):
- `dev` — `tsx src/index.ts`
- `start` — `node dist/src/index.js`
- `rlm:node` — `node dist/src/index.js`

Keep unchanged:
- `bin.rlm` → `scripts/rlm-runtime.mjs` (already correct)
- `rlm` script → `node scripts/rlm-runtime.mjs`
- `rlm:rust` — optional keep for dev ergonomics

Update `main` field: remove or set to a non-CLI placeholder comment in package.json is not valid JSON — **remove `"main": "dist/src/index.js"`** entirely (no main entry after Node CLI retirement).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust-only rlm-runtime dispatcher</name>
  <files>scripts/rlm-runtime.mjs</files>
  <action>
    Rewrite `scripts/rlm-runtime.mjs` to Rust-only dispatch per 113-GATES Phase 115:

    1. Remove `runNode()`, `RLM_RUNTIME` env branching, and all references to `dist/src/index.js`.
    2. Keep `resolveRustBinary()` logic (release preferred, debug fallback, auto `cargo build -p rlm-cli` if missing).
    3. Update header comment: sole path is Rust `rlm-cli`; Node escape hatch removed in Phase 115.
    4. If `RLM_RUNTIME` is set to any value, log a one-line stderr warning that it is ignored (do not error — avoids breaking stale env in shells) and proceed with Rust.
    5. Preserve `stdio: "inherit"` and exit-code forwarding.

    Do NOT add new dependencies. Do NOT change binary resolution paths.
  </action>
  <verify>
    <automated>npm run test:rlm-runtime</automated>
  </verify>
  <done>Rust-only dispatcher; no code path references dist/src/index.js or runNode</done>
</task>

<task type="auto">
  <name>Task 2: Update rlm-runtime tests for Rust-only</name>
  <files>scripts/rlm-runtime.test.mjs</files>
  <action>
    Update tests to match Rust-only dispatcher:

    1. Remove `RLM_RUNTIME=node preserves node path` test entirely.
    2. Remove assertions matching `runtime === "node"` or `function runNode()` in source inspection tests.
    3. Replace with tests that:
       - Source contains no `runNode` function.
       - `spawnSync(RUNTIME_SCRIPT, ["--help"])` succeeds and output mentions Rust CLI subcommands (e.g. `ask`, `ui`, `plugin`) — not "Node CLI entry missing".
       - Optional: `RLM_RUNTIME=node` still invokes Rust (warn + rust path), not node dist.
    4. Keep RAM preload import pattern used by other script tests.

    Use grep-safe assertions: `assert.doesNotMatch(source, /function runNode/)` not bare `grep -c runNode`.
  </action>
  <verify>
    <automated>npm run test:rlm-runtime</automated>
  </verify>
  <done>All rlm-runtime tests pass against Rust-only implementation</done>
</task>

<task type="auto">
  <name>Task 3: Prune package.json Node CLI scripts</name>
  <files>package.json</files>
  <action>
    Update `package.json` per 113-AUDIT Phase 115 row:

    1. **Delete scripts:** `dev`, `start`, `rlm:node`.
    2. **Delete field:** `"main": "dist/src/index.js"`.
    3. **Keep:** `bin.rlm` → `scripts/rlm-runtime.mjs`, `rlm`, `rlm:rust`, `dev:ui`, `dev:ui:full`, Tauri scripts.
    4. Do NOT remove `build`, `typecheck`, `lint`, or test scripts — Phase 119 owns full toolchain cleanup.
    5. Do NOT run `npm install` unless lockfile references change (it should not for script-only edits).

    Note for executor: `npm run build` may fail after Plan 02 deletes cli/runtime — that is expected until Phase 116–119.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts.dev||p.scripts.start||p.scripts['rlm:node']||p.main) process.exit(1); if(p.bin.rlm!=='scripts/rlm-runtime.mjs') process.exit(1); console.log('ok')"</automated>
  </verify>
  <done>package.json has no Node CLI entry scripts; bin.rlm still points at dispatcher</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm script → rlm binary | User-invoked CLI; must not spawn unexpected interpreters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | T | scripts/rlm-runtime.mjs | mitigate | Only spawn resolved `target/*/rlm` binary; no shell interpolation of argv |
| T-115-02 | I | RLM_RUNTIME env | accept | Ignore stale env with warning; no secret handling in dispatcher |
</threat_model>

<verification>
- `npm run test:rlm-runtime` passes
- `npm run rlm -- --help` prints Rust clap help (ask/ui/plugin subcommands)
</verification>

<success_criteria>
- Dispatcher is Rust-only with no Node fallback
- package.json Node CLI scripts removed
- test:rlm-runtime green
</success_criteria>

<output>
After completion, create `.planning/phases/115-cli-entry-and-runtime-composition-removal/115-01-SUMMARY.md`
</output>

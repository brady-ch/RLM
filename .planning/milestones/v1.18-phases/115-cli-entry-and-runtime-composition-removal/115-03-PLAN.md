---
phase: 115-cli-entry-and-runtime-composition-removal
plan: 03
type: execute
wave: 2
depends_on:
  - "115-01"
  - "115-02"
files_modified:
  - tests/application/bootstrap/bootstrap-runtime.unit.test.ts
  - tests/application/config/project-config-scopes.test.ts
  - tests/application/plugins/plugin-registry-service.test.ts
  - tests/domain/recursion/recursive-language-model.test.ts
  - tests/plugins/plugin-loader.test.ts
  - tests/depcruise/concern-map-rules.unit.test.ts
  - scripts/agent-safe-verify.mjs
  - AGENTS.md
autonomous: false
requirements:
  - RETIRE-115-02
  - RETIRE-115-03
  - RETIRE-115-04
user_setup: []
must_haves:
  truths:
    - "All Phase 113-GATES Phase 115 gate commands pass"
    - "npm run rlm -- ask --help invokes Rust CLI"
    - "Shipped subcommands (ask, ui, plan-node, plugin, workflow-export, workflow-import) expose --help via npm rlm"
    - "Tauri embeds in-process Rust server — no Node child for RLM server"
    - "test:agent:verify:light passes after cli-dependent TS tests pruned"
  artifacts:
    - path: src-tauri/src/main.rs
      provides: "In-process Rust control server via rlm_core::start_server"
      contains: "start_server"
    - path: scripts/agent-safe-verify.mjs
      provides: "Light verify without deleted cli imports"
  key_links:
    - from: npm run rlm
      to: crates/rlm-cli
      via: scripts/rlm-runtime.mjs spawn
      pattern: "ask --help"
    - from: src-tauri/src/main.rs
      to: rlm_core::start_server
      via: "EmbeddedRuntime in setup hook"
      pattern: "start_server\\(ServerConfig"
---

<objective>
Prune orphaned TS tests that imported deleted cli/runtime modules, run Phase 115 verification gates, and confirm Tauri + subcommand smoke.

Purpose: Keep agent-safe light verify green; prove Rust CLI parity for all shipped subcommands; validate Tauri has no Node RLM server child.
Output: Pruned tests, updated agent-safe-verify, all 113-GATES Phase 115 commands green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-cli-entry-and-runtime-composition-removal/115-CONTEXT.md
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-GATES.md
@crates/rlm-cli/src/commands/mod.rs
@src-tauri/src/main.rs
@scripts/agent-safe-verify.mjs

<interfaces>
Rust shipped subcommands (verify --help for each via `npm run rlm --`):
- `ui` (default when no subcommand)
- `ask`
- `plan-node`
- `plugin` (+ list/install/enable/disable/uninstall/doctor/inspect/validate)
- `workflow-export`
- `workflow-import`

113-GATES Phase 115 gate commands:
1. `node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core`
2. `npm run rlm -- --help`
3. `test ! -f src/index.ts`
4. `node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-tauri`
5. Manual: `npm run tauri:dev` — window loads Rust-served UI

Tauri already uses in-process server (no Node child):
```rust
// src-tauri/src/main.rs
let server = rt.block_on(start_server(ServerConfig { ... }))?;
```

Tests still importing deleted modules (must fix or delete):
- `tests/application/bootstrap/bootstrap-runtime.unit.test.ts` — parseArgs + bootstrap
- `tests/application/config/project-config-scopes.test.ts` — resolveLaunchMode block
- `tests/application/plugins/plugin-registry-service.test.ts` — parseArgs plugin tests
- `tests/domain/recursion/recursive-language-model.test.ts` — parseArgs + renderResult (top-level imports)
- `tests/plugins/plugin-loader.test.ts` — ExtensionHost from runtime
- `tests/depcruise/concern-map-rules.unit.test.ts` — cruises deleted `src/runtime/composition/build-runtime-context.ts`

agent-safe-verify light profile (must stay green):
- cargo check
- tests/application/config/validation.unit.test.ts (no cli imports — keep)
- tests/ui/reg03-static-wiring.test.ts (keep)

reg01/reg03 profiles include `approval mode contract` test from recursive-language-model.test.ts — uses deleted cli; **remove that step** from agent-safe-verify profiles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune cli/runtime-dependent TS tests</name>
  <files>tests/application/bootstrap/bootstrap-runtime.unit.test.ts, tests/application/config/project-config-scopes.test.ts, tests/application/plugins/plugin-registry-service.test.ts, tests/domain/recursion/recursive-language-model.test.ts, tests/plugins/plugin-loader.test.ts, tests/depcruise/concern-map-rules.unit.test.ts</files>
  <action>
    Remove or delete tests that cannot load without deleted cli/runtime modules:

    1. **Delete entire file** `tests/application/bootstrap/bootstrap-runtime.unit.test.ts` — sole purpose is Node bootstrap + parseArgs.

    2. **project-config-scopes.test.ts:** Remove `resolveLaunchMode` import and the test `"resolveLaunchMode maps env and tty combinations"`. Keep config scope tests that only touch `src/application/config/`.

    3. **plugin-registry-service.test.ts:** Remove parseArgs import and these tests only:
       - `"parseArgs handles plugin list with json flag"`
       - `"parseArgs handles plugin doctor --fix flag"`
       - `"parseArgs handles plugin install --yes flag"`
       Keep plugin registry service tests that do not import cli.

    4. **recursive-language-model.test.ts:** Remove top-level imports of `parseArgs` and `renderResult`. Delete every test block that calls `parseArgs(` or `renderResult(` (grep-driven removal). This includes `"approval mode contract is consistent across cli api and ui labels"` — CLI contract moves to Rust (`crates/rlm-cli`, `crates/rlm-core`). Keep domain orchestrator tests that use mocks only.

    5. **plugin-loader.test.ts:** Delete entire file — requires ExtensionHost from deleted runtime. Plugin loader TS coverage retires in Phase 118.

    6. **concern-map-rules.unit.test.ts:** Remove or skip the test `"no-runtime-to-cli passes for build-runtime-context after bootstrap injection"` that dependency-cruises deleted `src/runtime/composition/build-runtime-context.ts`. Keep rule-registration tests that only read `.dependency-cruiser.js` text.

    Do NOT delete entire `tests/domain/recursion/recursive-language-model.test.ts` unless more than half the file is cli-coupled — prefer surgical removal of cli-coupled tests only.

    Update `AGENTS.md` concern map table: mark `cli` and `runtime` rows as **removed Phase 115** (Rust `rlm-cli` / `rlm-core` plugins+interop only) so docs match repo state.
  </action>
  <verify>
    <automated>node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/application/config/validation.unit.test.ts && node --import ./scripts/test-ram-preload.mjs --import tsx --test tests/ui/reg03-static-wiring.test.ts</automated>
  </verify>
  <done>No remaining test files import from src/cli/ or src/runtime/; light-profile tsx tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update agent-safe-verify and run Rust gates</name>
  <files>scripts/agent-safe-verify.mjs</files>
  <action>
    1. **agent-safe-verify.mjs:** Remove the `"approval mode contract (tsx)"` step from `reg01` and `reg03` profiles (deleted cli render/parse tests). Do not add replacement unless a Rust test already exists — cargo check + reg01_uat_smoke cover runtime.

    2. Run 113-GATES Phase 115 automated gates (use RAM gate wrapper for cargo):
       ```bash
       node scripts/cargo-with-ram-gate.mjs -- cargo test -p rlm-core
       npm run rlm -- --help
       npm run rlm -- ask --help
       test ! -f src/index.ts
       node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-tauri
       ```

    3. **Subcommand smoke** (RETIRE-115-04): verify each shipped subcommand exposes help via dispatcher:
       ```bash
       for cmd in "--help" "ask --help" "ui --help" "plan-node --help" "plugin --help" "workflow-export --help" "workflow-import --help"; do
         npm run rlm -- $cmd || exit 1
       done
       ```

    4. Run `npm run test:agent:verify:light` — must pass 3/3 steps.

    5. Confirm `src-tauri/src/main.rs` uses `rlm_core::start_server` directly (no `Command::new("node")` for RLM). Grep gate:
       `grep -v '^#' src-tauri/src/main.rs | grep -c 'start_server'` should be ≥ 1;
       `grep -v '^#' src-tauri/src/main.rs | grep -c 'node.*index'` should be 0.

    Document in SUMMARY: `npm run build` / full `npm test` may still fail until Phase 116–119 (transitional bootstrap/plugin-loader imports).
  </action>
  <verify>
    <automated>npm run test:agent:verify:light</automated>
  </verify>
  <done>All automated Phase 115 gates green; light agent verify passes; subcommand --help smoke passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tauri dev smoke — Rust server without Node child</name>
  <files>src-tauri/src/main.rs</files>
  <action>
    Human verifies Tauri desktop shell after automated gates pass. Built stack: in-process Rust control server via `rlm_core::start_server` in `src-tauri/src/main.rs`; Node CLI/runtime deleted; `npm rlm` is Rust-only.

    Pre-check (automated before checkpoint): `grep -v '^#' src-tauri/src/main.rs | grep -q start_server` and no `node.*index` spawn in same file.
  </action>
  <verify>
    <automated>node scripts/cargo-with-ram-gate.mjs -- cargo check -p rlm-tauri</automated>
  </verify>
  <done>
    Operator confirms: (1) `npm run tauri:dev` opens window with UI loaded, stderr shows `RLM UI listening at http://...`; (2) `pgrep -af 'node.*dist/src/index'` returns nothing while Tauri runs.
  </done>
  <what-built>Tauri desktop shell with in-process Rust control server (src-tauri/src/main.rs uses rlm_core::start_server). Node CLI/runtime deleted; npm rlm is Rust-only.</what-built>
  <how-to-verify>
    1. Ensure UI built: `npm run build:ui` (if ui/dist missing).
    2. Run `npm run tauri:dev`.
    3. Confirm desktop window opens and loads UI (URL redirected to Rust server — check stderr for `RLM UI listening at http://...`).
    4. In another terminal, verify no Node process serving RLM API: `pgrep -af 'node.*dist/src/index'` should return nothing while Tauri is running.
    5. Optional: open DevTools network tab — API calls hit the Rust server port from stderr, not a Node process.
  </how-to-verify>
  <resume-signal>Type "approved" if Tauri loads UI via Rust server, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri webview → embedded server | Localhost-only Rust Axum server |
| npm rlm → rlm binary | User shell invokes packaged CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-05 | S | Tauri embedded server | accept | Localhost binding; same trust model as Phase 114 Rust server |
| T-115-06 | D | Test pruning | mitigate | Grep verify no imports from deleted paths before commit |
</threat_model>

<verification>
113-GATES Phase 115 full checklist:
- [ ] cargo test -p rlm-core
- [ ] npm run rlm -- --help
- [ ] npm run rlm -- ask --help
- [ ] test ! -f src/index.ts
- [ ] cargo check -p rlm-tauri
- [ ] npm run test:agent:verify:light
- [ ] Tauri dev smoke (checkpoint)
</verification>

<success_criteria>
- No test imports from deleted cli/runtime paths
- All shipped subcommands reachable via npm rlm
- Tauri confirmed Rust-only server (human checkpoint)
- Light agent verify green
</success_criteria>

<output>
After completion, create `.planning/phases/115-cli-entry-and-runtime-composition-removal/115-03-SUMMARY.md`
</output>

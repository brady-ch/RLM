---
phase: 113-node-runtime-retirement-audit-and-cutover-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
autonomous: true
requirements:
  - AUDIT-113-01
user_setup: []
must_haves:
  truths:
    - "Every src/ concern directory is mapped to a teardown phase (114–119)"
    - "Every script or npm script that boots the TS control server or Node CLI is listed"
    - "tests/ mirror paths are inventoried alongside their src/ counterparts"
    - "Audit document states Phase 113 is audit-only — no src/ deletions"
  artifacts:
    - path: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
      provides: "Authoritative TS-only path inventory with deletion order"
      contains: "## Deletion order"
  key_links:
    - from: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md
      to: .planning/notes/rust-only-runtime-migration-decisions.md
      via: "teardown order cross-reference"
      pattern: "Phase 11[4-9]"
---

<objective>
Produce a complete TS-only path inventory mapped to incremental teardown phases 114–119, including scripts and tests that boot or depend on the Node runtime.

Purpose: Phase 113 is audit-and-gates only — executors in Phases 114–119 need a single authoritative inventory before any deletion.
Output: `113-AUDIT.md` in the phase directory.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-CONTEXT.md
@.planning/notes/rust-only-runtime-migration-decisions.md
@.planning/ROADMAP.md
@AGENTS.md

<interfaces>
Teardown order from migration decisions (authoritative — inventory MUST align):

1. Phase 114 — `src/application/control-server/`, TS UI bootstrap, parity scripts booting TS server
2. Phase 115 — `src/index.ts`, `src/cli/`, `src/runtime/`
3. Phase 116 — `src/application/` (execution, graph, memory, config, bootstrap, plugins facade)
4. Phase 117 — `src/domain/`, `src/ports/`
5. Phase 118 — `src/adapters/`, `src/plugins/`, mirrored `tests/`
6. Phase 119 — npm toolchain (TS build deps, depcruise, Node-specific scripts)

Known TS-only integration points (verify and extend during audit):
- `scripts/rlm-runtime.mjs` — dispatcher (default flip in Plan 02, not this plan)
- `scripts/parity/compare-runtimes.mjs` — imports `dist/src/application/control-server`
- `tests/integration/control-server-fixtures.test.ts` — boots TS control server
- `package.json` scripts: `check:parity`, `dev`, `start`, `build`, `rlm:node`, `test`
- `package.json` bin: `"rlm": "dist/src/index.js"` (bypasses dispatcher — note for Plan 02)

src/ concern directories (148 `.ts` files as of 2026-05-24):
`src/cli/`, `src/runtime/composition/`, `src/runtime/interop/`, `src/application/*`, `src/domain/`, `src/ports/`, `src/adapters/`, `src/plugins/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate src/ and tests/ file inventory</name>
  <files>.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md</files>
  <read_first>
    - .planning/notes/rust-only-runtime-migration-decisions.md
    - AGENTS.md (concern map table)
    - .planning/ROADMAP.md (Phases 114–119 success criteria)
  </read_first>
  <action>
Create `113-AUDIT.md` with frontmatter-free markdown body starting with `# Phase 113: TS-Only Path Inventory`.

Run these commands and embed results in structured tables (do not hand-wave counts):

```bash
find src -type f -name '*.ts' | sort
find tests -type f -name '*.ts' | sort
find src -type d | sort
find tests -type d | sort
```

For each top-level `src/` concern (`cli`, `runtime`, `application`, `domain`, `ports`, `adapters`, `plugins`), create a section with:
- File count
- Subdirectory list
- Mirrored `tests/` path (from AGENTS.md tests mirror table)
- Target teardown phase (114–119) per migration note order
- Rust counterpart path under `crates/rlm-core/src/` or `crates/rlm-cli/src/` (use `find crates -type f -name '*.rs' | grep -E '<concern>'` to locate)

Add explicit row for `src/index.ts` → Phase 115.

State at top of doc: **Phase 113 scope is audit-only — no `src/` deletions in this phase.**
  </action>
  <acceptance_criteria>
    - `113-AUDIT.md` exists and contains the exact heading `# Phase 113: TS-Only Path Inventory`
    - Document contains sections for all eight src concerns: cli, runtime, application, domain, ports, adapters, plugins, and index.ts
    - Each concern section includes a `Target phase:` line with value 114, 115, 116, 117, or 118
    - Document lists total TS file count matching `find src -type f -name '*.ts' | wc -l` output
    - Document contains phrase `audit-only — no src/ deletions`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md && grep -c 'Target phase:' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md | awk '$1 >= 8'</automated>
  </verify>
  <done>Complete src/ and tests/ inventory with per-concern teardown phase assignments</done>
</task>

<task type="auto">
  <name>Task 2: Inventory Node-dependent scripts and CI entry points</name>
  <files>.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md</files>
  <read_first>
    - package.json (scripts and bin sections)
    - scripts/parity/compare-runtimes.mjs
    - scripts/rlm-runtime.mjs
    - tests/integration/control-server-fixtures.test.ts
  </read_first>
  <action>
Append section `## Node-dependent scripts and CI` to `113-AUDIT.md`.

Grep and document every match:

```bash
grep -rl 'dist/src\|startControlServer\|control-server\|src/index' scripts/ package.json tests/ --include='*.{mjs,ts,json,sh}' 2>/dev/null | sort
grep -E '"(dev|start|build|check|check:parity|rlm|rlm:node|test)"' package.json
```

For each hit, record in a table with columns: `Path`, `What it does`, `Boots TS server? (yes/no)`, `Removal phase (114–119 or keep)`.

Required rows (verify presence — add if grep misses):
- `scripts/parity/compare-runtimes.mjs` — boots TS + Rust servers for session route parity — **yes** — Phase 114 (retire or Rust-only rewrite)
- `tests/integration/control-server-fixtures.test.ts` — boots TS server for golden fixtures — **yes** — Phase 114 (delete; Rust fixture remains)
- `package.json` `check:parity` — runs TS fixture test + compare-runtimes + Rust golden — **yes** — Phase 114 (narrow to Rust-only)
- `package.json` `bin.rlm` — points to `dist/src/index.js` bypassing dispatcher — **no server boot** — Phase 115 (point at Rust wrapper)
- `scripts/rlm-runtime.mjs` — Node/Rust dispatcher — **no** — keep until Phase 115 simplifies to Rust-only

Append section `## Explicitly kept (not deleted in 114–119)` listing: `ui/`, `scripts/` (packaging, RAM gates, rust boundaries), `crates/`, `src-tauri/`.
  </action>
  <acceptance_criteria>
    - `113-AUDIT.md` contains heading `## Node-dependent scripts and CI`
    - Table includes rows for `compare-runtimes.mjs`, `control-server-fixtures.test.ts`, and `check:parity`
    - Table includes row for `package.json bin.rlm` with Removal phase 115
    - Document contains heading `## Explicitly kept`
    - `ui/`, `crates/`, and `src-tauri/` appear under Explicitly kept
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Node-dependent scripts and CI' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md && grep -q 'compare-runtimes.mjs' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md && grep -q 'control-server-fixtures' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md && grep -q '## Explicitly kept' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md</automated>
  </verify>
  <done>All Node boot paths and CI scripts catalogued with removal phase assignments</done>
</task>

<task type="auto">
  <name>Task 3: Add deletion-order summary cross-linked to migration note</name>
  <files>.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md</files>
  <read_first>
    - .planning/notes/rust-only-runtime-migration-decisions.md (Incremental teardown order section)
    - .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md (from Tasks 1–2)
  </read_first>
  <action>
Append final section `## Deletion order (summary)` to `113-AUDIT.md` as a numbered list Phases 114–120 mirroring the migration note table, but with concrete paths from this audit (not generic descriptions).

Each line format:
`{N}. Phase {phase#} — {paths from audit} — Gate: {one-line gate from migration note}`

Add cross-reference line at bottom:
`Canonical teardown policy: .planning/notes/rust-only-runtime-migration-decisions.md`

Add link from migration note: in `.planning/notes/rust-only-runtime-migration-decisions.md`, under item 1 "**Audit & gates (113)**", append parenthetical: `(inventory: .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md)`.
  </action>
  <acceptance_criteria>
    - `113-AUDIT.md` contains heading `## Deletion order (summary)`
    - Summary lists phases 114 through 120 (8 entries minimum)
    - `rust-only-runtime-migration-decisions.md` references `113-AUDIT.md` under Audit & gates item
    - Each phase line includes at least one concrete path from the audit
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Deletion order (summary)' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md && grep -q '113-AUDIT.md' .planning/notes/rust-only-runtime-migration-decisions.md && grep -cE 'Phase 11[4-9]|Phase 120' .planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-AUDIT.md | awk '$1 >= 7'</automated>
  </verify>
  <done>Deletion order summary links audit inventory to migration note and downstream phases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit doc → executor | Inventory must be accurate; stale paths cause wrong deletions in 114+ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | I | 113-AUDIT.md | mitigate | Generate inventory from `find`/`grep` commands, not memory |
| T-113-02 | D | Migration note cross-ref | accept | Doc-only; no runtime impact |
</threat_model>

<verification>
- `113-AUDIT.md` exists with all required sections
- Migration note cross-references audit doc
- No files under `src/` modified (audit-only phase)
</verification>

<success_criteria>
- TS-only path inventory documented with deletion order mapped to Phases 114–119
- Node boot scripts and CI entry points catalogued
- Cross-reference from migration note to audit doc present
</success_criteria>

<output>
After completion, create `.planning/phases/113-node-runtime-retirement-audit-and-cutover-gates/113-01-SUMMARY.md`
</output>

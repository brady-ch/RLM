---
phase: 40-domain-engine-decomposition
plan: "01"
type: execute
wave: 1
depends_on:
  - 39-adapters-tools-taxonomy
autonomous: true
requirements:
  - RLM-01
  - RLM-02
  - RLM-03
  - RLM-04
  - RLM-05
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`src/domain/recursion/prompt-utilities.ts` holds shared non-quality prompt helpers (preview, limitPrompt, parse helpers, toModelPurpose, fallbackFromMessages)."
    - "`npm run check` green after extraction."
---

<objective>

Peel prompt utilities from `recursive-language-model.ts` into `domain/recursion/` with no behavior change.

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/40-domain-engine-decomposition/40-CONTEXT.md  
@/home/brady/workspace/RLM/.planning/phases/40-domain-engine-decomposition/40-RESEARCH.md

</context>

<tasks>

<task id="40-01-t1" type="auto">

<action>

Add `src/domain/recursion/prompt-utilities.ts` exporting `preview`, `parseClarificationRequest`, `parseFirstInteger`, `isCodeTask`, `clamp`, `fallbackFromMessages`, `toModelPurpose`, `limitPrompt`. Import domain `types` only; use port message types via `LanguageModelPort` / `TracePort` parameter types as today.

Wire `recursive-language-model.ts` to import these and delete local duplicates. Run `npm run check`.

</action>

</task>

</tasks>

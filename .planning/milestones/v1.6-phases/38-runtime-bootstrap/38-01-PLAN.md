---
phase: 38-runtime-bootstrap
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/bootstrap/types.ts
  - src/application/bootstrap/build-runtime-context.ts
  - src/application/bootstrap/index.ts
  - tests/bootstrap-runtime.unit.test.ts
autonomous: true
requirements:
  - BOOT-01
  - BOOT-04
  - BOOT-05
  - BOOT-06
  - REG-01
  - REG-02
must_haves:
  truths:
    - "Single exported buildRuntimeContext() composes ExtensionHost load order, MCP-backed createMcpTools cleanup.track registration, createToolsResolver, createAgentRegistry, createModel factory wiring, CancellationController + createExecutionControl, and installShutdownHandlers in the same relative order as pre-refactor src/index.ts (preserve init-order contract)."
    - "Extension/tool registration stays one pipeline: builtins → optional loadExternal → interopTools registered on extensionHost (BOOT-05)."
    - "`npm run check` exits 0 (REG-02); existing integration tests unchanged in behavior (REG-01)."
  artifacts:
    - path: src/application/bootstrap/types.ts
      provides: "RuntimeContext typed fields for composition root consumers"
    - path: src/application/bootstrap/build-runtime-context.ts
      provides: "buildRuntimeContext async builder"
    - path: tests/bootstrap-runtime.unit.test.ts
      provides: "Node:test coverage without spawning dev server — asserts stable tool ordering / registry profiles from bootstrap under temp cwd"
---

<objective>

Introduce typed `RuntimeContext` and `buildRuntimeContext()` under `application/bootstrap/` as the single composition entry point for full CLI runtime (BOOT-01, BOOT-04, BOOT-05) with builders unit-tested without CLI HTTP servers (BOOT-06).

</objective>

<context>

@/home/brady/workspace/RLM/.planning/phases/38-runtime-bootstrap/38-CONTEXT.md
@src/index.ts

</context>

<threat_model>

Mitigations:

- Integrity: Preserve ResourceCleanup+MCP kill tracking semantics from index — no orphaned subprocess registration paths.

Test strategy: Unit-test bootstrap with DEFAULT_PROJECT_CONFIG in temp dir; dependency-cruise gate unchanged (REG-02).

</threat_model>

<tasks>

<task id="38-01-t1" type="auto">

<action>

Add `RuntimeContext` interface in `application/bootstrap/types.ts` covering composition outputs: LoadedProjectConfig + resolved ProjectConfig/RecursiveModelConfig snapshot, stderr logger optional, ResourceCleanup, MemoryManager, CancellationController, ExecutionControl, ShutdownController, ExtensionHost with registered tools, toolsFor resolver, FileRunStateStore + runState handle, vector index + embedding adapter, SemanticMemory-enabled factory closure, mutable memory holder, AgentRegistry, model cache + createModel, McpSkillRuntime, CLI options passthrough, cwd string.

Implement `buildRuntimeContext` in `application/bootstrap/build-runtime-context.ts` by lifting lines composing extension host→interop tools→resolver→registry→memory semantic stack→factory from current `src/index.ts` preserving exact sequencing and side-effects (patchScope run-manifest + project-preferences seeding, CompositeEventSink path under cwd). Export barrel `application/bootstrap/index.ts`.

</action>

<read_first>

src/index.ts
src/application/extension-host.ts
src/application/resource-cleanup.ts
src/application/interop-runtime.ts
src/application/runtime-composition.ts
src/application/agent-registry.ts
src/application/execution-controller.ts
src/cli/shutdown.ts

</read_first>

<acceptance_criteria>

`buildRuntimeContext` exists at `build-runtime-context.ts` with export from `bootstrap/index.ts`.
Typecheck succeeds.
Init block order matches pre-change `src/index.ts` grep sequence for: createExecutionControl, installShutdownHandlers, new ExtensionHost, loadBuiltins, createMcpTools, createToolsResolver, createAgentRegistry, createModelFactory.

</acceptance_criteria>

</task>

<task id="38-01-t2" type="auto" tdd="false">

<action>

Create `tests/bootstrap-runtime.unit.test.ts` using Node test runner: mkdir temp project root under `tmpdir`, call `buildRuntimeContext` with `DEFAULT_PROJECT_CONFIG` from project-config barrel, CliOptions minimally constructed (command ask, defaults from parseArgs([\"noop\"]) or object literal satisfying CliOptions without running full parse), LoadedProjectConfig `{ config: cloned default, path: undefined }`. Assert registry profile ids include `default` and `coding`, extensionHost.tools exposes `skill` tool name after compose, cleanup.closeAll resolves.

</action>

<read_first>

tests/config-resolution.unit.test.ts
src/cli/args.ts
src/application/config/types.ts

</read_first>

<acceptance_criteria>

Test file executes under `npm test`.
At least two assertions fail if tool registration loop or registry construction is skipped.

</acceptance_criteria>

</task>

</tasks>
